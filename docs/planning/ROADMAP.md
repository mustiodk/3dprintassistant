# 3D Print Assistant — Roadmap

**Purpose:** Live planning surface for 3dpa web + iOS. Active release tracking, current work queue, deferred and parked work, and pointers into archive + spec + session history.

**🚀 2026-07-24 TERMINAL: iOS 1.1.0 SUBMITTED FOR APP REVIEW — TASKS 11 + 12 (Steps 1–5) COMPLETE.** Task 11 composed the release (`MARKETING_VERSION` 1.1.0 `9a0197f`; web `privacy.html` opt-in push disclosure `d012cc8`; **1.1.0 overlay baseline `60d32ea` — a plan gap: bumping MARKETING_VERSION RED's the overlay validator's anti-staleness gate, recurs every bump**; ratified §11 copy verbatim, 78 printers / 14 brands recounted). Full gate battery green (web provider 62, Worker/analytics 33, validators/walkthrough/export-audit 0-FAIL, wrangler dry-run public-send `"false"`, engine+data byte-identical; iOS **183/183**; Release config `aps-environment=production`, no UIBackgroundModes; distribution `codesign` check is CI-only — dev cert only locally). Final `bridge --mode codex-only` review (codex-only since controller = Claude): NO-GO on 3 P2 doc-accuracy defects → all fixed (`edf8466`,`aa0c8a6`) → no open P0/P1/P2. **Under explicit owner GO, Task 12 shipped:** web ff-merged provider→`main` + pushed `df9aa8e..21fca0e` → Cloudflare prod verified (server paths 404, `/api/push/register` routes, privacy live, `PUSH_PUBLIC_SEND_ENABLED="false"`); iOS ff-merged release→`main` + single push `f2f1f3b..aa0c8a6`; one TestFlight run [`30035346767`](https://github.com/mustiodk/3dprintassistant-ios/actions/runs/30035346767) built `aa0c8a6` = reviewed HEAD (success). **All nine owner acceptance gates GREEN** — ①②③⑤⑦ owner on the TestFlight build, ④⑥⑧⑨ controller on a sim build from the same HEAD + signed canary sends via the owner CLI. Owner completed ASC (App Privacy Identifiers→Device ID opt-in/not-tracking/not-linked/App-Functionality, owner-revised What's New, build `202607231851`, Manual Release) and **submitted 1.1.0 for review**. **Remaining (owner-gated): Apple approval → manual release → DK-storefront 1.1.0 confirm; then the separately-gated first public notification (Task 12 Step 6) — `PUSH_PUBLIC_SEND_ENABLED` stays `"false"` until a deliberate send.** Ledger: [`IOS-1.1.0-GATE-LEDGER.md`](IOS-1.1.0-GATE-LEDGER.md) · session log: [`../sessions/2026-07-24-cowork-appdev-ios-1.1.0-task11-task12-submit.md`](../sessions/2026-07-24-cowork-appdev-ios-1.1.0-task11-task12-submit.md). `[iOS + Web provider]`

**Previous (🎯 2026-07-23 evening) TERMINAL: iOS 1.1.0 TASK 10 COMPLETE — G0 EXECUTED, DARK PROVIDER LIVE, PHYSICAL-DEVICE CANARY PASSED, OWNER AUTHORIZED TASK 11.** Owner G0 was given and executed end-to-end: Apple Push capability enabled + dedicated APNs key created (Sandbox & Production, owner-assisted in-browser), EU D1 `3dpa-push-production` (`jurisdiction=eu` verified) + both queues provisioned, rate-limit namespace `11001` proven collision-free, six Worker secrets entered interactively (never printed), `0001_push.sql` applied, and the Worker deployed **live-dark** — `PUSH_REGISTRATION_ENABLED="true"` (flipped for the canary, `8a8f6ce`), `PUSH_PUBLIC_SEND_ENABLED="false"` throughout. A post-deploy probe caught a real manual-deploy hazard — `wrangler deploy` serves *untracked* working-tree dirs (`.superpowers/`, `.worktrees/`, `ai-handoffs/` briefly public) — fixed in `bb49ccf` + runbook caveat. **The physical-device canary (owner iPhone 17 Pro Max, development APNs) passed all six proofs** — lock-screen arrival + configurator deep-route to X1 Carbon, app_update → App Store, foreground banner, full opt-out (row deleted + relaunch-persistent + old-ID `not found`), APNs-accepted wording, public send off — **and caught two defects every prior review missed**, both TDD-fixed one per commit on `codex/ios-1.1.0-release`: `0a8759a` (registrar captured `UIApplication.shared` at App.init before it exists → every registration a silent message-to-nil no-op; now resolved at call time) and `76aca9e` (owner-found: opt-out kept `selectedTopics`, gave no toggle feedback, and silently re-registered on next launch; now clears+persists). Unit bundle 179/179; on-device file-protection test proves `.completeUntilFirstUserAuthentication`; 20 controlled device launches: median 87.3 ms / p95 99.3 ms (limit 750). **Owner authorization for Task 11 recorded (`fe654b5`). Locked next step: Task 11 — compose 1.1.0** (MARKETING_VERSION bump, privacy/copy from the prepared submit package, full ship gate); Task 12 (iOS push → TestFlight → App Review → public send) keeps its own owner gates. iOS remains unpushed (main 9 ahead; release branch 14 above main, local-only); web `main`==`origin/main` with the provider branch local-only. Ledger: [`IOS-1.1.0-GATE-LEDGER.md`](IOS-1.1.0-GATE-LEDGER.md) · session log: [`../sessions/2026-07-23-cowork-appdev-ios-1.1.0-task10-canary.md`](../sessions/2026-07-23-cowork-appdev-ios-1.1.0-task10-canary.md). `[iOS + narrow Web provider]`

**Previous (🎯 2026-07-23) iOS 1.1.0 TASKS 0–9 COMPLETE — INDEPENDENT REVIEW OBTAINED, ALL FINDINGS CLOSED, CONFIRMATION `GO` — STOPPED AT G0 AWAITING OWNER.** The 2026-07-19 review blocker is resolved: the canonical protected `bridge --mode claude-only` review produced its first valid verdict (870.1 s, exit 0, non-empty) — NO-GO with 1 P1 (cold-launch notification routing race) + 6 P2 + 5 optionals. Every accepted finding landed one per commit (web `8682ab6` TopicDisallowed halt, `18c41ad` atomic finishCampaign, `247bea7` atomic public-cadence insert, `8f8acce` replay blocked-restore, `29c3e94` DLQ-advisory catch; iOS `3f886bf` timing-independent refresh-on-miss routing per design §6.2), P2-E was refuted with code evidence, and the full cross-repo gate battery was rerun green: provider 62/62, Worker/analytics 33/33, data/overlay validators, 18-combo walkthrough, export audit 0 FAIL, Wrangler dry-run (both send flags `"false"`), byte-identical engine/data, iOS XCTest 177/177 + XCUITest 4/4, clean diff checks. The confirmation review then verified the APPLIED diffs and returned **`GO` with no remaining or new P0/P1/P2** (one optional O-1 recorded as a runbook note). **Locked next step: owner G0** — the exact non-secret prerequisites (Apple Push capability + dedicated APNs key, Cloudflare D1/Queues/rate-limit/secrets approval, ASC canary Device ID + privacy confirmation) are listed in the gate ledger; provider work stays local on `codex/ios-1.1.0-provider`, all iOS work stays local on `codex/ios-1.1.0-release` under the push gate, and no provisioning/TestFlight/App Review/public send has occurred. Ledger: [`IOS-1.1.0-GATE-LEDGER.md`](IOS-1.1.0-GATE-LEDGER.md) · disposition: [`../reviews/2026-07-18-ios-1.1.0-notification-release/implementation/implementation-review-disposition.md`](../reviews/2026-07-18-ios-1.1.0-notification-release/implementation/implementation-review-disposition.md). `[iOS + narrow Web provider]`

**Previous (⛔ 2026-07-19) iOS 1.1.0 TASKS 0–8 + TASK 9 LOCAL GATE COMPLETE; INDEPENDENT IMPLEMENTATION REVIEW BLOCKED FAIL-CLOSED BEFORE G0.** The source-complete local candidate now contains the reviewed notification provider/iOS consent+routing train, issues #2–#5, and visible Bambu/Orca/Prusa exports. Task 1's isolated security fix `df9aa8e` is the only new web code pushed: production source/config paths now return 404. Provider work remains local on `codex/ios-1.1.0-provider`; all iOS work remains local on `codex/ios-1.1.0-release`, preserving the original nine commits plus ten new commits without push. Full local proof is green: provider 56, Worker 33, XCTest 175, XCUITest 4, data/overlay/walkthrough/export/Wrangler/diff gates, and byte-identical engine/data. Two gate defects were fixed separately (`91323c1`, iOS `b20be21`). The mandatory independent hostile review did **not** produce evidence: Bridge timed out empty at 900 seconds, and the one direct read-only Claude fallback was also empty at 900 seconds. **G0 was not reached. Locked next step:** resume only Task 9 Step 2 in a fresh session; require a valid non-empty Claude verdict, fix every P0/P1/P2 one per commit, rerun affected/full gates, and obtain final `GO` before declaring G0. Do not provision Apple/Cloudflare resources, push iOS, dispatch TestFlight, submit App Review, or send a public notification. Ledger: [`IOS-1.1.0-GATE-LEDGER.md`](IOS-1.1.0-GATE-LEDGER.md) · design: [`../superpowers/specs/2026-07-18-ios-1.1.0-notification-release-design.md`](../superpowers/specs/2026-07-18-ios-1.1.0-notification-release-design.md) · plan: [`../superpowers/plans/2026-07-18-ios-1.1.0-notification-release-plan.md`](../superpowers/plans/2026-07-18-ios-1.1.0-notification-release-plan.md). `[iOS + narrow Web provider/analytics]`

**✅ 2026-07-18 TERMINAL: SNAPMAKER U1 CORRECTED, RE-ENTERED, AND SHIPPED LIVE; NOTIFICATION RECOVERED WITH OWNER-GATED FREEZE RETAINED.** Owner-approved corrections changed the supported nozzles to `[0.2, 0.4, 0.6, 0.8]` and replaced the absolute purge-tower claim with accurate prime/wiping-tower wording. Fresh production run `run-20260718T112636Z` passed the owner-decision re-entry check, evidence gate, and fresh PD5 reviews (`R1 GO`, `R2 GO`), then merged/pushed `cc65622`, verified the live overlay on attempt 3 and the production picker at `snapmaker / U Series / u1`, created local-only iOS mirror `cdf5906`, and closed custody in `732cbc8`; final report: `1 shipped · 0 parked · 0 errored`, `POSTRUN ok=true`, `SYNCBOOT ok=true`. The isolated runner checkout initially lacked the gitignored notifier config, so the shipped run correctly created a `shipped-and-unreported` freeze. The existing mode-600 config was restored to that checkout and the saved report alone was replayed successfully (`posted=true`) without rerunning U1; per PD8, the freeze remains until the owner explicitly clears it, so the next daily 12:00 run will fail closed. Follow-up: harden `scripts/install-intake-runner.sh` to migrate/verify the protected notifier config on reinstall. **iOS is clean/nine ahead and unpushed; no 1.0.7/1.0.8 train started.** Session log: [`../sessions/2026-07-18-cowork-appdev-u1-owner-reentry-ship.md`](../sessions/2026-07-18-cowork-appdev-u1-owner-reentry-ship.md).

**✅ 2026-07-17 TERMINAL: SYNC-FIRST ISOLATED INTAKE RUNNER CUT OVER + PRODUCTION-PROVEN; OWNER DECISIONS APPLIED FAIL-CLOSED.** Reviewed/TDD implementation merged and pushed (web `67d7913`, ai-om `64f46c6`), installed an automation-owned checkout under `~/.local/share/3dpa-intake`, and replaced the LaunchAgent. Supervised control run `run-20260717T103753Z` ended `PREFLIGHT`/`POSTRUN`/`SYNCBOOT ok=true`, 0/0 divergence, launchd exit 0, and skipped both owner-gated candidates. `i7_i` was then proven to duplicate the already-live Creality `sparkx_i7`, closed as `was-duplicate-missed`, and custody-pushed in `6113f04` with no catalog/overlay change; live picker green. Owner-approved U1 `series_group:"U Series"` passed `verify-reentry`, but production run `run-20260717T104818Z` stopped at the single-shot evidence gate before review: prior packet evidence labels were schema-invalid, notes drifted, and one optional field was absent. U1 is safely `research-defect`, packet + `intake/u1@3ff3811` preserved, no merge/push; final `POSTRUN`/`SYNCBOOT` green. **iOS remains clean/eight ahead and unpushed; no 1.0.7/1.0.8 train started.** Evidence: [`INTAKE-AUTONOMY-V2-GATE-LEDGER.md`](INTAKE-AUTONOMY-V2-GATE-LEDGER.md).

**✅ 2026-07-16 TERMINAL: INTAKE PARKED-PATH ROOT CAUSE FIXED + PRODUCTION WRAPPER E2E GREEN.** `run-20260715T100124Z` correctly parked ambiguous `i7_i` but also called raw `writeParked('i7_i', sidecar)`, creating an untracked repo-root file that made POSTRUN and the next preflight fail `web-dirty`. TDD fix `104251c` adds a candidate-owned canonical writer plus absolute/basename path guards; AI-OM `8151868` advances the runner contract to v2.4; independent Claude hostile review returned PASS. Live rerun `run-20260716T201219Z` passed `PREFLIGHT`/`POSTRUN`, notified (`posted=true`), and left web clean/current at custody `537cd14`: `i7_i` remained owner-gated and skipped, while Snapmaker U1 was officially researched then safely parked `new-series-group` before branch creation. **No catalog, overlay, engine, UI, or iOS data changed; iOS remains clean/eight ahead under its push gate.** Session log: [`../sessions/2026-07-16-cowork-appdev-intake-parked-path-root-cause.md`](../sessions/2026-07-16-cowork-appdev-intake-parked-path-root-cause.md).

**✅ 2026-07-14 TERMINAL: OWNER-AUTHORIZED SYNTHETIC SV06 ACE E2E AUTO-SHIPPED — REAL DISCORD/KV CAPTURE, FRESH `GO/GO`, LIVE-VERIFIED, CUSTODY CLOSED.** Sovol SV06 ACE was proven absent, then submitted through production `/api/feedback` as an explicitly synthetic missing-printer request (`req:1784025588168:43e000dc`). The first run correctly parked fail-closed when R1 returned contract-invalid structured content; that run also exposed a POSTRUN preserved-ref mismatch. Both root causes were fixed TDD-first in `c2fe6fd` and `16606a7`. The clean sidecar re-entry passed evidence plus fresh R1/R2 `GO/GO`, merged/pushed `3bba2c2`, verified live overlay `content_version=2026071402` and the production picker, created local-only iOS mirror `80c26dd`, and pushed custody `54dc4c8`; wrapper `POSTRUN ok=true`. Candidate branch, packet, parked sidecar, lock, and freeze marker are absent. **iOS is clean and eight commits ahead locally under the push gate; iOS 1.0.7/1.0.8 were not started or pushed.** Session log: [`../sessions/2026-07-14-cowork-appdev-sv06-ace-synthetic-e2e.md`](../sessions/2026-07-14-cowork-appdev-sv06-ace-synthetic-e2e.md).

**✅ 2026-07-14 TERMINAL: CENTAURI CARBON 2 SHIPPED THROUGH THE FIXED v2.3 PIPELINE — FRESH `GO/GO`, LIVE-VERIFIED, CUSTODY CLOSED.** The owner resolved the prior review split by approving `multi_color_systems: ["canvas"]` and correcting the CANVAS note to say the base printer supports the separately sold system while the Combo includes it. The repaired candidate `48f48a2` passed all mechanical and single-shot evidence gates; fresh R1 and R2 both returned structured `GO` with zero objections. The runner merged/pushed `954cfa3`, verified live overlay `content_version=2026071401` plus the production picker, created the local-only iOS mirror `f9a810c`, and pushed custody `88816bd`; wrapper `POSTRUN ok=true`. A newly exposed notifier presentation defect (`[object Object]`) was then fixed TDD-first in `2ae4ddc`, and a second clean control run finished `0 shipped · 0 parked · 0 errored`, treated Centauri as already shipped, and also ended `POSTRUN ok=true`. Candidate branch, packet, and parked sidecar are correctly absent after ship. **iOS remains clean and seven commits ahead locally under the push gate; iOS 1.0.7/1.0.8 were not started or pushed in this session.** Ship log: [`../sessions/2026-07-14-cowork-appdev-centauri-owner-decision-ship.md`](../sessions/2026-07-14-cowork-appdev-centauri-owner-decision-ship.md).

**🧭 2026-07-12: ACCOUNT-PLATFORM CANDIDATES COMPARED + MERGED INTO ONE CANONICAL PLAN — PARKED AS A LOCKED ENTRY POINT.** The owner comparison gate ran: comparison audit of both candidates → owner locked four decisions (**sync = Pro** · **Firebase Auth** · **~15–20 gates** · **full scope with V0–V5 milestones**) → merged decision set (SYN-00–SYN-17) + gated plan (MG0–MG19) authored and hardened through 3 internal hostile review rounds (architecture + QA lanes; 26→12→2 findings, all patched). **Locked entry point (parked for later, owner-sequenced): after the current 1.0.7 → 1.0.8 trains, the next platform train starts at MG0 (owner ratification) + the SYN-17 Codex cross-model round — no account implementation or external provisioning before both.** Full row in Active Work Queue; both source candidates stay parked as evidence. Audit: [`../reviews/2026-07-12-account-candidates-comparison-audit.md`](../reviews/2026-07-12-account-candidates-comparison-audit.md) · decision set: [`../superpowers/specs/2026-07-12-my3dpa-merged-decision-set.md`](../superpowers/specs/2026-07-12-my3dpa-merged-decision-set.md) · plan: [`../superpowers/plans/2026-07-12-my3dpa-merged-plan.md`](../superpowers/plans/2026-07-12-my3dpa-merged-plan.md) · review ledger: [`../reviews/2026-07-12-my3dpa-merged-plan-review.md`](../reviews/2026-07-12-my3dpa-merged-plan-review.md).

**✅ 2026-07-13: EXPORT PHASES 2–4 COMPLETE; ACTIVE SEQUENCE IS iOS 1.0.7 → SEPARATE iOS 1.0.8 TIP JAR.** Phase 2 is merged/live, Phase 3 Orca JSON and Phase 4 Prusa INI are merged on web `main`, owner-import verified, and byte-identically mirrored in the six clean local iOS commits under the push gate. Execute the reviewed iOS 1.0.7 issue-fix plan next; do not bundle the separate 1.0.8 tip-jar train. Overall plan: [`../superpowers/plans/2026-07-11-export-first-overall-plan.md`](../superpowers/plans/2026-07-11-export-first-overall-plan.md); ledgers: [`EXPORT-PHASE2-GATE-LEDGER.md`](EXPORT-PHASE2-GATE-LEDGER.md), [`EXPORT-PHASE3-GATE-LEDGER.md`](EXPORT-PHASE3-GATE-LEDGER.md), [`EXPORT-PHASE4-GATE-LEDGER.md`](EXPORT-PHASE4-GATE-LEDGER.md).

**📱 2026-07-11: iOS 1.0.7 ISSUE-FIX RELEASE — CROSS-MODEL REVIEWED GO, UPDATED AFTER CLAUDE-BRANCH VALIDATION, READY TO EXECUTE; IMPLEMENTATION NOT STARTED.** Approved scope: iOS fixes for GitHub #2 (unchanged Workshop re-save), #3 (permanent Discord invite), #4 (visible Workshop transfer + slicer-honest Output action), and #5 (iOS feature analytics), plus only the narrow current-main-compatible web Worker/dashboard contract needed for the four Workshop events. Claude's `origin/claude/cold-start-3dpa-issues-ihs5du` branch is web-only reference work, not an iOS fix; the plan now requires reconciling it against current `origin/main` before any web edit and forbids wholesale merge/cherry-pick. Keep `MARKETING_VERSION=1.0.7`; Fastlane supplies a new build number. K2 SE, Centauri Carbon 2, and SV06 ACE are **already live** through overlay `2026071402`; local iOS commits `bfc9a2b`, `f9a810c`, and `80c26dd` carry their bundled offline mirrors. Native Orca/Prusa file serializers stay separate. App Store submission is gated on owner acceptance of the exact new TestFlight build. Final completion now includes a mandatory clean-state audit: no uncommitted dirty state may remain in any touched repo. Design: [`../superpowers/specs/2026-07-11-ios-1.0.7-issue-fix-release-design.md`](../superpowers/specs/2026-07-11-ios-1.0.7-issue-fix-release-design.md) · plan: [`../superpowers/plans/2026-07-11-ios-1.0.7-issue-fix-release-plan.md`](../superpowers/plans/2026-07-11-ios-1.0.7-issue-fix-release-plan.md) · review: [`../reviews/2026-07-11-ios-1.0.7-plan-review.md`](../reviews/2026-07-11-ios-1.0.7-plan-review.md).

**✅ 2026-07-10 (night): INTAKE AUTONOMY v2.1 MERGED + LIVE.** Small-gates R0→R8 landed through PR #7 (`452fb94`) with green Workers build; ai-om runner-contract fixes were published. Evidence remains in [`INTAKE-AUTONOMY-V2-GATE-LEDGER.md`](INTAKE-AUTONOMY-V2-GATE-LEDGER.md). Do not restart R0–R8.

**✅ 2026-07-10: K2 SE SHIPPED through the owner-gated manual lane.** Web bundled data + iOS overlay landed through PR #8 (`eeedf47`); production overlay is `content_version=2026071005` and includes `k2_se`. Existing users receive it remotely without TestFlight. The local iOS bundled mirror `bfc9a2b` remains under the normal push gate for the next binary train.

**✅ 2026-07-10: analytics Worker 5-event schema + android HMAC auth LIVE** (owner "do 1" done — `551b092`+`14cb97a`, TDD, hostile-reviewed, prod-verified: `troubleshoot_used`/`export_clicked` now stored, android bad-sig 401s) **+ Intake Autonomy v2 impl plan dual-reviewed & committed** (`bff1604`; sub-agent 17 findings incl. CRITICAL + Codex 3 must-fix, all applied). New owner-decision row queued: 4 selection events still 400 (see Active Work Queue).

**🤖 ANDROID v1 PLANNED — triple-reviewed bundle ready, awaiting owner AG0 (2026-07-09):** the Android planning session ran (autonomous, planning-only — no app code): audit-delta [`../reviews/2026-07-08-android-v1-audit-delta.md`](../reviews/2026-07-08-android-v1-audit-delta.md) · spec [`../superpowers/specs/2026-07-08-android-v1-design.md`](../superpowers/specs/2026-07-08-android-v1-design.md) (AD0–AD10: native Kotlin/Compose restart in the `3dprintassistant-android` repo, QuickJS runtime w/ mandatory AG1 spike, improved-from-day-one bar incl. native Mine loop, sibling Android overlay, EN+DA day-one) · plan [`../superpowers/plans/2026-07-08-android-v1-plan.md`](../superpowers/plans/2026-07-08-android-v1-plan.md) (AG0–AG8, one gate/session, 12–18 sessions + 3–5 wk Play calendar: 12-tester/14-day closed test is the long-pole) · dispositions [`../reviews/2026-07-08-android-v1-review.md`](../reviews/2026-07-08-android-v1-review.md) (2 hostile sub-agents + Codex via bridge — GO-WITH-PATCHES ×3, non-overlapping catches, all applied) · ledger [`ANDROID-V1-GATE-LEDGER.md`](ANDROID-V1-GATE-LEDGER.md) (PENDING-GO). macOS: light companion [`../superpowers/specs/2026-07-08-macos-companion-assessment.md`](../superpowers/specs/2026-07-08-macos-companion-assessment.md) — sequenced after Android; owner 5-min check: ASC "Mac Availability" checkbox (the iOS app may already run on Apple-silicon Macs). **Owner next (AG0): read spec §2 + §4, reply GO/override; create the Play Console account; push (or write off) the mac-mini's prototype commits.** Mined web findings queued in Active Work Queue (analytics Worker auth + EVENT_KEYS full-schema fix; feedback Worker auth; iOS overlay validator RED on missing 1.0.7 baseline).

**🔁 OWNER PIVOT (2026-07-08 evening): the "iOS v2 rebuild" goal was mis-stated — the real target is an ANDROID app (first priority), then a macOS app.** The iOS v2 planning bundle below is **PARKED as a reference/evidence base** (Gate 0 never ratified; nothing deleted): its audit + §4 contracts are platform-neutral and fed the Android planning session above. `3dpa-context.md` Android-scope line fixed 2026-07-09.

**📐 iOS v2 REBUILD PLANNED — PARKED AS REFERENCE (2026-07-08, see pivot above):** a triple-reviewed planning bundle for rebuilding the iOS app as **v2.0.0** ("improved copy": web feature parity incl. native Mine learns-loop/troubleshooter/journal/share, rebuilt architecture, a11y/i18n/motion polish) is ready: audit [`../reviews/2026-07-08-ios-v2-rebuild-audit.md`](../reviews/2026-07-08-ios-v2-rebuild-audit.md) · spec [`../superpowers/specs/2026-07-08-ios-v2-rebuild-design.md`](../superpowers/specs/2026-07-08-ios-v2-rebuild-design.md) · plan [`../superpowers/plans/2026-07-08-ios-v2-rebuild-plan.md`](../superpowers/plans/2026-07-08-ios-v2-rebuild-plan.md) · review dispositions [`../reviews/2026-07-08-ios-v2-rebuild-review.md`](../reviews/2026-07-08-ios-v2-rebuild-review.md). Gates G0–G8, one autonomous session per gate, 10–15 session envelope; owner-blocking only at G0 (ratify decisions D0–D7, esp. D1a branch-push-ON + D5b rules-freeze + comparison cut) and G8 (TestFlight from `--ref ios-v2` → on-device acceptance → merge → ASC). **Owner next: read spec §2, reply GO or override.** No code touched; engine/data untouched.

**✅ MERGED + LIVE ON WEB + iOS v1.0.7 ON TESTFLIGHT (2026-07-07):** the **W3 Mine-tier engine train** is **merged to `main` (`6a828c2`) and LIVE on web** (Cloudflare auto-deploy — production `engine.js` `setPersonalTuning`-verified by curl ~30s post-push; golden NO DRIFT). **Accepted Workshop tuning now changes the generated profile** — the learns-loop payoff. `profileMode:'mine'` = Safe base + personal deltas via `Engine.setPersonalTuning` (engine-side vocabulary re-clamp + pairKey stale-injection guard), temp deltas at env-adjustment points w/ NEW lower floors, fan delta on the closed 3-emission set w/ NEW 0–100 bounds, retraction delta post-`_scaleRetraction` pre-cap, provenance `personal` on effectively-touched values only, conditional Mine segment (engine `getFilters`), share URLs map mine→safe at the share affordance. **OWNER-VERIFY PASSED (owner-run 2026-07-07):** 2 failed Layer-Separation prints → "Nozzle temperature +5 °C" suggestion → Accept → Mine segment → nozzle temps 225/220 → 230/225 on screen. Cross-model review (Codex NO-GO → 2 findings fixed one-per-commit `cae683d`+`55beff8`, 1 deferred) + fresh-context subagent 9/9 verified; golden 36→39, non-mine bytes unmoved, zero `data/` changes. **iOS: v1.0.7 pushed to `main` (`51356de`, engine byte-identical to live web main, 135/135 XCTests) → ✅ UPLOADED TO TESTFLIGHT** ([run 28830781702](https://github.com/mustiodk/3dprintassistant-ios/actions/runs/28830781702), deploy green 4m3s). First two dispatches failed on an Apple account-agreement precondition (*"A required agreement is missing or has expired"* — App Store Connect Account-Holder action, not code); owner signed it, re-dispatch after ~30 min propagation succeeded. **Awaiting ASC processing + owner on-device Mine acceptance** (iOS has no journal surface → import a web Workshop backup with accepted tuning to surface the Mine segment).** Baseline fallback tag `mine-tier-baseline-20260706` retained. Deferred follow-ups (Active Work Queue): rules-table→remedy migration (W3-T7) + text/reference-export retraction display (Codex HIGH-2). Session log `../sessions/2026-07-06-cowork-appdev-mine-tier.md`; gate ledger [`MINE-TIER-GATE-LEDGER.md`](MINE-TIER-GATE-LEDGER.md).

**Previous (✅ MERGED + DEPLOYED 2026-07-06):** the **learns-export** dual-track work is **merged to `main` (`9e7890d`) and live on web** (Cloudflare auto-deploy): IMPL-044 W3/W4 design spec + plan (reviewed) + the app-layer journal→suggestion harvesting (Workshop "Suggestions" + op-log tuning ledger; engine untouched, golden-diff-proven), AND IMPL-043 Phase 0+1 (export-audit vs owner golden fixtures; passthrough export via `_slicer_value` sidecars with `USE_LEGACY_EXPORT` one-line fallback retained; HIGH-001/ironing_pattern/support_style-5-map/version-split all fixed; audit 0 FAIL). **Bambu Studio import test PASSED (owner-run 2026-07-06 — both real profile values AND `ZZ` sentinel markers landed byte-for-byte; recorded in [`LEARNS-EXPORT-GATE-LEDGER.md`](LEARNS-EXPORT-GATE-LEDGER.md) OWNER-VERIFY block; this is the recorded result that gates IMPL-043 Phase 2 Beta-badge removal).** iOS mirror `c647982` stays local (push gate) for the next TestFlight train. Session log `../sessions/2026-07-06-cowork-appdev-learns-export.md`.

**Current live status (2026-07-06):** **Phase-2 Share + Persist + Workshop is LIVE on web** (session persistence `3dpa_state_v1`, shareable profile URLs + Share button, Workshop with saved profiles / JSON backup / print journal + troubleshooter deep-link; IMPL-042 A+B and IMPL-044 W1+W2, all engine-untouched). **iOS v1.0.6 is pushed to `main`** (`791d78b`: app-state persistence + Workshop with byte-compatible backup; journal deferred — no iOS troubleshooter surface) — **awaiting owner TestFlight dispatch** (`gh workflow run testflight.yml --ref main`; check the gate ledger I5 note about the other machine's 3 unpushed data-mirror commits first). Gate-by-gate record: [`PHASE2-GATE-LEDGER.md`](PHASE2-GATE-LEDGER.md); session log `../sessions/2026-07-05-cowork-appdev-phase2-share-persist-workshop.md`. S1 (IMPL-042 Phase C landing pages) not started. IMPL-043 stays blocked on owner golden files.

**Previous status (2026-07-04):** Creality Ender-3 V4 Combo is shipped on web and iOS via the normal printer-intake data path: web `data/printers.json` plus remote iOS overlay `content_version=2026070401`. This did not require an iOS App Store/TestFlight release. The observer-only autonomy proof is **contaminated** because Codex manually ran the Assistant half after Scout staged the candidate; the next locked step is a postmortem and an explicit observer-only S5/autonomy contract.

**Previous major update:** 2026-06-20 (**overlay Option 2 SHIPPED + LIVE**; research-capable screening SHIPPED + LIVE; first autonomous printer-add succeeded). **Overlay Option 2 (validator Part-C-aware + republish) — DONE + LIVE 2026-06-20:** made `scripts/validate-ios-printer-overlay.js` Part-C-aware (`collectBaselineUnion` collides overlay ids only against baselines in `[min_app_version, FIRST_OVERRIDE_MERGE_VERSION="1.0.5")`; builds ≥1.0.5 override-merge by id → excluded from the collision union), so the overlay KEEPS aries/mega_x (graduated into the v1.0.5 bundle) while still delivering them to the v1.0.4 App-Store majority that doesn't bundle them + adds `snapmaker` brand/`snapmaker_2_a350`/`creator_5_pro` (rows byte-identical to web). Backfilled the `1.0.5` baseline (ground-truth iOS `518f781`: 13 brands/71 printers); republished overlay **`content_version=2026062001`**, recomputed `payload_sha256`. TDD RED→GREEN (**13/13**); **Codex `bridge --mode codex-only`: GO** (applied its 2 optional items — split the collision-union from a new `collectKnownBrandIds` known-brand union to kill a latent future-`min_app_version`≥1.0.5 false-reject footgun + accurate `[min,1.0.5)` error text); runbook **Phase 4b reconciled** to the override-merge boundary (its literal old "always remove graduated printers" rule would have regressed v1.0.4). Owner go → web pushed `c3afd27..e6b6945` → **curl-verified live** (live `payload_sha256` = committed → delivered, not just published). iOS untouched (push gate). 1 K3 finding (`2026-06-20-runbook-rule-stale-after-runtime-behavior-change`). Session log: [`../sessions/2026-06-20-cowork-appdev-overlay-option2.md`](../sessions/2026-06-20-cowork-appdev-overlay-option2.md). **Earlier 2026-06-20 (research-capable screening):** **Research-capable screening** turns missing-printer intake into capture-any-plausible-request → research-resolve → add: **G1** Worker capture (Tier-1 brand-list expansion + **Tier-2 brand-less `intent:"unresolved-brand"`** via `extractBrandlessIntent`) + **G2** Scout brand-less routing (manufacturer-null **`needs-research(researchReason:"resolve-brand")`** + shared null-brand sentinel + digit-only-id guard) + **G3** agent contracts (+ S4 ledger enum). Hostile-reviewed **2 sub-agent + 3 Codex rounds** — Codex caught a **PII phone-leak** + fused-platform (`iPhone16Pro`) + prose-overmatch the sub-agent reviews missed; all patched (55 lib + 3 integration + full Scout green). Merged to web `main` `97c2b25`. **First autonomous Printer Addition Assistant run** (branch, owner-ratified) added **Snapmaker 2.0 A350** (NEW brand) + **FlashForge Creator 5 Pro** (arrived brand-less → resolved to FlashForge) — ground-truth-verified + merged `3776127` → **LIVE on web** (curl: 73 printers; `snapmaker_2_a350`+`creator_5_pro`+`snapmaker` brand). iOS mirror local (push gate; 2 commits). **Live smoke proved e2e:** 2 exact-wording fakes → captured in prod KV (Snapmaker brand-present; Creator `intent:unresolved-brand`) → Scout `needs-research`; test KV cleaned. **Overlay Option 2 — SHIPPED + LIVE 2026-06-20** (details in the banner top): the collision-validator is now Part-C-aware and the overlay was republished at `content_version=2026062001` with snapmaker/creator added + aries/mega_x kept. 2 findings (`2026-06-20-autonomous-*`) + 3 new Assistant-contract "Autonomous-run rules" added. Session log: [`../sessions/2026-06-20-cowork-appdev-research-capable-screening.md`](../sessions/2026-06-20-cowork-appdev-research-capable-screening.md). **Earlier 2026-06-16** (S4 Intake Retrospective + S2 intake capture EXECUTED + pushed). Two feedback-pipeline topics shipped end-to-end under Work Protocol Full lane (per-gate hostile review→patch→QA→commit). **S4** (learning loop, Approach A — deterministic at runtime, **no LLM in the Scout's decision path**): committed outcomes ledger `scripts/printer-intake-outcomes.jsonl` + Assistant permission amendment (G1) · `scripts/validate-guardrails-diff.js` chaining S3's validator (G2) · deterministic `scripts/intake-retrospective-gather.js` + owner-approved idempotent `scripts/apply-guardrails-diff.js` + retrospective contract + calibration ledger (G3). The retrospective only PROPOSES diffs into the S3 config; the owner ratifies via `--apply`; **dormant until the outcomes ledger has real entries** (the Assistant appends one per processed candidate). **S5 (autonomy ladder) now unblocked.** **S2** (capture completeness — fixes the v1 BLOCK): Worker-side `functions/api/_lib/printer-mention.js` `extractPrinterMention` + widened `/api/feedback` tee (general/feature/bug only — `missing*` excluded; structured brand/model so the Scout is **actionable not `unactionable`**; bounded matched span / 30d TTL / fail-open) (G1) · Scout lane provenance + order-independent **form-wins** `collapse()` + `heuristicCandidates` grouping (G2). Server-side → also catches iOS general-feedback mentions (no iOS binary). **Gate 3 (web nudge) deferred — owner-pick.** Commits 3dpa `736a6a8`→`9969cff` (pushed `589bad7..9969cff`; web auto-deploys) + ai-om `b51c4b6`/`8710494` (pushed). Reviews caught a real first-draft defect EVERY gate (incl. an **S2 HIGH PII leak** — email/phone token in the stored span — patched before the live push). **Open:** S2 live smoke (owner; not done to avoid polluting live KV) + Gate-3 nudge + `functions/` not asset-ignored (spawned task). Session log: [`../sessions/2026-06-16-cowork-appdev-s4-retrospective-s2-capture.md`](../sessions/2026-06-16-cowork-appdev-s4-retrospective-s2-capture.md). **Earlier 2026-06-15 (S3 Scout robustness + learned-guardrails config EXECUTED — Gates 1–3).** **Executed the 3 QA-green S3 feedback-pipeline gates** on the printer-intake Scout: **G1** externalised the hardcoded guardrails into a versioned, validated `scripts/printer-intake-guardrails.json` + `scripts/validate-guardrails.js` (behaviour-preserving refactor proven **byte-identical** — this is the config surface S4's learning loop proposes diffs into); **G2** added brand-alias seeds (`bmbulab→bambu_lab`, `sparkx→creality` — the 2026-06-14 backlog false positives now dedupe) + a conservative `possibleBrandTypo` hint + `report.version`→3; **G3** added model-suffix-strip (`i7 w/CFS` dedupes to bundled `sparkx_i7`). Each gate ran implement→hostile sub-agent review→patch→QA-green→commit (verdicts GO/GO/GO-WITH-NITS; a real MEDIUM silent-degradation gap + LOWs caught + patched + test-locked); 35-case Scout suite green; commits `1a6e256`→`13f602b`→`bd23085` + runbook `ff7508e` (3dpa) + Scout-contract `ce19a05` (ai-om). **S4 is now unblocked** (S3 landed); S2 remains independent. Plan-defect caught at grounding: the plan/spec "validator asserts `length===14`" would have broken Gate 2 (16 aliases) — validator made count-agnostic, plan/spec corrected. Session log: [`../sessions/2026-06-15-cowork-appdev-s3-scout-robustness-guardrails.md`](../sessions/2026-06-15-cowork-appdev-s3-scout-robustness-guardrails.md). **Earlier 2026-06-15 (S1 + overlay-collision hardening SHIPPED to TestFlight as v1.0.5):** **v1.0.5 is on TestFlight** ([run 27569280416](https://github.com/mustiodk/3dprintassistant-ios/actions/runs/27569280416) = success): executed **S1** (iOS contextual-feedback prefill fix — migrated all 8 contextual entry points/6 screens from `.sheet(isPresented:)`+separate `@State` optional to `.sheet(item:)` + `FeedbackPrefill: Identifiable` + VM `init(prefill:)`; `ae03510`) + **Part C** overlay-collision hardening (dropped both all-or-nothing `isDisjoint` guards in `PrinterCatalogProvider.validatePayload` → `mergedArray` override-by-id, so one colliding overlay id no longer drops the WHOLE overlay; de-risks the 1.0.5 Aries/Mega-X graduation; `af4bbe0`) + **MARKETING_VERSION 1.0.4→1.0.5** (`518f781`, hand-edited project.yml + both pbxproj configs, no xcodegen-regen). **This mac-mini became a full iOS build/test machine** — installed Xcode 26.5 + iOS 26.5 sim + created `Config.xcconfig` from the gitignored template → real local XCTest **112 unit + 2 UITest green** (S1 genuine RED→GREEN + Part C override-merge tests). Pushed `a2c1bc3..518f781`. **PENDING: ASC processing + owner on-device S1 acceptance** (built+uploaded ≠ accepted). Follow-ups: overlay **Phase-4b graduation** (republish overlay without now-bundled Aries/Mega-X + add a 1.0.5 baseline) + S2/S3. Part C was the deferred spec Part C (status flipped DEFERRED→shipped-1.0.5). Carried "Cloudflare Pages" md-hygiene finding verified **STALE/resolved** (docs already say Workers+Assets). Session log: [`../sessions/2026-06-15-cowork-appdev-s1-overlay-hardening-1.0.5-ship.md`](../sessions/2026-06-15-cowork-appdev-s1-overlay-hardening-1.0.5-ship.md). **Earlier 2026-06-15 (S1–S5 specs+plans, autonomous):** **All five feedback-pipeline topics (S1→S5) now have a QA-green design spec AND a gated implementation plan**, each run write→sub-agent-review→patch→QA; **execution deferred to explicit owner command** (plans are creation artifacts — no code written). Specs `docs/superpowers/specs/2026-06-15-s*`, plans `docs/superpowers/plans/2026-06-15-s*`. See Active Work Queue → "Feedback-pipeline evolution" for per-topic status + gating (S4 needs S3; S5 needs S4). **Earlier 2026-06-15 (brainstorm + S1 spec):** Brainstormed 3 owner feedback items (general-feedback printer requests invisible to the Scout; a more-automated Printer Addition Assistant; an iOS feedback-form bug) into a **5-spec plan**: **S1** iOS contextual-feedback prefill fix · **S2** intake capture completeness (tee widen + misroute catch-all) · **S3** Scout dedupe/triage robustness + learned-guardrails config · **S4** Intake Retrospective learning loop (**Approach A**: learn between runs via propose-and-approve, deterministic at runtime; reuses lesson-spotter + Curator patterns) · **S5** Assistant autonomy ladder (branch + review note + PR + owner go/no-go, gated on guardrail maturity). Per-topic process: spec→review→patch→QA→plan→review→patch→QA with an owner check-in between topics; **execution deferred to explicit owner command.** **S1 spec is written, sub-agent-reviewed (10 findings, `patch-then-proceed`), patched, and QA-green** ([spec](../superpowers/specs/2026-06-15-ios-feedback-prefill-fix-design.md)); the **S1 implementation plan is the locked next step.** Root cause confirmed: iOS `.sheet(isPresented:)` + a separate `@State` optional delivers a stale-nil prefill, so every contextual feedback entry point (8 sites/6 screens) opens on General feedback — which the web tee never forwards to the Scout (S1 is the upstream repair for one cause of "requests we never see"; S2 catches the residual). No code shipped this session; cold-start ff-pulled 49 commits of mac-mini work. **Earlier 2026-06-14 (mega-x-ship):** **Real Discord missing-printer backlog run COMPLETE — Anycubic Mega X SHIPPED + verified live.** Seeded 16 verbatim Discord entries into the `PRINTER_INTAKE` KV → ran the deterministic Scout → screened **3 needs-research / 1 duplicate / 5 declined-non-fdm / 7 unactionable**. **Anycubic Mega X** is the one genuine novel FDM add: web `ebff194` (data + walkthrough combo 13) + `8ad4293` (overlay `content_version=2026061402`, mega_x under the bundled anycubic brand, aries preserved) + `115e628` (ROADMAP/findings); **verified live** (`curl` → `2026061402 [aries,mega_x]`); iOS bundled mirror `9ea28c3` local-only (push gate). `max_acceleration=400` is sourced from Anycubic's OFFICIAL firmware (`DEFAULT_MAX_ACCELERATION {400,400}`, Configuration.h:517) — a real manufacturer ceiling, NOT an app-cap; adversarial reviewer GO; visual picker OK. The 2 other needs-research survivors were already-bundled false positives the Assistant's catalog cross-check caught (X2D = Bambu Lab X2D, "Bmbulab" typo defeated brand-dedupe; Sparkx i7 w/CFS = Creality i7, "w/CFS" suffix defeated model-dedupe). **Two P2 Scout follow-up findings filed (dedupe robustness; general-feedback-request invisibility) = locked next entry** (see Active Work Queue → Printer Intake Automation). Session log: [`../sessions/2026-06-14-cowork-appdev-printer-intake-real-backlog-mega-x.md`](../sessions/2026-06-14-cowork-appdev-printer-intake-real-backlog-mega-x.md). **Earlier 2026-06-14 (overlay-fix) — iOS overlay Aries-collision FIXED — Aries now actually reaches v1.0.4 users.** The 2026-06-13 "Aries SHIPPED" overlay (`content_version=2026061301`) carried `sparkx_i7` alongside `aries`; since v1.0.4 bundles i7, the iOS runtime (`PrinterCatalogProvider.validatePayload`) rejects the **whole** overlay on the i7 collision — so Aries was **published but never delivered on v1.0.4** (published ≠ delivered; the prior wrap asserted "SHIPPED/overlay LIVE" without verifying on-device visibility). Fixed under an owner-approved gated plan (spec→review→plan→review→per-gate exec, each gate subagent-reviewed + QA): republished an **aries-only** overlay `content_version=2026061401` (i7 dropped — it's bundled), hardened the ship validator (collide overlay ids vs the **union of all baselines ≥ min_app_version** + require a MARKETING_VERSION baseline + added the 1.0.4 baseline + a 6-case self-test), and added a runbook **Phase 4b** overlay→bundled graduation rule. **Verified live: `curl` → `content_version=2026061401`, printers `[aries]`, brands `[voxelab]`.** Web pushed `e611cc9..3a1c4a5` (Workers+Assets auto-deploys; v1.0.4 devices pick up Aries on next launch). Deferred (needs an iOS binary): drop the all-or-nothing Swift disjoint guard. Spec/plan: [`docs/superpowers/specs/2026-06-14-ios-overlay-aries-collision-fix-design.md`](../superpowers/specs/2026-06-14-ios-overlay-aries-collision-fix-design.md). **Earlier 2026-06-14 — Printer-intake process patched + Voxelab Aries published to overlay (delivery corrected above).** The two NO-GO gaps are closed in the canonical protocol + both agent contracts: (1) a sanctioned `app-cap` field-confidence class for `max_acceleration` only (advisory-only in the engine — sole consumer is the HIGH-012 warning copy, never a clamp), gated by four conditions (documented null-source sweep; value ≤ lowest sibling bedslinger ceiling; required `notes[]` provenance line; reviewer GO) + a new reviewer trigger; (2) a scoped data-only iOS XCTest waiver for value-only adds (no engine/Swift/schema/new-key change; byte-identical `diff -q`; web gates + overlay validator green; void conditions otherwise). Re-ran Aries through the updated flow under a gated plan (8 gates, per-gate subagent review + QA; Gate 2 went NO-GO→patched→GO). Owner supplied the **official Voxelab manual** (confirms FDM, 200×200×200mm, 0.4mm nozzle, 1.75mm PLA/ABS/PETG, 250°C nozzle, **110°C bed — beats reseller 100°C**, ≤180mm/s, glass plate; **no acceleration figure published** → `max_acceleration=5000` app-cap = the lowest shipped bedslinger ceiling [Ender-3 V3 SE / Mini+ / SV06 Plus / SV04], with `notes[]` provenance). All gates green, risk review **GO**, owner visual-picker check OK. **Web pushed `9760f2d..60b231a`** (Cloudflare auto-deploys); overlay **`content_version=2026061301`** (voxelab + aries, sparkx_i7 preserved); iOS bundled mirror committed **local-only** (push gate, `7cf5b73`). ai-om contracts pushed (`7b5356c`). **Real Discord backlog NOT yet seeded — that is the next, separate decision; Photon stays `declined-non-fdm`.** Plan/outcome: [`docs/superpowers/plans/2026-06-13-printer-intake-process-patch-and-aries-rerun.md`](../superpowers/plans/2026-06-13-printer-intake-process-patch-and-aries-rerun.md). **Earlier 2026-06-13 (rehearsal)** — Printer Intake Automation Aries delivery rehearsal returned NO-GO: web/iOS/overlay Aries data was reverted after review because `max_acceleration` remains unsourced and local iOS XCTest is unavailable without full Xcode. Locked next entry: decide the app-side acceleration-cap/protocol path and the non-TestFlight iOS test/waiver gate before seeding the real Discord backlog. **Earlier 2026-06-04** — **v1.0.4 is LIVE on the App Store** (submitted 2026-05-20, owner-confirmed approved + live 2026-06-04). The accuracy/honesty release (env-aware fan/temp/first-layer scaling + honest post-clamp warnings + Creality i7 bundled + Advanced cooling detail) cleared Apple review with no App Privacy change; v1.0.4 now supersedes 1.0.3 as the live build. Active Release header + Current Snapshot iOS row flipped from "in review" → live. Also this session: closed stale Cloudflare autoconfig bot PR #1 (CONFLICTING/DIRTY, superseded by hand-maintained `wrangler.toml` + `.assetsignore`) flagged by the personal-dashboard repo-health card — 0 open PRs/issues now. **Earlier 2026-05-28** — Post iCloud → GitHub workdir migration (parent `CLAUDE.md` + `scripts/MIGRATION.md`). **iOS push gate currently holds 2 local net-revert data commits** on `3dprintassistant-ios/main`: `63c291c` (add Voxelab Aries) + `8c38d02` (revert Voxelab Aries). They restore bundled data to web parity and remain unpushed until the next TestFlight-ready iOS push decision. **Earlier 2026-05-20** — **v1.0.4 SUBMITTED for App Store review.** Owner tested TestFlight build `202605192119` (run [26125919796](https://github.com/mustiodk/3dprintassistant-ios/actions/runs/26125919796), includes env-prefix-dedupe) → GO, submitted for review with Manual Release. v1.0.4 is an accuracy/honesty release (env-aware fan/temp/first-layer scaling + honest post-clamp warnings + Creality i7 bundled + Advanced cooling detail); **no new data collection → App Privacy unchanged from 1.0.3.** Submit doc at `3dprintassistant-ios/docs/app-store-v1.0.4-submit.md` (parallel to v1.0.3 doc). Pre-submit decision: bundled NO backlog into the tested build (a validated TestFlight build is known-good; bundling resets the test cycle + re-adds regression risk; nothing cleared the iOS-binary + user-facing + high-value + low-risk bar). No code/engine/data touched this session; iOS HEAD `ed08507`. Owner will report approval/release. **Earlier 2026-05-19 — bed-first-layer env-name prefix dedupe — slid into v1.0.4 TestFlight pre-ASC submission.** Two warning banners on cold-garage scenario both led with `Cold garage (5–15°C)` (the dedicated MEDIUM-01 bed-first-layer banner + the consolidated nozzle/first-layer-speed banner). Matrix-style brainstorm → Option 1 (minimal — strip env.name from bed warning; preserves MEDIUM-01's 3-branch clamp-attribution copy intact). TDD clean: RED `v1.0.5 env-prefix-dedupe` walkthrough block (3 cases × 2 assertions incl. pair contract) → GREEN engine.js:1684/:1688/:1692. All gates green (validate-data 6/6, walkthrough clean, matrix-audit 55/55 curated + 0 broad). iOS engine.js byte-identical sync + mirror `testV105_EnvPrefixDedupeOnBedFirstLayerWarning` (XCTest 110/110 unit, was 109 + 1 new). Visual verification via preview server + DOM read confirmed dedup. **Web `46334e6` pushed (`9bdc4fc..46334e6` — flushed 2 prior PoC wrap commits + this fix; Cloudflare auto-deploys).** **iOS amended-then-pushed `ed08507` (`c99a797..ed08507`); TestFlight dispatched [run 26125919796](https://github.com/mustiodk/3dprintassistant-ios/actions/runs/26125919796) — new v1.0.4 build uploads with same MARKETING_VERSION 1.0.4, workflow auto-increments build number.** Decision: ride-along v1.0.4 (not v1.0.5) since v1.0.4 was on TestFlight but not yet submitted to App Store review — safe to slide in. v1.0.5 carry bundle reverts to "all items pending" status. PoC paths (v5 mechanical pass / Gate 1 desk research / bridge cwd-scope preamble) remain valid for next session. **Earlier 2026-05-17 (evening, session 2): Resin-scaling PoC bridge rounds 2 + 3 + v4 problem-statement shipped.** v4 now in place at `docs/resin-scaling/problem-statement.md`; replaces v3; integrates R2's 2 MUST-FIX + 6 SHOULD-FIX (most notably §7 restructured as Option A decision tree with Gate 2 evaluated first; §5 gained "Fires if" column; printer-count drift footnote rewritten with correct sources). R3 on v4 returned 1 MUST-FIX (`index.html:8` over-correction regression) + 8 SHOULD-FIX (3 Codex-surfaced Claude-turn-1 misses) + 3 OPTIONAL — **v5 mechanical pass queued but not blocking; no R4 needed**. 2 new K1 findings filed in `ai-operating-model/docs/findings/` (K1-2 + K1-3); K3 sub-component of K1-3 = bridge invocation cwd controls Claude-turn-1 read sandbox scope (operational not structural). G2 (bridge multi-round amplifier) materially answered: 3-round value trend HIGH → MEDIUM-HIGH → MEDIUM. G3 autonomy ceiling docs: K1 chain + K4-1 + K3. **No engine / data / UI touched. iOS untouched (`c99a797`). v1.0.4 → ASC submission still parked behind PoC.** Next session: owner-pick v5 mechanical pass + Gate 1 (`technical-differences.md`) desk research + `bridge/CLAUDE.md` cwd-scope standing rule preamble. **Earlier 2026-05-17 (morning):** PoC formally started as docs-only discovery — see kickoff session log + v3 (now superseded by v4) for full context. **Earlier 2026-05-15:** printer-addition protocol v3 → v4 + Photon Mono M7 Pro request declined (resin / MSLA, out of scope); single web doc commit `6314fda` pushed; iOS untouched; new v1.0.5 carry added (FDM-only scope copy on a user-facing surface). Earlier same day: **v1.0.4 SHIPPED to TestFlight ([run 25892826819](https://github.com/mustiodk/3dprintassistant-ios/actions/runs/25892826819)).** v1.0.3 still live on the App Store until v1.0.4 review acceptance; remote overlay `content_version=2026051202` ships i7 under Creality / i Series. **S9 closed the v1.0.4 arc as "(known) bug-free" per owner directive:** triaged the Codex post-S8.7 carry list + reviewer carry list into 3 v1.0.4 items remediated and the rest as explicit v1.0.5 carries. S9 commits: web `3335a30` (M1 engine fan_multiplier upper-bound clamp at engine.js:1246+:2655, schema/engine asymmetry closure) + `2f0ccb8` (OBS-01 plate-API debt note in docs/3dpa-context.md per Codex accept-criterion + missing getCompatibleNozzlesForPrinter helper row added to API table); iOS `2207f8f` (engine.js byte-identical sync), `c82dac2` (Obs1 slow_layer_time row in iOS Advanced cooling — closes parity gap with web app.js:1253; AdvancedFilamentSettings.slowLayerTime + decoder + FilamentSettingsView ParamRow + Strings.slowLayer verbatim parity with web locales/en.json:134 + new XCTest pinning regex `\d+\s*s$` contract), `c99a797` (MARKETING_VERSION 1.0.3 → 1.0.4 + xcodegen). Internal reviewer GO: 0 Critical / 0 Important / 0 Medium + 2 Minor v1.0.5 carries (PLA-only test coverage tightening + NSNumber decoder dead-code path). **Reviewer-pattern now 7-for-7 across v1.0.4 arc** (S7-N + S8 + S8.5 + S8.7 + S9 all surfaced real findings). All gates green at ship: validate-data 6/6, walkthrough 11 v1.0.4 OK lines, matrix-audit 47196/0 broad + 55/55 curated, iOS XCTest 109/109 unit (108 prior + 1 new S9 `testV104_S9_iOS_Obs1_AdvancedCoolingExposesSlowLayerTime`). **Web pushed `d9e58fa..2f0ccb8` (auto-deploys to Cloudflare). iOS pushed `eeb2915..c99a797` (10 commits flushed; 0 ahead).** Earlier same-day: Phase 1.5 + Phase 2.1 + S8.5 + S8.7 all CLOSED; Codex post-S8.5 audit returned **GO for S9** with 0 Critical / 0 Important; S8.7 shipped the optional 2 Medium + 2 Low findings before the S9 ship gate per owner directive. S8.7 commits: web `746244e` (Low #1 — TDD-RED breadcrumb convention split into posture-specific rules in docs/ai-collab.md) + `48304d3` (Medium #2 — validate-data env `fan_multiplier` range `> 0 && <= 1`, Path A locked over Path B engine-change); iOS `41c9956` (Medium #1 — FilamentSettingsView cooling tab Advanced mode now reads env-scaled `advancedSettings.fanMinSpeed` + `coolingFanOverhang` instead of raw filProfile, mirrors web app.js:1244-1253; new XCTest `testV104_S8_7_CoolingUI_AdvancedSourceIsEnvScaled` pins source-of-truth contract; inverted-first RED demo verified) + `ad46fd3` (Low #2 — T5 x1c AMS-like sub-case mirrors web walkthrough negative assertions for `ams_lite_material_incompat` + `mcs_empty_no_multicolor`; inverted-first RED demo verified). Internal code-review subagent VERDICT: GO. 1 Medium (M1 — engine.js has no upper-clamp on `fan_multiplier > 1`; schema is stricter than engine on upper bound) + 4 Minor — none Codex-originated, all carry to v1.0.5 cleanup pass. Reviewer-pattern stays 6-for-6. iOS XCTest 108 unit (was 107 + 1 new S8.7 test). Web pushed; iOS local-only **7 commits ahead** of `eeb2915` (Phase 2 gate active). **S9 cold-starts on Phase 2.2 / Task 9 ship gate directly — no audit gate remaining.** Codex audit response at `codex/v1.0.4-audit/codex-2026-05-13-v1.0.4-audit-response.md`; triage 0 CRITICAL / 2 HIGH / 2 MEDIUM / 1 LOW / 1 OBSERVATION. Owner-approved scope: HIGH-01 + HIGH-02 + MEDIUM-01 + LOW-01 in v1.0.4; MEDIUM-02 + OBSERVATION-01 deferred to v1.0.5. **S7-4 shipped Codex LOW-01 (test-contract hardening for retired warning IDs) in two test-only commits:** `d863635` (impl) + `bc9c655` (tightening after internal code-review subagent caught a real Creality empty-MCS pairing bug). Added negative regression guards for `nozzle_too_small`, `creality_no_multicolor`, `k2_plus_cfs` on their successor positive cases — across both `scripts/walkthrough-harness.js` (4 new assertions + 1 new Creality empty-MCS sub-case on ender3_v3_se for manufacturer-gated regression coverage) and `scripts/profile-matrix-audit.js` (2 new `noWarning('k2_plus_cfs')` additions). TDD-RED demo on `nozzle_too_small` (deliberately inverted assertion → verified machinery → flipped). 10 cumulative v1.0.4 OK lines (same count, guards fold into existing HIGH-12 + MCS blocks); matrix-audit 47196/0 + 0 curated; validate-data clean. iOS untouched (`eeb2915`, Phase 2 gated). Owner pivoted finding order — S7-3 MEDIUM-01 is the only remaining Phase 1.5 web finding. **Next step:** S7-3 cold-starts on MEDIUM-01 — first-layer bed-clamp attribution honesty (engine.js:1227 `env.bed_first_layer_adj` + :1624-1631 warning copy + :1706-1727 bed-clamp warning attribution discipline). After S7-3 ships, Phase 1.5 closes and S8 Phase 2.1 iOS sync opens.

**2026-06-13 note:** Printer Intake tee activation is resolved. The deployed Worker was already writing remote KV; the false negative came from Wrangler KV reads without `--remote` in both the smoke recipe and Scout live-KV path. `scripts/printer-intake-scout.js` now passes `--remote` for list/get; Worker diagnostic logging was removed and the cleaned Worker redeployed as version `a55212e9-7998-4d20-a902-108dcb3a021c`.

**Evergreen project context:** [`../3dpa-context.md`](../3dpa-context.md) (architecture, engine API, app state shape, slicer routing, standing rules).
**Session history:** [`../sessions/INDEX.md`](../sessions/INDEX.md) — reverse-chronological one-line entries; full logs in `../sessions/`.
**Archive:** [`archive/`](archive/) — completed IR cycle, release-readiness history, completed milestones, legacy backlog.

---

## Current Snapshot

| Area | Status |
|---|---|
| **Web app** | Live worldwide at [3dprintassistant.com](https://3dprintassistant.com). Cloudflare Workers/Assets deploys from `main`. Feedback → Discord `#web-app-feedback` via `/api/feedback`. Owner analytics dashboard live at `/analytics` (admin token required) with side-by-side/filterable Web vs iOS views. |
| **iOS app** | **v1.0.4 LIVE on the App Store** (build `202605192119`; owner-confirmed 2026-06-04). v1.0.5 remains on TestFlight. The owner-approved next train is **1.1.0** (notifications + #2–#5 + native exports + bundled printers); **Tasks 0–10 are complete — the push provider is LIVE-DARK in production (registration on, public send off), the physical-device canary passed, and the owner authorized Task 11 (compose 1.1.0)** (see the 2026-07-23 evening banner). All 1.1.0 code work is local: web provider on `codex/ios-1.1.0-provider`, iOS on `codex/ios-1.1.0-release` (nine mirror commits + the release train + two canary fixes) under the iOS push gate; no TestFlight dispatch or App Review yet (Task 12 gates). Remote printer overlay **`content_version=2026071402`** is live with Aries, Mega X, Snapmaker 2.0 A350, FlashForge Creator 5 Pro, Creality Ender-3 V4 Combo, K2 SE, Elegoo Centauri Carbon 2, and Sovol SV06 ACE; Snapmaker U1 is also live from the 2026-07-18 ship. |
| **Engine** | Web is master — edit there, `cp` byte-identical to iOS. Walkthrough harness + iOS XCTest re-run after every engine/data edit. Latest profile-temperature audit added nozzle-cap clamps across simple output, advanced output, warnings, and export paths. |
| **Export** | Phases 2–4 are complete on web `main`: hardened Bambu JSON, verified Orca JSON (`f34e15c`), and verified Prusa INI (`3f923eb`). Owner import gates passed; iOS carries byte-identical local engine mirrors under the push gate. The approved 1.1.0 composition now includes user-visible native file export for all three slicers, subject to exact-TestFlight desktop import gates. |

---

## Active Release: v1.0.4 (LIVE on App Store) — v1.0.3 batch shipped

**v1.0.4** (accuracy/honesty release) is **LIVE on the App Store** — submitted 2026-05-20 (build `202605192119`, Manual Release), owner-confirmed approved + live 2026-06-04; now the live build, supersedes 1.0.3. See the current status and previous-update banners above + the v1.0.4 SHIPPED entry in Active Work Queue for the full arc. The v1.0.3 batch below is the prior shipped release, retained for history.

### v1.0.3 batch (shipped)

5-item v1.0.3 batch across web + iOS; **3/5 shipped into the live App Store build** (items 1 + 3 + 4 — see status table below). Owner pivoted from DQ-3 to this batch on 2026-05-08. Cross-AI collaboration remains available via the canonical [`ai-collab.md`](../../../ai-operating-model/docs/ai-collab.md) routing doc plus the 3dpa overlay in [`CLAUDE.md`](../../CLAUDE.md), but research/review packets are optional and risk-based, not a mandatory pilot workflow. Released build `202605101637` also includes the remote printer catalog hardening pass, profile-temperature/nozzle-cap audit fixes, and the Advanced filament temperature UI fix. Items 2 + 5 are post-v1.0.3 work unless owner explicitly reopens scope.

| # | Item | Platform | Status |
|---|---|---|---|
| 1 | Missing printers (Anycubic Kobra X correction + Elegoo Centauri Carbon) | Web + iOS | ✅ **SHIPPED 2026-05-08** — 4 commits per one-finding-one-commit-per-platform. Web auto-deployed; iOS picked up in TestFlight run [`25572470387`](https://github.com/mustiodk/3dprintassistant-ios/actions/runs/25572470387). Web `0f47b44` (Kobra X) + `8de9381` (Centauri Carbon); iOS `b0d1315` + `15930c1`. |
| 2 | Expanded environments taxonomy | Web + iOS | ⏳ Gemini handover #2 drafted at [`../research/gemini-environments-taxonomy-research.md`](../research/gemini-environments-taxonomy-research.md); not yet sent. Multi-day workstream. |
| 3 | iOS App Store review prompt | iOS | ✅ **SHIPPED 2026-05-08** — 6 Codex passes (1 design + 4 milestone + 1 post-implementation) + 1 Gemini pass. 5 caught-before-ship bugs. `ReviewPromptService` + `StoreKitDistributionDetector` + `ProfileKeyHasher` + 18 new XCTests (46 → 64). MARKETING_VERSION 1.0.3 + TestFlight dispatched (run [25557115706](https://github.com/mustiodk/3dprintassistant-ios/actions/runs/25557115706)). Design packet at [`../../codex/ios-review-prompt/codex-2026-05-08-ios-review-prompt-packet.md`](../../codex/ios-review-prompt/codex-2026-05-08-ios-review-prompt-packet.md); post-impl review at [`../../codex/ios-review-prompt/codex-2026-05-08-ios-review-prompt-implementation-review.md`](../../codex/ios-review-prompt/codex-2026-05-08-ios-review-prompt-implementation-review.md). |
| 4 | Analytics platform + event taxonomy | Web + iOS | ✅ **SHIPPED 2026-05-08/09** — privacy-preserving first-party analytics with exactly 3 events (`app_opened`, `profile_generated`, `feedback_opened`), no user/session/device IDs, no free text, no generated profile output. Web Worker + Analytics Engine binding shipped; iOS client + tests shipped as `303f571`; iOS output-mode parity shipped as `153adbc` and uploaded in TestFlight run [`25596797349`](https://github.com/mustiodk/3dprintassistant-ios/actions/runs/25596797349), build `202605090842`. Owner dashboard `/analytics` added 2026-05-09 and refined in commits `eb66b18` / `e9ed6b9` / `5c60549`: owner readout, Product lens (`All` / `Web` / `iOS`), iOS version-aware diagnostics, and mobile-friendly diagnostic rows. Spec: [`../specs/analytics-v1.md`](../specs/analytics-v1.md). |
| 5 | Web output-panel UX deep-dive | Web | ⏳ Not started. Claude direct (no Gemini per per-tool routing). |

### Phase status

| Phase | Status |
|---|---|
| Phase A — Printer changes | ✅ Shipped (item 1). Hold lifted 2026-05-08. |
| Phase B — Research handovers | 🟨 Partial (handover #2 drafted; #3 sent + responded; #4 intentionally implemented directly after privacy plan; item 5 doesn't need Gemini). |
| Phase C — Codex design packets | 🟨 Partial (item 3 design + post-impl complete; item 4 implemented with focused local tests; items 2 / 5 not started). |
| Phase D — Implementation | 🟨 Partial (items 1 + 3 + 4 implemented; items 2 / 5 not started). |
| Phase E — Ship cycle | 🟩 Released on the App Store 2026-05-11 after replacement build `202605101637` passed review. `High-speed` remains the Bambu picker label because it fits on iPhone SE and avoids brittle marketed speed numbers in picker chrome. Items 2 / 5 not yet built and parked post-v1.0.3. |

---

## Active Work Queue

> Live, actionable items. Pick the next one based on owner priority.

- **~~Export Phase 2 current-main recovery~~ ✅ MERGED + LIVE 2026-07-11** — PR #10 (`d333501`); export-only diff, owner-verify PASS, web gates green, iOS 138/138 + byte-identical local mirror, production marker curl-verified. `[Web + iOS-local]`
- **~~Export Phase 3 — OrcaSlicer native JSON export~~ ✅ MERGED + LIVE 2026-07-11** — PR #11 (`875953b`); owner import PASS on the official Ender-3 V3 SE 0.4 profile, web/iOS gates green, and production `exportOrcaJSON` + UI markers curl-verified. `[Web + iOS-local]`
- **[Export Phase 4 — PrusaSlicer .ini export — OWNER-VERIFY PASS 2026-07-11]** — verified CORE One L standard-0.4 + PLA allowlist, `[print]`/`[filament]` config-bundle serializer, web `.ini` download, Copy fallback elsewhere, audit 0 FAIL/0 warn, browser smoke, and byte-identical iOS mirror with 140/140 XCTest are complete. Owner imported both odd-value print and filament sentinel profiles successfully. See [`EXPORT-PHASE4-GATE-LEDGER.md`](EXPORT-PHASE4-GATE-LEDGER.md). `[Web + iOS-local]`
- **[iOS 1.1.0 notification release — SPEC RATIFIED; PLAN HOSTILE-REVIEWED `GO`; TASKS 0–9 SELECTED 2026-07-18]** — Native opt-in APNs notifications (`new printers` + `app updates`) through the existing first-party Worker with EU D1/Queue; #2–#5; visible native Bambu/Orca/Prusa exports; nine local printer/export commits; full privacy/copy/TestFlight/App Store gates. Replaces the unstarted 1.0.7/1.0.8 sequence. Tip jar stays separate. Spec review closed 3 P1 + 6 P2; plan review closed 5 P1 + 5 P2 one-per-commit and final authenticated Claude confirmation returned `GO` with no remaining P0/P1/P2. **Locked next:** execute Tasks 0–9, including Task 1's isolated web security push/deploy, then stop at G0. Do not provision Apple/Cloudflare resources, push iOS, or dispatch TestFlight before the retained owner gates. Design: [`../superpowers/specs/2026-07-18-ios-1.1.0-notification-release-design.md`](../superpowers/specs/2026-07-18-ios-1.1.0-notification-release-design.md) · plan: [`../superpowers/plans/2026-07-18-ios-1.1.0-notification-release-plan.md`](../superpowers/plans/2026-07-18-ios-1.1.0-notification-release-plan.md) · plan review: [`../reviews/2026-07-18-ios-1.1.0-notification-release/plan-review-disposition.md`](../reviews/2026-07-18-ios-1.1.0-notification-release/plan-review-disposition.md). `[iOS + narrow Web provider/analytics]`
- **[Web security P1 — Worker source/config publicly served — LIVE-VERIFIED 2026-07-18, FIX GATED TO 1.1.0 IMPLEMENTATION]** — Production currently returns 200 for `/wrangler.toml`, `/worker.js`, and `/functions/api/feedback.js`. No secret values were printed, but `wrangler.toml` exposes current binding identifiers and future D1/Queue ids must not be added while this remains open. First implementation gate: exact `.assetsignore` exclusions + Worker defense-in-depth denies + production non-200 probes before notification bindings/migrations. `[Web/Worker security]`
- **[Android v1 program — OWNER GATE AG0]** — triple-reviewed bundle ready (see banner). Owner: spec §2+§4 GO/override + Play account + prototype-commit push. After GO: AG1 foundation (mac-mini execution session — QuickJS spike first). `[Android + process]`
- **[My 3DPA merged platform plan — SYNTHESIS COMPLETE 2026-07-12, PARKED — VERSION SEQUENCE STALE AFTER 1.1.0 PIVOT]** — the locked product/architecture decisions and MG0–MG19 evidence remain parked, but their V3 “iOS 1.1.0” reservation and old 1.0.7→1.0.8 predecessor sequence are superseded by the notification release. **Owner next (only when deliberately resumed): re-sequence the iOS version, then MG0 ratification + SYN-17 cross-model review; no implementation or provisioning before both.** Decision set: [`../superpowers/specs/2026-07-12-my3dpa-merged-decision-set.md`](../superpowers/specs/2026-07-12-my3dpa-merged-decision-set.md) · plan: [`../superpowers/plans/2026-07-12-my3dpa-merged-plan.md`](../superpowers/plans/2026-07-12-my3dpa-merged-plan.md). `[Web + iOS + backend + Android later]`
- **[Accounts Platform (Claude candidate) — SYNTHESIZED 2026-07-12 into the My 3DPA merged plan above; PARKED AS EVIDENCE — AP0 superseded by MG0]** — the optional accounts/cloud sync/inventory decisions and completed review remain parked as evidence. Its iOS 1.1.0 and 1.0.7→1.0.8 sequencing claims are stale after the notification-release pivot and are not executable until MG0 re-sequences them. Spec: [`../superpowers/specs/2026-07-11-accounts-platform-design.md`](../superpowers/specs/2026-07-11-accounts-platform-design.md) · plan: [`../superpowers/plans/2026-07-11-accounts-platform-plan.md`](../superpowers/plans/2026-07-11-accounts-platform-plan.md) · review dispositions: [`../reviews/2026-07-11-accounts-platform-review.md`](../reviews/2026-07-11-accounts-platform-review.md). `[Web + iOS + backend]`
- **[Independent Codex My 3DPA proposal — SYNTHESIZED 2026-07-12 into the My 3DPA merged plan above; PARKED AS EVIDENCE]** — independently derived after the parallel Claude PR merged, without reading its same-task spec/plan/session artifacts. Recommends optional Firebase Apple/Google identity + existing Cloudflare Worker + EU D1, local-first PDM2, free Workshop sync, local-first event-sourced inventory, signed/encrypted account export, deletion/DR erasure ledger, and later lifetime Pro without subscriptions. The implementation plan is decomposed into atomic PR/operational gates from G0/O0/C0 through staging, lifecycle, owner canaries, public rollout, iOS/Android, inventory, and entitlement. **Review record:** architecture subagent `ZERO P0-P2`; QA subagent `ZERO P0-P2`; isolated Claude cross-model `VERDICT: ZERO P0-P2`. **Owner next:** compare this candidate with the AP0 bundle above and ratify one merged decision set before any account/platform implementation gate starts. Spec: [`../superpowers/specs/2026-07-12-codex-my3dpa-account-cloud-design.md`](../superpowers/specs/2026-07-12-codex-my3dpa-account-cloud-design.md) · plan: [`../superpowers/plans/2026-07-12-codex-my3dpa-account-cloud-plan.md`](../superpowers/plans/2026-07-12-codex-my3dpa-account-cloud-plan.md) · spec review: [`../reviews/2026-07-12-codex-my3dpa-spec-review.md`](../reviews/2026-07-12-codex-my3dpa-spec-review.md) · plan review: [`../reviews/2026-07-12-codex-my3dpa-plan-review.md`](../reviews/2026-07-12-codex-my3dpa-plan-review.md). `[Web + iOS + backend + Android later]`
- **[Monetization Phase 1 — tip jar — APPROVED PRODUCT DECISIONS, PARKED; VERSION MUST BE RE-SEQUENCED]** — Strategy remains ratified, but Track I is not in notification release 1.1.0 and its old 1.0.8 reservation is invalid after 1.1.0. Keep it separate; **1.2.0 is recommended** at its future owner gate. Owner prerequisites remain ASC Paid Applications agreement + banking/tax forms + Small Business Program enrollment. Plan: [`../superpowers/plans/2026-07-11-monetization-phase1-tip-jar-plan.md`](../superpowers/plans/2026-07-11-monetization-phase1-tip-jar-plan.md). `[Web + iOS]`
- **~~Intake Autonomy v2.1~~ ✅ MERGED 2026-07-10** — R0→R8 landed through PR #7 (`452fb94`) with green production build; do not restart. `[Web + automation + process]`
- **~~Printer intake — Snapmaker U1~~ ✅ SHIPPED + LIVE 2026-07-18** — Owner approved `series_group:"U Series"`, nozzle set `[0.2,0.4,0.6,0.8]`, and corrected prime/wiping-tower wording. Fresh run `run-20260718T112636Z` passed evidence plus `R1/R2 GO`, merged at `cc65622`, verified live overlay/picker, created local-only iOS mirror `cdf5906`, and closed custody at `732cbc8`. The candidate packet/branch/parked sidecar were removed only after successful custody. `[Web + iOS-local + automation]`
- **[Printer intake runner — protected notifier-config migration + PD8 freeze clearance] (2026-07-18, OWNER-GATED)** — The isolated-checkout installer did not carry the gitignored `scripts/.printer-intake.local.json`, so U1 shipped but the first report delivery failed and correctly self-froze. The existing mode-600 config is now present in the installed checkout and the saved U1 report was replayed successfully without a candidate rerun. **Owner next:** explicitly authorize deletion of the installed checkout's `scripts/.intake-autonomy-freeze`; until then the daily 12:00 run must fail closed. Implementation follow-up: TDD-harden `scripts/install-intake-runner.sh` to migrate or verify the protected config without printing its secret. `[Web automation + owner gate]`
- **~~Printer intake — ambiguous `i7_i` / `i7! I`~~ ✅ RESOLVED AS EXISTING `sparkx_i7` DUPLICATE 2026-07-17** — Exact catalog target is Creality `sparkx_i7`, display name `i7`, group `i Series`; production picker green. Owner closure appended `was-duplicate-missed` custody in `6113f04`, archived the parked evidence, and changed no catalog or overlay bytes. `[Web automation]`
- **~~Printer intake — Elegoo Centauri Carbon 2~~ ✅ SHIPPED + LIVE 2026-07-14** — Owner resolved both CANVAS fields; candidate `48f48a2` passed fresh R1/R2 `GO/GO`, merged at `954cfa3`, and is live in bundled web data plus remote overlay `2026071401`. Production picker and overlay passed; custody `88816bd` pushed; local iOS mirror `f9a810c` remains unpushed under the normal gate. Notifier object rendering was fixed in `2ae4ddc`; a clean zero-candidate control wrapper then passed `PREFLIGHT`/`POSTRUN` and recognized Centauri as already shipped. `[Web + iOS-local + automation]`
- **~~Printer intake synthetic E2E — Sovol SV06 ACE~~ ✅ AUTO-SHIPPED + LIVE 2026-07-14** — Production Discord/KV capture `req:1784025588168:43e000dc`; first run parked fail-closed on contract-invalid R1 and exposed a POSTRUN SHA-format defect. TDD fixes `c2fe6fd` + `16606a7`; clean sidecar re-entry passed evidence and fresh `GO/GO`, merged `3bba2c2`, live overlay/picker green at `2026071402`, local iOS mirror `80c26dd` unpushed, custody `54dc4c8`, POSTRUN green. `[Web + iOS-local + automation]`
- **~~Web finding — analytics Worker rejects 2 of web's own 5 events + no android auth path~~ ✅ DONE 2026-07-10** (`551b092` + `14cb97a`, TDD RED-first, hostile-reviewed GO ×2, **live-verified on prod**: `troubleshoot_used`/`export_clicked` now `200 stored:true`; android bad-signature returns `401 signature_mismatch` not 403). Design note: Analytics Engine caps 20 blobs and all 20 were assigned → **blob19 is now a shared per-event detail column** (feedbackCategory | symptom | export type; spec `docs/specs/analytics-v1.md` updated); dashboard gained Troubleshooter-symptoms + Export-clicks cards + `troubleshoot`/`exports` canned queries; `app.js` export sender key `printer`→`printerModel`. Android AG3's analytics prerequisite is met. Follow-ups spawned below. `[Web]`
- **[Web finding — 4 MORE selection events still 400 on every chip click] (2026-07-10, hostile-review catch)** — `app.js:570/1532/1535/1537` send `printer_selected` / `nozzle_selected` / `material_selected` / `use_case_selected`; none allowlisted → `400 invalid_event` on every selection (request noise + silent data loss; pre-existing, NOT part of the 5-event fix). **Owner decision:** allowlist them (closed-vocabulary ids, same privacy class — but keys `brand`/`group`/`use_case` have no blob columns → same shared-column design work) or delete the dead `track()` calls. `[Web]`
- **[Android pre-AG3 analytics follow-ups] (2026-07-10, review flags)** — (a) **shared-secret decision:** `FEEDBACK_HMAC_SECRET` gates BOTH Workers for BOTH native platforms; an APK-extracted secret couples rotation to shipped iOS versions → decide per-platform secrets or a documented rotation plan BEFORE a real APK ships; (b) `/analytics` dashboard platform lens is hardcoded web/iOS (android rows appear only in diagnostics tables) → dashboard pass rides AG3. `[Web + Android]`
- **[Web finding — feedback Worker 403s native non-iOS clients] (2026-07-09)** — `functions/api/feedback.js` HMAC-checks only `X-App-Source: ios`; anything else needs a browser Origin → native Android feedback would never reach Discord. Fix = auth-branch extension + first-class `android` source rendering. Android AG4 prerequisite. `[Web]`
- **~~Web finding — iOS overlay validator RED~~ ✅ DONE 2026-07-09** (`c381638`) — added the 1.0.7 bundled-catalog baseline (ground-truth iOS `51356de`; bundle byte-identical to 1.0.5, verified id-for-id). Validator green, self-test 13/13 (live-smoke case (e) RED→GREEN), collision union unchanged at {1.0.3, 1.0.4}. Overlay republishes are unblocked. `[Web]`
- **~~Learns-export branch verification~~ ✅ DONE 2026-07-06** — Bambu import test PASSED (real + sentinel), branch **merged to `main` `9e7890d` + live**. **W3 Mine-tier engine train is now UNBLOCKED** — this is the locked next entry point (plan Part 2: `docs/superpowers/plans/2026-07-06-impl-044-w3w4-plan.md`). `[Web + iOS-local]`
- **~~W3 Mine-tier engine train~~ ✅ MERGED + LIVE 2026-07-07** (see banner; [`MINE-TIER-GATE-LEDGER.md`](MINE-TIER-GATE-LEDGER.md) has the completed OWNER-VERIFY record). Custom filaments (W4, plan Part 3) remain a future train after the export sequence. iOS emitter key-preservation for `tuning`/`userMaterials` + regenerated byte-compat fixture: ✅ shipped in the iOS local commits. `[Web + iOS-local]`
- **[W3 follow-up — rules-table→remedy migration] (deferred by explicit W3-T7 decision)** — migrate `workshop-tuning-rules.js` magnitudes into structured `remedy` blocks in `data/rules/troubleshooter.json`; owner train = the iOS journal/harvest train (the first real second consumer of the magnitudes). Rationale recorded in the mine-tier ledger. `[Web + iOS data]`
- **~~[W3 follow-up — export retraction display honesty]~~ ✅ MERGED + LIVE via PR #10** — text/reference export now reads the resolved `_slicer_value`; web walkthrough + iOS RED→GREEN mirror pin the 0.2-nozzle `0.6` raw vs `0.4` resolved case. `[Web + iOS-local]`
- **[DQ finding — max_mvs 0.8mm-nozzle data gap] (2026-07-06, app/web accuracy)** — `data/materials.json` `max_mvs` (max volumetric speed) tables carry only `0.2/0.4/0.6` keys while `k_factor_matrix` carries `0.2/0.4/0.6/0.8` for the same 17 materials (`hips` also missing `0.2`). So 0.8mm-nozzle users get a fallback MVS instead of a material-specific value — a real accuracy gap surfaced by the export-audit engine-schema check. Fix = source + add the `0.8` (and hips `0.2`) `max_mvs` entries; re-run validate-data + walkthrough + golden snapshot; web+iOS byte-mirror. Data-only, low-risk. `[Web + iOS data]`
- **~~[Export Phase 2 — original stale branch]~~ superseded and safely recovered** — retained as evidence only; its five export commits were replayed onto current main and merged through PR #10 without stale deletions. `[Web + iOS-local]`
- **[Export follow-up — ironing display de-hardcode] (deferred at 2026-07-09 plan review, data-triggered)** — `engine.js:2789` hardcodes `'Enabled — Monotonic line'` while canonical type/pattern live in sidecars; with today's data (`ironing_type ∈ {no ironing, top}`, `ironing_pattern ∈ {null, monotonicline}`) the fix is untestable and pure churn. **Trigger: any second ironing vocabulary value entering `objective_profiles.json`.** `[Web + iOS]`
- **Phase-2 follow-ups (2026-07-06)** — (a) **owner: dispatch iOS v1.0.6 TestFlight** (`gh workflow run testflight.yml --ref main`) after deciding the data-mirror question in [`PHASE2-GATE-LEDGER.md`](PHASE2-GATE-LEDGER.md) I5 note; (b) ~~owner: IMPL-043 Phase 0 golden files~~ ✅ **DONE 2026-07-06** (Bambu+Prusa landed; Orca pending reinstall — needed for Phase 3 only); (c) **S1 — IMPL-042 Phase C landing pages** (spec ready, whole-or-nothing, own session); (d) iOS troubleshooter surface + Workshop journal train (I3 deferral); (e) ~~other machine: `git pull --rebase` in `3dprintassistant-ios`~~ ✅ rebase DONE 2026-07-09 on the mac-mini (3 data-mirror commits now sit on top of v1.0.7 `51356de`, conflict-free); the push itself stays held by the iOS push gate — flush with the next TestFlight-ready train. `[Web + iOS]`

- **Feedback-pipeline evolution (5 specs, S1→S5)** — turn the printer-intake pipeline from
  semi-automated into a learning, misroute-robust system. Brainstormed 2026-06-15 from 3 owner items;
  **ALL 5 specs + 5 plans QA-green** (write→sub-agent-review→patch→QA, autonomous 2026-06-15). **Execution status:
  S1 ✅ (v1.0.5 TestFlight), S2 ✅ (2026-06-16, Gates 1–2 pushed; Gate 3 deferred), S3 ✅ (2026-06-15), S4 ✅
  (2026-06-16, dormant-until-ledger), S5 unblocked + owner-gated.** Specs `docs/superpowers/specs/2026-06-15-s*`, plans
  `docs/superpowers/plans/2026-06-15-s*`. Reviews caught a real defect in every first draft (S2 v1 BLOCK
  pipe-disconnected; S3 CRITICAL sparkx-is-alias + config-off-served-path; S4 no-run-history-corpus +
  Assistant-permission-forbids-ledger; S5 wrong-S4-signal + cross-repo-PR + iOS-branch-push-hole).
  - **S1** iOS contextual-feedback prefill fix — ✅ **SHIPPED in v1.0.5 to TestFlight 2026-06-15** (`ae03510`).
    Migrated all 8 contextual entry points/6 screens from `.sheet(isPresented:)`+separate `@State` optional to
    `.sheet(item:)` + `FeedbackPrefill: Identifiable` + VM `init(prefill:)`. Real XCTest green (112 unit + 2 UITest,
    incl. genuine RED→GREEN). **Owner on-device acceptance pending.**
    ([spec](../superpowers/specs/2026-06-15-ios-feedback-prefill-fix-design.md) · [plan](../superpowers/plans/2026-06-15-s1-ios-feedback-prefill-impl-plan.md)). `[iOS]`
  - **S2** intake capture completeness — ✅ **EXECUTED 2026-06-16 (Gates 1–2), pushed (web auto-deploys).**
    Fixes the v1 BLOCK: Worker-side `functions/api/_lib/printer-mention.js` `extractPrinterMention` + a widened
    `/api/feedback` tee (general/feature/bug only — `missing*` excluded; structured brand/model so the Scout is
    **actionable not `unactionable`**; bounded matched span / 30d TTL / fail-open) (G1) · Scout lane provenance +
    order-independent **form-wins** `collapse()` + `heuristicCandidates` grouping (G2). Server-side → also catches
    iOS general-feedback mentions (no iOS binary). Absorbs **Scout finding 2**. Per-gate hostile review GO-WITH-NITS
    (a real **HIGH PII leak** + 3 precision MEDIUMs patched before push). Commits `bfcb7c8`+`9969cff`.
    **Gate 3 (optional web nudge) deferred — owner-pick** (spec §8 risk 8). **Open: owner live smoke** (printer-ish
    general feedback → a `lane:"heuristic"` KV entry). `[Web/automation]`
  - **S3** Scout dedupe/triage robustness — ✅ **EXECUTED 2026-06-15 (Gates 1–3).** Externalised the Scout's
    hardcoded guardrails into a versioned, validated `scripts/printer-intake-guardrails.json` + `scripts/validate-guardrails.js`
    (behaviour-preserving refactor, **byte-identical** — the config surface **S4's learning loop diffs into**) → brand-alias
    seeds `bmbulab→bambu_lab`/`sparkx→creality` (2026-06-14 false positives now dedupe) + conservative `possibleBrandTypo`
    hint + `report.version`→3 → model-suffix-strip (`i7 w/CFS`→bundled `sparkx_i7`). Absorbs **Scout finding 1**. Per-gate
    hostile review GO/GO/GO-WITH-NITS (real MEDIUM silent-degradation + LOWs patched + test-locked; 35-case Scout suite green).
    Commits `1a6e256`→`13f602b`→`bd23085` + runbook `ff7508e` + ai-om contract `ce19a05`. **Unblocks S4.** `[automation]`
  - **S4** Intake Retrospective (learning loop, **Approach A**) — ✅ **EXECUTED 2026-06-16 (Gates 1–3), pushed.**
    A reflective propose-and-approve pass (reuses lesson-spotter + Curator) proposing guardrail diffs into S3's config;
    learn between runs, **deterministic at runtime (no LLM in the Scout path).** Built: committed outcomes ledger
    `scripts/printer-intake-outcomes.jsonl` + Assistant permission amendment (G1); `scripts/validate-guardrails-diff.js`
    chaining S3's validator (G2); deterministic `scripts/intake-retrospective-gather.js` + owner-approved idempotent
    `scripts/apply-guardrails-diff.js` (provenance/tombstone/retire-value-match) + contract `ai-operating-model/docs/agents/intake-retrospective.md`
    + calibration ledger (G3). **Dormant until the outcomes ledger has real entries** (the Assistant appends one per
    processed candidate). Per-gate hostile review GO-WITH-NITS. Commits 3dpa `736a6a8`→`a23b834` + ai-om `b51c4b6`/`8710494`.
    **Unblocks S5.** `[automation]`
  - **S5** Assistant autonomy ladder — ⛔ **SUPERSEDED 2026-07-09 by Intake Autonomy v2** (owner
    directive: "fully automated is the goal"; S5's supervised design — owner merges every PR, and its
    human-free rung excludes overlay changes — structurally cannot ship a printer unattended).
    S5 spec+plan stay in the tree as reference; see the **Intake Autonomy v2** queue item above. `[Web + process]`
- **Printer Intake Automation** — turn missing-printer Discord feedback into a reviewed,
  shippable printer add. Designed 2026-06-12 via ai-om Agent Factory; spec = two
  contracts at [`../../../ai-operating-model/docs/agents/printer-intake-scout.md`](../../../ai-operating-model/docs/agents/printer-intake-scout.md)
  + `printer-addition-assistant.md`. **Phase 1 (tee) + Phase 2 (Scout) SHIPPED
  2026-06-13; tee activation fixed same day:** `functions/api/feedback.js` tees
  `missingPrinter` to remote KV namespace `PRINTER_INTAKE` (`f3d89a4e…`, bound
  in `wrangler.toml`); `scripts/printer-intake-scout.js` is the deterministic
  triage core — built, 28-test suite, dual-hostile-reviewed (sub-agent +
  bridge→Codex, 4 patch rounds), QA'd (43 scenarios → 38/43, 0 safety issues),
  and now reads live KV via `wrangler kv ... --remote` (TDD regression added).
  Root cause of the apparent tee blocker was the diagnostic/read path querying
  local/default Wrangler KV, not a Worker write failure; `wrangler tail` showed
  `env.PRINTER_INTAKE` present and `putOk:true`. Smoke keys were deleted; live
  queue verified empty. **Researcher rehearsal complete 2026-06-13:**
  [`codex/printer-intake-rehearsal/2026-06-13-researcher-rehearsal.md`](../../codex/printer-intake-rehearsal/2026-06-13-researcher-rehearsal.md)
  validated the Assistant gates on Voxelab Aries + adversarial
  resin/nonexistent/conflict cases. Aries is a legitimate FDM candidate but
  blocked before shipping pending explicit Voxelab new-brand sign-off, taxonomy
  choice, and conflicting-source review. **Evidence-rules hardening complete
  2026-06-13:** source hierarchy, conflict handling, confidence rules, assisted
  outcome classes, and deterministic Scout skeleton policy are now codified
  before real backlog seeding. **Process patched + Aries SHIPPED 2026-06-14.**
  The two NO-GO gaps are closed in the runbook + both agent contracts: a
  sanctioned `app-cap` path for unpublished `max_acceleration` (advisory-only in
  the engine; four conditions + reviewer trigger) and a scoped data-only iOS
  XCTest waiver for value-only adds. Aries re-ran green under an 8-gate plan
  (per-gate subagent review + QA; Gate 2 went NO-GO→patched→GO). Owner's official
  Voxelab manual sourced every field (FDM, 200×200×200mm, 250°C nozzle, **bed
  110°C beats reseller 100°C**, ≤180mm/s, glass plate, single 0.4mm nozzle); no
  published acceleration → `max_acceleration=5000` app-cap (lowest shipped
  bedslinger ceiling) with `notes[]` provenance. All gates green, risk review
  **GO**, owner visual-picker check OK. **Web pushed `9760f2d..60b231a`** (CF
  auto-deploys); overlay **`content_version=2026061401`** (voxelab + aries;
  i7 dropped — bundled in v1.0.4; **collision fix verified live 2026-06-14**,
  supersedes the never-delivered `2026061301`); iOS bundled mirror local-only (`7cf5b73`, push gate);
  ai-om contracts `7b5356c`. Plan/outcome:
  [`docs/superpowers/plans/2026-06-13-printer-intake-process-patch-and-aries-rerun.md`](../superpowers/plans/2026-06-13-printer-intake-process-patch-and-aries-rerun.md).
  **Real backlog run COMPLETE 2026-06-14:** 16 verbatim Discord entries seeded to
  KV → Scout screened **3 needs-research / 1 duplicate / 5 declined-non-fdm / 7
  unactionable**. **Anycubic Mega X SHIPPED** (the one genuine novel FDM add) — web
  `ebff194` (data + walkthrough combo 13) + `8ad4293` (overlay
  `content_version=2026061402`: mega_x under the bundled anycubic brand, aries
  preserved); **verified live** (`curl` → `2026061402 [aries,mega_x]`); iOS bundled
  mirror local-only (`9ea28c3`, push gate). Manufacturer-sourced (`max_acceleration=400`
  is a real Anycubic-firmware ceiling, not an app-cap); adversarial review GO; visual
  picker OK. The other 2 needs-research survivors were false positives the Assistant's
  catalog cross-check caught (X2D = bundled Bambu Lab X2D, "Bmbulab" typo defeated
  brand-dedupe; Sparkx i7 w/CFS = bundled Creality i7, "w/CFS" suffix defeated
  model-dedupe). Photon stays `declined-non-fdm`. Ship-path = web bundled data +
  additive iOS overlay (no TestFlight for current users). FDM-only governs.
- **[Scout finding 1 — dedupe robustness] (P2, non-blocking)** — brand typos
  (`Bmbulab`→`bambu_lab`) and model-variant suffixes (`i7 w/CFS`→`sparkx_i7`) defeat
  the Scout's dedupe, surfacing already-shipped printers as needs-research false
  positives. Fix: brand-alias / fuzzy-brand map + model suffix-stripping in
  `scripts/printer-intake-scout.js` (`norm()` / match path) + tests. Safety net held
  this run (the Assistant's catalog cross-check caught both). `[automation]`
- **[Scout finding 2 — general-feedback requests invisible] (P2, non-blocking)** —
  printer requests filed via the general-feedback form (not the Missing-Printer form)
  never reach the Scout (the tee only tees `category==="missingPrinter"`) and screen as
  unactionable. This run: "Kobra 3 Max", "kobra X", "voxelab Aries" (all already in
  catalog → no loss this time, but a NEW printer so-filed would be silently dropped).
  Fix options (owner-pick before building): (a) form-UX nudge routing printer mentions
  into the Missing-Printer form; (b) widen the tee + a low-precision heuristic pass over
  general feedback for owner review. `[Web + automation]`
- **Resin-scaling discovery (PoC, docs-only)** — started 2026-05-17 as a multi-goal Proof of Concept: (1) substantive resin sub-project discovery producing decision-grade material, (2) testing bridge as a multi-round quality amplifier, (3) honest autonomy ceiling probe. Resin product artifacts live at `docs/resin-scaling/` (**v4 problem-statement.md current** + parked survey v1); PoC meta-track lives at `../../../ai-operating-model/docs/autonomy-poc-2026-05-resin/` (charter, scorecard, 3 bridge-rounds + 3 round-analyses, owner-asks). **Audience is the owner, not 3dpa users.** v4 §7 is an Option A decision tree with Gate 2 evaluated first (Step 1 owner pick → Step 2 5 branches → Step 3 park rules with 4-week charter clock → Step 4 success criterion); §5 has a "Fires if" column making conditional-cost framing visible. **3 bridge rounds complete:** R1 changed v2's foundation, R2 caught structural+mechanical in v3, R3 caught only mechanical in v4 (1 MUST-FIX + 8 SHOULD-FIX). **v5 mechanical pass queued** but no R4 needed (mechanical fixes don't introduce new claims). 3 K1 findings filed across the arc documenting the operational autonomy ceiling; 1 K3 finding (bridge cwd-scope contract). Off-limits during discovery: live engine / data / UI changes to FDM product. **Next: owner-pick v5 mechanical pass + Gate 1 (`technical-differences.md`) desk research + `bridge/CLAUDE.md` cwd-scope standing rule preamble.** `[Web docs only]`
- **v1.0.3 batch items 2 / 5** — see Active Release section above.
- **v1.0.3 live monitoring** — version `1.0.3` is live since 2026-05-11. Monitor `/analytics`, feedback, Sentry, App Store reviews, and any owner/user reports from the remote printer overlay path.
- **v1.0.4 — SHIPPED to TestFlight 2026-05-15.** All planned phases closed: Phase 1 + Phase 1.5 + Phase 2.1 + S8.5 + S8.7 + S9 (bug-free ship arc). Monitor [TestFlight run 25892826819](https://github.com/mustiodk/3dprintassistant-ios/actions/runs/25892826819) for build acceptance + on-device feedback. v1.0.5 hygiene pass cold-starts on the carry-forward bundle (helper extraction across 4 math-duplication sites, m2 test rename, Min-1 PLA+PETG+ABS slow_layer_time test coverage, Min-2 NSNumber decoder cleanup, magic constants, mobile-card warning text length check, smoke assertion for emit-vs-claim parity, shared RETIRED_IDS const, walkthrough hardcoded baseline, MEDIUM-02 packet-text accuracy decision, **FDM-only scope copy on a user-facing surface — owner-pick web About/footer/iOS Settings; phrase as "FDM (filament extrusion) only today; MSLA / resin out of scope (future implementation)" to preserve optionality vs. the runbook's harder operational "no plans" stance**).
- **v1.0.4 config-impact implementation — (CLOSED)** Phase 1 + Phase 1.5 + Phase 2.1 + S8.5 + S8.7 + S9 all done. S8.7 commits: web `746244e` (Low #1) + `48304d3` (Medium #2); iOS `41c9956` (Medium #1) + `ad46fd3` (Low #2). All 4 Codex post-S8.5 findings fixed: TDD-RED breadcrumb convention tightened (now lives in `../../CLAUDE.md`; old `docs/ai-collab.md` is a pointer); validate-data range check `> 0 && <= 1` on env `fan_multiplier` (Path A locked — engine alignment); FilamentSettingsView cooling tab Advanced mode rewired to env-scaled `advancedSettings.fanMinSpeed` + `coolingFanOverhang` (mirrors web app.js:1244-1253); T5 x1c AMS-like sub-case mirrors web walkthrough negative assertions. Internal reviewer GO; 1 Medium (M1 engine upper-clamp asymmetry) + 4 Minor all carry to v1.0.5 — reviewer-pattern 6-for-6. Setup complete (S1, 2026-05-13): [`merged.md`](../reviews/2026-05-11-config-impact-qa/merged.md) is the owner-accepted queue (locked SHA `5bcd68b`); [`2026-05-13-v1.0.4-config-impact.md`](../superpowers/plans/2026-05-13-v1.0.4-config-impact.md) is the per-task implementation plan. Phase 1 commits: S2 Tasks 1/2/3 (web `133be38` → `b238a82`); S3 Task 4 (`bc070af` + `bf05586`); S4 Task 5 (`dc49c52` + `1695cba`); S5 Tasks 6/7 (`6f9e542` + `901153a`). **Phase 1.5:** S6 prepped audit packet (web `690519e`); owner ran Codex; response captured in S7-1 (`fe2964e`). Codex returned 0 CRITICAL / 2 HIGH / 2 MEDIUM / 1 LOW / 1 OBSERVATION. **Owner-triaged S7 scope (locked):** HIGH-01 + HIGH-02 + MEDIUM-01 + LOW-01 in v1.0.4; defer MEDIUM-02 + OBSERVATION-01 to v1.0.5. **S7-1 shipped Codex HIGH-01** (`eaf3f09`, fan/draft_shield export wiring). **S7-2 shipped Codex HIGH-02 (Option B)** in two commits: `a4b8873` impl + `f2aa84f` tightening after internal code-review subagent. Data: `safe_chamber_temp_max: 50` on all 5 PLA-family `enclosure_behavior` blocks. Engine: pla_heat_creep extended to all enclosed printers; getChecklist preheat-enclosure suppressed for PLA + enclosed; getWarnings env-verbatim filter for PLA + enclosed catches "Keep door closed" / "Preheat the enclosure" / "Extended preheat (...)" patterns. Tightening narrowed all three engine predicates to `material.group === 'PLA'` (preserves PVA's existing humidity posture); added vcold pin + PVA scope-guard harness assertions + validate-data range checks on `safe_chamber_temp_max` + `open_door_threshold_bed_temp`. 10 cumulative v1.0.4 walkthrough OK lines; matrix-audit + validate-data clean. **S7-4 shipped LOW-01** (`d863635` + `bc9c655` tightening) — test-only negative assertions for retired IDs + harness Creality empty-MCS sub-case. **S7-3 shipped MEDIUM-01** (`243be51` + `58d919b` tightening) — first-layer bed-clamp attribution honesty: env first-layer bed warning rewritten to compute effective post-clamp delta with three honest copy branches (full apply / fully clipped / partially clipped); paired `printer_max_bed_temp_clamped` initTarget now includes `bed_first_layer_adj` so cap-warnings fire when env pushes past printer cap (PETG + Kobra 3 Max + cold demonstrated the gap). Attribution discipline parity with MEDIUM-05. 11 cumulative v1.0.4 walkthrough OK lines. **Phase 2.1 closed:** iOS HEAD `fc3ee6b` carries byte-identical engine + data from web HEAD post-S8 (S8 commits `350d23f` initial sync + `fc3ee6b` tightening); iOS XCTest 107 unit + 1 UI. **S8.5 closed** the AdvancedFilamentSettings Codable extension (`fef685e` added `fanMinSpeed`/`fanMaxSpeed`/`coolingFanOverhang` for direct test access) alongside Codex post-Phase-1 audit remediation. **S9 (2026-05-15) closed the v1.0.4 arc as "(known) bug-free."** Owner pivoted from ship-gate-only to ship+all-known-bugs cleanup; triaged the Codex post-S8.7 carry list + reviewer carry list into 3 v1.0.4 items: **M1** web engine `fan_multiplier` upper-bound clamp at engine.js:1246+:2655 (`3335a30`, schema/engine asymmetry closure); **Obs1** iOS Advanced cooling missing slow_layer_time row — parity gap with web app.js:1253 (iOS `2207f8f` engine sync + `c82dac2` AdvancedFilamentSettings.slowLayerTime + decoder + FilamentSettingsView ParamRow + Strings.slowLayer + new XCTest with regex `\d+\s*s$` contract); **OBS-01** plate-API debt doc note at docs/3dpa-context.md (`2f0ccb8`, per Codex accept-criterion + missing `getCompatibleNozzlesForPrinter` row added to API table). MARKETING_VERSION 1.0.3 → 1.0.4 (iOS `c99a797`, xcodegen regenerated). Internal reviewer GO: 0 Critical / 0 Important / 0 Medium + 2 Minor v1.0.5 carries (Min-1 PLA-only test coverage tightening + Min-2 NSNumber decoder dead-code path). Reviewer-pattern **7-for-7** across the v1.0.4 arc. All gates green at ship: validate-data 6/6, walkthrough 11 v1.0.4 OK lines, matrix-audit 47196/0 broad + 55/55 curated, iOS XCTest 109/109 unit. **Web pushed `d9e58fa..2f0ccb8` (Cloudflare auto-deploy). iOS pushed `eeb2915..c99a797` (10 commits flushed; 0 ahead of `origin/main`).** TestFlight dispatched ([run 25892826819](https://github.com/mustiodk/3dprintassistant-ios/actions/runs/25892826819)). **Next step:** v1.0.4 live monitoring (TestFlight + App Store review + analytics + Sentry + feedback) OR v1.0.5 hygiene pass cold-start — see active-queue v1.0.4 SHIPPED entry above for the carry-forward bundle.
- **Profile/data change gate** — use [`../runbooks/profile-data-change-test-protocol.md`](../runbooks/profile-data-change-test-protocol.md) before future engine/data/warning/export pushes. Default gate: `validate-data`, profile matrix audit, iOS sync/tests when relevant, and a tiny UI smoke only for visible changes.
- **Remote printer catalog operations** — for any printer addition, republish, or deprecation, follow [`../runbooks/printer-addition-protocol.md`](../runbooks/printer-addition-protocol.md). Source of truth = bundled `data/printers.json`; overlay is an additive same-day patch, not an alternative path. Latest live overlay is `content_version=2026071402` (Voxelab Aries + Anycubic Mega X + Snapmaker 2.0 A350 + FlashForge Creator 5 Pro + Creality Ender-3 V4 Combo + Creality K2 SE + Elegoo Centauri Carbon 2 + Sovol SV06 ACE). The validator is **Part-C-aware** (`FIRST_OVERRIDE_MERGE_VERSION="1.0.5"`): it collides overlay ids only against baselines in `[min_app_version, 1.0.5)`, since v1.0.5+ override-merge by id. See Phase 4b in the protocol. Keep remote payload data-only: brands/printers only, no engine/rules/materials/nozzles/UI. Current iOS applies fetched overlays on the next launch.
- **Analytics dashboard observation** — after real traffic accumulates, read `/analytics` (admin token) using the Product lens: `All` for side-by-side Web/iOS comparison, `Web` or `iOS` for single-surface inspection. Watch generated profiles, app opens, feedback opens, release adoption, profile combinations, mode mix, printers/materials, and mobile diagnostics. Ignore setup rows with appVersion values like `setup-test`, `setup-test-2`, `setup-output-mode` from 2026-05-09 setup.
- **`[CRITICAL-001-followup]`** — Worker `/api/feedback` currently routes iOS + web to single `DISCORD_WEBHOOK_URL` → `#web-app-feedback`. Branch on `payload.context.appSource === "ios"` to a separate `DISCORD_WEBHOOK_URL_IOS` env var so iOS feedback lands in `#ios-app-feedback` as originally intended. Scope: ~15 LoC in `functions/api/feedback.js` + new Cloudflare secret + new iOS-channel webhook + redeploy. No iOS binary change. v1.0.3-safe. Filed 2026-04-23. `[Web/Worker]`
- **`[LOW-011]` Feedback email visibility** — (a) web copy parity helper text under email field matching iOS copy ("Leave it blank to stay anonymous. If provided, we may reply.") + new `fbEmailHelp` key in en/da; (b) Worker reorder of `Reply-to email` to top of Discord embed (currently `functions/api/feedback.js:259`). ~10 LoC web-only. Noticed 2026-04-27 after first iOS feedback received without email. `[Web]`
- **`[LOW-005]`** — `prc_0.2` keep + add siblings, or drop. Owner decision (product-taste).
- **`[MEDIUM-018]` Part B** — `nozzles.json` ID-vs-group convention. Owner decision.

---

## Deferred / Parked Work

> Real items, parked behind another decision or work item. Promote to Active Work Queue when the gating condition resolves.

### v1.0.4 config-impact findings (parked behind `merged.md`)

Surfaced 2026-05-11 cold-start and configuration-impact QA. Both independent passes and both cross-pass reviews are complete. Hold implementation until `docs/reviews/2026-05-11-config-impact-qa/merged.md` is created as the owner-accepted queue.

- [ ] **Wire `environment_options[].bed_first_layer_adj` into `getAdvancedFilamentSettings`.** Currently DEAD — defined in `data/rules/environment.json` but never read by `engine.js`. Cold env's intended +7°C first-layer bed bump silently disappears. ~5 LoC engine fix; iOS engine sync; new walkthrough assertion + iOS XCTest. `[Web+iOS]`
- [ ] **Material-aware environment compensation.** Current `nozzle_adj` / `bed_adj` are scalars applied uniformly across all materials; community guidance is material-group-specific (PLA needs less, PETG/ABS/ASA/PC need more). Engine extension shape: either `environment_options[].nozzle_adj` becomes a `by_group` map (`{PLA: 5, PETG: 10, ABS: 10, PC: 15, default: 5}`), or per-material `env_compensation` block in `materials.json`. Needs sourced research before values are picked — Gemini handover candidate, mirroring [`gemini-environments-taxonomy-research.md`](../research/gemini-environments-taxonomy-research.md). `[Web+iOS]`
- [ ] **Core v1.0.4 must-fix set.** Strength `speed_multiplier`; environment `fan_multiplier` / `force_draft_shield` / warning-copy parity; printer nozzle and plate guards; AMS Lite and practical multicolor tiers; chamber safe-cap guard-only; nozzle min-diameter warning cleanup. `[Web+iOS]`
- [ ] **Rest backlog.** Advisory chip-copy lane (`decorative`, `special`, `advanced`, `miniature`, `filament_condition`), surface ironing data/runtime single source, material-value research lane (`shrinkage_factor`, moisture/storage humidity, plate `bed_temp_range`, cold-env magnitudes), and lower metadata fields. `[Web+iOS/docs]`

### Post-v1.0.3 safe-follow-up todo list

These are Claude/Codex review findings explicitly classified as **not release blockers** for v1.0.3. Do not disturb the live v1.0.3 binary solely for these; schedule them into v1.0.4 or later unless owner priority changes.

| Priority | Item | Scope |
|---|---|---|
| P1 | ~~**CR3-M1:** fix `scripts/profile-matrix-audit.js` `DEFAULT_STATE.level` -> `userLevel` so beginner paths are actually covered.~~ ✅ **Shipped 2026-05-13** (web `8ff25eb`) as part of S1 v1.0.4 setup. | Web/test |
| P1 | **CR3-L1 / CR3-O4:** add the overlay validator to the profile/data runbook when overlay files change, and remove stale hand-maintained test counts from runbook examples. | Docs |
| P1 | **O7:** replace drifting test-count claims in `docs/3dpa-context.md` and `3dprintassistant-ios/CLAUDE.md` with "see CI/current test run" style wording. | Docs |
| P1 | **O8:** schedule or consciously defer aging active-queue items `[CRITICAL-001-followup]` and `[LOW-011]` so the active queue stays live. | Planning |
| P2 | **CR-M2 / M-D:** add local logging or clearer error taxonomy for remote-catalog cache/app-version rejection paths. | iOS |
| P2 | **M-B:** decide equal-`content_version` overwrite policy before any non-empty production overlay is used. | iOS/spec |
| P2 | **M-C / M-E / CR-L3:** tighten launch-refresh concurrency docs/tests if a manual refresh/retry path is added. | iOS/tests/spec |
| P2 | **CR-M6 / L-C:** make the overlay validator less dependent on sibling iOS repo layout before adding it to CI. | Web/scripts |
| P3 | **M-A:** align pathological semver overflow behavior between Node validator and Swift runtime. | Web+iOS |
| P3 | **CR1-M1:** replace string-matched warning suppression with structured warning metadata before broader i18n/copy drift. | Web+iOS |
| P3 | **CR1-M4:** guard advanced bed-temp clamp against missing future `bed_temp_max` values. | Web+iOS |
| P3 | **CR3-M2:** isolate iOS catalog/cache tests from sandbox cache state. | iOS/tests |
| P3 | **L-A / L-B / L-D / L-E:** clean low-risk `PrinterCatalogProvider` performance/source-tag/test-name/cold-start polish if remote catalog grows. | iOS |
| ✅ Done | **Overlay collision hardening — SHIPPED in v1.0.5 (2026-06-15, `af4bbe0`).** Dropped both all-or-nothing `isDisjoint` guards in `PrinterCatalogProvider.validatePayload`; a colliding overlay id is now overridden by id (`mergedArray`) instead of dropping the **whole** overlay. TDD: the 2 reject-on-collision tests rewritten to assert override-merge (RED→GREEN) + a whole-entry assertion; 112 unit + 2 UITest green. Spec Part C: [`../superpowers/specs/2026-06-14-ios-overlay-aries-collision-fix-design.md`](../superpowers/specs/2026-06-14-ios-overlay-aries-collision-fix-design.md). | iOS |
| P3 | **CR1-L1:** document or correct Bambu export inheritance placeholders before export UI is re-enabled. | Web+iOS/export |
| P3 | **O-A / CR1-L6-O-D / CR3-O2 / CR3-O3:** spec/audit/UI-order polish: `min_app_version` ops note, `expires_at` reserved note, H2D Pro ordering, and audit desc-builder duplication. | Docs/web |

### Phase DQ — Data Quality & Pro-Relevance (sub-phases 3-5)

**Master spec:** [`../specs/IMPL-041-data-quality-phase.md`](../specs/IMPL-041-data-quality-phase.md). Goal: every number traceable; Safe vs Tuned profiles; pro-tier fields (PA/LA, material-specific retraction, structured cooling) on both platforms.

**Progress:** 2/5 shipped (DQ-1 + DQ-2 — see [`archive/2026-04-release-readiness-history.md § DQ-1 / DQ-2`](archive/2026-04-release-readiness-history.md)). DQ-3/4/5 deferred to post-v1.0.3 ship.

#### DQ-3 — Pressure / Linear Advance per material
- [ ] `scripts/scrape/` — Node.js scraper for 6 sources (Bambu wiki, Prusa KB, Polymaker, Prusament, Overture, eSun). Owner runs locally. `[Code-once]`
- [ ] `docs/research/scraped/` — raw dumps committed for reproducibility. `[You to run]`
- [ ] Gemini cross-reference + propose values with confidence tiers. `[You, external tool]`
- [ ] `materials.json`: `pressure_advance` map per printer × nozzle, provenance-tagged. `[Web]`
- [ ] `engine.js`: emits `pressure_advance` (Bambu/Orca) / `linear_advance_factor` (Prusa). `[Web]`
- [ ] Walkthrough: ≥ 3 combos assert PA/LA presence + sanity range. `[Web]`
- [ ] Coverage floor: top 10 materials × 4 mainstream printer series; `_default` for all 18 materials. `[Web]`
- [ ] iOS sync + XCTest. `[iOS]`
- [ ] Folds in `[MEDIUM-019-followup]` (0.8mm `max_mvs` gaps for 16 mainstream materials + HIPS 0.2mm — same scraper pass produces both). `[Web+iOS]`

#### DQ-4 — Retraction deltas per material
- [ ] `materials.json`: optional `retraction` override (distance_multiplier, speed, provenance). `[Web]`
- [ ] `engine.js`: `_resolveRetraction(printer, nozzle, material)` rule. `[Web]`
- [ ] Safe tier keeps current model; Tuned applies override. `[Web]`
- [ ] Coverage floor: TPU 85A/90A, PETG, ASA, PC, PA, PLA-CF. `[Web]`
- [ ] iOS sync + new XCTest (`retraction(PETG) > retraction(PLA)` on same printer/nozzle). `[iOS]`

#### DQ-5 — Cooling curves
- [ ] `materials.json`: `cooling` block (fan min/max, slow_down_layer_time, overhang fan, close_fan_first_n_layers). `[Web]`
- [ ] `engine.js`: emits into slicer-specific fields via `SLICER_PARAM_LABELS`. `[Web]`
- [ ] `app.js` + iOS `PrintProfileTabView`: new "Cooling" section. `[Web+iOS]`
- [ ] Coverage floor: cooling block for all 18 materials; Safe/Tuned differentiation for PLA, PETG, ASA, TPU minimum. `[Web+iOS]`

### IR-3 — Failure-mode rehearsal

> "What happens when this breaks" work. Owner-deferred. ~1 session.

- [ ] Force `engine.init()` to throw on iOS (corrupt or remove a bundled data file locally). Verify error UI + Sentry event. `[iOS]`
- [ ] Force `/api/feedback` 500. Verify web modal error + iOS error. `[Web+iOS]`
- [ ] Simulate malformed `printers.json` deploy on a preview branch; verify site doesn't brick. `[Web]`
- [ ] Rollback rehearsal: web `git revert` + push → verify auto-deploy serves old; iOS TestFlight expire-build. Document in new runbook. `[Both]`
- [ ] Pull production Sentry + Cloudflare Analytics 14-day baselines. `[You]`
- [ ] Create `docs/runbooks/incident-response.md` with rollback procedures, Sentry + CF links, current baseline. `[Code]`

### IR-1 — Walkthrough harness second-pass coverage

> Add combos to `COMBOS[]` in [`scripts/walkthrough-harness.js`](../../scripts/walkthrough-harness.js) and re-run when needed.

- [ ] Environment presets (cold / hot / damp).
- [ ] Non-`none` support presets.
- [ ] Multi-color / AMS combos.
- [ ] `calcPrintTime` accuracy.
- [ ] `calcPurgeVolumes`.
- [ ] `getTroubleshootingTips` output.

### Phase 2.7a — Bambu Studio export re-enable ✅ COMPLETE

> IMPL-036 was absorbed by IMPL-043. Phase 0+1 merged in `9e7890d`; Phase 2 hardening merged/live through PR #10 (`d333501`) with recorded owner imports, `0 FAIL / 0 warn`, and iOS 138/138. The display-only ironing split remains intentionally deferred until a second ironing vocabulary value exists.

- [x] `objective_profiles.json`: canonical export fields added. `[Web]`
- [x] `engine.js` resolveProfile(): canonical `_slicer_value` sidecars drive export. `[Web]`
- [x] `engine.js` exportBambuStudioJSON(): capability-valid patterns, ironing pattern, 5-option support map, dual arrays. `[Web]`
- [ ] SLICER_TABS + SLICER_PARAM_LABELS display-only ironing split — intentionally deferred until a second vocabulary value exists. `[Web]`
- [x] Real + sentinel Bambu Studio import gates recorded as PASS. `[Web]`
- [x] Web export active; Phase 2 Beta badge removed. `[Web]`
- [x] Engine synced byte-identically to iOS; 138/138 XCTest, local under push gate. `[iOS-local]`

### IR-deferred export-path findings (re-activate when export UI re-enables)

- [x] **[HIGH-001]** scaled retraction exported from resolved `_slicer_value`.
- [x] **[MEDIUM-006]** non-canonical string fallback removed from the active passthrough path; legacy path remains only as the emergency switch.
- [x] **[MEDIUM-008]** pattern values use the canonical sidecar/capability mapping pipeline.
- [x] **[LOW-007]** process/filament versions hoisted to separate module constants from golden evidence.
- [x] Live Bambu Studio import tests: real + sentinel profiles, including Phase 2 P1S dual-array gate.
- [x] **v1.0.4 env-data export propagation** — env-scaled fan and `draft_shield` export wiring are covered by the walkthrough.

### Phase 2.7b — Web rendering of explanatory descriptions

> Engine + data complete (see [`archive/2026-04-release-readiness-history.md § Phase 2.7b`](archive/2026-04-release-readiness-history.md)); web UI rendering not yet wired.

- [ ] Render filter option `desc` as subtitle under chips/options. `[Web]`
- [ ] Add tab description text at top of each output tab. `[Web]`

---

## Backlog

> Unscheduled. Pick from here when current release work is done and the iOS app is stable.

### iOS post-release refactoring (PR-*)

| ID | Item | Notes |
|---|---|---|
| PR-1 | Stronger typing for AppState | Enums for environment, support, colors, user level, slicer IDs. `[iOS]` |
| PR-2 | View models for major screens | `OutputViewModel` already extracted (BR-6b). Remaining: `GoalsViewModel`, picker view models. `[iOS]` |
| PR-3 | `Codable` bridge decoding for remaining EngineService paths | Replace residual manual JSON parsing with `Codable`. (Was originally tied to RB-4; deferred — `ExceptionStore` solved the silent-failure problem without requiring Codable.) `[iOS]` |
| PR-4 | Navigation architecture refinement | Deeper route state, restoration support, coordinator pattern if needed. `[iOS]` |
| PR-5 | Accessibility & visual polish | Dynamic Type review, VoiceOver labels, contrast audit, tap targets. iPad layout optimization. Animation consistency pass. `[iOS]` |
| PR-6 | Design system separation | Split `SharedComponents.swift` into typography, feedback, settings, utility. `[iOS]` |
| PR-7 | UI prototype items | Advanced rows stagger reveal — cascade fade-in on mode switch. Consistent pill animation on native segmented controls. `[iOS]` |

### Future features

| # | Feature | Scope | Notes |
|---|---|---|---|
| **v1.1 candidate** | Light mode / system appearance support (iOS) | Large | Web already has both. iOS dark-only because `ColorTheme.swift` was hardcoded dark during MVP. Spec: add `light` variants for all 10–15 `ColorTheme` tokens; switch hardcoded `Color(hex:)` to environment-aware; remove the 4 `.preferredColorScheme(.dark)` calls; sweep contrast in light; decide system-follow (default) vs explicit toggle. |
| #001 | AMS Purge Volume Calculator | Medium | Previously shipped on web, needs restore. |
| #026 | Smart Simple/Advanced Configurator Mode | Medium | Input-side simple/advanced split. |
| #006 | Parameter Tooltip / Info Panel | Medium | `param-info.json` with increase/decrease effects. |
| #005 | Shareable Profile URL | Small | Encode state as URL params. |
| #010 | More Printer Support | Medium | A1 Combo, MK4, XL, Voron, K1 Max. |
| #011 | More Material Support | Medium | PLA+, Sparkle, PA12-CF, PPA-CF, ABS-GF. |
| #012 | Saved Presets (localStorage) | Medium | Name + save filter state. |
| #019 | Multi-Language Expansion | Medium | DE, NL, SV. |
| #029 | Source Attribution for Recommendations | Medium | Brand/community/calculated badges (effectively shipped via DQ-1 provenance — keep as nice-to-have for explicit display in non-Advanced views). |
| #037 | macOS App (WKWebView wrapper) | Medium | **Decided 2026-04-17: Path 3** — SwiftUI + WKWebView loading bundled offline assets via `loadFileURL`. Required mitigations: `sync-web-assets.sh` build phase; `feature-detect location.protocol === 'file:'` in `feedback-form.js` to submit to absolute `https://3dprintassistant.com/api/feedback` when running from bundle; document data-sync rule. New folder: `3dprintassistant-macos/`. Kickoff prompt: `docs/prompts/macos-app-kickoff.md`. |
| #016 | Troubleshooter Expansion (20+ symptoms) | Large | Expand from 8 to 20+. |
| #018 | Print Time Estimator Improvements | Large | Accel modeling, preset comparison. |
| #020 | Filament Database / Brand Profiles | Large | Brand-specific temp/speed overrides. |
| #015 | Community-Contributed Profiles | Large | GitHub PR-based. |
| #022 | User Profiles & Community Layer | Large | **Accounts + sync + profile hub formalized 2026-07-12 as the Accounts Platform spec/plan (see Active Work Queue).** Remaining under this ID: the community/sharing layer (public profiles, shared profile browsing) — builds on the accounts rails. |
| #039 | Email magic-link auth + passkeys | Medium | Post-accounts-v1 auth expansion. Magic link needs an email provider (Cloudflare Email Service candidate); passkeys ride the same user records. |
| #040 | bambuinventory → 3dpa inventory import bridge | Small | One-time JSON import; `import_meta` passthrough + `source:'import'` already reserved in the inventory schema, so the bridge is lossless. |
| #041 | AMS/RFID-aware inventory + low-stock notifications | Large | Port bambuinventory's two-band color matcher + ambiguity gate; requires a data path from printer/AMS (out of accounts-v1 scope). |
| #042 | Cost-per-print estimates | Medium | Spool price × estimated usage on the output page. **First engine-adjacent inventory feature — requires the mandatory data/logic-change evaluation** (engine untouched in accounts v1). |
| #043 | Filament drying tracker | Small | Journal-adjacent: log drying sessions per spool; ties into materials.json drying data. |
| #044 | Hosted engine API | Large | engine.js as a public API (API keys on the accounts rails). Long-standing extraction goal; accounts platform provides auth + rate-limit plumbing. |
| #045 | Spoolman import/export adapter | Small | Portable inventory interoperability after the native inventory contract stabilizes; no credentialed live sync in the first gate. |
| #046 | QR / spool label printing | Medium | Generate local labels only after inventory usage proves demand; keep scanning/label formats portable. |
| #047 | Account security and device history | Medium | User-visible registered-device/session history, revocation, key-rotation state, and recovery guidance on My 3DPA. |
| #048 | Owner-local printer/AMS bridge | Large | Optional local bridge for consumption, AMS assignment, and humidity data; requires a separate credential/network threat model and never runs through the public Worker by default. |
| #038 | Windows App | Large | Tauri (preferred over Electron — ~10MB vs ~200MB). Only after macOS port (#037) validates desktop demand. Deferred to post-v1.2. |

### Technical debt

| # | Item | Scope | Notes |
|---|---|---|---|
| #032 | JSDoc Typedefs for Engine | Small | Zero runtime cost, VS Code autocomplete. |
| #033 | Engine Modularization (ES Modules) | Medium | Only if engine keeps growing. |
| #034 | app.js Render/Event Refactor | Large | Deferred — high risk, low near-term reward. |
| #035 | CI Pipeline (web) | Small | ESLint + data validation. When 2nd contributor joins. |

---

## Standing Planning Rules

> Rules that affect planning decisions specifically. Engine / data behavior rules live in [`../3dpa-context.md`](../3dpa-context.md); session-lifecycle / project-organization rules live in `/Users/mragile.io/dev/Claude/Projects/CLAUDE.md`.

- **Web is master; iOS mirrors `engine.js` + `data/*.json` byte-identical.** Walkthrough harness + iOS XCTest re-run after every engine/data edit.
- **Data/logic-change evaluation (mandatory).** Every engine/data improvement must include an evaluation of whether web + iOS UI need changes to best use it.
- **One finding = one commit per platform.** Web + iOS commits with matching subject + tag. Don't bundle unrelated findings.
- **iOS push gate.** No iOS push to GitHub `main` until ship-ready for TestFlight (all planned changes landed, XCTest green, walkthrough green, MARKETING_VERSION bumped). Local commits between findings are fine.
- **ROADMAP is live planning truth.** Detailed history archive is `archive/`; spec docs are `../specs/`; per-session decisions are `../sessions/`.

---

## Archive Index

| Archive file | Covers |
|---|---|
| [`archive/README.md`](archive/README.md) | Archive entry point + conventions. |
| [`archive/2026-04-internal-review-tracker.md`](archive/2026-04-internal-review-tracker.md) | IR-0 through IR-5 + IR-deferred + IR tracking table from the 2026-04-20 internal review cycle. |
| [`archive/2026-04-release-readiness-history.md`](archive/2026-04-release-readiness-history.md) | Release blockers (RB-1…5), pre-release polish (BR-1…12), Phase 2.7b descriptions (engine done), DQ-1 + DQ-2 shipped narratives, PR-8 web retirement. |
| [`archive/completed-milestones-and-legacy-backlog.md`](archive/completed-milestones-and-legacy-backlog.md) | Completed phases table, Legacy backlog `#001–#036` ID index, Research & prompts index. |
| [`archive/2026-04-asc-upload-kickoff-prompt.md`](archive/2026-04-asc-upload-kickoff-prompt.md) | Pre-launch ASC upload kickoff prompt (2026-04-14/15). |

**Other canonical lookups:**
- Internal review findings (canonical text): [`../reviews/2026-04-20-internal/`](../reviews/2026-04-20-internal/)
- HIGH-003 external Codex review kit: [`../../../3dprintassistant-ios/docs/reviews/2026-04-23-high-003-codex/`](../../../3dprintassistant-ios/docs/reviews/2026-04-23-high-003-codex/)
- v1.0.3 batch Codex packets: [`../../codex/`](../../codex/)
- Implementation specs: [`../specs/`](../specs/) — IMPL-036, IMPL-039, IMPL-040, IMPL-041.
- Pre-ROADMAP `BACKLOG.md` (last real content 2026-04-03): [`../../../_archive/3dprintassistant-BACKLOG-legacy-2026-04-03.md`](../../../_archive/3dprintassistant-BACKLOG-legacy-2026-04-03.md).
