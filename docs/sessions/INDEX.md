# Session log index

**Rolling table of contents for `docs/sessions/`. Reverse chronological — newest first.**

Each entry is a single line: `date — topic — one-line outcome`. Read the full log when the outcome references something the current task depends on.

**Protocol:** every session end, append a new bullet at the top. See `CLAUDE.md → Session-log protocol`.

---

- **2026-05-13** — v1.0.4 arc restructure ([log](2026-05-13-cowork-appdev-v1.0.4-arc-restructure.md)) — Meta-session between S4 and S5 (not numbered as Sn). Split Phase 2 into 2.1 (engine + data sync + iOS XCTest as one iOS local commit) and 2.2 (UI walkthrough + MARKETING_VERSION bump as second iOS local commit + 5-point ship-ready handoff); inserted new Phase 1.5 — Codex audit gate (owner-gated Option B low-touch dispatch) between Phase 1 and Phase 2.1 per `docs/ai-collab.md` criteria 1/2/3/5. Plan-review-confirm-execute discipline applied: Plan agent returned 2 BLOCKERs + 5 SHOULD-FIX + 4 NICE under HIGH confidence; all addressed in single commit `12fd33c`. Stop conditions for Phase 1.5: ≥1 CRITICAL or ≥5 HIGH → owner pause; 1-4 HIGH → autonomous web-only remediation; MEDIUM/LOW → owner-triaged. Cold-start surfaced a real iCloud-sync race (MacBook saw phantom drift on `engine.js` + `walkthrough-harness.js` while small files had already synced); diagnosed via reflog `commit:` entries proving shared `.git/` between iMac and MacBook + mtimes preserved across sync. Drift evaporated mid-investigation as iCloud caught up — protocol's cold-start `git status` guard caught it correctly, no protocol gap. Commit `12fd33c` unpushed: MacBook lacks GitHub HTTPS credential helper; owner deferred push + identity fix.

- **2026-05-13** — v1.0.4 S4 — Task 5 5-tier MCS resolver ([log](2026-05-13-cowork-appdev-v1.0.4-s4-impl.md)) — Practical multicolor 5-tier resolver shipped to web as `dc49c52` + `1695cba`. New `_mcsTier(printer)` returns `none`/`ams_lite`/`ams_like`/`cfs`/`generic_non_ams`. Empty-MCS printers (centauri_carbon) suppress prime_tower + flush_into_infill + AMS checklist; A1/A1 Mini gate by material.ams_lite_compatible; CFS/CFS Lite/MMU3/ACE/IDEX/Toolchanger/Filament Hub get tier-appropriate guidance copy. Retired Creality-only manufacturer hardcode + k2_plus_cfs. Walkthrough has 6 cumulative v1.0.4 OK lines (9 sub-assertions in MCS block). Fixup also closed the curated audit drift (k2_plus_cfs → mcs_tier_cfs_guidance) and polished generic_non_ams sysLabel to use display-name map ('Filament Hub' / 'Toolchanger' / etc.) instead of underscore-bearing UPPERCASE. iOS untouched. 5 of 7 Phase-1 web tasks shipped → S5 picks up Task 6 (chamber safe-cap).

- **2026-05-13** — v1.0.4 S3 — Task 4 plate guard ([log](2026-05-13-cowork-appdev-v1.0.4-s3-impl.md)) — Physical printer × plate guard + material plate range bundle (HIGH-02 + HIGH-03 material half) shipped to web as `bc070af` + `bf05586`. Fixup found a real silent-regression: original steady-state-only bed-temp recipe missed 3 PETG-on-plate combos that go out-of-range on the initial layer (initial_layer_bed_offset + PETG +5 bump pushes 75°C base to 85°C on a 80°C-max textured_pei plate). Full engine recipe now mirrored. Walkthrough has 5 cumulative v1.0.4 OK lines; profile-matrix-audit clean across 47196 broad configs. iOS untouched. Per-finding checkpoint wrapped at Task 4 (Task 5 is 4-engine-site refactor; better fresh).
- **2026-05-13** — v1.0.4 S2 — Tasks 1, 2, 3 shipped ([log](2026-05-13-cowork-appdev-v1.0.4-s2-impl.md)) — Three v1.0.4 web tasks under subagent-driven-development: strength `speed_multiplier` (web `133be38` + `246171d`), env data layer + cold-warning copy + env clamp attribution (web `f7b34f1` + `d959537`, with Bambu export-path gap filed as deferred), physical printer × nozzle guard (web `1724f86` + `b238a82`). Walkthrough now has 4 cumulative v1.0.4 OK lines; profile-matrix-audit clean across 47196 configs. iOS untouched (Phase 2 gated). Pattern: code-quality reviewer flagged Important issues on every task; tightening to exact-value / exact-ID asserts is the standing recommendation for S3+.
- **2026-05-13** — v1.0.4 setup ([log](2026-05-13-cowork-appdev-v1.0.4-setup.md)) — `merged.md` locked at SHA `5bcd68b` (web `05e85bd`); per-task TDD-first implementation plan written at `docs/superpowers/plans/2026-05-13-v1.0.4-config-impact.md` (web `129d6a7`); CR3-M1 audit-script `userLevel` fix shipped (web `8ff25eb`). Multi-session autonomous arc starts S2; per-finding checkpoint rule + Trigger A between sessions; iOS push gate enforced.
- **2026-05-12** — SPARKX i7 overlay hotfix ([log](2026-05-12-cowork-appdev-sparkx-i7-overlay-hotfix.md)) — User-requested i7 printer shipped to current iOS via remote overlay `2026051202` and web/iOS catalogs; corrected taxonomy is Creality → i Series → i7 with no standalone SPARKX brand; validator now compares overlay IDs against shipped iOS bundled baselines.
- **2026-05-11** — v1.0.4 core planning ([log](2026-05-11-cowork-appdev-v1.0.4-core-planning.md)) — Codex cross-pass is complete; owner locked v1.0.4 planning defaults (core-first, advisory chips, practical MCS tiers, chamber guard-only, material-side nozzle authority, research before numeric formulas); reviewer agent checked the plan twice; next step is `merged.md`, then core implementation.
- **2026-05-11** — config-impact QA pass + cross-pass review ([log](2026-05-11-cowork-appdev-config-impact-qa.md)) — Two real bugs filed (web Advanced filament temps fix shipped as web `809549d`; environment compensation deferred behind structured QA pass). Authored parallel-then-cross-pass QA handover, ran Claude pass (29 findings: 12 HIGH / 6 MEDIUM / 6 LOW / 5 OBS), Codex pass (10 findings) ran separately, Claude reviewed Codex with meta-comparison (5 mutual HIGHs as v1.0.4 safe core; ~12 unique-to-each; severity disagreements narrow). Codex cross-pass prompt ready. No CRITICAL findings. Web `809549d` + `9925f86` + `ab979f4` + this session-close commit; iOS untouched.
- **2026-05-10** — v1.0.3 App Review submission wrap ([log](2026-05-10-cowork-appdev-v1.0.3-app-review-wrap.md)) — Owner reported phone QA passed and build `202605101637` was submitted to App Review; ROADMAP now marks stale builds, captures the post-v1.0.3 safe-follow-up todo list, and sets the next lane to App Review monitoring before any cleanup work.
- **2026-05-10** — multi-surface wrap protocol + vault collaboration guide ([log](2026-05-10-cowork-appdev-multi-surface-wrap-protocol.md)) — Repaired protocol drift from an earlier wrap by updating ROADMAP to the current TestFlight candidate (`202605101130`), created and wired the vault AI collaboration area with copy-paste triggers, audited vault `CLAUDE.md`, added multi-surface wrap-up guidance, and regenerated `NEXT-SESSION.md` with the full Trigger C read order. Vault Bambu raw-source changes remain intentionally untouched.
- **2026-05-10** — profile temperature audit + TestFlight ([log](2026-05-10-cowork-appdev-profile-temperature-audit-testflight.md)) — Built a repeatable profile-matrix audit (`scripts/profile-matrix-audit.js`) after phone testing exposed bed/nozzle/chamber/warning confusion. Broad sweep found one bug family: nozzle adjustments could exceed printer/material caps. Fixed simple, advanced, legacy export, and Bambu export nozzle clamping; added printer/material cap warnings; suppressed enclosed-printer open-frame PC copy; added chamber max copy; replaced raw picker speeds with `High-speed`; generalized Creality CFS warning. Added lightweight profile/data test protocol. Verification: data validation green, 55/55 curated audit pass, 46,512 broad configs with 0 failures, 15/15 independent post-fix pass, walkthrough pass, UI smoke pass, iOS `engine.js` byte-identical, 85 iOS tests green. Pushed web `39a8d0e`, iOS `6bd1210`, and dispatched successful TestFlight run [`25627557344`](https://github.com/mustiodk/3dprintassistant-ios/actions/runs/25627557344), version `1.0.3`, build `202605101130`.
- **2026-05-10** — iOS remote printer catalog + TestFlight ([log](2026-05-10-cowork-appdev-ios-remote-printer-catalog.md)) — Added App Store-safe, offline-first iOS remote printer overlay support: web-hosted data-only overlay + validator, Swift `PrinterCatalogProvider`, shared DataService/EngineService merged catalog, strict allowlist/hash/version/value validation, rollback via `enabled:false`, and 17 targeted catalog tests. Senior + QA gates went red on Bool-as-number and cross-runtime hash canonicalization, then green after fixes; full web checks, 85 iOS unit tests, and UI screenshot walkthrough passed. Pushed web `e206a89`, iOS `170114c`, verified live overlay URL, and dispatched successful TestFlight run [`25614975605`](https://github.com/mustiodk/3dprintassistant-ios/actions/runs/25614975605), version `1.0.3`, build `202605092356`.
- **2026-05-09** — analytics dashboard UX + platform lens ([log](2026-05-09-cowork-appdev-analytics-dashboard-ux.md)) — Redesigned `/analytics` from raw query tables into an owner dashboard, added `summary` + `profile_mix` canned queries, shipped Product lens (`All` / `Web` / `iOS`) so Web and iOS can be read side by side or filtered globally, added iOS version-aware diagnostics labels (`iOS 1.0.3`), made diagnostics mobile-friendly, pushed web commits `eb66b18`, `e9ed6b9`, and `5c60549`, and verified production after each deploy.
- **2026-05-09** — iOS analytics parity + v1.0.3 App Store submission prep ([log](2026-05-09-cowork-appdev-ios-analytics-app-store-submit.md)) — Added iOS `outputMode` parity for privacy-preserving analytics, preserving App Store review-prompt hashing separately; 4/4 targeted analytics tests + 68 unit tests + 1 UI screenshot walkthrough passed; pushed iOS `153adbc` and dispatched successful TestFlight run [`25596797349`](https://github.com/mustiodk/3dprintassistant-ios/actions/runs/25596797349), build `202605090842`. Updated analytics spec/ROADMAP/NEXT-SESSION, checked current public App Store listing (1.0.2, Diagnostics-only privacy card), created v1.0.3 App Store submit notes with Product Interaction privacy label guidance, final Promotional Text, and final owner-used What's New copy; owner submitted v1.0.3 to App Review on 2026-05-09.
- **2026-05-09** — PLA Metal + analytics dashboard ([log](2026-05-09-cowork-appdev-pla-metal-and-analytics-dashboard.md)) — Added Bambu PLA Metal across web+iOS from owner-curated Bambu vault sources, mirrored engine/data byte-identically, verified walkthrough + DQ-2 + full iOS XCTest, pushed both repos, and dispatched TestFlight run [`25576365270`](https://github.com/mustiodk/3dprintassistant-ios/actions/runs/25576365270) as same-version v1.0.3 build `202605081953`. Built and deployed owner analytics dashboard at `/analytics` backed by `/api/analytics-query` and Cloudflare Workers Analytics Engine dataset `3dpa_usage_v1`; helped configure Wrangler secrets/dataset/binding; debugged SQL dialect quirks; added web `outputMode` analytics + "Generated profiles" dashboard card. Fixed a live Workers Assets hygiene issue where `/.git/config` was publicly served; added `.assetsignore` + Worker hard-blocks and verified `404`.
- **2026-05-08** — AI operating-model cleanup ([log](2026-05-08-cowork-appdev-ai-operating-model-cleanup.md)) — Retired the active AI Operating Model pilot workflow without archiving the `ai-operating-model/` project. Promoted useful pilot artifacts to 3dpa `main`, rewrote `docs/ai-collab.md` as lightweight tool-routing guidance, marked analytics v1 shipped, deleted remote `ai/operating-model-pilot`, and left local stale-branch doc edits as an explicit cleanup finding.
- **2026-05-08** — ROADMAP slim + lifecycle protocol refactor under multi-gate Codex review ([log](2026-05-08-cowork-appdev-roadmap-slim-and-lifecycle-refactor.md)) — Long meta-layer doc refactor under 5-packet Codex gate sequence (Gate 0 plan → Gate 1a Phase 1a output → Gate 2 Phase 2 output → Gate 2.5 pre-execute fix-plan → Gate 2.6 post-execute output). 3dpa ROADMAP slimmed 773 → 224 lines + 4 archive files (663 lines). `docs/3dpa-context.md` promoted to canonical owner of engine API + app state shape (closes authority loop with iOS CLAUDE.md). iOS CLAUDE.md trimmed 184 → 47 lines. `Projects/CLAUDE.md` + AGENTS.md byte-identical lifecycle refactor: Trigger A monolithic paragraph → 5 phases × 12 numbered steps; md-hygiene moved BEFORE log finalization; Trigger A destination labels renamed to semantic forms `(5-vault)/(5-documented)/(5-sessions-dir)/(5-session-log)/(5-fallback)`; Trigger C 3dpa cold-start read order updated to include `docs/3dpa-context.md`; new "what goes where" subsection enforces 3-tier ownership (live ROADMAP / evergreen 3dpa-context / archive). Memory entry `feedback_session_lifecycle_triggers.md` updated. Codex caught 5 MF + 4 SF on Gate 2 — most importantly authority-loop risk that would have orphaned canonical engine API + state shape if iOS CLAUDE.md had been trimmed pre-promotion. **Durable lessons:** (a) multi-gate Codex review pattern is appropriate-once for cross-project blast-radius doc refactors, NOT normalized for ordinary feature work; (b) 3dpa now has 3-tier doc ownership (live planning / evergreen architecture / historical archive) enforced by Trigger A's "what goes where" subsection; (c) auto-sync on Projects/ root means meta-layer protocol edits commit silently across the session — 3 auto-sync commits for this session's lifecycle work. Projects/ root edits already in `4ae2c51 + 4d70be1 + 70bfc3f` auto-sync commits; 3dpa-web + iOS uncommitted (owner commit-shape decision). 5 Codex packets preserved at `3dprintassistant/codex/roadmap-slim-and-lifecycle/`.
- **2026-05-08** — v1.0.3 batch items 1 + 3 shipped end-to-end ([log](2026-05-08-cowork-appdev-v1.0.3-items-1-3-ship.md)) — Long, ambitious session: item 3 (iOS App Store review prompt) shipped through 6 Codex peer-review passes (1 design + 4 milestone + 1 post-implementation) + 1 Gemini research pass; 5 caught-before-ship bugs (4 design + 1 runtime stranded-eligibility); `ReviewPromptService` + `StoreKitDistributionDetector` + `ProfileKeyHasher` + 18 new XCTests (46 → 64); MARKETING_VERSION 1.0.2 → 1.0.3; TestFlight dispatched. Item 1 (printer changes) Phase A scope unlocked + applied: Kobra X 6-field correction (open-frame bedslinger, not enclosed CoreXY) + Centauri Carbon new entry (CoreXY passive enclosure, 320°C / 110°C / 500 mm/s), 4 commits per one-finding-one-commit-per-platform rule, web auto-deployed via Cloudflare Pages, iOS in main awaiting next TestFlight build; 2 IR-5 followups filed (0.25mm nozzle catalog, `pla_specific` plate identifier). Item 2 (environments) assessed: handover drafted but never sent to Gemini — multi-day workstream, not bundlable with item 1 in this session. **Durable lessons:** (a) multi-pass Codex review (design + post-impl) catches different bug classes — both worth running for hard-to-reverse features; (b) wiki-pages-as-primary-source workflow validated 3 times — default for Apple/manufacturer JS-SPA pages; (c) novelty controls counter increments only, not eligibility evaluation (engagement-tracking pattern); (d) ROADMAP truth forks between feature branch and main — update both at ship time. Web `0f47b44 / 8de9381 / 047a7f8` on main + `381dac1 / f3549ee` on `ai/operating-model-pilot`; iOS `756b107 / 1bb543e / de4aa9d / 2309af1 / 066add8 / b0d1315 / 15930c1` on main (8 commits). 14 walkthrough + 64 iOS XCTests green throughout.
- **2026-05-08** — v1.0.3 batch planning + Gemini research handovers ([log](2026-05-08-cowork-appdev-v1.0.3-batch-planning-and-handovers.md)) — Owner pivoted from DQ-3 to a 5-item v1.0.3 batch (printers, environments, iOS App Store review prompt, analytics, web output-panel UX). Workflow locked: Claude plans + codes, Gemini researches via `docs/research/gemini-*.md` handovers, Codex reviews via `codex/<feature>-review/` packets. **Created `docs/3dpa-context.md`** as evergreen project-context reference linked from every handover/packet. **Drafted 3 Gemini handovers**: printer audit (declined — Gemini failed source-or-null bar; switched to owner-manual procedure with Anycubic/Elegoo wiki pages added by owner to `Obsidian Vault/50-Wiki/raw/3dpa/`), environment-taxonomy (drafted + linter-tightened with engine-consumption reality table), iOS review prompt (lessons from #1's failure baked in: Task 0 starting-state self-check + Quality bar explicitly cites #1's decline). **Phase A printer-audit scope locked + held by owner choice** — Kobra X 6-field correction (existing data wrongly classified as enclosed CoreXY; Anycubic FAQ confirmed open-frame bedslinger with 100°C bed, 0.25/0.4/0.6/0.8 swappable nozzles) + new Centauri Carbon entry (CoreXY passive enclosure, CANVAS multi-color "On the Roadmap" so ships `multi_color_systems: []`). 2 IR-5 followups deferred: 0.25mm nozzle catalog, `pla_specific` plate identifier. **Tool-fit lesson:** spec-table / hard-data lookups → not Gemini; synthesis tasks → Gemini OK. **Gemini #3 response landed during wrap-up** — substantive Resolution next session. Codex review skipped on Phase A per per-phase filter (0 of 7 triggers). DQ-3 deferred to post-v1.0.3. 8 commits on `ai/operating-model-pilot` (12 ahead of origin total, all local-only). No engine/data/test changes; all work in `docs/`.
- **2026-04-30** — AI collaboration pilot v1 shipped ([log](2026-04-30-cowork-appdev-ai-collab-pilot.md)) — 4-round multi-tool review (ChatGPT discovery → Claude Code v1+v2 → ChatGPT v2 → Claude Code v3 → ChatGPT v3 adoption) produced `docs/ai-collab.md` (operating-model v1, single file ~280 lines). Review trail of 6 handoff files committed. Branch `ai/operating-model-pilot` 2 commits ahead of `main` (`dece4a2` trail + `b453098` ai-collab). New meta-track project established at `Projects/ai-operating-model/` with own CLAUDE.md (cold-start + fresh-session + wrap-up procedures), tokens `ai-om` / `ai-operating-model` / `aiom`. Top-level `Projects/CLAUDE.md` + memory updated. **First Codex review queued: DQ-3 scraper-vs-stub at `NEXT-SESSION.md:84`.** End-of-pilot decision target 2026-05-14; merge decision on the branch is part of the evaluation. ROADMAP not touched (operating model is operational, not feature work). No code, walkthrough, or XCTest changes.
- **2026-04-27** — App Store link on web + session policy hardening ([log](2026-04-27-cowork-appdev-app-store-link-and-policies.md)) — EU DSA Trader Status approved (app live worldwide); PR-8 retired in same commit (`7d35f92`) — Beta nav + view + Tally embed deleted, `iOS App ↗` nav + small phone-glyph header icon added, both linking to storefront-less `apps.apple.com/app/id6761634761`. Generic SVG glyph (no Apple branding). EN+DA locales updated (`navIOS`). Browser-verified live. **Two new Standing Rules** locked into top-level CLAUDE.md + memory: (1) **iOS push gate** — don't push iOS to `main` until ship-ready for TestFlight (supersedes "free to push if XCTest passes"); primary = repo hygiene, secondary = guardrail against burning Actions minutes via TestFlight-as-iteration-loop. (2) **No-guessing** — read the actual file before recommending anything about how a system behaves. Triggered by an in-session false premise about feedback email capture (it does capture; Worker logic at `functions/api/feedback.js:254-260`). `[LOW-011]` filed in IR-5 (web copy parity for email helper text + Worker reorder of `Reply-to email` to top of embed). Owner ran `git config --global user.name/.email` to fix unattributed commits going forward (no-reply email form). NEXT-SESSION.md rule #8 wording is stale — needs update on next regenerate.
- **2026-04-24** — DQ-2 Safe/Tuned + DQ-1-followup + LOW-003-followup-label ([log](2026-04-24-cowork-appdev-dq-2-safe-tuned.md)) — 15 commits across both repos in a single autonomous session. **DQ-2 shipped end-to-end** (Phase DQ 1/5 → 2/5): `_tier(preset, field, mode)` helper + sparse `_tuned` override data convention on speed_priority.fast/.balanced + strength_levels.strong, 5 MVP tiered fields (outer wall speed bedslinger/corexy, outer wall accel bedslinger/corexy, strong infill density), profileMode filter group on web + `SlidingSegmentedControl` on iOS GoalsView, locale keys in en + da, prov refs extended with `(_tuned)` / `(base from _tuned)` marker, walkthrough harness DQ-2 assertion block (Safe baseline byte-equal + Tuned differentiation ≥3/5 + prov tier-tagged), 2 new XCTests mirror the assertion (`testSafeBaselineByteEqualToDefault` + `testTunedEmissionDiffersOnTieredFieldsAndTagsProvenance`). **DQ-1-followup shipped** (3 commits): `_prov` sidecar on `getAdjustedTemps` / `getAdvancedFilamentSettings` / `getFilamentProfile` (12 prov entries), web `row()` helper + iOS `ParamRow`/`FilamentSettingsView` render ⓘ in Advanced view, 2 new XCTests. **`[LOW-003-followup-label]` fixed** (web-only, 2-line): `adv.retraction_length` → `adv.retraction_distance` at `app.js:1187` resolves "undefined" in Filament panel. Session ran on owner-granted session-scoped autonomous authorization after confirming 2 go/no-go decisions (warm-up order + bundle DQ-1-followup). v1.0.2 approved + released overnight per owner; ROADMAP synced in session-close. 46/46 iOS XCTest (was 42). Web `2441b18…270d17b`, iOS `7a913f3…949f95b`.
- **2026-04-24** — DQ-1 provenance infrastructure end-to-end ([log](2026-04-24-cowork-appdev-dq-1-provenance.md)) — Phase DQ kickoff + DQ-1 shipped in a single overnight session (6 commits web + 3 iOS). Owner opened brainstorming pro-credibility data work, converged on 5-sub-phase plan (provenance → Safe/Tuned → PA/LA → retraction deltas → cooling curves), wrote IMPL-041 master spec, shipped DQ-1 end-to-end. Spec revised mid-execution: inline `prov` via `S`/`A` extension instead of separate `_tag` helper + return-shape split (scouted existing `engine.js:35` — the infra was already there). 28 numeric emissions in `resolveProfile` tagged with baseline prov (7 default, 19 calculated, 2 rule); 18 qualitative stay `prov=null`. Walkthrough invariant at check 0c (lands red before commit 3, green after). Web `[IMPL-041 / DQ-1 commit 4]` ⓘ icon + native `title` tooltip on Advanced view — iterated from dotted-underline (too subtle per owner) to explicit icon. iOS side: new `Provenance` struct + Codable decode + `ParamRow` `info.circle` + alert; 2 new XCTests → 42/42 green (was 40). Byte-identity maintained every commit. Two IR-5 followups filed: `[DQ-1-followup]` filament panel extension + `[LOW-003-followup-label]` `adv.retraction_length` → "undefined" bug at `app.js:1187`. Autonomous sweep on the close + log + NEXT-SESSION regen per owner authorization. Web `4cde0dd…1941ec8`, iOS `720d26a…837ca10`.
- **2026-04-23** — v1.0.2 ship sequence ([log](2026-04-23-cowork-appdev-ship-v1.0.2.md)) — full end-to-end ship in one session: What's New tone-pass (casual short English, ~530 chars), `MARKETING_VERSION` 1.0.1 → 1.0.2 + xcodegen + push (iOS `11b9c8d`), mid-session scope add **Home-screen version-display footer** (iOS `15c1002`, `AppConstants.appVersion`/`appBuildNumber` + HomeView muted line), TestFlight dispatched via `gh workflow run testflight.yml --ref main` (run [24848532846](https://github.com/mustiodk/3dprintassistant-ios/actions/runs/24848532846), SUCCESS), on-device smoke tests 4a–4e green (feedback via Worker confirmed via CF Logs, no engine-init crash, MK4 acceleration names MK4 not A1), old `#ios-app-feedback` webhook deleted (attack surface from v1.0.0/v1.0.1 binaries closed), **v1.0.2 submitted to App Review** with Manual release toggle + Promotional Text C1 ("Generate optimized 3D print profiles for 64 printers across 12 brands…"). **`[CRITICAL-001-followup]` filed in IR-5 backlog:** Worker routes all feedback (iOS+web) to single `DISCORD_WEBHOOK_URL` → `#web-app-feedback`; original `#ios-app-feedback` intent never landed. v1.0.3 scope (~15 LoC + new CF secret, no iOS binary). 40/40 iOS XCTest green. NEXT-SESSION regenerated on explicit owner ask (first test of 2026-04-23 owner-triggered-only rule — clean). Awaiting Apple (24–48h typical).
- **2026-04-23** — IR-4 + IR-5 bundle into v1.0.2 ([log](2026-04-23-cowork-appdev-ir4-ir5-bundle.md)) — autonomous session-scoped run; 10 findings shipped + HIGH-003 actor→class refactor via branch + **3 Codex external-review passes** (VERDICT: merge). Bundle: MEDIUM-021 slicer-display-name bridge, HIGH-007 getNozzleSize bridge, MEDIUM-020 getFilamentTabs bridge, LOW-004 TPU drying alignment, LOW-006 flexible dedup, HIGH-006 SLICER_TABS bridge (270+ lines of SlicerLayout.swift static data deleted + snapshot test), LOW-003 retraction_length→retraction_distance collapse (unblocks export HIGH-001), HIGH-011 structured numeric per chip (value field), MEDIUM-009 Codable decode, HIGH-003 actor→final class+serial DispatchQueue+assertOnEngineQueue guards. Test count 37 → 40. External review kit preserved at `3dprintassistant-ios/docs/reviews/2026-04-23-high-003-codex/`. NEXT-SESSION not regenerated per owner rule. Web `f6dd64b…9aa164e`, iOS `9093b24…2579fe8`.
- **2026-04-23** — IR-5 backlog sweep + correctness-tier test ([log](2026-04-23-cowork-appdev-ir5-backlog-sweep.md)) — autonomous session-scoped run; 6 findings shipped (material_warnings retire as MEDIUM-017 follow-up, MEDIUM-003 doc contract, MEDIUM-013 getFilters throw, MEDIUM-012 jsonLit helper + 5 ID-embed sites + closed getAdjustedTemps zero-escape gap, MEDIUM-005 strength chip through mapForSlicer, MEDIUM-011 correctness-tier XCTest 8 combos); 11 commits; test count 37 → 38; walkthrough 9/9 + Combo 3 pre-existing. New `scripts/extract-correctness-tuples.js` regenerates MEDIUM-011 expected values. NEXT-SESSION **not regenerated** (owner-triggered-only rule locked in earlier today). Web `481049d…dc7ee95`, iOS `19ee59b…4300400`.
- **2026-04-23** — IR-2 / IR-5 cleanup sweep + CI quota fix ([log](2026-04-23-cowork-appdev-ir2-cleanup-sweep.md)) — autonomous session-scoped run; 10 findings shipped (MEDIUM-017, MEDIUM-010, LOW-009, OBS-006, MEDIUM-022, LOW-008, MEDIUM-004, LOW-010, MEDIUM-019 partial, R8 `_validateSchema`); 21 commits; 37/37 iOS tests every commit; walkthrough 9/9 + Combo 3 pre-existing every commit. Mid-session **TestFlight workflow switched to `workflow_dispatch`-only + concurrency cancel** after GitHub quota hit 100% — every iOS push was triggering a 10-min macos-26 build. Owner-ship-workflow change: `gh workflow run testflight.yml --ref main` after version bump. R8 surfaces 16 soft warnings documenting new `[MEDIUM-019-followup]` IR-5 item (max_mvs 0.8 data gap). Web `358591d…7601c72`, iOS `a797f2f…88047ec`.
- **2026-04-23** — IR-2a engine-sweep + CRITICAL-001 code landing ([log](2026-04-23-cowork-appdev-ir2a-engine-sweep.md)) — autonomous overnight run; 13 findings landed (HIGH-009, MEDIUM-002, MEDIUM-001, MEDIUM-007, HIGH-012-followup A+B, HIGH-008, HIGH-004, HIGH-005, CRITICAL-001 code, HIGH-010-A sanitize, MEDIUM-018-A); 20 commits; 37/37 iOS tests every commit; walkthrough 9/9 every commit. CRITICAL-001 code-complete but awaits owner HMAC secret config in CF+GH+local (3 places). HIGH-010-B rate-limit deferred to owner CF dashboard action. Simplified Phase 2 runbook at `docs/runbooks/incident-response.md`. v1.0.2 "What's New" locked at `docs/app-store-whats-new-v1.0.2.md`. Web `62d7ae9…597499b`, iOS `250f187…5a360dc`.
- **2026-04-22** — IR-0 overnight sweep: 5 findings shipped ([log](2026-04-22-cowork-appdev-ir0-sweep.md)) — autonomous overnight run; `[CRITICAL-003]` preset validation + `[HIGH-002]` IMPL-040 test tightened + `[HIGH-012]` A1 why-text templated + `[MEDIUM-015]` lightning Prusa fallback dropped + `[MEDIUM-016]` adaptivecubic added; 9 commits, 37/37 iOS tests; 4 IR-0 items deferred to IR-2a (v1.0.2 ship pass — new ROADMAP section); partner-reviewer hook installed at session start; HIGH-012-followup filed in IR-5. Web `6173839…2ddff99`, iOS `2cbf5c7…34fba25`.
- **2026-04-22** — Security rotations + md hygiene ([log](2026-04-22-cowork-appdev-housekeeping.md)) — rotated 4 credentials (Sentry DSN, ASC API key `MHDMJN32AP`→`SF8C66YLJY`, provisioning+CSR deleted, GitHub PAT stripped → `gh` OAuth via keychain); tightened iOS `.gitignore`; retired 9 iOS root stubs; promoted 10 untracked web docs + archived iOS-RELEASE-PLAN. **Bonus round:** iOS doc-drift triage — promoted App Store metadata + screenshots/{iphone,ipad}; retired `research/` stubs + `memory/` + 5.4 MB backup screenshots + stale `privacy-policy.md`; moved `rb2-actor-prompt.md` to web; moved `.p8` to `~/.appstoreconnect/private_keys/`. 6 commits across web+iOS. `[LOW-001]` ✅.
- **2026-04-21** — IR-0 [CRITICAL-002] bed-temp clamp ([log](2026-04-21-cowork-appdev.md)) — engine clamps `initial_layer_bed_temp` / `other_layers_bed_temp` to `min(material.bed_temp_max, printer.max_bed_temp)`; new `printer_max_bed_temp_clamped` + `printer_bed_temp_incompatible` warnings; Combos 6 + 10 ❌→✓; 35/35 iOS tests; web `dc87961`, iOS `378861a`.
- **2026-04-21** — Review consolidation + fresh-session prep ([log](2026-04-21-cowork-appdev-consolidation.md)) — merged domain-findings + PLAN.md into 8 review files + IR-* ROADMAP section; deleted 2 redundant docs; 59 findings triaged.
- **2026-04-20** — 3rd-party review package ([log](2026-04-20-cowork-appdev-review-package.md)) — starter kit (6 docs) dispatched against web `c4c5071`, iOS `24aef66`; `fresh-session-kickoff-post-review.md` prepared.
- **2026-04-20** — IMPL-040 chip-desc / resolveProfile single source of truth ([log](2026-04-20-cowork-appdev-impl040.md)) — every UI number now computed by the same function that emits it; 20/20 tests; 118-combo matrix clean.
- **2026-04-20** — IMPL-039 printer-capability clamping + slicer-aware values ([log](2026-04-20-cowork-appdev-impl039.md)) — `getPrinterLimits` + `_clampNum` + `mapForSlicer` + `slicer_capabilities.json`; 32/32 tests; Bambu export byte-matches vendor format.
- **2026-04-19** — Cowork App Dev ([log](2026-04-19-cowork-appdev.md)) — Draft layer 0.30→0.28 (Bambu rejected import); committed the 114-line RB-1 engine diff that had been sitting uncommitted (production was rendering "undefined" for every warning).
- **2026-04-17** — Cloudflare Pages → Workers migration blocker ([log](2026-04-17-cloudflare-functions-blocker.md)) — Tally retired; Discord `/api/feedback` Worker stood up; `wrangler.toml` + `worker.js` required because project had migrated to Workers Builds.
- **2026-04-16 → 17** — App Store release + Discord restructure ([log](2026-04-16-cowork-appdev.md)) — iOS v1.0 approved + released (~121 non-EU countries); EU blocked on DSA Trader Status; Discord server restructure.
- **2026-04-15** — BR-11 feedback system + BR-12 empty-output hardening + App Store submission ([log](2026-04-15-cowork-appdev.md)) — native feedback sheet → Discord; 2-tap Reset scoped per-screen; submitted to Review 13 days ahead of April 28 deadline.
- **2026-04-14** — BR-10 cross-device UI review ([log](2026-04-14-cowork-appdev.md)) — UITest target + 33 screenshots across SE/17 Pro/17 Pro Max; 4 P0/P1 layout fixes; App Store screenshots retaken.
- **2026-04-13** — App icon + branding ([log](2026-04-13-cowork-appdev.md)) — clay Benchy 1024×1024; nav bar + home logo; web header swap.
- **2026-04-10** — Release blockers + pre-release polish (RB-1…6, BR-1…7) ([log](2026-04-10-cowork-appdev.md)) — structured warnings, actor bridge, Sentry DSN extraction, OutputView split, device QA.
