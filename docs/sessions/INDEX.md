# Session log index

**Rolling table of contents for `docs/sessions/`. Reverse chronological — newest first.**

Each entry is a single line: `date — topic — one-line outcome`. Read the full log when the outcome references something the current task depends on.

**Protocol:** every session end, append a new bullet at the top. See `CLAUDE.md → Session-log protocol`.

---

- **2026-07-23** — iOS 1.1.0 Task 9 review closed → G0 reached + release copy prepared ([log](2026-07-23-cowork-appdev-ios-1.1.0-task9-review-g0.md)) — First valid independent review (bridge claude-only 870s): NO-GO with 1 P1 + 6 P2 → six one-finding-one-commit fixes (incl. iOS `3f886bf` timing-independent cold-launch routing) + P2-E refuted → full gates rerun green (provider 62, Worker 33, XCTest 177, XCUITest 4) → confirmation review of applied diffs `GO`, no open P0/P1/P2. Tasks 0–9 complete; stopped at G0 with exact owner prerequisites in the gate ledger. 1.1.0 App Store submit package prepared (ratified §11 copy, §10.1 Device ID privacy change, 78/14 recount). Nothing pushed or provisioned.

- **2026-07-19** — iOS 1.1.0 Tasks 0–9 execution → local gate green, review transport blocked ([log](2026-07-19-cowork-appdev-ios-1.1.0-tasks-0-9-blocked-review.md)) — Task 1 security fix alone shipped and production private paths are 404; provider/iOS Tasks 2–8 are committed locally, all local gates are green (56+33 web, 175+4 iOS, validators/audits/identity), but Bridge and its one direct read-only Claude fallback both returned empty at 900 seconds. Task 9 Step 2 remains blocked, G0 was not reached, iOS was not pushed, and no provisioning/TestFlight/App Review/public send occurred.
- **2026-07-18** — iOS 1.1.0 plan ratification + hostile review ([log](2026-07-18-cowork-appdev-ios-1.1.0-plan-ratification.md)) — Ratified the 13-task notification/release plan after 5 P1 + 5 P2 fixes and a final authenticated Claude `GO`; owner selected Tasks 0–9 for fresh-session execution, with Task 1's isolated web security deploy allowed and a mandatory stop at G0 before provisioning/iOS push/TestFlight.
- **2026-07-18** — Snapmaker U1 owner re-entry and live ship ([log](2026-07-18-cowork-appdev-u1-owner-reentry-ship.md)) — Corrected four-nozzle support and prime/wiping-tower wording, then fresh `R1/R2 GO` shipped U1 live with custody and a local-only iOS mirror. The isolated runner's notifier config was restored and the saved report replayed; the PD8 freeze remains owner-gated and blocks the next scheduled run.
- **2026-07-16** — Intake parked-path root cause → v2.4 fix → production rerun green ([log](2026-07-16-cowork-appdev-intake-parked-path-root-cause.md)) — Reconstructed `writeParked('i7_i', sidecar)` creating the repo-root dirty artifact; TDD path-owner guard + exact runner contract shipped (`104251c`/`8151868`), hostile review PASS, and live `run-20260716T201219Z` exited with `PREFLIGHT`/`POSTRUN` green, notifier posted, web clean/current. `i7_i` stayed owner-gated; Snapmaker U1 was researched then safely parked on new-series-group; no catalog/overlay/iOS change.
- **2026-07-14** — Sovol SV06 ACE synthetic production E2E → debug → clean GO/GO → live ([log](2026-07-14-cowork-appdev-sv06-ace-synthetic-e2e.md)) — Explicitly synthetic missing-printer request traversed real `/api/feedback` → Discord/KV → Scout/research/evidence/review. First run parked fail-closed on contract-invalid R1 and exposed a POSTRUN SHA-format defect; TDD fixes `c2fe6fd` + `16606a7`; clean sidecar re-entry received fresh `GO/GO`, merged `3bba2c2`, live overlay/picker green at `2026071402`, local iOS mirror `80c26dd` not pushed, custody `54dc4c8`, POSTRUN green. iOS 1.0.7/1.0.8 not started.
- **2026-07-14** — Centauri owner decision → fresh GO/GO → shipped + live ([log](2026-07-14-cowork-appdev-centauri-owner-decision-ship.md)) — Owner approved CANVAS compatibility + corrected base/Combo wording; repaired candidate `48f48a2` passed single-shot evidence and fresh R1/R2 `GO/GO`, merged at `954cfa3`, live overlay `2026071401` + production picker verified, local iOS mirror `f9a810c` not pushed, custody `88816bd` pushed. New notifier `[object Object]` defect fixed TDD-first in `2ae4ddc`; clean zero-candidate control wrapper passed POSTRUN and treated Centauri as already shipped. iOS 1.0.7/1.0.8 not started.
- **2026-07-14** — Centauri v2.3 pipeline debug + clean rerun ([log](2026-07-14-cowork-appdev-centauri-v23-debug-rerun.md)) — Scout packet-custody and R1 schema-transport defects fixed and regression-locked. Clean wrapper reached PD5: R1 structured GO / R2 structured NO-GO on CANVAS compatibility + Combo-only wording, so `review-split → decision-required`; `0 shipped · 1 parked · 0 errored`, POSTRUN green, branch `c4f5075` + packet preserved, custody `a358e47` pushed, no live/iOS ship.
- **2026-07-14** — Centauri Carbon 2 v2.2 mac-mini e2e rerun ([log](2026-07-14-cowork-appdev-centauri-v22-e2e-rerun.md)) — Terminal fail-closed outcome: `0 shipped · 1 parked · 0 errored`, wrapper `POSTRUN ok=true` / exit 0. Candidate rebased to `1bc4f8f`; all mechanical gates green, then evidence gate parked `research-defect` before R1/R2 because Scout replaced the preserved filled packet with a fresh null-field packet at the same path. No merge/live/iOS; custody `5c0879c` pushed. Next re-entry is owner/decision-required; product lane returns to iOS 1.0.7.
- **2026-07-13** — Centauri intake full audit + R1/park fixes, Air ([log](2026-07-13-cowork-appdev-centauri-intake-audit-fix.md)) — Owner reopened the intake lane; all three failures audited (file-path bug fixed+held, review-split answered by evidence repair, 19:08 prose-GO diagnosed as single-sample stochastic prose-compliance after 3 no-repro live probes incl. nested-dirty env). Boundary now appends a superseding STRUCTURED OUTPUT CONTRACT block + preserves the effective prompt; POSTRUN park-preservation invariant added; contract v2.2. Hostile review NO-GO→patched; suites green; 2 post-fix probes valid. E2E rerun handed to the mac-mini.
- **2026-07-13** — Centauri intake terminal park + recovery ([log](2026-07-13-cowork-appdev-centauri-intake-terminal.md)) — All deterministic gates passed, but the one authorized R1 boundary turn returned exit 65 `structured-output-missing`; prose GO did not count, R2/merge/live/iOS/ship did not run. Wrapper caught the runner's stale-report/evidence-cleanup miss; candidate `1694af2`, packet, tags, report, and custody were restored and POSTRUN proved green. Intake is owner-parked; next product lane is iOS 1.0.7, then separate 1.0.8 tip jar.
- **2026-07-13** — R1 structured-output boundary root-caused + fixed (Air) ([log](2026-07-13-cowork-appdev-r1-boundary-rootcause.md)) — Proven: `--json-schema` is inline-JSON-only; retry-2 passed a file path and CLI <2.1.205 silently degraded to unstructured output (exit 0, prose, no `structured_output`). Fail-closed boundary `scripts/intake-r1-structured-review.sh` merged via PR #24 (`c0da52a`): 58-assertion suite + N=4 exact-scale live proof (probe verdicts discarded). Candidate still parked; the ONE authorized evidence/PD5 continuation remains for the mac-mini (session ran on the Air — no local runner state).
- **2026-07-13** — Centauri PD5 recovery + Claude handover ([log](2026-07-13-cowork-appdev-centauri-pd5-handover.md)) — Recovery implementation merged through web PRs #21/#22/#23 and ai-om PRs #1/#2; candidate deterministic gates are green, but the latest strict-schema R1 exited 0 without `structured_output`, so no verdict/R2/ship. Candidate `f39d7f9` + checkpoint tags + ignored evidence remain preserved; next Claude session must reproduce the exact long prompt/parse failure before one fresh PD5 continuation.
- **2026-07-12** — Intake-runner post-run fail-closed gate (remote) ([log](2026-07-12-remote-intake-runner-postrun-gate.md)) — Debugged the 22:20 runner failure (session exited 0 after candidate `be49fea` with no PD5/report/notify/cleanup; wrapper trusted `claude -p` rc alone): reproduced the false-success with a stub claude, then TDD'd `scripts/intake-post-run-invariants.sh` (fresh `last-run-report.md` ⇒ notify ran; non-empty session log; HEAD on `main`; clean-or-custody tree; custody-only ahead — exit 65, `--shipped-unknown` notify) gated into `intake-run-wrapper.sh`, plus kickoff pin of the only allowed bridge forms (the session had burned a full-mode review on the non-existent `bridge config`). All suites green. `centauri_carbon_2` still parked, nothing shipped; mac-mini rerun + Bridge smoke remain (remote session — no LaunchAgent/Bridge access).
- **2026-07-12** — Intake preflight recovery + supervised rerun ([log](2026-07-12-cowork-appdev-intake-preflight-recovery.md)) — Restored a clean launchd checkout from 179-behind via ff-only, proved full preflight green, and manually ran the daily path. Elegoo Centauri Carbon 2 passed evidence/validators + reviewer 1 GO but parked fail-closed as `review-unavailable` because Codex CLI v0.139.0 could not use `gpt-5.6-sol`; custody `1e3aec5` pushed, shipped 0, lock released, Discord report posted.
- **2026-07-12** — Account-candidate comparison audit + My 3DPA merged plan (remote) ([log](2026-07-12-remote-audit-merge-my3dpa.md)) — Owner comparison gate executed: audit of both candidates → owner locked sync=Pro/Firebase/15–20 gates/full-scope-milestones → merged decision set SYN-00–SYN-17 + plan MG0–MG19, 3 hostile review rounds (26→12→2, all patched); ROADMAP carries it as a parked locked entry point after 1.0.7/1.0.8 (MG0 + SYN-17 Codex round gate implementation). Docs-only.
- **2026-07-12** — Independent My 3DPA account/cloud/inventory candidate ([log](2026-07-12-cowork-appdev-independent-accounts-platform.md)) — Blind A/B candidate created only after parallel PR #16 merged; implementation-grade spec + atomic plan reached architecture/QA/Claude zero P0–P2, merged docs-only through PR #17, and was added to ROADMAP for owner comparison. No runtime or iOS change.
- **2026-07-12** — Accounts Platform spec + gated plan (parallel Claude session) ([log](2026-07-12-cowork-appdev-accounts-platform.md)) — Optional accounts/cloud sync/inventory/My 3DPA proposal merged through PR #16 and preserved as the parallel AP0 comparison candidate in ROADMAP; this index entry repairs the previously verified parity gap without reopening its same-task log during the independent design phase.
- **2026-07-11** — Monetization brainstorm → Phase 1 tip-jar plan APPROVED ([log](2026-07-11-cowork-appdev-monetization-phase1.md)) — Strategy ratified A+B phased: tip jar now (web Ko-fi + iOS 3-tier IAP 15/45/95 kr as own 1.0.8 train after 1.0.7; one-lifetime 10th-profile card; zero nag), "3dpa Pro" one-time unlock deferred until filament inventory; shipped features stay free forever; no subscription. Plan: `docs/superpowers/plans/2026-07-11-monetization-phase1-tip-jar-plan.md`. Docs-only, own branch/PR (parallel codex export session untouched).
- **2026-07-11** — Air → mac-mini Export Phase 2 handoff ([log](2026-07-11-cowork-appdev-air-to-mini-export-handoff.md)) — Corrected a stale-feature-branch cold start (`export-phase2-20260709` was 117 commits behind current main), fast-forwarded local web `main` to `bb2409d`, and pushed the three existing iOS P2 mirror-test commits to transfer branch `codex/export-phase2-ios-sync-20260711`. That branch is XCTest/reference material only; mac-mini must recover P2 onto current web main, byte-copy the final engine, run the RED breadcrumb + full XCTest, then PR/merge/live-verify.
- **2026-07-11** — Export-first pivot wrap ([log](2026-07-11-cowork-appdev-export-pivot-wrap.md)) — Verified printer export state before starting iOS 1.0.7 implementation: native export is Bambu-only today (`exportBambuStudioJSON`; no `exportOrcaJSON`/`exportPrusaINI`; non-Bambu uses copy fallback). Export Phase 2 is implemented + owner-verified on `origin/export-phase2-20260709` but not merged; direct merge is unsafe because the stale branch diff would remove newer intake/K2/analytics work. New overall plan created at `docs/superpowers/plans/2026-07-11-export-first-overall-plan.md`; NEXT-SESSION now starts with Export Phase 2 recovery/merge, then Phase 3 Orca, Phase 4 Prusa, then the already-reviewed iOS 1.0.7 issue-fix release plan.
- **2026-07-10** — Intake Autonomy v2.1 cross-model review + patched small-gate plan merged ([log](2026-07-10-cowork-appdev-intake-v2.1-review-plan.md)) — Independent Claude hostile review of the v2.1 spec found 10 findings (3 MF / 4 SF / 3 OP), all patched one-per-commit; small-gate implementation plan R-1/R0–R8 written, then hostile-reviewed again (5 must-fix + 8 should-fix + 3 optional), all patched. PR #6 created, Cloudflare Workers build passed, and PR #6 merged to `main`. **No v2.1 implementation code built yet.** Next entry point: execute the plan at R-1, then R0.
- **2026-07-10** — First-run postmortem → Intake Autonomy v2.1 design spec (no code) ([log](2026-07-10-cowork-appdev-intake-v2.1-spec.md)) — The live pipeline parked K2 SE; the postmortem found the **root cause** (research never records its sources, so the reviewer judged a safety-critical `max_speed` with zero provenance and correctly NO-GO'd — the controller's "overcautious reviewer" reading was wrong) and a **live safety defect**: a `NO-GO` retried on a weekly timer is a repeated sample of a stochastic gate that only terminates in one direction. **Live fix shipped: runner contract v1.1** (`review-no-go` event-only; `others → weekly ×4` catch-all deleted) ahead of K2 SE's 2026-07-17 retry. v2.1 spec written → hostile sub-agent GO-WITH-PATCHES ×14 (incl. the controller's central argument being motivated reasoning) → owner ratified RD0–RD10 → **Codex NO-GO ×9** → **Codex NO-GO ×5** (round 2 refuted round 1's own fix advice — K1 filed). Three drafts reintroduced the ratchet three times; killed by one invariant (**NO-GO taint**) + a graph-reachability test all three would have failed. Retrospective agent **deferred**; provenance file added. **Owner halted before the build citing work quality.** Next: independent adversarial cross-model review of the spec, then small-gate plan.
- **2026-07-10** — Intake Autonomy v2 FULL BUILD B0→B5 — one step from live ([log](2026-07-10-cowork-appdev-intake-autonomy-build.md)) — Autonomous single-session build of the entire pipeline, per-gate hostile review ×6 (50 findings, all applied): B0 launchd probe 4/4 (`ppid=1`; keychain 401-stale → oauth.env canonical) · B1 `republish-overlay.js` (PD6 version rules, exit-2 CRITICAL) · B2 full-envelope live verify + live picker probe (green on prod) · B3 preflight/lock/freeze/KV-hygiene/notifier (real preflight `ok=true`; shipped-and-unreported freezes even on UNKNOWN ship state) · B4 runner contract v1 + Assistant autonomous mode + runbook v5 waivers + PD7 last-line-wins + scripted diff guards + **K2 SE dry-run that caught the printers.json reserialize hazard** (fix: string splice +21/-0) · B5 latency measured (30–60s → 14×15s calibrated) + **REAL prod rollback drill** (`2026071003`→`2026071004` = max(bad,snapshot)+1 live-proven). **B5.4 deliberately not executed — webhook hard gate. Owner: webhook → test POST → bootstrap plist → first 12:00 run ships K2 SE e2e.** Discovered + mitigated: Scout tests wipe `.printer-intake-out/` → runner state moved to `.intake-runner-state/`.
- **2026-07-10** — analytics Worker 5-event fix LIVE + Intake Autonomy v2 impl plan dual-reviewed ([log](2026-07-10-cowork-appdev-analytics-fix-intake-plan.md)) — Owner "do 1" shipped: `EVENT_KEYS` full-schema patch (blob19 = shared per-event detail column — AE 20-blob cap, all 20 assigned) + android HMAC auth branch, TDD RED-first, hostile-reviewed GO ×2, **prod-verified live** (`551b092`+`14cb97a`: `troubleshoot_used`/`export_clicked` now `200 stored:true`; android bad-sig `401` not 403). Review refuted the premise: **4 MORE selection events still 400 on every chip click** → owner-decision row queued (+ pre-AG3 shared-secret & dashboard-lens flags). Then the **v2 impl plan** (`bff1604`, 5 build sessions B0–B5, K2 SE first candidate): hostile sub-agent GO-WITH-PATCHES ×17 (CRITICAL: git-tracked outcomes ledger would deadlock the fail-closed preflight at run 2) + Codex via bridge ×6 (payload-only hash lets `enabled:false` pass live verify; `--stat` COMBOS guard unimplementable; crash-replay window) — all applied. **Next: build session 1 = Gate B0 launchd environment probe (mac-mini).**
- **2026-07-09/10** — overlay baseline fix + K2 SE intake debug + **Intake Autonomy v2 RATIFIED** ([log](2026-07-09-cowork-appdev-intake-autonomy.md)) — Cold-start sync reconcile (web ff 77; iOS rebase 3-on-v1.0.7, gate intact) → fixed the queued overlay-validator RED (`c381638`, 1.0.7 baseline, 13/13). Debugged the "failed" intake: the real request (Creality K2 SE) was captured + triaged correctly — **nothing is scheduled anywhere; the pipeline was semi-automated by design**; live queue cleaned (19 stale keys deleted, 3 real kept). Owner pivot → full-pipeline audit (2 agents): **S5 structurally cannot ship unattended** (owner-merge terminal; its human-free rung excludes overlay) → **superseded by Intake Autonomy v2** (audit + spec `a99aa27`; hostile review GO-WITH-PATCHES ×14 incl. CRITICAL rollback version-ordering vs the iOS poisoned-cache guard — all applied). **Owner ratified PD0–PD8 + struck the shadow phase** ("no babysitting") → full autonomy from run 1 (`895c3a9`). Next: analytics EVENT_KEYS fix (owner-declared) and/or v2 impl plan → build (mac-mini).
- **2026-07-08/09** — Android v1 (+ macOS companion): audit-delta + design spec + gated master plan ([log](2026-07-08-cowork-appdev-android-v1-planning.md)) — Post-pivot autonomous planning run reusing the parked iOS v2 evidence base: Android audit-delta (§4 contracts re-verified against real code; QuickJS-wrapper runtime researched + picked w/ mandatory AG1 spike; Play facts: 12-tester/14-day closed test = the calendar long-pole) → spec AD0–AD10 (native Kotlin/Compose restart in `3dprintassistant-android`, improved-from-day-one bar incl. native Mine loop, sibling Android overlay, `dk.mragile.printassistant`) → plan AG0–AG8 (one gate/session, 12–18 sessions + 3–5 wk Play calendar) + ledger PENDING-GO + light macOS companion (M0 = ASC Mac-Availability 5-min check; M2 rides a future iOS-v2 un-park, post-Android). Triple-reviewed (2 hostile sub-agents + Codex — GO-WITH-PATCHES ×3, non-overlapping; standouts: both Workers 403 native non-iOS clients at the AUTH layer, overlay CONSUMER unscheduled, overlay validator run-verified RED on missing 1.0.7 baseline), all patches applied. **3 real web findings mined + queued** (analytics EVENT_KEYS full-schema + auth; feedback auth; validator baseline — blocks any overlay republish today). 3dpa-context Android-scope line fixed. **No app code; prototype no-push boundary intact. Owner next: AG0 — spec §2+§4 GO/override + Play account + prototype-commit push.** Commits `8a2da77`+`3560b31`+wrap.
- **2026-07-08** — iOS v2 rebuild: audit + design spec + gated master plan ([log](2026-07-08-cowork-appdev-ios-v2-audit-plan.md)) — Autonomous planning-only run: deep audit of both codebases (parity matrix, contracts, debt) → design spec (D0–D7: hybrid rebuild on `ios-v2` branch, same bundle id, ships as 2.0.0; learns layer in JSCore; Swift write-side store APIs; share outbound-only; comparison cut) → gated master plan G0–G8 (one gate/session, per-gate micro-plans at entry, 10–15 session envelope). Triple-reviewed (2 hostile sub-agents + Codex via bridge, all GO-WITH-PATCHES, non-overlapping catches incl. CRITICAL v1-data-migration invariant + D5a byte contradiction); all patches applied. Also fixed stale 3dpa-context (env ids/mine/test count — closes the carried doc-drift finding). **No code touched; awaiting owner Gate 0 ratification (spec §2).** Commits `250f37a`+`4c17f96`+wrap.
- **2026-07-06/07** — W3 Mine-tier engine train — ON BRANCH, owner verifies then merges ([log](2026-07-06-cowork-appdev-mine-tier.md)) — Autonomous run on branch `mine-tier-20260706` (8 commits off `main` `6c9d4a0`, tag `mine-tier-baseline-20260706`, pushed as backup; **NOT merged/deployed**). **The learns-loop payoff: accepted Workshop tuning now changes the generated profile.** `profileMode:'mine'` = Safe base + personal deltas via `Engine.setPersonalTuning` (vocabulary re-clamp + pairKey stale-injection guard, all RED-first): temp deltas at env-adjustment points w/ NEW lower floors; fan delta on exactly 3 emissions w/ NEW 0–100 bounds; retraction delta post-`_scaleRetraction` pre-cap (sidecar carries it — card==output==export); prov `personal` only on values the delta actually changed; conditional Mine segment (engine `getFilters`); share maps mine→safe at the share affordance. Golden 36→39 states, non-mine bytes provably unmoved; validate-data/matrix-audit hashes baseline-identical (zero data/ changes). **Cross-model review: Codex NO-GO → HIGH-1 (Mine silently reverted to Safe on refresh — smoke had masked it via stripped-path navigation; K3 finding filed) + MEDIUM-3 (prov claimed personal when caps erased the delta) both CONFIRMED + FIXED one-per-commit (`cae683d`, `55beff8`); HIGH-2 (text/reference-export raw retraction) deferred-with-rationale to the HIGH-001/Export-P2 family; post-fix all green.** Fresh-context subagent gate verification 9/9 ALL-VERIFIED. iOS: 5 LOCAL commits (engine byte-sync, WorkshopStore tuning/userMaterials key-preservation + op-union import + regenerated fixture [the §7 MUST-item], EngineService bridge + 6 XCTest W3 mirrors, Mine third segment) — **135/135 XCTests, push gate intact**. T7 rules-table→remedy migration explicitly DEFERRED (trigger unfired; Codex concurred). **Owner next: OWNER-VERIFY walkthrough + merge — commands in [`MINE-TIER-GATE-LEDGER.md`](../planning/MINE-TIER-GATE-LEDGER.md).**
- **2026-07-06** — Learns + Export: W3/W4 design + journal harvesting + IMPL-043 Phase 0+1 — ALL ON BRANCH ([log](2026-07-06-cowork-appdev-learns-export.md)) — Autonomous dual-track run on branch `learns-export-20260706` (pushed; **NOT merged/deployed — owner verifies then merges**; main `ad21aa1` = fallback, tag `learns-export-baseline-20260706`). **Track B:** W3/W4 design spec + impl plan (both dual/cross-model-reviewed, all findings applied) + the app-layer journal→suggestion harvesting SHIPPED to the branch (rules table w/ resolved troubleshooter refs, op-log tuning ledger w/ atomic fork-lossless backup merge, harvest w/ same-nozzle threshold + anti-ride + conflict/advice cards, Workshop UI EN+DA) — engine/data untouched, golden-diff-proven. **Track A:** export-audit harness vs owner golden fixtures resolved all contested claims (zig-zag valid; BS filament schema 2.5.0.18≠process; 2-element extruder-variant arrays discovered), then IMPL-043 P1 executed: canonical `_slicer_value` sidecars + passthrough export (April regex path kept behind `USE_LEGACY_EXPORT=false` as instant fallback) + HIGH-001 scaled retraction + ironing_pattern + support_style 5-map + split version constants — **audit 0 FAIL**; iOS mirror local `c647982` (XCTest 125/125, unpushed). New value-level engine golden snapshot net (36 states) born in G0. **✅ EVENING: Bambu import test PASSED (owner-run — real 225/220 + `ZZ` sentinel markers 199/188/0.88/44/12/brim-11 all landed); branch MERGED → `main` `9e7890d` → live on web; iOS `c647982` stays local. Orca golden fixtures captured (`orca-x1c-*-ref.json`; Orca = BS fork, v2.1.0.x). App/web finding mined: `max_mvs` missing 0.8-nozzle keys across 17 materials → queued. Next: W3 Mine-tier engine train.**
- **2026-07-05** — Phase 2: Share + Persist + Workshop LIVE on web; iOS v1.0.6 pushed ([log](2026-07-05-cowork-appdev-phase2-share-persist-workshop.md)) — Autonomous gated run (ledger `docs/planning/PHASE2-GATE-LEDGER.md`, W0–W6 + I0–I5 all ticked): web shipped session persistence (`3dpa_state_v1`), shareable profile URLs + ⤴ Share, the **Workshop** (saved profiles, JSON backup, print journal w/ troubleshooter deep-link) + 3 hygiene fixes — curl-verified live. iOS **v1.0.6** (persistence + Workshop with **byte-compatible** backup, fixture-pinned against real web output; journal deferred — no troubleshooter surface on iOS) pushed `518f781..791d78b`; **TestFlight dispatch = owner** (`gh workflow run testflight.yml --ref main`; see ledger I5 note re other machine's 3 data-mirror commits first). S1 landing pages not started (budget; whole-or-nothing). 63 web Node tests + 125 iOS XCTests green; engine/data untouched (baseline diff-proven every gate).
- **2026-07-04** — Ender-3 V4 Combo intake + live overlay ([log](2026-07-04-cowork-appdev-ender3-v4-combo-intake.md)) — Scout consumed the original failed/resend queue and staged `creality / ender 3 v4 combo`; Codex then incorrectly took over the Assistant half, so the run is **contaminated as an autonomy proof**. Still shipped the valid request: web `075583e` adds `ender3_v4_combo`; remote iOS overlay `content_version=2026070401` includes the row and live hash verification passed; iOS bundled mirror `8129316` is local-only under the push gate. Locked next entry: postmortem observer-boundary failure and define the next observer-only S5/autonomy test before making any autonomy claim.
- **2026-06-20** — overlay Option 2: Part-C-aware validator + republish (snapmaker/creator LIVE) ([log](2026-06-20-cowork-appdev-overlay-option2.md)) — Made `scripts/validate-ios-printer-overlay.js` Part-C-aware: `collectBaselineUnion` now collides overlay ids only against baselines in `[min_app_version, FIRST_OVERRIDE_MERGE_VERSION="1.0.5")` (builds >=1.0.5 override-merge by id → excluded), letting the overlay KEEP aries/mega_x (graduated into the v1.0.5 bundle) while still delivering them to the v1.0.4 majority that doesn't bundle them. Backfilled the 1.0.5 baseline (ground-truth, iOS `518f781`), republished overlay `content_version=2026062001` adding snapmaker brand + snapmaker_2_a350 + creator_5_pro (rows byte-identical to web), recomputed hash. TDD genuine RED→GREEN (13/13). **Codex review GO** — applied its 2 optional items (split collision-union from known-brand-union via `collectKnownBrandIds` to kill a future-`min_app_version` footgun; accurate error text). Reconciled runbook **Phase 4b** to the override-merge boundary (the literal old rule would have regressed v1.0.4). Owner go → web pushed `c3afd27..e6b6945` → **curl-verified live** (sha256 matches committed). iOS untouched (push gate). 1 K3 finding (`2026-06-20-runbook-rule-stale-after-runtime-behavior-change`). Overlay Option 2 = the queue's last locked item, now done.
- **2026-06-20** — research-capable screening shipped + LIVE; first autonomous printer-add ([log](2026-06-20-cowork-appdev-research-capable-screening.md)) — Built **research-capable screening** (G1 Worker capture: brand-list expansion + Tier-2 brand-less `intent:"unresolved-brand"`; G2 Scout brand-less routing → manufacturer-null `needs-research(resolve-brand)` + digit-id guard; G3 agent contracts + S4 ledger enum). Hostile-reviewed **2 sub-agent + 3 Codex rounds** — Codex caught a **PII phone-leak** + fused-platform + prose-overmatch the sub-agent reviews missed; all patched (55 lib + 3 integration + full Scout green). Merged to web `main` (`97c2b25`). **First autonomous Printer Addition Assistant run** (branch, owner-ratified) added **Snapmaker 2.0 A350** (new brand) + **FlashForge Creator 5 Pro** (resolved brand-less → FlashForge), ground-truth-verified + merged `3776127` → **LIVE on web** (73 printers). iOS mirror local (push gate). Live smoke proved e2e (2 fakes → prod capture → Scout needs-research; KV cleaned). **Overlay Option 2 deferred = locked next** (validator Part-C-aware + republish; runtime verified safe, owner: no risk at session-end). 2 findings + 3 Assistant-contract autonomous-run rules. Process slip: `bridge --out-dir` cwd-relative → 2 re-reviews silently no-op'd, caught via git ground truth.
- **2026-06-16** — S4 Intake Retrospective + S2 intake capture (executed, pushed) ([log](2026-06-16-cowork-appdev-s4-retrospective-s2-capture.md)) — Two feedback-pipeline topics end-to-end under Work Protocol Full lane (per-gate hostile review→patch→QA→commit). **S4** (learning loop, Approach A — deterministic runtime, no LLM in the Scout path): outcomes ledger + Assistant permission amendment (G1) · `validate-guardrails-diff.js` chaining S3's validator (G2) · deterministic `intake-retrospective-gather.js` + owner-approved idempotent `apply-guardrails-diff.js` + retrospective contract + calibration (G3). Dormant until the ledger has entries; **S5 unblocked.** **S2** (capture completeness): Worker-side `extractPrinterMention` + widened tee fixing the v1 BLOCK (wrong-form requests → structured → actionable; bounded-span / 30d TTL / fail-open) (G1) · Scout lane provenance + order-independent form-wins `collapse()` + `heuristicCandidates` (G2); server-side → catches iOS too. Gate 3 nudge deferred (owner-pick). 3dpa `736a6a8`→`9969cff` + ai-om `b51c4b6`/`8710494` pushed. Reviews caught a real defect every gate (incl. an S2 HIGH PII leak), all patched. md-hygiene: `functions/` not asset-ignored → spawned task.
- **2026-06-15** — S3 Scout robustness + learned-guardrails config executed (Gates 1–3) ([log](2026-06-15-cowork-appdev-s3-scout-robustness-guardrails.md)) — Executed the 3 QA-green S3 gates on the printer-intake Scout under the Work Protocol Full lane: **G1** externalised the hardcoded guardrails into a versioned, validated `scripts/printer-intake-guardrails.json` + `scripts/validate-guardrails.js` (**behaviour-preserving refactor, byte-identical** — the config surface S4's learning loop diffs into); **G2** brand-alias seeds `bmbulab→bambu_lab`/`sparkx→creality` (2026-06-14 backlog false positives now dedupe) + conservative `possibleBrandTypo` hint + `report.version`→3; **G3** model-suffix-strip (`i7 w/CFS`→bundled `sparkx_i7`). Per-gate hostile review **GO/GO/GO-WITH-NITS** — caught a real MEDIUM (schema-valid-but-partial config silently disabled FDM declines) + LOWs, all patched + test-locked (35-case Scout suite green). Commits `1a6e256`→`13f602b`→`bd23085` + runbook `ff7508e` (3dpa) + Scout-contract `ce19a05` (ai-om). **Unblocks S4; S2 independent.** Plan-defect caught: "validator asserts length===14" would break Gate 2 — validator made count-agnostic, plan/spec corrected.
- **2026-06-15** — S1 prefill fix + overlay hardening → v1.0.5 on TestFlight ([log](2026-06-15-cowork-appdev-s1-overlay-hardening-1.0.5-ship.md)) — Executed **S1** (iOS contextual-feedback prefill fix — `.sheet(item:)` across 8 sites/6 screens; `ae03510`) + **Part C** overlay-collision hardening (dropped the all-or-nothing `isDisjoint` guard in `PrinterCatalogProvider.validatePayload` → `mergedArray` override-by-id, so one colliding overlay id no longer drops the whole overlay; `af4bbe0`) + **MARKETING_VERSION 1.0.5** (`518f781`). This mac-mini gained full **Xcode 26.5** + iOS 26.5 sim + `Config.xcconfig` → real local XCTest: **112 unit + 2 UITest green** (S1 RED→GREEN, Part C override-merge). Pushed `a2c1bc3..518f781`; **TestFlight run [27569280416](https://github.com/mustiodk/3dprintassistant-ios/actions/runs/27569280416) = success** (pending ASC processing + owner on-device S1 acceptance). Follow-ups: overlay **Phase-4b graduation** (drop now-bundled Aries/Mega-X) + S2/S3. Carried "Cloudflare Pages" md-hygiene finding verified **STALE/resolved**.
- **2026-06-15** — S1–S5 feedback-pipeline specs + impl plans (autonomous) ([log](2026-06-15-cowork-appdev-s1-s5-specs-plans.md)) — Produced **all 5 topics' QA-green spec + gated impl plan** (9 new artifacts: S1 plan + S2–S5 spec+plan), each write→sub-agent-review→patch→QA. Owner waived the per-topic check-in (autonomous run). Hostile reviews caught a real defect in **every** first draft (S2 v1 **BLOCK** pipe-disconnected → Worker-side `extractPrinterMention`; S3 **CRITICAL** sparkx-is-an-alias-not-suffix + config-off-served-path; S4 no-run-history-corpus + Assistant-permission-forbids-ledger; S5 wrong-S4-signal + cross-repo-PR + iOS-branch-push-hole). **Execution deferred to explicit owner command; no code touched.** Caught + filed a process miss: the S3 plan was committed before its review (belated review found 2 HIGH, patched). Locked next = owner go/no-go on execution.
- **2026-06-15** — Feedback-pipeline brainstorm + S1 spec ([log](2026-06-15-cowork-appdev-feedback-pipeline-brainstorm-s1-spec.md)) — Brainstormed 3 owner feedback items into a **5-spec plan** (S1 iOS prefill fix · S2 intake capture completeness · S3 Scout robustness + learned-guardrails config · S4 Intake Retrospective learning loop [Approach A: learn-between-runs, deterministic runtime; reuses lesson-spotter + Curator] · S5 Assistant autonomy ladder w/ PR + owner go/no-go). Key insight: the iOS contextual-feedback prefill bug is a *mechanical cause* of misrouted printer requests (opens General feedback → never teed to the Scout). **S1 spec written → hostile sub-agent review (10 findings, `patch-then-proceed`) → patched → QA-green.** Owner-defined per-topic process: spec+plan each get review/patch/QA; execution deferred. **Locked next = S1 impl plan.** No code shipped; cold-start ff-pulled 49 commits of mac-mini work.
- **2026-06-14** — Printer-intake real backlog run + Anycubic Mega X ship ([log](2026-06-14-cowork-appdev-printer-intake-real-backlog-mega-x.md)) — Seeded 16 verbatim Discord entries into `PRINTER_INTAKE` KV → Scout screened **3 needs-research / 1 duplicate / 5 declined-non-fdm / 7 unactionable**. **Anycubic Mega X SHIPPED** (the one genuine novel FDM add; `max_acceleration=400` from Anycubic OFFICIAL firmware, not an app-cap) — web `ebff194`+`8ad4293`+`115e628`, overlay `content_version=2026061402` **verified live** `[aries,mega_x]`; iOS mirror `9ea28c3` local (push gate). The 2 other needs-research survivors (X2D, i7 w/CFS) were already-bundled false positives caught by the Assistant catalog cross-check. Adversarial reviewer GO; visual picker OK. **2 P2 Scout follow-up findings filed (dedupe robustness; general-feedback invisibility) = locked next entry.** Attachments don't reach the agent session — owner pasted the backlog as text after 5 failed attach attempts.
- **2026-06-14** — iOS overlay Aries-collision fix ([log](2026-06-14-cowork-appdev-ios-overlay-aries-collision-fix.md)) — Aries was invisible on live iOS (v1.0.4): the overlay still carried `sparkx_i7` (bundled in v1.0.4) and the iOS runtime drops the **whole** overlay on any bundled-id collision, taking aries with it (so the prior entry's "overlay LIVE" was published ≠ delivered). Fixed under an owner-requested gated flow (spec→review→plan→review→5 gates, each subagent-reviewed + QA): aries-only overlay `content_version=2026061401` (**verified live**), ship validator hardened (collide vs union of baselines ≥ min_app_version + MARKETING_VERSION baseline + 6-case self-test), runbook Phase 4b overlay→bundled graduation rule. Web-only; iOS untouched. "Published ≠ delivered" K4 finding filed.
- **2026-06-14** — Printer-intake process patch + Voxelab Aries SHIPPED ([log](2026-06-14-cowork-appdev-printer-intake-process-patch-aries-ship.md)) — Closed the two NO-GO gaps (sanctioned `app-cap` for unpublished `max_acceleration`, advisory-only in the engine + scoped data-only iOS XCTest waiver) in the runbook + both agent contracts, then re-ran Aries green under an 8-gate plan (per-gate subagent review + QA; Gate 2 NO-GO→patched→GO). Official Voxelab manual sourced every field (bed 110°C beats reseller 100°C; no published accel → app-cap 5000 = lowest shipped bedslinger ceiling, notes[] provenance). Web pushed `9760f2d..60b231a`; overlay `content_version=2026061301` confirmed **LIVE**; iOS bundled mirror local-only (push gate). Real Discord backlog seeding is the next decision; Photon stays declined-non-fdm.
- **2026-06-13** — Voxelab Aries delivery rehearsal no-go + locked process entry ([log](2026-06-13-cowork-appdev-voxelab-aries-no-go-wrap.md)) — Aries was attempted then fully reverted after review because `max_acceleration` remained unsourced and local iOS XCTest was unavailable; locked next step is process adjustment before real backlog seeding.
- **2026-06-13** — Printer Intake Scout build + hostile review + ship ([log](2026-06-13-cowork-appdev-printer-intake-scout-ship.md)) — Built + tested (28 tests) + dual-hostile-reviewed (sub-agent + bridge→Codex, 4 patch rounds) + QA'd (43 scenarios, 38/43, 0 safety issues); pushed (13 commits). KV namespace created + bound. **Live tee deployed but NOT writing at runtime** (binding present in deployed version, POST ok:true, KV empty after 3min) → handed to Codex (`codex/printer-intake-tee-handover/HANDOVER.md`). 2 probe-the-wrong-layer misdiagnoses corrected + filed (REST-vs-wrangler token; config-vs-running-version binding). **Next: finish tee activation per handover, then researcher rehearsal (Voxelab Aries).**
- **2026-06-12** — Printer Intake Automation (design + Phase 1 tee) ([log](2026-06-12-cowork-appdev-printer-intake.md)) — New automation to turn missing-printer Discord feedback into a reviewed, shippable printer add (designed via ai-om Agent Factory; contracts in `ai-operating-model/docs/agents/`). **Phase 1:** `functions/api/feedback.js` now tees `missingPrinter` submissions to a `PRINTER_INTAKE` KV namespace — fail-open + dormant until the wrangler binding lands. Commit `2995ece` **local only, intentionally unpushed (owner: no deploy).** Ship-path is web-only (printers.json + overlay → Cloudflare); no app-version/TestFlight for printer adds. Scout will be a Node script in `scripts/`. **Next: build `scripts/printer-intake-scout.js` against fixtures.**
- **2026-06-04** — v1.0.4 confirmed live + stale PR #1 cleanup ([log](2026-06-04-cowork-appdev-v1.0.4-live-and-pr1-cleanup.md)) — Owner confirmed v1.0.4 is approved + live on the App Store (submitted 2026-05-20, no App Privacy change; supersedes 1.0.3). Investigated the personal-dashboard repo-health "ISSUES" flag on 3dpa → it was **1 open PR, not an issue** (issues=0): stale Cloudflare autoconfig bot **PR #1** (opened 2026-04-01, CONFLICTING/DIRTY), redundant + inferior to the hand-maintained `wrangler.toml` (no `main=worker.js` → would break `functions/` API routing; `directory="."` w/o `.assetsignore` → would re-expose `/.git`). **Closed PR #1** with note (owner-approved); 0 open PRs/issues now. No clean Cloudflare toggle to stop autoconfig PRs (opened once on detection) — closing is the fix. Trigger A: ROADMAP + CLAUDE/AGENTS hot-cache + memory flipped to v1.0.4-live; flagged `memory/project_3dprintassistant.md` as stale (2026-04-03 snapshot) for a consolidation pass. No code/engine/data/iOS-binary touched. **Next: lanes open (W web-feedback-routing / A resin PoC / B v1.0.5 hygiene) + memory consolidation.**

- **2026-05-20** — v1.0.4 submitted for App Store review ([log](2026-05-20-cowork-appdev-v1.0.4-asc-submit.md)) — Owner tested the v1.0.4 TestFlight build (build `202605192119`, run [26125919796](https://github.com/mustiodk/3dprintassistant-ios/actions/runs/26125919796)) → GO, picked cold-start Lane C (ASC submission). Pre-submit backlog-bundle assessment (matrix-style) → **recommend submit as-is, bundle nothing**: a tested TestFlight build is a known-good artifact; bundling resets the test cycle + re-introduces regression risk, and nothing in backlog cleared the iOS-binary + user-facing + high-value + low-risk bar (v1.0.5 carries are refactor/test-only; web-only items like `[CRITICAL-001-followup]` / `[LOW-011]` are decoupled and ship independently). Owner agreed. No v1.0.4 submit doc existed (only v1.0.3) → created [`3dprintassistant-ios/docs/app-store-v1.0.4-submit.md`](../../../3dprintassistant-ios/docs/app-store-v1.0.4-submit.md) grounded in the actual changeset since `3a59cd1`. v1.0.4 is an accuracy/honesty release (env-aware fan/temp/first-layer scaling + honest post-clamp warnings + Creality i7 bundled + Advanced cooling detail) — **no new data collection → App Privacy unchanged from 1.0.3** (simpler than the 1.0.3 submit). Owner submitted v1.0.4 for review (Manual Release). No code/engine/data touched; iOS HEAD `ed08507`, web HEAD post-wrap. Md-hygiene: flipped stale ROADMAP "Active Release: v1.0.3" header. Findings sweep: 0 K1/K2/K3/K4. **Next: owner reports approval/release; meanwhile web-only `[CRITICAL-001-followup]` OR Lane A (resin PoC) OR Lane B (v1.0.5 hygiene).**

- **2026-05-19** — bed-first-layer env-name prefix dedupe — slid into v1.0.4 TestFlight pre-ASC ([log](2026-05-19-cowork-appdev-bedfirstlayer-env-prefix-dedupe.md)) — Owner-reported screenshot: two warning banners on cold-garage scenario both led with `Cold garage (5–15°C)` (the dedicated bed-first-layer banner from MEDIUM-01 attribution-honesty path + the consolidated nozzle/first-layer-speed banner). Cold-start pivoted off the 3 PoC paths within ~1 turn. Matrix-style brainstorm surfaced 3 options; owner picked Option 1 (minimal — drop env.name prefix from bed-first-layer warning; preserves MEDIUM-01's 3-branch clamp-attribution copy intact; Option 2 full consolidation would have made partial/full-clip branches read as parenthetical soup in a comma-joined parts list). TDD applied cleanly: RED via new `v1.0.5 env-prefix-dedupe` block in walkthrough-harness.js at :1268-1313 (3 cases × 2 assertions: bed warning MUST NOT start with env.name + consolidated env_*_0 MUST start with env.name, pair contract); GREEN via stripping `${env.name}` from engine.js:1684/1688/1692 (3 MEDIUM-01 attribution branches). All gates green: validate-data 6/6, walkthrough clean (new dedup block + MEDIUM-01 non-regression both OK), matrix-audit 55/55 curated + 0 broad failures. Visual verification via preview server + DOM read confirmed "First-layer bed compensation: +7°C applied..." (no env prefix) + "Cold garage (5–15°C) compensation applied — nozzle +5°C, first-layer speed reduced 20%." (env framing preserved). iOS engine.js byte-identical sync; mirror XCTest `testV105_EnvPrefixDedupeOnBedFirstLayerWarning` added after `testV104_P1_5_MEDIUM01_BedClampAttribution`. iOS XCTest 110/110 unit (was 109 + 1 new). Web `46334e6` pushed (`9bdc4fc..46334e6` — flushed the 2 prior PoC wrap commits + my fix; Cloudflare auto-deploys). **Mid-session pivot:** owner asked to slide fix into v1.0.4 TestFlight (parked but not yet ASC-submitted, so safe). iOS commit message amended pre-push (`e2985f1` → `ed08507`) to reflect ride-along rationale instead of "v1.0.5 not yet ship-ready"; iOS pushed `c99a797..ed08507`; TestFlight dispatched [run 26125919796](https://github.com/mustiodk/3dprintassistant-ios/actions/runs/26125919796) — new v1.0.4 build uploads with same MARKETING_VERSION 1.0.4 (workflow auto-increments build number). v1.0.5 carry bundle reverts to all items pending. PoC paths (v5 mechanical pass / Gate 1 desk research / bridge cwd-scope preamble) remain valid for next session. Findings sweep: 0 K1/K2/K3/K4. Md-hygiene: clean. **Next session: owner-pick PoC re-surface OR v1.0.5 hygiene continuation OR v1.0.4 → App Store submission (after TestFlight build [26125919796](https://github.com/mustiodk/3dprintassistant-ios/actions/runs/26125919796) is accepted).**

- **2026-05-17 (evening)** — Resin-scaling bridge rounds 2+3 + v4 problem-statement ([log](2026-05-17-cowork-appdev-resin-bridge-r2-r3-v4.md)) — Session 2 of 2 today on the resin PoC. **R2 on v3 (7m wall)** caught 2 MUST-FIX + 6 SHOULD-FIX + 4 OPTIONAL → K1-2 filed for 3 Codex-surfaced Claude-turn-1 misses (C3 L-row, §5 sizing math, C14 errors). Owner-asks 5+6 batched → resolved Option A (decision tree restructure) + Option 2 (full v4 this session). **v4 produced**: §7 fully restructured as Option A decision tree (Step 1 owner picks Gate 2 → Step 2 5 branches by pick → Step 3 park rules + 4-week charter clock → Step 4 success criterion), §5 gained "Fires if" column (15 rows backfilled), printer-count drift footnote rewritten with correct sources at `app.js:101`+`:107`+`index.html:7,14,20,30`+`app-store-metadata.md:40`. **R3 on v4 (28m wall)** caught 1 MUST-FIX (index.html line-ref regression — v4 over-corrected v3) + 8 SHOULD-FIX (3 Codex-surfaced Claude-turn-1 misses: C4 ref `:68` is comment / struct at `:108`; §7 A/B vs §5 C1 contradiction; C5 F-speculation) + 3 OPTIONAL → K1-3 filed as combined K1+K3 (K3 sub-component: **bridge invocation cwd controls Claude-turn-1 read sandbox scope** — was assumed structural for 2 prior rounds, turned out operational). **v4 is foundation-correct in shape; v5 mechanical pass queued** (no R4 needed per R3 synthesis). **G2 (bridge multi-round amplifier) materially answered:** 3 rounds, value trend HIGH → MEDIUM-HIGH → MEDIUM, diminishing returns past R3 expected. **G3 (autonomy ceiling):** K1 chain documents operational ceiling (Claude turn 1 unreliable single reviewer); K4-1 (session 1) is structural ceiling; K3 sandbox-cwd is operational learning. iOS untouched (`c99a797`). v1.0.4 → ASC submission still parked behind PoC. **Next session: owner-pick v5 + Gate 1 desk research (`technical-differences.md`) + bridge/CLAUDE.md cwd-scope standing rule preamble.**

- **2026-05-17** — Resin-scaling discovery kickoff (PoC vehicle for autonomy + bridge multi-round test) ([log](2026-05-17-cowork-appdev-resin-scaling-poc-kickoff.md)) — Owner pivoted from v1.0.4 → ASC submission queue to a docs-only PoC simultaneously starting resin sub-project, testing bridge multi-round, and probing autonomy limits. New `docs/resin-scaling/` directory: README + problem-statement v3 (replaces v2 same session after bridge round 1) + parked `audience-overlap-survey.md` (drafted but owner reframed audience as himself — hobby project, not user-facing — dissolving Gate 1 entirely). v3 incorporates bridge round 1's 4 MUST-FIX (coupling list expanded C1→C15, gate order reversed, decision space widened with off-axis spectrum A–G + park outcome, brand-enumeration drop) + 6 SHOULD-FIX + load-bearing §2 audience reframe. **First K1 of the PoC: bridge Claude turn 1 fact-claim self-own on worker.js, caught by Codex turn 2 + retracted.** **K4 captured: audience-as-users assumption NOT bridge-catchable — headline autonomy-ceiling data point.** Mitigation pattern: explicit owner-intent statements at the top of foundation documents (v3 §2). PoC meta-track lives in [`ai-operating-model/docs/autonomy-poc-2026-05-resin/`](../../../ai-operating-model/docs/autonomy-poc-2026-05-resin/). Backups before launch: `Claude/backups/claude-snapshot-2026-05-17.tar.gz` (747MB) + `memory-snapshot-2026-05-17.tar.gz` (111KB) + `recovery-2026-05-17.md`. iOS untouched (`c99a797`). **Next session: bridge round 2 on v3 + technical-differences.md (Gate 1) drafting.**

- **2026-05-15** — printer-addition protocol trial: Photon Mono M7 Pro declined (resin/MSLA) + v3 → v4 surgical edit ([log](2026-05-15-cowork-appdev-printer-addition-protocol-trial.md)) — Owner brought Anycubic Photon Mono M7 Pro as the first real Trigger-C trial of the v3 protocol. Phase 1 step 3 sanity question caught it (Anycubic shelves Photon under "Resin 3D Printer"; 3dpa is FDM-only — engine routes to `bambu_studio`/`prusaslicer`/`orcaslicer`, materials are thermoplastics, nozzles + retraction are first-class). Recommended decline + v4 surgical edit; owner picked that option. **v4 edit:** added Phase 1 step 0 (FDM-only scope check) listing disqualified tech (MSLA / LCD / SLA / DLP / SLS / MJF) + example brands (Photon, Saturn / Mars, Form, Sonic, Halot); 12 insertions / 1 deletion in `docs/runbooks/printer-addition-protocol.md`. No risk trigger fired (docs-only; no engine / app / validator / spec / brand / overlay touched) — reviewer dispatch correctly skipped. All gates green: validate-data 6/6 + picker-dry-run.test.js 6/6 + walkthrough clean + matrix-audit 0/0 failures. **Commit `6314fda` pushed (`9754c47..6314fda`, also flushed previously-local wrap commit `8339a2a`).** iOS untouched (`c99a797`). New v1.0.5 carry added to ROADMAP: "FDM-only scope copy on a user-facing surface (web About / footer / iOS Settings); phrase 'MSLA / resin out of scope (future implementation)' to preserve optionality vs. runbook's harder operational 'no plans' stance." Decline note drafted for the requester (paste-ready). Trial findings: Phase 1 step 3 caught the resin classification cleanly; v3 → v4 closes the implicit-assumption gap; risk-triggered reviewer logic feels correctly calibrated. **Next session: gated on owner's TestFlight v1.0.4 feedback — if GO, submit to App Store review; if regression, focused fix arc.**

- **2026-05-15** — printer-addition protocol authored + 2 rounds of Codex review ([log](2026-05-15-cowork-appdev-printer-addition-protocol.md)) — Investigated the 2 prior post-overlay printer mishaps (X2D `b2f4367` overlay-only + Bambu correction-sweep `9ec6f4f` + validator strip `c153a42`; SPARKX i7 `d21e452`→`0a4cc2f`→`2284207` wrong-brand taxonomy caught in-app by owner). Authored runbook at [`docs/runbooks/printer-addition-protocol.md`](../runbooks/printer-addition-protocol.md) (v1 247 → v2 172 → v3 186 lines, locked after Codex SIMPLIFY-PROPOSED then TIGHTEN-SPECIFICS, both high confidence). Codex caught a real bug in v1: pasted Node snippets were invalid (`require('./engine.js')` doesn't work; `getPrintersByBrand()` shape misremembered). Replaced with checked-in [`scripts/picker-dry-run.js`](../../scripts/picker-dry-run.js) (116 lines) + [`scripts/picker-dry-run.test.js`](../../scripts/picker-dry-run.test.js) (99 lines, 6 tests including inverted-first TDD-RED demo for TC6). Phase 5 self-check (10 bullets) + risk-triggered `superpowers:requesting-code-review` (7 triggers: new brand, new series_group, overlay publish, engine/app/validator/spec touch, conflicting sources, deprecation, multi-printer). Standing rule 10 in `docs/3dpa-context.md` makes it binding. Vault trigger "Printer Addition Gate" under "3dpa Work Gates." 6 web commits pushed `fa8f31b..9754c47`; iOS untouched. Codex closing: "stop sanding it." Owner next: test protocol in fresh session.

- **2026-05-15** — v1.0.4 S9 — bug-free ship + TestFlight dispatch ([log](2026-05-15-cowork-appdev-v1.0.4-s9-bug-free-ship.md)) — Owner pivoted S9 from ship-gate-only to ship+all-known-bugs cleanup ("fix all bugs found by codex and all known bugs found by you. i want 1.0.4 to be (known) bug free"). Triaged the Codex post-S8.7 carry list + reviewer carry list into 3 v1.0.4 items: **M1** web engine fan_multiplier upper-bound asymmetry at engine.js:1246+:2655 (`3335a30`, RED probe humid.fan_multiplier 1.0→1.5 caught fan_max_speed=150% → fix clamped to 100%); **Obs1** iOS Advanced cooling missing slow_layer_time row — parity gap with web app.js:1253 (`c82dac2`, AdvancedFilamentSettings.slowLayerTime: String? + decoder + FilamentSettingsView ParamRow + Strings.slowLayer="Slow below layer time" verbatim parity + new XCTest with inverted-first RED via compile-error breadcrumb at line 1137:43, runtime-RED caught test-design bug `Int("10 s")` since engine forwards pre-formatted string → regex `\d+\s*s$`); **OBS-01** web doc note at docs/3dpa-context.md (`2f0ccb8`, plate-API debt documented per Codex's accept-criterion + missing getCompatibleNozzlesForPrinter row added to API table). Out of scope per owner-locked split: m2 test rename, F2 doc breadcrumb, MEDIUM-02 matrix-audit claim, all reviewer hygiene (helper extraction, magic constants, retired-IDs const, walkthrough hardcoded baseline). iOS engine sync `2207f8f` byte-identical; MARKETING_VERSION bump `c99a797` (xcodegen regenerated; 2× 1.0.4 / 0× 1.0.3 in pbxproj). Internal reviewer GO: 0 Critical / 0 Important / 0 Medium + 2 Minor v1.0.5 carries (Min-1 PLA-only test coverage tightening, Min-2 NSNumber decoder dead-code path). Reviewer-pattern now 7-for-7 across v1.0.4 arc. validate-data 6/6 + walkthrough 11 v1.0.4 OK lines + matrix-audit 47196/0 broad + 55/55 curated + iOS XCTest 109/109 unit (108 prior + 1 new S9 test). **Web pushed `d9e58fa..2f0ccb8` (Cloudflare auto-deploy). iOS pushed `eeb2915..c99a797` (10 commits flushed; 0 ahead). TestFlight dispatched [run 25892826819](https://github.com/mustiodk/3dprintassistant-ios/actions/runs/25892826819).** v1.0.4 is (known) bug-free per owner's bar. Next: v1.0.4 live monitoring + v1.0.5 hygiene cold-start.

- **2026-05-15** — v1.0.4 S8.7 — Codex post-S8.5 re-audit cleanup (2 Medium + 2 Low) ([log](2026-05-15-cowork-appdev-v1.0.4-s8-7-codex-cleanup.md)) — Shipped all 4 findings from the 2026-05-15 Codex post-S8.5 re-audit before S9 ship gate. Web `746244e` (Low #1: split TDD-RED breadcrumb convention into posture-specific rules in docs/ai-collab.md) + `48304d3` (Medium #2: validate-data range check `> 0 && <= 1` on env `fan_multiplier`, Path A locked over Path B engine-change). iOS `41c9956` (Medium #1: FilamentSettingsView cooling tab Advanced mode rewired to read env-scaled `advancedSettings.fanMinSpeed` / `coolingFanOverhang` instead of raw filProfile; new XCTest `testV104_S8_7_CoolingUI_AdvancedSourceIsEnvScaled` pins the source-of-truth contract with inverted-first RED demo verified `XCTAssertGreaterThan failed: ("63.0") is not greater than ("70.0")`; new `Strings.FilamentTab.fanOverhang`) + `ad46fd3` (Low #2: T5 x1c AMS-like sub-case mirrors web walkthrough negative assertions for `ams_lite_material_incompat` + `mcs_empty_no_multicolor`; inverted-first RED demo verified `XCTAssertTrue failed - ... got ids: ["pla_heat_creep"]`). Internal code-review subagent: GO. 1 reviewer Medium (M1 — engine has no upper-clamp on `fan_multiplier > 1`; schema/engine asymmetry) + 4 Minor — none Codex-originated, all carry to v1.0.5 cleanup. Reviewer-pattern stays 6-for-6. validate-data 6/6 + walkthrough 11 v1.0.4 OK lines + matrix-audit 47196/0 broad + 55/55 curated + iOS XCTest 108/108 (was 107 + 1 new S8.7 test). iOS local-only 7 commits ahead of `eeb2915` — Phase 2 push gate active. **S9 cold-starts on Phase 2.2 / Task 9 ship gate (no audit gate remaining).**

- **2026-05-15** — v1.0.4 S8.5 — Codex post-Phase-1 audit remediation (4 findings + 1 reviewer catch) ([log](2026-05-15-cowork-appdev-v1.0.4-s8-5-codex-findings.md)) — Remediates the 2026-05-14 Codex audit verdict (NO-GO for iOS push). F1 web `b3111cf`: cooling_fan_overhang env-scaled across 3 surfaces (Important #1). F2 iOS `fef685e`: AdvancedFilamentSettings Codable struct extended with fan fields + T2a/T12 direct assertions (Important #2). F3 iOS `6970c86`: testV104_T5_PracticalMCSTiers extended with 6 new sub-cases — ams_lite gate, CFS-Lite, generic_non_ams (mk4), multi_5 empty-MCS (Medium #1). F4 web `be5c4e8`: TDD-RED breadcrumb convention in ai-collab.md (Low #1). **Tightening (web `6e06404` + iOS `fd4761a`)** after internal-reviewer caught a 5th env-scaling miss both S8.5 + Codex missed: `resolveProfile.p.fan_speed` at engine.js:2641-2667 emitted raw 100%/50%/25%/0% from fanMap without env.fan_multiplier. Same intent as F1, different surface (the "Fan speed" column in every slicer tab). PLA+cold now correctly emits fan_speed=90%. M-3 hardening: hoisted `fanMultIsIdentity = abs(fanMult - 1.0) < 1e-9` replacing 5 sites of strict `fanMult !== 1.0`. Reviewer pattern now 5-for-5 across v1.0.4 arc. 11 cumulative v1.0.4 walkthrough OK lines (last asserts profile fan_speed=90%); validate-data 6/6; matrix-audit 47196/0 + 55/55. iOS XCTest 108/108. **iOS local-only 4 commits ahead — Phase 2 gate active.** **New Codex audit packet at `codex/post-s8-5-audit/codex-2026-05-15-post-s8-5-audit-packet.md`** + 612-line diff snapshot for owner dispatch before S9.

- **2026-05-14** — v1.0.4 S8 — Phase 2.1 iOS engine + data sync + 11 XCTest mirrors (**Phase 2.1 closes**) ([log](2026-05-14-cowork-appdev-v1.0.4-s8-ios-sync.md)) — Two iOS local commits on top of `eeb2915`: `350d23f` (initial sync — byte-identical engine.js 174618→201074 bytes + materials.json + nozzles.json + 11 new `testV104_*` tests + 4 helpers) and `fc3ee6b` (tightening per internal code-review subagent — addressed 4 Important + 2 Minor findings: T6 PLA chamber-cap branch added entirely, T2a HIGH-05 banner check added, T3/T4/T2b loose substring matchers tightened to exact IDs). **NO push** — Phase 2 gate active until S9 5-point ship-ready check. iOS XCTest 96 → 107 unit (108 incl. ScreenCaptureUITests); all green. TDD-RED demo on T1 (`XCTAssertEqual(strongOuter, 999, ...)` inverted → flipped to 90). Smoke test pre-additions confirmed forgiving-Codable rule held for all v1.0.4 additive data shapes. AdvancedFilamentSettings doesn't expose fan; T2a covered transitively via T12 (P1.5 HIGH-01 export); S9 carry-forward: extend the struct. Reviewer pattern now 4-for-4 across Phase 1.5+S8 — caught real coverage gaps each session. Web close commit also lands the S8 implementation plan + this log + INDEX + ROADMAP + NEXT-SESSION regen. **S9 cold-starts on Phase 2.2 / Task 9: UI walkthrough + MARKETING_VERSION 1.0.3 → 1.0.4 + 5-point ship-ready handoff.**

- **2026-05-14** — v1.0.4 S7-3 — Phase 1.5 MEDIUM-01 first-layer bed-clamp attribution honesty (**Phase 1.5 closes**) ([log](2026-05-14-cowork-appdev-v1.0.4-s7-3-medium-01.md)) — Two web commits: `243be51` (impl) + `58d919b` (tightening after internal code-review subagent surfaced 3 Important findings; partial-clip branch coverage gap was the substantive must-fix). The env first-layer bed warning previously claimed `"+N°C applied"` regardless of clamping — Codex's static example (PETG Basic + cold + small-cap printer) made the lie visible. Fix (Option B per Codex wording): rewrite warning copy with effective post-clamp delta + 3 branches (full apply / fully clipped / partially clipped, naming the binding cap source). Pair fix: `printer_max_bed_temp_clamped` initTarget now includes `bed_first_layer_adj`, so PETG + Kobra 3 Max + cold (initTarget 92 > 90°C printer cap) now fires the cap-warning correctly (was the gap pre-fix). Attribution discipline parity with MEDIUM-05's nozzle-side env_compensation_capped_by_material. Tightening added partial-clip branch coverage via PLA + X1C + vcold (the only naturally-reachable partial-clip combo with current data: effective +5°C, requested +10°C). 11 cumulative v1.0.4 walkthrough OK lines (new MEDIUM-01 block covers all 3 copy branches: full / fully-clipped / partially-clipped + printer-cap pair + non-regressions); validate-data 6/6; matrix-audit 47196 broad / 0 failures + 0 curated. iOS untouched (`eeb2915`, Phase 2 gated). **All 4 owner-triaged Phase 1.5 web findings now shipped (HIGH-01 ✅ + HIGH-02 ✅ + MEDIUM-01 ✅ + LOW-01 ✅). S8 cold-starts on Phase 2.1 / Task 8 iOS engine + data byte-identical sync + XCTest mirroring + one iOS local commit (no push).**

- **2026-05-14** — v1.0.4 S7-4 — Phase 1.5 LOW-01 test-contract hardening (retired warning IDs) ([log](2026-05-14-cowork-appdev-v1.0.4-s7-4-low-01.md)) — Owner pivoted per-finding order: LOW-01 first, MEDIUM-01 still pending as S7-3. Two test-only web commits: `d863635` (impl) + `bc9c655` (tightening after internal code-review subagent surfaced a real placement bug). Added negative assertions for retired warning IDs in successor positive cases: `nozzle_too_small` absent on harness HIGH-12 (paired with existing `cf_small_nozzle`); `creality_no_multicolor` absent on harness MCS empty-MCS case (centauri_carbon — generalized re-introduction guard) + ender3_v3_se sub-case added in tightening (Creality manufacturer-gated re-introduction guard); `k2_plus_cfs` absent on harness MCS CFS + CFS-Lite + matrix-audit `creality-k2plus-multicolor-cfs` line 146 + `postfix-k2-pla-multicolor-cfs-copy` line 283. TDD-RED demo on `nozzle_too_small` (deliberately inverted assertion → harness threw with full warning-ID dump → flipped to correct form). Reviewer findings: 0 Critical / 3 Important / 3 Minor; 2 addressed in tightening (Creality empty-MCS pairing + HIGH-12 console.log symmetry), 1 carried as informational follow-up (shared `RETIRED_IDS` const array — multi-arc audit, deferred). 10 cumulative v1.0.4 walkthrough OK lines (same count — guards fold into existing HIGH-12 + MCS blocks); validate-data 6/6; matrix-audit 47196 broad / 0 failures + 0 curated failures. iOS untouched (`eeb2915`, Phase 2 gated). **S7-3 cold-starts on Codex MEDIUM-01** (first-layer bed-clamp attribution honesty — only remaining Phase 1.5 web finding before S8 Phase 2.1 iOS sync).

- **2026-05-13** — v1.0.4 S7-2 — Phase 1.5 HIGH-02 PLA cold/chamber safety, Option B ([log](2026-05-13-cowork-appdev-v1.0.4-s7-2-high-02.md)) — Two web commits: `a4b8873` (impl) + `f2aa84f` (tightening after internal code-review subagent surfaced 3 Important findings: PVA scope creep + vcold harness gap + validate-data schema gap). Data: `safe_chamber_temp_max: 50` inside `enclosure_behavior` on all 5 PLA-family materials (pla_basic/matte/silk/metal/cf) — mirrors PETG Basic shape; `chamber_above_material_safe` guard now fires for X1E+PLA naturally (zero engine-side change required for the guard itself, validates the `mat?.field ?? mat?.enclosure_behavior?.field` pattern from S5). Engine: pla_heat_creep extended from `enclosure === 'passive'` to `enclosure !== 'none'` (X1E now gets open-door guidance); getChecklist "Preheat enclosure" suppressed for PLA-family + enclosed (pre-existing item #5 "Open front door" carries the positive guidance); getWarnings env-verbatim pass filters `/door closed|preheat the enclosure|preheat \(/i` for PLA-family + enclosed. Tightening narrowed all three suppression predicates from "marker-presence" to `material.group === 'PLA'` (PVA still gets "Keep door closed" per existing humidity posture); added (g) PVA+X1C+cold scope-guard assertion + (h) PLA+X1E+vcold pin assertions to harness; added optional-field type+range schema check in validate-data.js for safe_chamber_temp_max + open_door_threshold_bed_temp. 10 cumulative v1.0.4 walkthrough OK lines (HIGH-02 line covers cold+vcold + PETG+PVA non-regressions); validate-data 6/6 clean; profile-matrix-audit 55/55 curated + 47196 broad / 0 failures. iOS untouched (`eeb2915`, Phase 2 gated). **S7-3 cold-starts on Codex MEDIUM-01** (first-layer bed-clamp attribution honesty — engine.js:1227 + :1624-1631 + :1706-1727).

- **2026-05-13** — v1.0.4 S7-1 — Phase 1.5 HIGH-01 export env-fan + draft_shield ([log](2026-05-13-cowork-appdev-v1.0.4-s7-1-high-01.md)) — Owner-triaged Phase 1.5 scope: 2 HIGH + MEDIUM-01 + LOW-01 in v1.0.4; defer MEDIUM-02 + OBS-01 to v1.0.5. HIGH-02 modeling Option B (right-thing, not corner-cutting). S7-1 shipped Codex finding HIGH-01 web-only: BAMBU_PROCESS_MAP gained `draft_shield → enable_draft_shield`; `exportBambuStudioJSON` filament fan reads env-scaled `adv.fan_min_speed`/`fan_max_speed`; `formatProfileAsText` fan line reads env-scaled; app.js advanced filament panel renders env-scaled with inline S-wrapped prov. New walkthrough block "v1.0.4 P1.5 HIGH-01-export" asserts text + BS process + BS filament all use env-scaled fan (63/90 for PLA + cold) + `enable_draft_shield="1"`. 9 cumulative v1.0.4 OK lines; validate-data 6/6 clean; profile-matrix-audit 55/55 curated + 47196 broad / 0 failures. Two web commits: `fe2964e` (docs: capture Codex response) + `eaf3f09` (fix: HIGH-01). iOS untouched (`eeb2915`). Skill protocol invoked mid-execution per owner prompt: `superpowers:test-driven-development` + `verification-before-completion` loaded; TDD-first RED→GREEN→verify pattern applied. **S7-2 cold-starts on HIGH-02 PLA cold/chamber safety, Option B** — `safe_chamber_temp_max` for PLA-family + material-aware preheat/cold-warning rewrite + harness X1E+PLA assertion flip. Per-finding-checkpoint splits S7 into S7-1/2/3/4 sub-sessions.

- **2026-05-13** — v1.0.4 S6 — Phase 1.5 Codex audit packet prepped ([log](2026-05-13-cowork-appdev-v1.0.4-s6-codex-packet.md)) — Owner-gated dispatch (Option B low-touch). S6 prepared the v1.0.4 Codex audit packet + diff snapshot + empty response placeholder under `codex/v1.0.4-audit/` and surfaced a copy-paste-ready dispatch command in the Trigger A close note. **Owner runs Codex manually.** Diff snapshot covers Phase 1 commit range `5bcd68b..901153a` filtered to engine.js + data/ + walkthrough-harness.js + profile-matrix-audit.js (1303 lines, `+744/-225`). Packet authored per `docs/ai-collab.md` Review Packet template with 10 concrete uncertainties + 8 canonical new Phase 1 warning IDs grepped from `engine.js` (Task 1 introduced no warning ID — NEXT-SESSION's drift-risk flag confirmed). PHASE1_END used explicit `901153a` instead of HEAD per S5-learned plan-staleness lesson (HEAD = `e36a91b` is docs-only S5 close). Verification gate green: 8 v1.0.4 walkthrough OK lines + curated 11/11 + DQ-2 Safe/Tuned + profile-matrix-audit "No core failures" across 55/55 curated + 47196 broad + validate-data 6/6. One commit `690519e` pushed (3 files, +1388 lines). iOS untouched (`eeb2915`). **S7 cold-starts on owner-pasted response file** at `codex/v1.0.4-audit/codex-2026-05-13-v1.0.4-audit-response.md`.

- **2026-05-13** — v1.0.4 S5 — Tasks 6 + 7 shipped; Phase 1 complete ([log](2026-05-13-cowork-appdev-v1.0.4-s5-impl.md)) — Two Phase 1 web commits landed in one session with zero reviewer round-trips. Task 6 (chamber safe-cap guard, HIGH-05): warning-only emission `chamber_above_material_safe` adjacent to the existing `active_chamber_heating` block at engine.js:1750, independent of the PA/PC group gate so PETG/PET (safe_cap=50°C) on active-chamber printers (X1E max 60°C, etc.) fire the guard. Canonical combo was switched from plan's X1E+PLA Basic (no safe_cap field) to X1E+petg_basic; harness has 2 silent-for cases too. Web `6f9e542`. Task 7 (nozzle min-diameter cleanup + nozzle-side authority drop, HIGH-12+HIGH-06): retired `cf_small_nozzle` block entirely; renamed `nozzle_too_small` → `nozzle_below_min_diameter` with generic parameterized body (no more "0.2mm" hardcode); stripped `suitable_for`/`not_suitable_for` from all 9 nozzles + matching schema soft-check + validate-data array-type checks; S4 sweep applied — profile-matrix-audit.js:280 updated in the same commit. Web `901153a`. Walkthrough now has 8 cumulative v1.0.4 OK lines; profile-matrix-audit 55/55 curated + 0 broad failures; nozzle-side arrays absent across all 9 entries. iOS untouched (`eeb2915`). **Phase 1 complete (7/7 web tasks shipped)** → S6 picks up Phase 1.5 packet prep (owner-gated Codex dispatch).

- **2026-05-13** — v1.0.4 arc restructure ([log](2026-05-13-cowork-appdev-v1.0.4-arc-restructure.md)) — Meta-session between S4 and S5 (not numbered as Sn). Split Phase 2 into 2.1 (engine + data sync + iOS XCTest as one iOS local commit) and 2.2 (UI walkthrough + MARKETING_VERSION bump as second iOS local commit + 5-point ship-ready handoff); inserted new Phase 1.5 — Codex audit gate (owner-gated Option B low-touch dispatch) between Phase 1 and Phase 2.1 per `docs/ai-collab.md` criteria 1/2/3/5. Plan-review-confirm-execute discipline applied: Plan agent returned 2 BLOCKERs + 5 SHOULD-FIX + 4 NICE under HIGH confidence; all addressed in single commit `12fd33c`. Stop conditions for Phase 1.5: ≥1 CRITICAL or ≥5 HIGH → owner pause; 1-4 HIGH → autonomous web-only remediation; MEDIUM/LOW → owner-triaged. Cold-start surfaced a real iCloud-sync race (MacBook saw phantom drift on `engine.js` + `walkthrough-harness.js` while small files had already synced); diagnosed via reflog `commit:` entries proving shared `.git/` between iMac and MacBook + mtimes preserved across sync. Drift evaporated mid-investigation as iCloud caught up — protocol's cold-start `git status` guard caught it correctly, no protocol gap. Commit `12fd33c` unpushed: MacBook lacks GitHub HTTPS credential helper; owner deferred push + identity fix.

- **2026-05-13** — v1.0.4 S4 — Task 5 5-tier MCS resolver ([log](2026-05-13-cowork-appdev-v1.0.4-s4-impl.md)) — Practical multicolor 5-tier resolver shipped to web as `dc49c52` + `1695cba`. New `_mcsTier(printer)` returns `none`/`ams_lite`/`ams_like`/`cfs`/`generic_non_ams`. Empty-MCS printers (centauri_carbon) suppress prime_tower + flush_into_infill + AMS checklist; A1/A1 Mini gate by material.ams_lite_compatible; CFS/CFS Lite/MMU3/ACE/IDEX/Toolchanger/Filament Hub get tier-appropriate guidance copy. Retired Creality-only manufacturer hardcode + k2_plus_cfs. Walkthrough has 6 cumulative v1.0.4 OK lines (9 sub-assertions in MCS block). Fixup also closed the curated audit drift (k2_plus_cfs → mcs_tier_cfs_guidance) and polished generic_non_ams sysLabel to use display-name map ('Filament Hub' / 'Toolchanger' / etc.) instead of underscore-bearing UPPERCASE. iOS untouched. 5 of 7 Phase-1 web tasks shipped → S5 picks up Task 6 (chamber safe-cap).

- **2026-05-13** — v1.0.4 S3 — Task 4 plate guard ([log](2026-05-13-cowork-appdev-v1.0.4-s3-impl.md)) — Physical printer × plate guard + material plate range bundle (HIGH-02 + HIGH-03 material half) shipped to web as `bc070af` + `bf05586`. Fixup found a real silent-regression: original steady-state-only bed-temp recipe missed 3 PETG-on-plate combos that go out-of-range on the initial layer (initial_layer_bed_offset + PETG +5 bump pushes 75°C base to 85°C on a 80°C-max textured_pei plate). Full engine recipe now mirrored. Walkthrough has 5 cumulative v1.0.4 OK lines; profile-matrix-audit clean across 47196 broad configs. iOS untouched. Per-finding checkpoint wrapped at Task 4 (Task 5 is 4-engine-site refactor; better fresh).
- **2026-05-13** — v1.0.4 S2 — Tasks 1, 2, 3 shipped ([log](2026-05-13-cowork-appdev-v1.0.4-s2-impl.md)) — Three v1.0.4 web tasks under subagent-driven-development: strength `speed_multiplier` (web `133be38` + `246171d`), env data layer + cold-warning copy + env clamp attribution (web `f7b34f1` + `d959537`, with Bambu export-path gap filed as deferred), physical printer × nozzle guard (web `1724f86` + `b238a82`). Walkthrough now has 4 cumulative v1.0.4 OK lines; profile-matrix-audit clean across 47196 configs. iOS untouched (Phase 2 gated). Pattern: code-quality reviewer flagged Important issues on every task; tightening to exact-value / exact-ID asserts is the standing recommendation for S3+.
- **2026-05-13** — v1.0.4 setup ([log](2026-05-13-cowork-appdev-v1.0.4-setup.md)) — `merged.md` locked at SHA `5bcd68b` (web `05e85bd`); per-task TDD-first implementation plan written at `docs/superpowers/plans/2026-05-13-v1.0.4-config-impact.md` (web `129d6a7`); CR3-M1 audit-script `userLevel` fix shipped (web `8ff25eb`). Multi-session autonomous arc starts S2; per-finding checkpoint rule + Trigger A between sessions; iOS push gate enforced.
- **2026-05-12** — SPARKX i7 overlay hotfix ([log](2026-05-12-cowork-appdev-sparkx-i7-overlay-hotfix.md)) — User-requested i7 printer shipped to current iOS via remote overlay `2026051202` and web/iOS catalogs; corrected taxonomy is Creality → i Series → i7 with no standalone SPARKX brand; validator now compares overlay IDs against shipped iOS bundled baselines.
- **2026-05-11** — v1.0.4 core planning ([log](2026-05-11-cowork-appdev-v1.0.4-core-planning.md)) — Codex cross-pass is complete; owner locked v1.0.4 planning defaults (core-first, advisory chips, practical MCS tiers, chamber guard-only, material-side nozzle authority, research before numeric formulas); reviewer agent checked the plan twice; next step is `merged.md`, then core implementation.
- **2026-05-11** — config-impact QA pass + cross-pass review ([log](2026-05-11-cowork-appdev-config-impact-qa.md)) — Two real bugs filed (web Advanced filament temps fix shipped as web `809549d`; environment compensation deferred behind structured QA pass). Authored parallel-then-cross-pass QA handover, ran Claude pass (29 findings: 12 HIGH / 6 MEDIUM / 6 LOW / 5 OBS), Codex pass (10 findings) ran separately, Claude reviewed Codex with meta-comparison (5 mutual HIGHs as v1.0.4 safe core; ~12 unique-to-each; severity disagreements narrow). Codex cross-pass prompt ready. No CRITICAL findings. Web `809549d` + `9925f86` + `ab979f4` + this session-close commit; iOS untouched.
- **2026-05-10** — v1.0.3 App Review submission wrap ([log](2026-05-10-cowork-appdev-v1.0.3-app-review-wrap.md)) — Owner reported phone QA passed and build `202605101637` was submitted to App Review; ROADMAP now marks stale builds, captures the post-v1.0.3 safe-follow-up todo list, and sets the next lane to App Review monitoring before any cleanup work.
- **2026-05-10** — multi-surface wrap protocol + vault collaboration guide ([log](2026-05-10-cowork-appdev-multi-surface-wrap-protocol.md)) — Repaired protocol drift from an earlier wrap by updating ROADMAP to the current TestFlight candidate (`202605101130`), created and wired the vault AI collaboration area with copy-paste triggers, audited vault `CLAUDE.md`, added multi-surface wrap-up guidance, and regenerated `NEXT-SESSION.md` with the full Trigger C read order. Vault Bambu raw-source changes remain intentionally untouched.
- **2026-05-10** — profile temperature audit + TestFlight ([log](2026-05-10-cowork-appdev-profile-temperature-audit-testflight.md)) — Built a repeatable profile-matrix audit (`scripts/profile-matrix-audit.js`) after phone testing exposed bed/nozzle/chamber/warning confusion. Broad sweep found one bug family: nozzle adjustments could exceed printer/material caps. Fixed simple, advanced, legacy export, and Bambu export nozzle clamping; added printer/material cap warnings; suppressed enclosed-printer open-frame PC copy; added chamber max copy; replaced raw picker speeds with `High-speed`; generalized Creality CFS warning. Added lightweight profile/data test protocol. Verification: data validation green, 55/55 curated audit pass, 46,512 broad configs with 0 failures, 15/15 independent post-fix pass, walkthrough pass, UI smoke pass, iOS `engine.js` byte-identical, 85 iOS tests green. Pushed web `39a8d0e`, iOS `6bd1210`, and dispatched successful TestFlight run [`25627557344`](https://github.com/mustiodk/3dprintassistant-ios/actions/runs/25627557344), version `1.0.3`, build `202605101130`.
- **2026-05-10** — iOS remote printer catalog + TestFlight ([log](2026-05-10-cowork-appdev-ios-remote-printer-catalog.md)) — Added App Store-safe, offline-first iOS remote printer overlay support: web-hosted data-only overlay + validator, Swift `PrinterCatalogProvider`, shared DataService/EngineService merged catalog, strict allowlist/hash/version/value validation, rollback via `enabled:false`, and 17 targeted catalog tests. Senior + QA gates went red on Bool-as-number and cross-runtime hash canonicalization, then green after fixes; full web checks, 85 iOS unit tests, and UI screenshot walkthrough passed. Pushed web `e206a89`, iOS `170114c`, verified live overlay URL, and dispatched successful TestFlight run [`25614975605`](https://github.com/mustiodk/3dprintassistant-ios/actions/runs/25614975605), version `1.0.3`, build `202605092356`.
- **2026-05-09** — analytics dashboard UX + platform lens ([log](2026-05-09-cowork-appdev-analytics-dashboard-ux.md)) — Redesigned `/analytics` from raw query tables into an owner dashboard, added `summary` + `profile_mix` canned queries, shipped Product lens (`All` / `Web` / `iOS`) so Web and iOS can be read side by side or filtered globally, added iOS version-aware diagnostics labels (`iOS 1.0.3`), made diagnostics mobile-friendly, pushed web commits `eb66b18`, `e9ed6b9`, and `5c60549`, and verified production after each deploy.
- **2026-05-09** — iOS analytics parity + v1.0.3 App Store submission prep ([log](2026-05-09-cowork-appdev-ios-analytics-app-store-submit.md)) — Added iOS `outputMode` parity for privacy-preserving analytics, preserving App Store review-prompt hashing separately; 4/4 targeted analytics tests + 68 unit tests + 1 UI screenshot walkthrough passed; pushed iOS `153adbc` and dispatched successful TestFlight run [`25596797349`](https://github.com/mustiodk/3dprintassistant-ios/actions/runs/25596797349), build `202605090842`. Updated analytics spec/ROADMAP/NEXT-SESSION, checked current public App Store listing (1.0.2, Diagnostics-only privacy card), created v1.0.3 App Store submit notes with Product Interaction privacy label guidance, final Promotional Text, and final owner-used What's New copy; owner submitted v1.0.3 to App Review on 2026-05-09.
- **2026-05-09** — PLA Metal + analytics dashboard ([log](2026-05-09-cowork-appdev-pla-metal-and-analytics-dashboard.md)) — Added Bambu PLA Metal across web+iOS from owner-curated Bambu vault sources, mirrored engine/data byte-identically, verified walkthrough + DQ-2 + full iOS XCTest, pushed both repos, and dispatched TestFlight run [`25576365270`](https://github.com/mustiodk/3dprintassistant-ios/actions/runs/25576365270) as same-version v1.0.3 build `202605081953`. Built and deployed owner analytics dashboard at `/analytics` backed by `/api/analytics-query` and Cloudflare Workers Analytics Engine dataset `3dpa_usage_v1`; helped configure Wrangler secrets/dataset/binding; debugged SQL dialect quirks; added web `outputMode` analytics + "Generated profiles" dashboard card. Fixed a live Workers Assets hygiene issue where `/.git/config` was publicly served; added `.assetsignore` + Worker hard-blocks and verified `404`.
- **2026-05-08** — AI operating-model cleanup ([log](2026-05-08-cowork-appdev-ai-operating-model-cleanup.md)) — Retired the active AI Operating Model pilot workflow without archiving the `ai-operating-model/` project. Promoted useful pilot artifacts to 3dpa `main`, rewrote `docs/ai-collab.md` as lightweight tool-routing guidance, marked analytics v1 shipped, deleted remote `ai/operating-model-pilot`, and left local stale-branch doc edits as an explicit cleanup finding.
- **2026-05-08** — ROADMAP slim + lifecycle protocol refactor under multi-gate Codex review ([log](2026-05-08-cowork-appdev-roadmap-slim-and-lifecycle-refactor.md)) — Long meta-layer doc refactor under 5-packet Codex gate sequence (Gate 0 plan → Gate 1a Phase 1a output → Gate 2 Phase 2 output → Gate 2.5 pre-execute fix-plan → Gate 2.6 post-execute output). 3dpa ROADMAP slimmed 773 → 224 lines + 4 archive files (663 lines). `docs/3dpa-context.md` promoted to canonical owner of engine API + app state shape (closes authority loop with iOS CLAUDE.md). iOS CLAUDE.md trimmed 184 → 47 lines. `Projects/CLAUDE.md` + AGENTS.md byte-identical lifecycle refactor: Trigger A monolithic paragraph → 5 phases × 12 numbered steps; md-hygiene moved BEFORE log finalization; Trigger A destination labels renamed to semantic forms `(5-vault)/(5-documented)/(5-sessions-dir)/(5-session-log)/(5-fallback)`; Trigger C 3dpa cold-start read order updated to include `docs/3dpa-context.md`; new "what goes where" subsection enforces 3-tier ownership (live ROADMAP / evergreen 3dpa-context / archive). Memory entry `feedback_session_lifecycle_triggers.md` updated. Codex caught 5 MF + 4 SF on Gate 2 — most importantly authority-loop risk that would have orphaned canonical engine API + state shape if iOS CLAUDE.md had been trimmed pre-promotion. **Durable lessons:** (a) multi-gate Codex review pattern is appropriate-once for cross-project blast-radius doc refactors, NOT normalized for ordinary feature work; (b) 3dpa now has 3-tier doc ownership (live planning / evergreen architecture / historical archive) enforced by Trigger A's "what goes where" subsection; (c) auto-sync on Projects/ root means meta-layer protocol edits commit silently across the session — 3 auto-sync commits for this session's lifecycle work. Projects/ root edits already in `4ae2c51 + 4d70be1 + 70bfc3f` auto-sync commits; 3dpa-web + iOS uncommitted (owner commit-shape decision). 5 Codex packets preserved at `3dprintassistant/codex/roadmap-slim-and-lifecycle/`.
- **2026-05-08** — v1.0.3 batch items 1 + 3 shipped end-to-end ([log](2026-05-08-cowork-appdev-v1.0.3-items-1-3-ship.md)) — Long, ambitious session: item 3 (iOS App Store review prompt) shipped through 6 Codex peer-review passes (1 design + 4 milestone + 1 post-implementation) + 1 Gemini research pass; 5 caught-before-ship bugs (4 design + 1 runtime stranded-eligibility); `ReviewPromptService` + `StoreKitDistributionDetector` + `ProfileKeyHasher` + 18 new XCTests (46 → 64); MARKETING_VERSION 1.0.2 → 1.0.3; TestFlight dispatched. Item 1 (printer changes) Phase A scope unlocked + applied: Kobra X 6-field correction (open-frame bedslinger, not enclosed CoreXY) + Centauri Carbon new entry (CoreXY passive enclosure, 320°C / 110°C / 500 mm/s), 4 commits per one-finding-one-commit-per-platform rule, web auto-deployed via Cloudflare Pages, iOS in main awaiting next TestFlight build; 2 IR-5 followups filed (0.25mm nozzle catalog, `pla_specific` plate identifier). Item 2 (environments) assessed: handover drafted but never sent to Gemini — multi-day workstream, not bundlable with item 1 in this session. **Durable lessons:** (a) multi-pass Codex review (design + post-impl) catches different bug classes — both worth running for hard-to-reverse features; (b) wiki-pages-as-primary-source workflow validated 3 times — default for Apple/manufacturer JS-SPA pages; (c) novelty controls counter increments only, not eligibility evaluation (engagement-tracking pattern); (d) ROADMAP truth forks between feature branch and main — update both at ship time. Web `0f47b44 / 8de9381 / 047a7f8` on main + `381dac1 / f3549ee` on `ai/operating-model-pilot`; iOS `756b107 / 1bb543e / de4aa9d / 2309af1 / 066add8 / b0d1315 / 15930c1` on main (8 commits). 14 walkthrough + 64 iOS XCTests green throughout.
- **2026-05-08** — v1.0.3 batch planning + Gemini research handovers ([log](2026-05-08-cowork-appdev-v1.0.3-batch-planning-and-handovers.md)) — Owner pivoted from DQ-3 to a 5-item v1.0.3 batch (printers, environments, iOS App Store review prompt, analytics, web output-panel UX). Workflow locked: Claude plans + codes, Gemini researches via `docs/research/gemini-*.md` handovers, Codex reviews via `codex/<feature>-review/` packets. **Created `docs/3dpa-context.md`** as evergreen project-context reference linked from every handover/packet. **Drafted 3 Gemini handovers**: printer audit (declined — Gemini failed source-or-null bar; switched to owner-manual procedure with Anycubic/Elegoo wiki pages added by owner to `Obsidian Vault/50-Wiki/raw/3dpa/`), environment-taxonomy (drafted + linter-tightened with engine-consumption reality table), iOS review prompt (lessons from #1's failure baked in: Task 0 starting-state self-check + Quality bar explicitly cites #1's decline). **Phase A printer-audit scope locked + held by owner choice** — Kobra X 6-field correction (existing data wrongly classified as enclosed CoreXY; Anycubic FAQ confirmed open-frame bedslinger with 100°C bed, 0.25/0.4/0.6/0.8 swappable nozzles) + new Centauri Carbon entry (CoreXY passive enclosure, CANVAS multi-color "On the Roadmap" so ships `multi_color_systems: []`). 2 IR-5 followups deferred: 0.25mm nozzle catalog, `pla_specific` plate identifier. **Tool-fit lesson:** spec-table / hard-data lookups → not Gemini; synthesis tasks → Gemini OK. **Gemini #3 response landed during wrap-up** — substantive Resolution next session. Codex review skipped on Phase A per per-phase filter (0 of 7 triggers). DQ-3 deferred to post-v1.0.3. 8 commits on `ai/operating-model-pilot` (12 ahead of origin total, all local-only). No engine/data/test changes; all work in `docs/`.
- **2026-04-30** — AI collaboration pilot v1 shipped ([log](2026-04-30-cowork-appdev-ai-collab-pilot.md)) — 4-round multi-tool review (ChatGPT discovery → Claude Code v1+v2 → ChatGPT v2 → Claude Code v3 → ChatGPT v3 adoption) produced `docs/ai-collab.md` (operating-model v1, single file ~280 lines). Review trail of 6 handoff files committed. Branch `ai/operating-model-pilot` 2 commits ahead of `main` (`dece4a2` trail + `b453098` ai-collab). New meta-track project established at `Projects/ai-operating-model/` with own CLAUDE.md (cold-start + fresh-session + wrap-up procedures), tokens `ai-om` / `ai-operating-model` / `aiom`. Top-level `Projects/CLAUDE.md` + memory updated. **First Codex review queued: DQ-3 scraper-vs-stub at `NEXT-SESSION.md:84`.** End-of-pilot decision target 2026-05-14; merge decision on the branch is part of the evaluation. ROADMAP not touched (operating model is operational, not feature work). No code, walkthrough, or XCTest changes.
- **2026-04-27** — App Store link on web + session policy hardening ([log](2026-04-27-cowork-appdev-app-store-link-and-policies.md)) — EU DSA Trader Status approved (app live worldwide); PR-8 retired in same commit (`7d35f92`) — Beta nav + view + Tally embed deleted, `iOS App ↗` nav + small phone-glyph header icon added, both linking to storefront-less `apps.apple.com/app/id6761634761`. Generic SVG glyph (no Apple branding). EN+DA locales updated (`navIOS`). Browser-verified live. **Two new Standing Rules** locked into top-level CLAUDE.md + memory: (1) **iOS push gate** — don't push iOS to `main` until ship-ready for TestFlight (supersedes "free to push if XCTest passes"); primary = repo hygiene, secondary = guardrail against burning Actions minutes via TestFlight-as-iteration-loop. (2) **No-guessing** — read the actual file before recommending anything about how a system behaves. Triggered by an in-session false premise about feedback email capture (it does capture; Worker logic at `functions/api/feedback.js:254-260`). `[LOW-011]` filed in IR-5 (web copy parity for email helper text + Worker reorder of `Reply-to email` to top of embed). Owner ran `git config --global user.name/.email` to fix unattributed commits going forward (no-reply email form). NEXT-SESSION.md rule #8 wording is stale — needs update on next regenerate.
- **2026-04-24** — DQ-2 Safe/Tuned + DQ-1-followup + LOW-003-followup-label ([log](2026-04-24-cowork-appdev-dq-2-safe-tuned.md)) — 15 commits across both repos in a single autonomous session. **DQ-2 shipped end-to-end** (Phase DQ 1/5 → 2/5): `_tier(preset, field, mode)` helper + sparse `_tuned` override data convention on speed_priority.fast/.balanced + strength_levels.strong, 5 MVP tiered fields (outer wall speed bedslinger/corexy, outer wall accel bedslinger/corexy, strong infill density), profileMode filter group on web + `SlidingSegmentedControl` on iOS GoalsView, locale keys in en + da, prov refs extended with `(_tuned)` / `(base from _tuned)` marker, walkthrough harness DQ-2 assertion block (Safe baseline byte-equal + Tuned differentiation ≥3/5 + prov tier-tagged), 2 new XCTests mirror the assertion (`testSafeBaselineByteEqualToDefault` + `testTunedEmissionDiffersOnTieredFieldsAndTagsProvenance`). **DQ-1-followup shipped** (3 commits): `_prov` sidecar on `getAdjustedTemps` / `getAdvancedFilamentSettings` / `getFilamentProfile` (12 prov entries), web `row()` helper + iOS `ParamRow`/`FilamentSettingsView` render ⓘ in Advanced view, 2 new XCTests. **`[LOW-003-followup-label]` fixed** (web-only, 2-line): `adv.retraction_length` → `adv.retraction_distance` at `app.js:1187` resolves "undefined" in Filament panel. Session ran on owner-granted session-scoped autonomous authorization after confirming 2 go/no-go decisions (warm-up order + bundle DQ-1-followup). v1.0.2 approved + released overnight per owner; ROADMAP synced in session-close. 46/46 iOS XCTest (was 42). Web `2441b18…270d17b`, iOS `7a913f3…949f95b`.
- **2026-04-24** — DQ-1 provenance infrastructure end-to-end ([log](2026-04-24-cowork-appdev-dq-1-provenance.md)) — Phase DQ kickoff + DQ-1 shipped in a single overnight session (6 commits web + 3 iOS). Owner opened brainstorming pro-credibility data work, converged on 5-sub-phase plan (provenance → Safe/Tuned → PA/LA → retraction deltas → cooling curves), wrote IMPL-041 master spec, shipped DQ-1 end-to-end. Spec revised mid-execution: inline `prov` via `S`/`A` extension instead of separate `_tag` helper + return-shape split (scouted existing `engine.js:35` — the infra was already there). 28 numeric emissions in `resolveProfile` tagged with baseline prov (7 default, 19 calculated, 2 rule); 18 qualitative stay `prov=null`. Walkthrough invariant at check 0c (lands red before commit 3, green after). Web `[IMPL-041 / DQ-1 commit 4]` ⓘ icon + native `title` tooltip on Advanced view — iterated from dotted-underline (too subtle per owner) to explicit icon. iOS side: new `Provenance` struct + Codable decode + `ParamRow` `info.circle` + alert; 2 new XCTests → 42/42 green (was 40). Byte-identity maintained every commit. Two IR-5 followups filed: `[DQ-1-followup]` filament panel extension + `[LOW-003-followup-label]` `adv.retraction_length` → "undefined" bug at `app.js:1187`. Autonomous sweep on the close + log + NEXT-SESSION regen per owner authorization. Web `4cde0dd…1941ec8`, iOS `720d26a…837ca10`.
- **2026-04-23** — v1.0.2 ship sequence ([log](2026-04-23-cowork-appdev-ship-v1.0.2.md)) — full end-to-end ship in one session: What's New tone-pass (casual short English, ~530 chars), `MARKETING_VERSION` 1.0.1 → 1.0.2 + xcodegen + push (iOS `11b9c8d`), mid-session scope add **Home-screen version-display footer** (iOS `15c1002`, `AppConstants.appVersion`/`appBuildNumber` + HomeView muted line), TestFlight dispatched via `gh workflow run testflight.yml --ref main` (run [24848532846](https://github.com/mustiodk/3dprintassistant-ios/actions/runs/24848532846), SUCCESS), on-device smoke tests 4a–4e green (feedback via Worker confirmed via CF Logs, no engine-init crash, MK4 acceleration names MK4 not A1), old `#ios-app-feedback` webhook deleted (attack surface from v1.0.0/v1.0.1 binaries closed), **v1.0.2 submitted to App Review** with Manual release toggle + Promotional Text C1 ("Generate optimized 3D print profiles for 64 printers across 12 brands…"). **`[CRITICAL-001-followup]` filed in IR-5 backlog:** Worker routes all feedback (iOS+web) to single `DISCORD_WEBHOOK_URL` → `#web-app-feedback`; original `#ios-app-feedback` intent never landed. v1.0.3 scope (~15 LoC + new CF secret, no iOS binary). 40/40 iOS XCTest green. NEXT-SESSION regenerated on explicit owner ask (first test of 2026-04-23 owner-triggered-only rule — clean). Awaiting Apple (24–48h typical).
- **2026-04-23** — IR-4 + IR-5 bundle into v1.0.2 ([log](2026-04-23-cowork-appdev-ir4-ir5-bundle.md)) — autonomous session-scoped run; 10 findings shipped + HIGH-003 actor→class refactor via branch + **3 Codex external-review passes** (VERDICT: merge). Bundle: MEDIUM-021 slicer-display-name bridge, HIGH-007 getNozzleSize bridge, MEDIUM-020 getFilamentTabs bridge, LOW-004 TPU drying alignment, LOW-006 flexible dedup, HIGH-006 SLICER_TABS bridge (270+ lines of SlicerLayout.swift static data deleted + snapshot test), LOW-003 retraction_length→retraction_distance collapse (unblocks export HIGH-001), HIGH-011 structured numeric per chip (value field), MEDIUM-009 Codable decode, HIGH-003 actor→final class+serial DispatchQueue+assertOnEngineQueue guards. Test count 37 → 40. External review kit preserved at `3dprintassistant-ios/docs/reviews/2026-04-23-high-003-codex/`. NEXT-SESSION not regenerated per owner rule. Web `f6dd64b…9aa164e`, iOS `9093b24…2579fe8`.
- **2026-04-23** — IR-5 backlog sweep + correctness-tier test ([log](2026-04-23-cowork-appdev-ir5-backlog-sweep.md)) — autonomous session-scoped run; 6 findings shipped (material_warnings retire as MEDIUM-017 follow-up, MEDIUM-003 doc contract, MEDIUM-013 getFilters throw, MEDIUM-012 jsonLit helper + 5 ID-embed sites + closed getAdjustedTemps zero-escape gap, MEDIUM-005 strength chip through mapForSlicer, MEDIUM-011 correctness-tier XCTest 8 combos); 11 commits; test count 37 → 38; walkthrough 9/9 + Combo 3 pre-existing. New `scripts/extract-correctness-tuples.js` regenerates MEDIUM-011 expected values. NEXT-SESSION **not regenerated** (owner-triggered-only rule locked in earlier today). Web `481049d…dc7ee95`, iOS `19ee59b…4300400`.
- **2026-04-23** — IR-2 / IR-5 cleanup sweep + CI quota fix ([log](2026-04-23-cowork-appdev-ir2-cleanup-sweep.md)) — autonomous session-scoped run; 10 findings shipped (MEDIUM-017, MEDIUM-010, LOW-009, OBS-006, MEDIUM-022, LOW-008, MEDIUM-004, LOW-010, MEDIUM-019 partial, R8 `_validateSchema`); 21 commits; 37/37 iOS tests every commit; walkthrough 9/9 + Combo 3 pre-existing every commit. Mid-session **TestFlight workflow switched to `workflow_dispatch`-only + concurrency cancel** after GitHub quota hit 100% — every iOS push was triggering a 10-min macos-26 build. Owner-ship-workflow change: `gh workflow run testflight.yml --ref main` after version bump. R8 surfaces 16 soft warnings documenting new `[MEDIUM-019-followup]` IR-5 item (max_mvs 0.8 data gap). Web `358591d…7601c72`, iOS `a797f2f…88047ec`.
- **2026-04-23** — IR-2a engine-sweep + CRITICAL-001 code landing ([log](2026-04-23-cowork-appdev-ir2a-engine-sweep.md)) — autonomous overnight run; 13 findings landed (HIGH-009, MEDIUM-002, MEDIUM-001, MEDIUM-007, HIGH-012-followup A+B, HIGH-008, HIGH-004, HIGH-005, CRITICAL-001 code, HIGH-010-A sanitize, MEDIUM-018-A); 20 commits; 37/37 iOS tests every commit; walkthrough 9/9 every commit. CRITICAL-001 code-complete but awaits owner HMAC secret config in CF+GH+local (3 places). HIGH-010-B rate-limit deferred to owner CF dashboard action. Simplified Phase 2 runbook at `docs/runbooks/incident-response.md`. v1.0.2 "What's New" locked at `docs/app-store-whats-new-v1.0.2.md`. Web `62d7ae9…597499b`, iOS `250f187…5a360dc`.
- **2026-04-22** — IR-0 overnight sweep: 5 findings shipped ([log](2026-04-22-cowork-appdev-ir0-sweep.md)) — autonomous overnight run; `[CRITICAL-003]` preset validation + `[HIGH-002]` IMPL-040 test tightened + `[HIGH-012]` A1 why-text templated + `[MEDIUM-015]` lightning Prusa fallback dropped + `[MEDIUM-016]` adaptivecubic added; 9 commits, 37/37 iOS tests; 4 IR-0 items deferred to IR-2a (v1.0.2 ship pass — new ROADMAP section); partner-reviewer hook installed at session start; HIGH-012-followup filed in IR-5. Web `6173839…2ddff99`, iOS `2cbf5c7…34fba25`.
- **2026-04-22** — Security rotations + md hygiene ([log](2026-04-22-cowork-appdev-housekeeping.md)) — rotated 4 credentials (Sentry DSN, ASC API key `MHDMJN32AP`→`SF8C66YLJY`, provisioning+CSR deleted, GitHub PAT stripped → `gh` OAuth via keychain); tightened iOS `.gitignore`; retired 9 iOS root stubs; promoted 10 untracked web docs + archived iOS-RELEASE-PLAN. **Bonus round:** iOS doc-drift triage — promoted App Store metadata + screenshots/{iphone,ipad}; retired `research/` stubs + `memory/` + 5.4 MB backup screenshots + stale `privacy-policy.md`; moved `rb2-actor-prompt.md` to web; moved `.p8` to `~/.appstoreconnect/private_keys/`. 6 commits across web+iOS. `[LOW-001]` ✅.
- **2026-04-21** — IR-0 [CRITICAL-002] bed-temp clamp ([log](2026-04-21-cowork-appdev.md)) — engine clamps `initial_layer_bed_temp` / `other_layers_bed_temp` to `min(material.bed_temp_max, printer.max_bed_temp)`; new `printer_max_bed_temp_clamped` + `printer_bed_temp_incompatible` warnings; Combos 6 + 10 ❌→✓; 35/35 iOS tests; web `dc87961`, iOS `378861a`.
- **2026-04-21** — Review consolidation + fresh-session prep ([log](2026-04-21-cowork-appdev-consolidation.md)) — merged domain-findings + PLAN.md into 8 review files + IR-* ROADMAP section; deleted 2 redundant docs; 59 findings triaged.
- **2026-04-20** — 3rd-party review package ([log](2026-04-20-cowork-appdev-review-package.md)) — starter kit (6 docs) dispatched against web `c4c5071`, iOS `24aef66`; `fresh-session-kickoff-post-review.md` prepared.
- **2026-04-20** — IMPL-040 chip-desc / resolveProfile single source of truth ([log](2026-04-20-cowork-appdev-impl040.md)) — every UI number now computed by the same function that emits it; 20/20 tests; 118-combo matrix clean.
- **2026-04-20** — IMPL-039 printer-capability clamping + slicer-aware values ([log](2026-04-20-cowork-appdev-impl039.md)) — `getPrinterLimits` + `_clampNum` + `mapForSlicer` + `slicer_capabilities.json`; 32/32 tests; Bambu export byte-matches vendor format.
- **2026-04-19** — Cowork App Dev ([log](2026-04-19-cowork-appdev.md)) — Draft layer 0.30→0.28 (Bambu rejected import); committed the 114-line RB-1 engine diff that had been sitting uncommitted (production was rendering "undefined" for every warning).
- **2026-04-17** — Cloudflare Pages → Workers migration blocker ([log](2026-04-17-cloudflare-functions-blocker.md)) — Tally retired; Discord `/api/feedback` Worker stood up; `wrangler.toml` + `worker.js` required because project had migrated to Workers Builds.
- **2026-04-16 → 17** — App Store release + Discord restructure ([log](2026-04-16-cowork-appdev.md)) — iOS v1.0 approved + released (~121 non-EU countries); EU blocked on DSA Trader Status; Discord server restructure.
- **2026-04-15** — BR-11 feedback system + BR-12 empty-output hardening + App Store submission ([log](2026-04-15-cowork-appdev.md)) — native feedback sheet → Discord; 2-tap Reset scoped per-screen; submitted to Review 13 days ahead of April 28 deadline.
- **2026-04-14** — BR-10 cross-device UI review ([log](2026-04-14-cowork-appdev.md)) — UITest target + 33 screenshots across SE/17 Pro/17 Pro Max; 4 P0/P1 layout fixes; App Store screenshots retaken.
- **2026-04-13** — App icon + branding ([log](2026-04-13-cowork-appdev.md)) — clay Benchy 1024×1024; nav bar + home logo; web header swap.
- **2026-04-10** — Release blockers + pre-release polish (RB-1…6, BR-1…7) ([log](2026-04-10-cowork-appdev.md)) — structured warnings, actor bridge, Sentry DSN extraction, OutputView split, device QA.
