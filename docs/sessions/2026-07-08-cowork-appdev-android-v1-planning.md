# 2026-07-08/09 — Cowork (appdev): Android v1 (+ macOS companion) — audit-delta + design spec + gated master plan (planning only, no code)

> Fully autonomous run (Fable 5, Claude Code desktop, MacBook Air). Owner ask: post-pivot planning session — Android app first (full depth), macOS second (light), reusing the PARKED iOS v2 bundle as the evidence base. **No app code written; engine/data untouched; the Android prototype repo was inspected via a read-only scratchpad clone — its no-push boundary is intact.** Session crossed midnight; docs are dated 2026-07-08 to match the bundle family.

## Durable context

- **The Android v1 program now exists as a triple-reviewed planning bundle awaiting owner AG0:** audit-delta `docs/reviews/2026-07-08-android-v1-audit-delta.md` → spec `docs/superpowers/specs/2026-07-08-android-v1-design.md` (AD0–AD10) → master plan `docs/superpowers/plans/2026-07-08-android-v1-plan.md` (AG0–AG8, one gate/session, per-gate micro-plans at gate entry, 12–18 sessions + 3–5-week Play calendar) → dispositions `docs/reviews/2026-07-08-android-v1-review.md` → ledger `docs/planning/ANDROID-V1-GATE-LEDGER.md` (PENDING-GO row). macOS: deliberately light companion `docs/superpowers/specs/2026-07-08-macos-companion-assessment.md` (M0 = 5-min ASC "Mac Availability" check; M2 = native target riding a future iOS-v2 un-park, decided only after Android ships).
- **Key design calls (owner can override any at AG0):** native Kotlin/Compose restart in the existing `3dprintassistant-android` repo (WebView prototype retired-in-history, parts mined); JS runtime = **QuickJS via vendored `HarlonWang/quickjs-wrapper`** (sync calls, ~0.6 MB/ABI; researched vs Javet/J2V8/Zipline/androidx.javascriptengine/WebView) with a **mandatory AG1-entry spike** (incl. which QuickJS it embeds) and a priced fallback (spec amendment + re-review + 1–2 sessions); learns layer = byte-identical JS on a Kotlin write-side op-log store (iOS D5a shape adopted); **improved-from-day-one scope bar** (native Mine loop, troubleshooter, journal, share outbound, estimators, EN+DA, a11y — AD0 argues the parity-first alternative honestly); sibling `android-printer-overlay-v1.json` (override-merge from day one, no Part-C legacy); `applicationId dk.mragile.printassistant`; minSdk 29 / targetSdk 36 (36 is proactive — official requirement is still 35, re-verify at AG7).
- **The Play calendar is the schedule long-pole, not the code:** personal accounts need a 12-tester × 14-day closed test + first review 7–14 days ⇒ 3–5+ weeks wall-clock; the plan front-loads account creation to AG0, defines the owner-triggered AG7a early-clock split (full precondition list — there is no lightweight early clock), and puts owner Console checkpoints at ~day 5/10 of the window. DSA: declaration mandatory but non-trader is the answer for a free app (no Apple-style block if declared).
- **Mined web findings (real today, independent of Android):** (1) analytics Worker drops `troubleshoot_used`/`export_clicked` for EVERY platform (`EVENT_KEYS` allowlists 3 events; fix = full schema patch incl. `BLOB_FIELDS`/query columns for `symptom`/`type`); (2) both Workers 403 native non-iOS clients at the auth layer (HMAC path is iOS-only — Android needs auth-branch extensions, 2 small web commits, pre-AG3/AG4); (3) **iOS overlay validator is RED right now** (missing 1.0.7 baseline — any overlay republish today would fail). All queued in ROADMAP Active Work Queue.
- **Toolchain ground truth:** this MacBook Air has NO Java/Android SDK/Xcode (verified by command); the mac-mini carries everything (Java 17, cmdline tools, API-36 emulator, Xcode 26.5) → Android execution sessions are machine-pinned to the mini; Node presence on the mini is an AG1-entry check (unverifiable from here).
- **Review pattern held 3-for-3 AGAIN with non-overlapping catches:** design sub-agent (HIGH: my "zero Worker change — verified" was true at the payload layer, false at the auth layer — the exact D5a-class error the bundle itself cites), plan sub-agent (HIGH: early-clock story impossible under strict sequencing; HIGH: AD8's overlay CONSUMER assigned to no gate), Codex (HIGH: EVENT_KEYS fix underspecified — bare allowlist would fake parity; HIGH: overlay validator run-verified RED). All GO-WITH-PATCHES; every patch applied same-session; both Codex HIGHs controller-verified before patching.

## What happened / Actions

1. Trigger C cold start (kickoff prompt): GitHub-first gate — 3dpa repos all `current`; `personal-dashboard behind:6` ff-pulled; `claude-sync behind:155+dirty` surfaced NOT auto-cleared (dirty parent = owner item per standing rule); `3dprintassistant-android` missing locally → cloned read-only to scratchpad. Read order: protocol → project CLAUDE.md → work-protocol SKILL → 3dpa-context → ROADMAP → INDEX → pivot session log + addendum → all four iOS v2 bundle docs.
2. Work Protocol: Full lane (new-platform release, cross-platform, architecture, ambiguity), plan mode; `superpowers:writing-plans` invoked for the master plan (gate-granularity adaptation per the accepted iOS shape).
3. Research: 3 parallel agents (Android JS runtimes — verified maintenance/sizes mid-2026; Google Play requirements — account/testing/DSA/API-level/CI facts; macOS routes — WWDC 25/26 signals, Designed-for-iPad default-ON discovery). Prototype assessed from the clone (WebView packaging proof, no Gradle/Kotlin/CI; restart-clean verdict, parts mined).
4. Contract-by-contract verification against real code: state-codec exports, envelope/op-log reuse, overlay schema, analytics/feedback Workers (→ mined the EVENT_KEYS + auth-layer findings), harness commands.
5. Wrote audit-delta → spec → master plan → macOS companion (+ gate ledger with PENDING-GO row). Two parallel hostile sub-agent reviews (design + plan gates) → GO-WITH-PATCHES ×2 → all 21 findings patched (incl. controller re-verifying the Worker-403 claim in code first). Codex cross-model round (`bridge --health` ok → `codex-only`, 234 s) → GO-WITH-PATCHES (5 findings, none overlapping) → all applied (both HIGHs verified by running the validator / reading BLOB_FIELDS).
6. Commits (one concern each): `8a2da77` planning bundle + codex packet · `3560b31` 3dpa-context Android-scope fix · wrap commit (ROADMAP banner+queue, this log, INDEX, NEXT-SESSION).

## Files touched

Web repo only: 6 new docs (audit-delta, spec, plan, macOS companion, dispositions, gate ledger) + `codex/android-v1-review/bridge-2026-07-09-001143-419640.md` + `docs/3dpa-context.md` (scope fix) + ROADMAP + sessions INDEX + this log + NEXT-SESSION. iOS repo: untouched. Android prototype repo: untouched (read-only clone in scratchpad only). Engine/data: untouched. Top-level `CLAUDE.md`+`AGENTS.md`: 3dpa-android row annotated (bundle exists; no-push boundary STILL ACTIVE until owner GO).

## Commits

Web `main`: `8a2da77` (bundle) · `3560b31` (context fix) · wrap-docs commit; pushed at wrap (docs-only, web push free). Parent repo: hot-cache row edit via auto-sync.

## Open questions / Follow-up

- **OWNER AG0 (the locked next step):** read spec §2 + §4 — GO or override AD0–AD10 (most consequential: AD0 scope bar, AD2 applicationId, AD1 prototype-push). Owner actions bundled at AG0: create the Play Console account ($25 + ID verification + DSA non-trader declaration); push (or write off) the mac-mini's prototype commits (`UNVERIFIED:` whether any are actually unpushed — resolvable only from the mini); answer the test-device question. Also still open from before: **v1.0.7 on-device Mine acceptance** (TestFlight) and the parked iOS v2 G0 (superseded in priority by Android per the pivot).
- Owner 5-min macOS check (anytime): ASC → Pricing & Availability → "Mac Availability" — the iOS app may already be installable on Apple-silicon Macs.
- Mined web findings queued in ROADMAP (analytics EVENT_KEYS full-schema + auth; feedback auth; **overlay validator RED — blocks any overlay republish today**, worth fixing soon independent of Android).
- Md-hygiene: protocol drift NONE (`diff CLAUDE.md AGENTS.md` clean); no stray tags; INDEX parity kept (this log + line same commit); carried observation now stronger — `docs/planning/` holds **5** gate ledgers (ANDROID-V1 added); archive sweep when convenient. `claude-sync` (~/.claude) remains `behind:155+dirty` (local deletions of paste-cache/plan artifacts block pull) — owner decision to reconcile; not auto-cleared per the dirty-parent standing rule.
- Lesson-spotter (compact): 1 candidate — the audit-delta repeated the verified-one-layer-asserted-all-layers pattern ("zero Worker change — verified" true for payload validation, false at auth), caught by the design-gate reviewer pre-commit. Disposition: **no new finding file** (assert-without-verify family already ledgered; second consecutive session where the review layer caught exactly this class at planning cost only) — calibration row appended to `ai-operating-model/docs/agents/lesson-spotter-calibration.md`. No K3 (skills behaved: writing-plans adaptation + work-protocol lane both clean), no K4, no K1 safety-net catches (all reviewer disagreements dispositioned in the review doc).
- verify-before-mutate summary: appended below the self-check in the wrap-up response (5 flags, all resolved same-turn with inline evidence).

## Next session

**If owner has ratified AG0:** AG1 foundation on the **mac-mini** (QuickJS spike first; ledger row + prototype retirement + scaffold). **If not:** any queue item stands — the 3 mined web findings (overlay-validator RED is the most urgent), v1.0.7 Mine acceptance (owner), `max_mvs` 0.8 data gap, Export Phase 2. Resume via `NEXT-SESSION.md`.
