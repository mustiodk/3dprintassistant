# 2026-07-10 — Cowork (appdev): analytics Worker 5-event fix (LIVE) + Intake Autonomy v2 impl plan (dual-reviewed)

> Owner-directed autonomous run on the **mac-mini** (Claude Code desktop, Fable 5). Owner picked at cold start: "first fix A, then do B, then wrap up." Track A = the declared "do 1" analytics Worker fix (shipped + live-verified). Track B = the Intake Autonomy v2 gated impl plan (hostile + Codex cross-model reviewed, committed). Work Protocol Full lane both tracks.

## Durable context

- **The analytics Worker now accepts all 5 web events, live-verified on prod** (`551b092` schema + `14cb97a` android auth, pushed 01:13Z; prod flipped ~50s later — probe attempt 1 hit the old Worker with `400 invalid_event`, attempt 2 returned `200 stored:true`; android bad-signature probe returned `401 signature_mismatch`, proving the new auth branch without needing the secret). **Design constraint that shaped everything: Analytics Engine hard-caps 20 blobs/data-point and all 20 were assigned** → `blob19` became a shared per-event detail column (`feedbackCategory` | `symptom` | export `type`), mutually exclusive by construction via per-event allowlists, backward compatible (only `feedback_opened` ever wrote it before). Spec `docs/specs/analytics-v1.md` documents this; the dashboard gained `troubleshoot`/`exports` canned queries + cards; `app.js` export sender key renamed `printer`→`printerModel` (one property name = one column).
- **The hostile review refuted the queue row's premise:** web sends SIX un-allowlisted event types, not two — `printer_selected`/`nozzle_selected`/`material_selected`/`use_case_selected` (app.js:570/1532/1535/1537) still 400 on every chip click. Queued as an owner-decision ROADMAP row (allowlist vs delete), NOT bundled into the commit.
- **Intake Autonomy v2 impl plan is committed (`bff1604`) and cleared for build:** 5 mac-mini sessions — B0 launchd environment probe FIRST → B1 `republish-overlay.js` → B2 full-envelope live-verify probes → B3 preflight/lock/KV-hygiene/notifier → B4 runner contract + contract/runbook amendments + PD7 gather change + plist → B5 latency measurement + self-run rollback drill + enablement (full autonomy from run 1). Review chain: hostile sub-agent GO-WITH-PATCHES (17 findings — **CRITICAL C1: the git-tracked `printer-intake-outcomes.jsonl` means runner ledger appends dirty the tree and deadlock the next run's fail-closed preflight → each candidate's ledger line = own commit+push before watermark advance**) + Codex via bridge GO-WITH-PATCHES (3 must-fix: **payload-only `payload_sha256` would let a live `enabled:false` overlay pass verification** → full-envelope compare; `git diff --stat` can't enforce the COMBOS-only guard → hunk-level `--unified=0` range check; crash-replay window → startup ledger reconciliation by normalized candidateKey). All applied; dispositions in `docs/reviews/2026-07-10-intake-autonomy-v2-plan-review.md`.
- **Grounding corrections that outlive the session:** the K2 SE staging file is machine-local and NOT on this mac-mini — KV `req:1783615951531:a03e6e7e` is the durable source (re-stage before B4.6); `picker-dry-run.js`'s fetch shim is local-file-only (verify-live-picker downloads live data to a temp root instead); `.assetsignore` excludes docs/scripts so deploy latency is only observable via served-asset changes; the exported `compareVersions` is semver-only (never use it for `content_version`).
- **Pre-AG3 flags from the android-auth review:** `FEEDBACK_HMAC_SECRET` is shared across BOTH Workers and both native platforms (APK extraction couples rotation to shipped iOS versions — decide per-platform secrets/rotation before a real APK); the `/analytics` dashboard platform lens is hardcoded web/iOS. Both queued.

## What happened / Actions

1. Trigger C cold start (3dpa token): health gate — web `current`, iOS `ahead:3` (expected under push gate), `personal-dashboard behind:6` + `3dprintassistant-android missing` surfaced out-of-scope. Full read spine; owner picked "A then B, autonomous, wrap at end."
2. **Track A commit 1 (`551b092`):** verified the AE 20-blob cap against Cloudflare docs before designing; TDD 7 RED (real `track()` payloads key-for-key) → GREEN; hostile sub-agent review GO (its exports-query-omits-nozzle LOW applied pre-commit; its 4-selection-events MEDIUM queued).
3. **Track A commit 2 (`14cb97a`):** TDD 2 RED android HMAC tests + a 403 guard test → GREEN (`HMAC_APP_SOURCES` set, `verifyAppSignature` rename — reviewer-verified module-local); same reviewer continued via SendMessage: GO; spec channel/buildNumber doc-drift LOW applied.
4. **Push + live verify:** `a26dd60..14cb97a`; polled prod until the new Worker answered; all three probes green (see Durable context).
5. **Track B:** read spec + audit in full; delegated a grounding sweep (Explore agent — 12-point fact sheet, all file:line-verified); wrote the plan in the house style (exemplar: the 2026-07-09 Export Phase 2 plan); hostile sub-agent plan review GO-WITH-PATCHES ×17 → all applied (3 load-bearing claims re-verified in-tree first); `bridge --health` ok → `bridge --mode codex-only` (fresh `Wrote` line confirmed; absolute `--out-dir`) → GO-WITH-PATCHES ×6 → all applied; committed `bff1604`.
6. **Wrap (Trigger A):** md-hygiene clean; ROADMAP banner + 3 queue-row changes; this log + INDEX + NEXT-SESSION; calibration row appended (ai-om); spawned a background-task chip for the out-of-scope agent-ops discovery (its launchd plist still hardcodes pre-migration `/Users/mragile.io/` paths — the daily job may have silently died at the 2026-05-27 migration).

## Files touched

Web: `functions/api/analytics.js` + `.test.mjs` · `functions/api/analytics-query.js` + `.test.mjs` · `app.js` (3 track-call key renames) · `analytics.html` (2 cards, labels, formatting) · `docs/specs/analytics-v1.md` · `docs/superpowers/plans/2026-07-10-intake-autonomy-v2-impl-plan.md` (new) · `docs/reviews/2026-07-10-intake-autonomy-v2-plan-review.md` (new) · `codex/intake-autonomy-v2-review/bridge-2026-07-10-013631-926039.md` (new) · ROADMAP · this log · INDEX · NEXT-SESSION. ai-om: lesson-spotter calibration row. iOS/engine/data: **untouched** (Worker + docs only; no byte-mirror needed).

## Commits

Web `main`: `551b092` (5-event schema) · `14cb97a` (android auth) — both pushed + live-verified · `bff1604` (impl plan bundle) · wrap commit (ROADMAP, log, INDEX, NEXT-SESSION). iOS: no commits, push gate intact (`ahead:3` = the pre-existing data mirrors).

## Open questions / Follow-up

- **Owner decision queued:** the 4 selection events (`printer_selected` etc.) — allowlist (needs shared-column design for `brand`/`group`/`use_case`) or delete the dead calls.
- **Pre-AG3 (queued):** shared HMAC secret rotation strategy; dashboard android lens pass.
- **Locked next: Intake Autonomy v2 build session 1 = Gate B0 (launchd environment probe), mac-mini.** Owner setup inputs during build: Discord webhook (hard B5 enablement gate) + schedule time (proposed 07:45).
- Md-hygiene: protocol drift NONE; stray-tag sweep clean; no untracked md; INDEX parity kept (log + line same commit). Carried observation: `docs/planning/` now holds **6** gate ledgers once INTAKE-AUTONOMY-V2's is created at B0 — archive sweep when convenient.
- Findings sweep: **no K1/K3/K4 finding files.** Lesson-spotter (compact): 1 candidate — the queue-row premise undercount ("2 dropped events" vs 6 un-allowlisted event types), assert-without-verify family already ledgered, caught by the review layer pre-push; calibration row appended. Note (not a finding): `superpowers:writing-plans` isn't in this harness's skill list — adapted via the newest reviewed plan as exemplar; outcome dual-review green.
- verify-before-mutate summary (verbatim): `verify-before-mutate ledger: 0 flags (0 resolved_same_turn, 0 resolved_late, 0 unresolved_by_session_end), 0 destructive-core, 4 unclassified, 0 generated-write` + `note: gate on unresolved_by_session_end; resolved_late = timing-health; resolved = not premise-proved (spec M1)`.
- Out-of-scope: agent-ops launchd path-drift → background-task chip spawned (owner one-click); `personal-dashboard behind:6` still needs a pull from its own session.

## Next session

**Option A (locked): Intake Autonomy v2 build session 1 — Gate B0 launchd environment probe** per `docs/superpowers/plans/2026-07-10-intake-autonomy-v2-impl-plan.md` (mac-mini only). **Option B:** Android AG0 (owner ratification) → AG1 QuickJS spike. **Option C:** owner decisions — selection-events row; v1.0.7 on-device Mine acceptance. Resume via `NEXT-SESSION.md`.
