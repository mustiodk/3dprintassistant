# 2026-07-10 — Cowork (appdev): Intake Autonomy v2 FULL BUILD — B0→B5, one step from live

> Owner-directed fully autonomous run on the **mac-mini** (Claude Code desktop, Fable 5). Directive: execute the ENTIRE build phase (Gate B0→B5) in one session, no owner check-ins between gates; two hard boundaries (structural launchd-auth failure → stop; never load the live plist / invent a webhook / ship a candidate). Owner also set the schedule to **12:00** (superseding the plan's 07:45) and designated the staged **K2 SE** as the real e2e test on the first scheduled run. Per-gate discipline held every gate: implement → hostile sub-agent review → patch → QA → commit → ledger tick. Full evidence: [`../planning/INTAKE-AUTONOMY-V2-GATE-LEDGER.md`](../planning/INTAKE-AUTONOMY-V2-GATE-LEDGER.md).

## Durable context

- **The pipeline is built, verified end-to-end, and exactly ONE owner input from live:** add `discordWebhookUrl` to `scripts/.printer-intake.local.json` → one test POST green → `launchctl bootstrap` per [`scripts/launchd/README.md`](../../scripts/launchd/README.md) → the first scheduled run (daily 12:00) processes the staged **Creality K2 SE** (KV `req:1783615951531:a03e6e7e`, confirmed alive; re-staged + researched this session — PD2-class, official specs, no app-cap) fully autonomously. B5.4 was deliberately NOT executed: the webhook test-POST is the Codex SF-3 HARD enablement gate and the owner boundary said stop there. **No re-send of the original request is needed** — the KV entry is the durable source the run consumes.
- **Headless `claude -p` auth on this machine goes through `~/.config/claude-code/oauth.env`, NOT the keychain.** The B0 probe caught the standalone CLI's keychain credential 401-stale (post-2026-06-17 rotation; reproduced in a scrubbed terminal env — NOT launchd-specific). The probe + wrapper source oauth.env (subshell-scoped) and pin PATH; probe/runner env parity is a standing rule. All 4 probe classes PASS launchd-initiated (`ppid=1`).
- **The Scout test suite rmSync-wipes `scripts/.printer-intake-out/`** (`printer-intake-scout.test.js:259,289`) — discovered via a VBM flag when the B0 probe logs vanished mid-session. Runner durable state therefore lives in gitignored **`scripts/.intake-runner-state/`** (parked/, reports, digest cursor, drill snapshots); `.printer-intake-out/` is Scout-scratch only. The B5 snapshot path deviation was *necessary* — the exit checklist itself runs the wiping suite.
- **`data/printers.json` is hand-formatted; a whole-file reserialize is a shippable hazard.** The B4.6 dry-run produced a 1342-line diff for a 1-row add before the string-splice fix (+21/-0). Now contract law + scripted: `scripts/intake-diff-guards.js` (COMBOS-hunk guard on a PINNED `main...HEAD` base + printers-splice guard: bounded additions, ZERO deletions).
- **The PD6 rollback rule is proven ON PRODUCTION, not just in unit tests:** the B5.2 drill published `2026071003` then rolled back to live `2026071004` = max(bad, snapshot)+1, payload byte-identical throughout (iOS monotonicity held; reviewer-verified by independent curl). Live overlay ends at `2026071004`, enabled:true, payload unchanged from `2026070401`'s. Deploy latency measured: 55.5/30.3/60.4s → `liveVerify` calibrated 14×15s (195s ≥ ceil(p95×3)=182s).
- **Fail-closed layers that now exist:** preflight (freeze 78 / lock 75 / repo sync / wrangler+KV-delete-scope / bridge health — green on the real mini, and green AGAIN post-drill = the C1 ledger-custody proof); atomic-noclobber lock custody in the wrapper with signal-traps-that-exit; shipped-and-unreported → freeze, **including UNKNOWN ship state** (`--shipped-unknown` after a crashed runner session); KV hygiene class policy that never deletes staged/parked/corrected requester records and now also expires shipped keys after the 7-day contact window.
- **PD7 semantics shipped:** gather does last-line-wins per normalized candidateKey over the FULL ledger (cross-watermark corrections land; `normalizeCandidateKey` exported for the runner's crash-replay reconciliation). Owner retro-tag rules: fresh `runId` + full original candidateKey (contract §7). The one-time marker migration is documented (runbook v5) — owner runs it, never the pipeline.
- **Review layer earned its cost AGAIN — 50 findings across 6 hostile reviews (8+8+9+14+14+5... counts per gate: B0×8, B1×8, B2×9, B3×14, B4×14, B5×5), every gate GO-WITH-PATCHES, all applied.** Standouts: NaN `--bad-version` silently zeroing the PD6 floor (B1 HIGH); a trailing network blip masking a real mismatch as exit 3 → skipped rollback (B2 HIGH); TERM'd wrapper stripping the lock while continuing to run — empirically verified by the reviewer (B3 HIGH); crash-after-ship + webhook-down bypassing the PD8 freeze via a hardcoded `shipped:0` (B3 HIGH); unqualified manual new-brand gates that would have parked every PD4 candidate (B4 HIGH); parked-candidate retry being unreachable as written (B4 HIGH); my own calibration arithmetic off by one interval (B5 MEDIUM).

## What happened / Actions

1. **Cold start (Trigger C):** mac-mini confirmed; web `current`; surfaced `personal-dashboard behind:6` + `3dprintassistant-android missing` (out of scope) + iOS `ahead:3` (push gate, expected). Full read spine + plan/spec in full as execution contract.
2. **B0 (`43ff60b`+ledger):** launchd probe — run 1 caught the stale keychain 401 + a zsh `:r` refspec mangle; remedied (oauth.env canonical, `${sha}` braced); patched probe re-proven 4/4 launchd-initiated.
3. **B1 (`c3abe4e`):** `republish-overlay.js` TDD 21/21 — forward `max(current+1, today01)`, rollback `max(bad,snapshot,current)+1`, counter-99/2099 exit 2 CRITICAL, validate-then-swap; live-overlay no-op sanity clean.
4. **B2 (`cc9ba95`+`d1f84cd`+`bcdf510`):** engine-bootstrap refactor (byte-for-byte proven) + `verify-live-overlay.js` (FULL envelope incl. enabled/min_app_version + recomputed hash; deterministic mismatch-beats-blip exit codes; `--measure`) + `verify-live-picker.js` (live engine+data in a temp root; garbage-200 → exit 3). Both green on real prod.
5. **B3 (`b90dad0`→`461daf8`):** config (12:00) + KV hygiene + notifier (freeze rule, monthly digest, mentions-off, surrogate-safe cap) + preflight + wrapper; real preflight fail-closed on dirt/ahead (correct) then `ok=true` post-push; live hygiene dry-run deletes=0 kept=3 (matching plan expectation; K2 SE alive).
6. **B4 (web ×5 + ai-om ×2 commits):** runner contract v1 + Assistant autonomous mode + runbook v5 (PD3/PD4 waivers as law) + PD7 gather + diff-guards + plist/kickoff/README. **B4.6 isolated K2 SE dry-run** ran the ship mechanics end-to-end (validators, overlay add `2026071001` on-branch, hunk guard, would-merge diff, scratch ledger line, local notify) and caught the reserialize hazard; teardown verified clean (k2_se absent everywhere, live overlay untouched at `2026070401`).
7. **B5 (`00de4c7` + bump/drill commits):** suites 82/82 → snapshot → 3 timed bump-publishes → REAL rollback drill on prod → calibration → preflight green-again → exit checklist stops at the webhook hard gate. Plist NOT loaded.
8. **Wrap (Trigger A):** this log + INDEX + ROADMAP + NEXT-SESSION regen + memory (headless-auth reference) + calibration row (ai-om) + pushes.

## Files touched

Web: `scripts/` — `intake-launchd-probe.sh`, `republish-overlay.js`+test, `verify-live-overlay.js`+test, `verify-live-picker.js`+test, `lib/engine-bootstrap.js`, `picker-dry-run.js` (refactor), `intake-kv-hygiene.js`+test, `intake-notify.js`+test, `intake-run-preflight.sh`, `intake-run-wrapper.sh`, `intake-diff-guards.js`+test, `intake-retrospective-gather.js`+test, `intake-runner.config.json`, `intake-run-kickoff.md`, `launchd/` (2 plists + README) · `docs/runbooks/printer-addition-protocol.md` (v5) · `docs/planning/INTAKE-AUTONOMY-V2-GATE-LEDGER.md` (new) · plan annotation · `.gitignore` · `catalog/ios-printer-overlay-v1.json` (B5 version bumps + rollback, payload unchanged) · this log + INDEX + NEXT-SESSION + ROADMAP. ai-om: `docs/agents/intake-pipeline-runner.md` (new) + `printer-addition-assistant.md` (amendment) + calibration row. iOS: **untouched** (push gate intact, `ahead:3` pre-existing). Engine/app/data-semantics: **untouched** (the dry-run row was discarded; overlay payload byte-identical end-to-end).

## Commits

Web `main` (all pushed): `43ff60b` B0 · `c3abe4e`+`815e597` B1 · `cc9ba95`,`d1f84cd`,`bcdf510`,`4d137f5` B2 · `b90dad0`,`221541a`,`19ee6cb`,`461daf8`,`10f764b` B3 · `04477ad`,`08561fd`,`42cc19d`,`78b118f`,`d766f6d`,`aa1fbe2` B4 · 3 bump + 1 drill + calibration + `00de4c7` B5 · wrap commit. ai-om (parent repo): `f38d270`+`58eadda` + calibration row. iOS: none.

## Open questions / Follow-up

- **THE one remaining step (owner):** Discord webhook URL → `scripts/.printer-intake.local.json` (`discordWebhookUrl`) → verify one test POST → `launchctl bootstrap` per `scripts/launchd/README.md` → **first 12:00 run ships K2 SE as the real e2e test.** Everything else is green and pushed.
- Owner one-time PD7 marker migration (documented in runbook v5 — one line-1 edit + commit).
- Missed-run semantics caveat: powered-off at 12:00 = NO run and NO report (asleep = fires on wake) — `scripts/launchd/README.md`.
- Md-hygiene: protocol drift NONE; stray-tag sweep clean; no untracked md; INDEX parity kept. Carried: `docs/planning/` now holds 5+ gate ledgers — archive sweep when convenient. Plan checkboxes left unticked by design (the gate ledger is the authoritative tick surface).
- Findings sweep: **no new K1/K3/K4 finding files** (families already ledgered / single occurrences). Lesson-spotter (compact): 2 candidates — (1) the dual-reviewed plan placed runner state in a test-suite-wiped dir, missed by 23 review findings and caught empirically via a **VBM flag chain** (positive VBM datapoint for the eval narrative); (2) index-wide `git commit` after partial `git add` swept pre-staged files into a B4 commit (caught pre-push, history redone with pathspec commits). Calibration row appended (ai-om); no finding files (cheap corrections, established families).
- verify-before-mutate summary (verbatim): `verify-before-mutate ledger: 5 flags (0 resolved_same_turn, 0 resolved_late, 5 unresolved_by_session_end), 3 destructive-core, 21 unclassified, 1 generated-write` + `note: gate on unresolved_by_session_end; resolved_late = timing-health; resolved = not premise-proved (spec M1)`. Flag list verbatim: LaunchAgents plist delete · b1-sanity-snapshot delete · `2>/dev/null` delete · repo_destructive ×2. **Controller statement for the owner's read:** flags 1, 2 and the teardown repo_destructive were each verified same-turn with explicit commands + inline "flagged on X — verified" statements (the ledger matcher didn't credit them — the known R2 resolver-semantics gap); the `2>/dev/null` flag is a parser artifact fired inside a reviewer sub-agent (the reviewer itself verified + stated it); flag 2's verification is what surfaced the staging-dir wipe discovery.
- Out of scope, still standing: `personal-dashboard behind:6`; `3dprintassistant-android missing` on this machine per health line (Android work is AG0-gated anyway); iOS `ahead:3` rides the next TestFlight train.

## Next session

**Locked: owner supplies the Discord webhook → test POST → load the plist → observe the first live run (12:00, K2 SE e2e).** After that: read the run report + `INTAKE-AUTONOMY-V2-GATE-LEDGER.md` B5 row → flip B5.4 ✅ with the first-run evidence. Parallel owner items: Android AG0; selection-events decision; v1.0.7 Mine acceptance. Resume via `NEXT-SESSION.md`.
