# 2026-07-10 — Cowork (appdev): Intake Autonomy v2.1 — independent hostile review (Task 1, leg 1 of 2); **no code**

> Cold-start session run in the **cloud** environment. Executed the locked Task 1 entry point (an adversarial hostile review of the v2.1 spec) as far as the environment allows: the **independent hostile sub-agent leg ran; the Codex cross-model leg could not** (no `bridge`/`codex` CLI, no `ai-operating-model` sibling repo in cloud). **Verdict: NO-GO** with one CRITICAL that both prior Codex passes missed. No spec edits — findings are review evidence for the next drafting round, and must not be applied ahead of the still-owed cross-model cross-check. **Owner is moving to a fresh LOCAL session** so cross-model reviews are available.

## Durable context

- **Task 1 is HALF done.** The cold start requires TWO uncorrelated review legs (independent hostile + Codex cross-model). Only the independent leg ran here. **The Codex leg is still owed and must run locally before Task 1 can be declared clear.** This matters especially given the arc's K1 finding: a single reviewer's *fix advice* has repeatedly launranded a defect (round-2 Codex refuted round-1 Codex). Applying the independent findings without the cross-model cross-check would repeat exactly that anti-pattern.
- **Verdict: NO-GO.** Independent review artifact: [`../reviews/2026-07-10-intake-autonomy-v2.1-independent-hostile-review.md`](../reviews/2026-07-10-intake-autonomy-v2.1-independent-hostile-review.md). Ten findings (1 CRITICAL, 1 HIGH, 5 MEDIUM, 3 LOW). Spot-verified against in-tree source by the controller; F1 CONFIRMED in code.
- **F1 (CRITICAL, novel — both Codex passes missed it): the NO-GO-taint invariant guards the retry path but not the fresh-intake path.** Taint lives in a parked sidecar keyed by `candidateKey`; enforcement is the store write-guard + a taxonomy-graph reachability test over retry lanes. **Neither touches normal intake.** `loadCatalog` (`scripts/printer-intake-scout.js:308-309`, indexes `:335-347`) reads its dedupe/id set from **`data/printers.json` only** — a printer parked `review-no-go` was never shipped, so it is absent from the catalog, so **re-requesting it** mints a fresh candidate that flows straight to a review turn and can ship on `{GO,GO}`. The prior NO-GO is never consulted (ship path writes no sidecar; the graph test's nodes don't include a fresh candidate, which enters upstream at stage 3). Cheapest possible NO-GO laundering: just re-submit the request. Directly contradicts §8 criterion 1 (the "K2 SE end-to-end acceptance test" expects a fresh ship/park). **Fix:** key taint by **printer identity**, consult it **before** any review turn, and extend the reachability test to the fresh-intake edge.
- **F2 (HIGH): the taint deny-set omits `transient-pipeline`.** The invariant says tainted ⇒ only `judgment-on-evidence`/`decision-required`, but `intake-parked-store.js`'s refusal list enumerates only 3 classes (RD5:169, test:260) and misses `transient-pipeline` (`main-moved | immediate | ×2`). A tainted candidate whose `main` advances mid-attempt flips into a timer-free ×2 review lane. Fix: derive the deny-set from the invariant, not a hand list.
- **F3 (MEDIUM): `tainted` is claimed "derived from `verdictRefs`, never hand-set," but v1 sidecars have no `verdictRefs`** (it's a v2 field). Mechanical derivation returns `false` on K2 SE; the spec hand-taints it from `reason`. A migrator trusting the stated derivation migrates K2 SE **untainted**, re-opening §13-must-fix-1. Fix: migration derives taint from v1 `reason`, unit-tested against the real K2 SE fixture.
- **F4 (MEDIUM): `unverified-model` — a known runbook outcome with a prior v2 policy — is unmapped in RD2**, so it silently rides the "unrecognised → decision-required" catch-all, regressing v2's autonomous weekly re-query and falsifying criterion 4 ("no park reason lacks a class"). Fix: map it → `world-absent` (with source sweep) or state the demotion deliberately.
- **F5/F6 (MEDIUM): RD3 and RD10 are directionally sound but oversold.** RD3's offline canonical-source novelty can't catch shorteners / archive snapshots / PDF-vs-HTML / a second real page repeating a rejected figure → add an excerpt **content hash** to the novelty baseline + denylist archive/mirror hosts. RD10's "exactly-recognised" custody pass is silent on subset-dirty (crash between the two writes), untracked-first-write (`docs/printer-provenance.json` shows as `??`), torn-`.jsonl` appends, and owner hand-edits of the same paths → define over the path *set*, handle untracked, re-parse committed content before advancing the watermark.
- **F7 (MEDIUM, SCOPE): v2.1 is scoped for imagined futures at N=1.** The one live defect (sources not recorded) needs ~3 components (RD1 + RD10 + reviewer structured-objections); the retry ratchet (D3) is already fixed by the shipped v1.1 contract patch (gate R0). The `research-defect`/`world-absent` split addresses candidate types that have never occurred (N=1 parked one `review-no-go`, zero of each). Recommend folding both into `decision-required` until a real park demands the split — which also shrinks the reachability surface F1/F2 must patrol.
- **F8/F9/F10 (LOW):** §9 risk row still says "second reviewer only on a NO-GO" (contradicts RD4 + §13#5 — settled, un-propagated); RD1's Scout build-item undercounts the missing skeleton slots vs the by-reference runbook field set; the `allowedPrinterFields` justification is misattributed (that validator gates the overlay, not `data/printers.json` — conclusion still right on RD10's stronger grounds).
- **Controller in-tree verifications (all held):** RD1's "no `extruder_type`/`max_acceleration`/`evidenceType` slot" (`printer-intake-scout.js:662,710-715`) VERIFIED; `allowedPrinterFields` rejects unknown keys (`validate-ios-printer-overlay.js:19,269`) VERIFIED; `docs/**` asset-ignored VERIFIED; preflight fail-closes on dirty AND `ahead!=0` (`intake-run-preflight.sh:45-53`) VERIFIED. **UNVERIFIABLE in cloud:** runner contract `:234` / "v1.1 patch" (sibling repo absent), K2 SE parked sidecar (gitignored/absent). **FALSE in this tree:** `docs/printer-provenance.json` "added" — not git-tracked, not present (and as an RD10 component should not exist pre-build).
- **Nothing built for v2.1** remains true — `intake-parked-store.js`, `intake-retry-gate.js`, `validate-candidate-evidence.js`, `docs/printer-provenance.json` all absent. K2 SE parked + tainted + stationary under contract v1.1.

## What happened / Actions

1. **Cold-start orientation** — read (in-tree) CLAUDE.md, NEXT-SESSION.md, ROADMAP banner + Active Work Queue, INDEX, the v2.1 spec (v4) in full, and both prior Codex transcripts. Established the environment shape: **cloud session, only the `3dprintassistant` repo present**; no cross-model tooling.
2. **Controller in-tree claim verification** — spot-checked every load-bearing spec claim whose evidence lives in this repo (results above). All in-tree claims held; the out-of-tree ones were marked UNVERIFIED, and the ROADMAP's "provenance file added" was found FALSE for this tree.
3. **Independent hostile sub-agent review** — distinct brief from the two Codex passes (attack the taint invariant + reachability test, RD3's cheapest current bypass, RD10's custody pass, the scope, and a verified/unverified/false claim audit). Returned **NO-GO, 10 findings.**
4. **Adversarial verification of the top finding** — F1 confirmed directly in `printer-intake-scout.js` (catalog loads from `data/printers.json` only; no parked-sidecar consultation). This is a genuine CRITICAL that neither prior Codex pass surfaced — and it is the strongest possible vindication of the spec's own "per-lane enumeration doesn't work" thesis.
5. **Logged the verdict** — review artifact + this session log + INDEX/ROADMAP/NEXT-SESSION updates. **No spec edits** (findings are input, not permission; the cross-model leg is still owed).

## Files

- Added `docs/reviews/2026-07-10-intake-autonomy-v2.1-independent-hostile-review.md` (the review).
- Updated `docs/sessions/INDEX.md`, `docs/planning/ROADMAP.md` (banner), `docs/sessions/NEXT-SESSION.md` (kickoff regenerated for a LOCAL session).
- No engine / `app.js` / `data/*` / spec changes. Tree left clean.

## Next (locked)

**Run the Codex cross-model leg of Task 1 — in a LOCAL session** (`bridge --health` → `bridge --mode codex-only` with an absolute `--out-dir`, confirm the `Wrote` line). Feed it the two prior transcripts AND this independent review so it neither repeats settled points nor re-derives F1–F10 blind. Only if BOTH legs' findings are reconciled and the owner gives the go does drafting v5 (or a scope cut) begin, followed by the small-gate impl plan. **Do not apply F1–F10 to the spec before the cross-model cross-check** — that is the exact anti-pattern the K1 finding documents.
