# Bridge session — 2026-07-12-004604-036303

## Task

Cross-model review ROUND 6 (final convergence gate) of docs/superpowers/specs/2026-07-11-accounts-platform-design.md + docs/superpowers/plans/2026-07-11-accounts-platform-plan.md (3dpa Accounts Platform pre-ratification design spec + gated plan, solo hobbyist dev). Prior reports R1-R5 in docs/reviews/bridge-2026-07-12-*.md; finding trend 16 -> 15 -> 8 -> 3 -> 2. Both R5 findings are patched: identity is strictly (provider, sub) with users.email demoted to nullable NON-UNIQUE metadata, cross-provider linking only via an explicit authenticated link flow (start?link=1) with same-email-isolation + link-attach tests; Apple grants are stored per-client_id (provider_grants AEAD map), revocation_outbox carries one row per (client_id, token) pair, the deletion batch enqueues every grant and AP9 tests web-only/native-only/both-grant revocation; My 3dpa shows linked providers + Link action. ROUND 6 JOB: verify the two R5 patches are coherent; report ONLY remaining or new P0/P1/P2 strictly at or above the altitude of a pre-ratification design spec + gated plan — do NOT report implementation-level details that per-gate build reviews will catch, and do not re-litigate accepted design decisions. If nothing at this altitude remains, issue GO plainly. Output numbered findings if any, then verdict GO / GO-WITH-PATCHES / NO-GO.

## Codex (review)

1. **P1 must-fix — §4.3 / AP2 — Explicit linking is not securely bound to the initiating account.** Apple’s cross-site `form_post` drops the Lax session cookie, while the pre-auth cookie carries no authenticated `user_id`/session link intent. Collision behavior is also undefined when `(provider, sub)` already belongs to another user. Bind a one-time link transaction to the initiating user/session/provider; reject session changes and already-owned identities without mutation; add both tests.

2. **P2 should-fix — APD7 / AP6 — Link UI patch was not propagated into the executable gate.** The spec requires linked-provider display and “Link Apple/Google,” but AP6 schedules only generic “identity.” Explicitly add the provider list, Link action, and UI acceptance test to AP6.

Optional: none.

**Verdict: NO-GO.**

