# Bridge session — 2026-08-20-230300-636803

## Task

Adversarially review a SYNC SPECIFICATION for a single-user, offline-first app that will
sync via CloudKit to Apple devices only in v1.

Read in this order:
1. docs/superpowers/specs/2026-08-20-sync-v1-spec.md — the spec under review
2. docs/superpowers/specs/2026-08-20-gear-model-v2-spec.md — the RATIFIED gear schema it must be consistent with
3. docs/reviews/2026-08-20-gear-model-owner-decisions.md — owner decisions D15, D16, D18
4. workshop-store.js — a LIVE, shipped store the spec claims has two sync-fatal defects
5. app.js and index.html only as needed to check what state is where

Attack these specifically:

A. MERGE RULES (section 3). Find a concrete sequence of operations across two devices that
   loses data or produces a wrong result under the stated rules. Test especially: the
   updated_at / last_used_at split; per-field-group LWW where a profile's value fields and
   its journal merge differently; tombstone-beats-edit; and whether "an array of pinned
   values is a value, not a collection" holds for gear fields.
B. THE TWO CLAIMED DEFECTS (section 5). Verify them against workshop-store.js. Is D-1
   (hard delete, no tombstone) real? Is D-2 (direction-dependent import merge) real, and is
   the spec right that the tuning op-union half is safely reusable? Are there OTHER
   sync-fatal problems in that file the spec missed? Read it properly.
C. WHAT SYNCS (section 1). Is the documents-vs-session split correct? Name anything
   classified wrongly, and anything persisted by this app that the table omits entirely.
D. EDGE CASES (section 6). Which real-world case is missing or wrong? Consider CloudKit
   specifics: zone deletion, iCloud storage full vs quota, record-not-found, the user
   deleting the app on one device, restoring from a backup, two devices first-syncing
   simultaneously.
E. CONSISTENCY with the ratified gear spec — any contradiction between the two documents.
F. Anything overstated, unverifiable, or where the spec asserts a platform behaviour it did
   not verify.

Return prioritized findings. Be blunt. If a section is sound, say which is weakest.

## Codex (review)

[🟩🟩🟩🟩🟩 100%] Review complete. Store tests pass (`node scripts/workshop-store.test.js`), so the live behavior is intentional, not accidental.

**Must-Fix**

- [docs/superpowers/specs/2026-08-20-sync-v1-spec.md](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-20-sync-v1-spec.md:69): Profile merge loses value edits when journal writes bump the same `updated`. Sequence: A renames profile at t1; B, on stale name, logs an outcome at t2; [addOutcome](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/workshop-store.js:186) sets `p.updated = _now()` at line 201, so B’s stale `name/state/notes` beat A while journal union survives. Need separate value timestamp vs journal timestamp, or journal writes must not drive value LWW.

- [workshop-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/workshop-store.js:206): Journal deletion is sync-fatal and missing from the spec. UI exposes outcome deletion in [app.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/app.js:1089), but `removeOutcome` hard-deletes. Under union-by-id, A removes an outcome, B later syncs an old copy, and the outcome resurrects. Need per-outcome tombstones or declare outcome deletion local/unsupported.

- [docs/superpowers/specs/2026-08-20-sync-v1-spec.md](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-20-sync-v1-spec.md:108): §3.5 understates loss. It says only “same value field” concurrent edits lose, but gear rules are whole-record LWW per the ratified gear spec [§4.2](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-20-gear-model-v2-spec.md:349). Sequence: A renames a gear; B pins `fields.useCase`; later record wins and the other edit is gone. Either add per-field timestamps or state the real loss.

- [docs/superpowers/specs/2026-08-20-sync-v1-spec.md](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-20-sync-v1-spec.md:91): Workshop tuning has no record model. The spec says each Workshop profile is a record, but live `workshop-store.js` also persists `tuning.accepted` and `tuning.dismissed` [here](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/workshop-store.js:72). Define whether tuning entries are per-op records, per pair+offset records, or one ledger record.

- [workshop-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/workshop-store.js:28): Existing Workshop reads are lossy. Invalid/future-version envelopes degrade to empty, and later writes can overwrite the original data via `_write`. That is fine for a local v1 store, not for sync/version skew. Need raw preservation or hostile-envelope rules like the gear spec.

- [docs/superpowers/specs/2026-08-20-sync-v1-spec.md](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-20-sync-v1-spec.md:28): Inventory sync is overcommitted. D18 says Inventory ships after 2.0, and D18b explicitly leaves local-first vs server-backed inventory unresolved [here](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/reviews/2026-08-20-gear-model-owner-decisions.md:349). The sync spec cannot promise Inventory sync yet.

**Claim Check**

- D-1 is real. [remove(id)](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/workshop-store.js:176) filters profiles out with no tombstone.

- D-2 is real. [importJSON](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/workshop-store.js:224) keeps existing profiles then lets incoming profiles overwrite by id.

- The tuning op-union half is mostly reusable, and tests cover fork-lossless/idempotent behavior. Caveat: `revertTuning` is only “append a negating op for currently known ops” [here](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/workshop-store.js:122). Concurrent accepts survive a revert. That may be correct, but the spec must say so.

**Should-Fix**

- [docs/superpowers/specs/2026-08-20-sync-v1-spec.md](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-20-sync-v1-spec.md:31): `3dpa_lang` is mis-described for web. Engine persists an explicit user toggle in [engine.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/engine.js:27), so it does not merely follow device language.

- Gear `settings` fields are not classified. `active_gear`, `catalog_seen`, and `save_prompt_dismissed` exist in the ratified envelope [here](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-20-gear-model-v2-spec.md:150). Some are document-like, some preference/session-like. Decide each.

- Section 6 misses CloudKit cases that must be handled before implementation: user-deleted zone, zone-not-found, record-not-found/unknown-item, change-token expiry, server-record-changed conflict retry, app deletion/reinstall, backup restore with stale sync metadata, and simultaneous first-sync zone creation.

- Quota is too vague. LocalStorage quota, CloudKit private iCloud storage full, request/record `limitExceeded`, and per-record 1 MB are different failures with different recovery paths.

**Optional / Sound**

- “Array of pinned values is a value” is sound for gear fields. Unioning `useCase`/`special` would be wrong; the gear spec’s empty-array and engine-order rules support LWW.

- `updated_at` vs `last_used_at` is sound for gears if implemented exactly: use bumps `last_used_at`, content edits bump `updated_at`, and the two merge independently.

- Privacy §7 is the strongest section. The E2E caveat is correctly cautious.

Apple docs checked: [CKRecord 1 MB limit](https://developer.apple.com/documentation/cloudkit/ckrecord), [quotaExceeded](https://developer.apple.com/documentation/cloudkit/ckerror/quotaexceeded), [serverRecordChanged](https://developer.apple.com/documentation/cloudkit/ckerror/serverrecordchanged), [userDeletedZone](https://developer.apple.com/documentation/cloudkit/ckerror/code/userdeletedzone), [CKSyncEngine accountChange](https://developer.apple.com/documentation/cloudkit/cksyncengine-5sie5/event/accountchange).



