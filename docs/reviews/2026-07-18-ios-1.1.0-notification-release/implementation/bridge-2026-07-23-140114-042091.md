# Bridge session — 2026-07-23-140114-042091

## Task

Read the implementation review prompt under 3dprintassistant/docs/reviews/2026-07-18-ios-1.1.0-notification-release/implementation/. Hostile-review both repo diffs against the ratified design and plan. Find security, privacy, D1 atomicity, Queue/DLQ, APNs, consent, token lifecycle, routing, export, analytics, and release-gate defects. Return P0/P1/P2 plus optional findings with exact file references; do not edit.

## Claude (review)

## VERDICT: NO-GO

Here's the full review — 1 P1, 6 P2s, 5 optionals, and a gap list.

---

**P1-A — Cold-launch notification routing broken** *(blocking)*
`AppNotificationDelegate.swift` lines 8–24. `application(_:didFinishLaunchingWithOptions:)` never extracts `launchOptions[.remoteNotification]` and calls `NotificationCoordinator.shared.receive()`. The design spec explicitly requires synchronous coordinator population before SwiftUI evaluates — the DEBUG benchmark path proves the intent. The async `userNotificationCenter(_:didReceive:)` callback fires after both `.task {}` blocks in `PrintAssistantApp.body` have already captured `hasPendingNewPrinterDestination = false`, so `LaunchCatalogBootstrapper` skips `prepareForLaunch()`, the catalog is stale, and every cold-launch tap on a `new_printer` notification shows "printer not ready."

---

**P2-A** `consumer.js` lines 211–239 — `finishCampaign` runs three sequential D1 reads + update without a transaction; concurrent queue messages can set incorrect terminal status (`complete` vs `partial`/`failed`).

**P2-B** `campaigns.js` lines 229–279 — Campaign duplicate + cadence checks are separate SELECTs from the batch INSERT; TOCTOU race allows two campaigns in the same 7-day window.

**P2-C** `campaigns.js` lines 363–374 — `replayCampaignCursor` sets campaign to `queued` before iterating PUSH_FANOUT sends; a mid-loop failure leaves the campaign `queued` with no queue message for un-sent cursors.

**P2-D** `apns.js` lines 87–100 — APNs 400/`TopicDisallowed` falls through to `failed` instead of `blocked`; a misconfigured topic burns all retries before halting the campaign.

**P2-E** `NotificationService.swift` lines 192–227 — `refreshForLaunch()` is not wired to `UIApplication.willEnterForegroundNotification`; permission revocation in iOS Settings while backgrounded is undetected until next cold launch.

**P2-F** `consumer.js` lines 195–209 — `PUSH_DLQ.send()` is outside the D1 batch in `preserveCursor`; a DLQ send failure leaves the campaign silently `blocked` with no automated recovery driver.

---

**Optionals:** announcement_id not unique-constrained (collapse suppression risk); `apns-id` response header discarded (debugging gap); token hex length unbounded; Content-Type not validated; `ProductUpdatesView.task` fires `refreshForLaunch()` on every sheet open.

---

**Evidence gaps (cannot verify locally):** Issues #2/#3/#4/#5 implementation, native export serializers, Vitest/XCTest notification test coverage, APS entitlements env-var resolution in the release scheme, twin-repo engine/data byte-identity, `push_replay_cursors` retention (no purge in `runRetention()`), and the canary round-trip evidence.

GO is valid only after P1-A is closed. Gate state (no provisioning, no iOS push, G0 not reached) is confirmed correct.

