# Bridge session — 2026-07-23-142532-902585

## Task

Confirmation round for your prior NO-GO review (transcript: 3dprintassistant/docs/reviews/2026-07-18-ios-1.1.0-notification-release/implementation/bridge-2026-07-23-140114-042091.md). The controller disposition is in implementation-review-disposition.md in that same directory — read both. Verify the APPLIED fixes in the actual diffs, not the proposals: web commits 8682ab6 (P2-D TopicDisallowed blocked), 18c41ad (P2-A atomic finishCampaign), 247bea7 (P2-B atomic cadence insert), 8f8acce (P2-C replay blocked restore), 29c3e94 (P2-F DLQ advisory catch) via git -C 3dprintassistant show <sha>; iOS commit 3f886bf (P1-A refresh-on-miss cold-launch routing) via git -C 3dprintassistant-ios show 3f886bf. Also evaluate the P2-E refutation (ContentView.onChange scenePhase .active calls refreshForLaunch). Check each fix is correct, complete, and introduces no new defect. Return VERDICT: GO or NO-GO plus any remaining or new P0/P1/P2 with exact file references; do not edit.

## Claude (review)

**VERDICT: GO**

All six commits verified against current source. No new P0/P1/P2.

---

**P2-D** (`apns.js` L97) — `reason === "TopicDisallowed"` is status-code-independent (Apple sends it on HTTP 400, not 403), so the `status === 403 || reason === ...` OR correctly covers both. ✅

**P2-A** (`consumer.js` L218–241) — `finishCampaign` is a single `UPDATE … CASE … WHERE status='sending' AND NOT EXISTS(pending/retryable deliveries)`. Atomic; concurrent finishers lose the `status='sending'` guard. ✅

**P2-B** (`campaigns.js` L253–318) — Public cadence check is now inside the `INSERT … SELECT … WHERE NOT EXISTS(7-day window)`, not a pre-read. Deliveries INSERT conditioned on `EXISTS(campaign_id)` so a cadence-rejected batch inserts nothing. Post-batch disambiguation (L310–318) correctly separates PK-duplicate from cadence-reject. ✅

**P2-C** (`campaigns.js` L409–432) — Sets `queued` before the loop; catch-with-`enqueued===0` restores `blocked / replay_incomplete`; partial progress stays `queued`. Double-dispatch idempotent via `processDelivery`'s `status IN ('pending','retryable')` guard. ✅

**P2-F** (`consumer.js` L208–215) — D1 batch commits first; `PUSH_DLQ.send` is wrapped in `try/catch {}`; swallow is justified (D1 `push_replay_cursors` is the authoritative recovery record). ✅

**P1-A** (iOS, three files) — `NotificationCoordinator.consumeWhenReady` keeps `pendingDestination` live on first miss, sets `needsCatalogRefresh=true`. `ContentView.consumePendingIfReady` calls `engineRetry()` on first miss; `LaunchCatalogBootstrapper.start` sees `hasPendingNewPrinterDestination==true` and runs `prepareForLaunch()` before the snapshot; `onChange(engineReady)` re-fires `consumePendingIfReady`; second miss shows the locked not-ready alert. `AppNotificationDelegate.didFinishLaunchingWithOptions` correctly left unchanged (canonical tap path is `didReceive` under UNUserNotificationCenter delegate). ✅

**P2-E refutation** — `ContentView.swift` L88–95 confirmed: `.onChange(of: scenePhase)` → `.active` → `notificationService.refreshForLaunch()`. Pre-existed. Refutation stands. ✅

---

**New optional (O-1):** `campaigns.js` `replayCampaignCursor` — if called when no `preserved` cursors exist (operator double-calls after a prior partial replay drained them), the `UPDATE … SET status='queued'` runs, the loop is a no-op, no exception fires, and the campaign is stuck `queued` with zero in-flight consumers. Rare but silent. Not a P2 (requires a precondition that shouldn't arise in normal operation), but worth a runbook note.

---

Task 9 Step 2 closes. Provisioning / iOS push / TestFlight / App Review / public send remain gated on owner G0.

