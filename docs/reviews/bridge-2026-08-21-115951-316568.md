# Bridge session — 2026-08-21-115951-316568

## Task

Hostile review of IMPLEMENTED code that freezes a storage format forever. Review commit 324fbc1 on main. Read gear-store.js and scripts/gear-store.test.js in FULL, plus docs/superpowers/specs/2026-08-20-gear-model-v2-spec.md sections 2.1 through 2.5, 3.1, 4.2 and 4.3, and the 2026-08-21 array amendment inside 2.4.

WHY THE BAR IS HIGH: the first real browser write freezes 3dpa_gear_v1 for iCloud sync and for a future iOS Swift implementation. A format defect here cannot be reformatted later. You reviewed the PLAN for this file twice and returned NO-GO both times; this is the implementation of the third revision.

Attack these, in priority order:

1. FORMAT CORRECTNESS. Diff the persisted bytes against spec 2.2 and 2.3. Is the persisted row exactly name/fields/labels/created_at/updated_at/last_used_at/archived_at with no id and no invalid? Is there ANY path - save, update, touch, archive, restore, settings writes - that persists a field the spec does not define, or drops one it does?

2. THE TOTAL ORDER. Spec 2.3 requires last_used_at desc nulls last, then created_at desc, then id ascending bytewise, and says two devices must compute the SAME order from the same data. Read _order and _cmpKey. Is the comparator a valid total order - irreflexive, antisymmetric, transitive? Can Array.prototype.sort produce a different result on the same data given a different initial key order? Is the created_at sentinel reachable as a real value?

3. READ NEVER WRITES. Spec 2.3 forbids read-side repair. Prove or disprove that no read path mutates storage. Check that normalization done in memory cannot leak into a later write of an UNRELATED row.

4. THE ARRAY AMENDMENT. Values sort UTF-8 bytewise; where a field has a parallel labels array the sort must be a permutation over the (value,label) PAIRS. Read that code. Does it hold when the arrays are different lengths, when labels is absent, when a value repeats, when the array is mixed-type?

5. update() SET EQUALITY. A reordering must not move updated_at. Is the equality check correct for nested arrays, for a field changing between string and array, and for the empty array meaning pinned-as-none?

6. HOSTILE ENVELOPE. Prototype pollution via gears, settings, fields or labels keys. Can any crafted envelope throw, lose an unrelated row, or pollute Object.prototype?

7. TEST DISCRIMINATION. You have caught vacuous assertions in this codebase three times. Sweep all 73 assertions and name any that pass with the implementation reverted or absent. Pay special attention to G17 and G18, which I added by hand after the implementation existed - tests written after the code are the most likely to be tautological.

Report MUST-FIX / SHOULD-FIX / OBSERVATION with file:line, and an explicit GO or NO-GO for freezing this format. Be adversarial. Do not praise. If a category is clean say so explicitly.

## Codex (review)

NO-GO for freezing `3dpa_gear_v1`.

**MUST-FIX**
- [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:60): `_validIso` is only a regex. It accepts impossible dates, offsets, and missing milliseconds, despite spec requiring UTC milliseconds and unparseable `created_at` last. `9999-99-99T99:99:99.999Z` sorts before real rows. [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:37): the created sentinel is also accepted by `_validIso`, so it is reachable as persisted data.
- [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:213): save/update can persist non-spec field values: numbers, objects, booleans, and mixed arrays are preserved verbatim. Spec 2.4 says values are string or array of strings.
- [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:187): label values are not shape-validated. A string field can persist array/object labels; an array field can persist a non-parallel or wrong-length label array. That violates the label mirror rule.
- [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:166): array amendment fails for different-length labels. The values sort, but labels are left attached under the same key without pair permutation, creating persisted misalignment.
- [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:446): `touch()` does not move `last_used_at` alone. `_mutate()` rebuilds the touched row from normalized DTO state at [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:349), so a use can dedupe/sort fields and drop labels on that same row.
- [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:501): settings writes preserve unknown settings keys. Spec 2.2 defines the settings shape; this silently freezes an undocumented extension point.

**SHOULD-FIX**
- [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:111): `gears` and `settings` are ordinary objects in `_emptyEnv`; [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:504): settings write staging also uses `{}`. Current code avoids obvious pollution, but it does not implement spec 2.5’s null-prototype rule for every map.
- [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:81): if `TextEncoder` is absent, `_cmpKey` falls back to UTF-16 ordering, which is explicitly not the frozen bytewise order.

**OBSERVATION**
- Top-level persisted gear row keys are correct through `_toPersist`: exactly `name/fields/labels/created_at/updated_at/last_used_at/archived_at`, no `id`, no `invalid`.
- The comparator is a valid JS total order for DTOs with unique string ids, so `Array.prototype.sort` should not vary by initial key order for equal data. The defect is spec-domain validity, not comparator mechanics.
- Pure read paths do not call `setItem`; unrelated gear rows are not normalized by gear mutations. Same-row non-content writes are the leak.
- `update()` equality is acceptable for valid string arrays, string↔array changes, and `[]` pinned-as-none. Nested/mixed arrays are outside spec but currently persist, which is already covered as MUST-FIX.
- No direct `Object.prototype` pollution found from JSON hostile envelopes.

**Test Discrimination**
- Current suite: 73 assertions pass.
- Reverted-to-parent/absent implementation: none execute; `gear-store.js` did not exist before this commit, so load fails before `check()`.
- Weak/non-discriminating assertions:
  - [scripts/gear-store.test.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/scripts/gear-store.test.js:113): fixture self-check only.
  - [scripts/gear-store.test.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/scripts/gear-store.test.js:214): JS language self-check only.
  - [scripts/gear-store.test.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/scripts/gear-store.test.js:295): G17 first label case also passes with independent label sorting.
  - [scripts/gear-store.test.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/scripts/gear-store.test.js:338): G18 “stored order” checks `get()` normalization, not persisted bytes.
  - [scripts/gear-store.test.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/scripts/gear-store.test.js:350): G18 tombstone/row survival is broad and would pass many broken no-op implementations.

