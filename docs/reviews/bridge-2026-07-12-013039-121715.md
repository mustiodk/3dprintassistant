# Bridge session — 2026-07-12-013039-121715

## Task

Cross-model review ROUND 16 (final confirmation) of docs/superpowers/specs/2026-07-11-accounts-platform-design.md + docs/superpowers/plans/2026-07-11-accounts-platform-plan.md (3dpa Accounts Platform, solo hobbyist dev). Reports R1-R15 in docs/reviews/bridge-2026-07-12-*.md. Both R15 findings patched: AP0 registers a stable staging hostname (accounts-staging.3dprintassistant.com) with both providers and AP1 ships [env.staging] (own D1/KV/secrets) making pre-AP9 real-provider E2E executable (the defer-to-AP9 alternative explicitly rejected); AP9's PR updates status.js so rights:true is an unconditional code change from that deploy, independent of both flag secrets, with before/after and flags-off tests. ROUND 16 JOB: confirm no remaining P0/P1/P2 at spec+plan altitude. If clean, issue GO plainly. Verdict: GO / GO-WITH-PATCHES / NO-GO.

## Codex (review)

**Verdict: NO-GO.** The R15 edits are present, but the staging patch introduces one P1.

### Must-fix

1. **P1 — AP0–AP2: staging is an unprotected public accounts deployment.** `accounts-staging.3dprintassistant.com` must enable real OAuth and stores name/email/user IDs before AP9 supplies the corrected policy, export, or deletion. The current [privacy policy](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/privacy.html:153) explicitly denies collecting that data, while the [plan](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-07-11-accounts-platform-plan.md:39) specifies no access control or post-test purge. **Fix:** add an owner/tester-only staging gate that survives provider callbacks, present the corrected staging privacy notice before consent, and wipe staging account data after E2E.

### Should-fix

2. **P2 — AP1 does not operationally deploy or migrate the named staging environment.** Its gate migrates a vaguely named “preview D1”; the production command also omits Wrangler’s required database argument. A named environment is a distinct Worker requiring `--env staging`, and bindings are non-inheritable. **Fix:** specify staging deploy/migration commands, repeat every required staging binding—including rate limiting—and use explicit commands such as `wrangler d1 migrations apply ACCOUNTS_DB --remote --env staging`. [Cloudflare environments](https://developers.cloudflare.com/workers/wrangler/environments/), [D1 command reference](https://developers.cloudflare.com/d1/wrangler-commands/).

### Optional

None.

AP9’s unconditional `rights:true` code transition is coherent. Baseline verification remains green: 90/90 tests; engine golden snapshot reports no drift.

