# Bridge session — 2026-08-21-122117-665790

## Task

Round-3 review of a storage format about to be frozen forever. You have returned NO-GO twice: on 324fbc1 (six MUST-FIX) and on a789317 (two MUST-FIX). Commit b709782 applies the round-2 findings. Your round-2 transcript is docs/reviews/bridge-2026-08-21-120959-769984.md.

Read gear-store.js and scripts/gear-store.test.js in FULL and diff b709782 against a789317.

WHAT CHANGED: mutators now return a PATCH of persisted keys applied to the RAW stored row instead of rebuilding it from a normalized DTO. Orphan labels are validated. Update validation was narrowed to only what the call writes. Only patched keys are normalized. The merge bases on raw.fields, not dto.fields.

PRIORITY ORDER:

1. VERIFY BOTH ROUND-2 MUST-FIX. CLOSED or STILL OPEN with code evidence.
   - Is there ANY remaining write path that rewrites a byte the caller did not touch? Enumerate every writer: save, update, touch, archive, restore, setActiveGear, setSavePromptDismissed, markCatalogSeen. For each, state exactly which persisted keys it can change.
   - Can a non-conforming value or a mis-shaped label still reach storage through any path?

2. THE NEW RISK THIS DIFF CREATES. Patching a raw row means a hostile or malformed stored row is now PRESERVED rather than normalized on write. Is there any case where preserving it is worse than normalizing it? Specifically: can a preserved row now break a later read, break the total order, break the sync merge, or grow unboundedly? Does _toPersist still bound the top-level key set on the paths that use it, and is the divergence between patch-writes and _toPersist-writes a problem?

3. CORRECTNESS OF THE NARROWED VALIDATION. Walk the exact case: a stored row has surface:42 and a mismatched material label. The user patches nozzle. What is validated, what is written, what is preserved? Then: the user patches material with a correct label. Is the lockstep permutation still correct when the base row is malformed?

4. REGRESSION. Does update() still move updated_at exactly when content changed and not otherwise? Does the set-equality no-op still hold now that the merge is raw-based? Can update() now silently DROP a key that the old rebuild would have kept, or keep one it should drop?

5. TEST DISCRIMINATION. 119 assertions. Sweep every NEW assertion (G24 through G27) plus the ones you flagged as weak in round 2. Name any that pass with its fix reverted.

6. FREEZE VERDICT. Explicit GO or NO-GO for allowing the first real browser write to freeze 3dpa_gear_v1. If NO-GO, say exactly what must change. If GO, say so plainly.

Be adversarial. Do not praise. If a category is clean, say so explicitly.

## Codex (review)

[🟩🟩🟩🟩🟩 100%] NO-GO for freezing `3dpa_gear_v1` at `b709782`.

Tests pass (`node scripts/gear-store.test.js`, 119 ok), but the narrowed `update()` path still has freeze-blocking holes.

**MUST-FIX Findings**
1. `update()` can create a mis-shaped label by changing a field without changing its existing label. Validation only checks `p.labels`, not existing labels affected by `p.fields`: [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:504), [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:511). Probe: stored `material:['a','b']`, label `['A','B']`, then `update({fields:{material:'pla_basic'}})` persists `field:"pla_basic"` with `label:["A","B"]`.

2. `update()` can move `updated_at` on a no-op against a malformed raw row. It compares normalized DTO maps to raw-based merged maps: [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:609). Probe: stored `useCase:['zulu','alpha','zulu']`, then `update({labels:{}})` moved `updated_at` despite no content change.

3. Lockstep can break when patching a same-set array field and label on a raw-order malformed row. Because `_sameMap()` treats array order as no-op, `out.fields` can be suppressed while `out.labels` is written: [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:619), [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:620). Probe: raw `material:['b','a']`; patch `fields.material:['a','b']`, `labels.material:['Ay','Bee']`; persisted raw remains `['b','a']` with labels `['Ay','Bee']`, so read-back pairing is wrong.

**Round-2 Status**
Orphan label validation: CLOSED for direct writes. `save()` now rejects non-conforming orphan labels: [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:393). `update()` rejects bad labels it is explicitly given: [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:516). Still open as a broader write-validation category because field-only updates can create label mismatch.

Raw-row laundering: mostly CLOSED. Writers:
- `save`: new gear row only: `name`, `fields`, `labels`, `created_at`, `updated_at`, `last_used_at`, `archived_at`.
- `update`: `name`, `fields`, `labels`, `updated_at`; still buggy above.
- `touch`: `last_used_at` only.
- `archive`: `archived_at`, `updated_at`.
- `restore`: `archived_at`, `updated_at`.
- `setActiveGear`: `settings.active_gear`, `settings.updated_at`, rewrites settings to the four documented settings keys.
- `setSavePromptDismissed`: `settings.save_prompt_dismissed`, `settings.updated_at`, same settings rewrite.
- `markCatalogSeen`: `settings.catalog_seen`, `settings.updated_at`, same settings rewrite.

**New Risk**
Preserving raw rows is not itself fatal for reads or total order: `_toDto()` degrades bad fields, `_order()` treats invalid timestamps as absent, and key ordering remains total. It is worse for sync when a no-op update manufactures `updated_at`; that can win a later merge without a real content edit.

`_toPersist()` still bounds top-level row keys only on `save()`: [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:297). `_mutate()` only bounds new patch keys and preserves existing top-level junk: [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:453). Public APIs cannot grow top-level junk, but hostile junk can be synced forever. SHOULD-FIX: explicitly test/document this, or prune top-level row keys deliberately.

**Test Discrimination**
Weak/pass-with-revert assertions:
`G24 well-formed orphan label allowed`; many `G25` positives (`reports ok`, `name changed`, `updated_at moved`, `surface` preserved, material orphan label survived, archive tombstone/moved/name, nozzle applied); `G26 genuinely newer valid row leads`; prior weak `G17` first case, `G18 stored order` and tombstone, `G19` ok/moved/updated_at, `G20` positive accepts, `G21 documented fields survive`, `G22` offset/missing-ms.

Required change: make `update()` validate every effective field/label pair whose field or label can be affected, keep field/label array ordering locked when either side is written, and compute no-op/update clocks from the intended patch effect rather than raw-vs-normalized divergence.

