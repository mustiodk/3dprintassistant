# Bridge session — 2026-07-12-003802-798321

## Task

Cross-model review ROUND 4 (convergence gate) of docs/superpowers/specs/2026-07-11-accounts-platform-design.md + docs/superpowers/plans/2026-07-11-accounts-platform-plan.md (3dpa Accounts Platform pre-ratification design spec + gated implementation plan, solo hobbyist dev). Prior rounds: docs/reviews/bridge-2026-07-12-000932-423960.md (R1), bridge-2026-07-12-002253-557468.md (R2), bridge-2026-07-12-003135-436387.md (R3). All R3 findings are patched: per-field revs propagated into AP5 + I2 with the disjoint-field fixture; AP4 separates device registration (last_seen) from explicit post-apply acks (X-Applied-Version / POST ack; GET never acks) and adds a persisted per-doc compaction watermark with below-watermark PUT rejection + tests; AP5 sign-out switches active namespace to local immediately + signed-out visibility test; deletion order is now idempotent KV intent FIRST then atomic D1 batch (outbox enqueue + child-first deletes) then independent retryable revoke, with retry/double-delete tests in AP9 and the full APD7 export scope enumerated; APD5 gains color_code/gradient/order_status/defect_note + an import_meta opaque lossless passthrough with a ratified field-mapping table and export-import-export round-trip fixture in AP7; APD1 stale entitlements mention removed and all ranges normalized to APD0-APD15; AP1/AP2 gained keyring-rotation, grace-AEAD-seal and cookie-attribute test assertions. ROUND 4 JOB: report ONLY remaining or new P0/P1/P2 findings at or above the altitude of a pre-ratification design spec + gated plan — implementation details that per-gate build reviews will catch are below this artifact's purpose and must not be reported. If the patches are coherent and nothing at this altitude remains, say so plainly and issue GO. Output numbered findings (severity + section + failure scenario + fix) if any, then verdict GO / GO-WITH-PATCHES / NO-GO.

## Codex (review)

No P0.

Must-fix

1. **P1 — APD3/APD7/APD8 §4.5, AP3/AP9 — GDPR kill-switch carve-out is not usable.** Access JWTs expire within one hour, but refresh is disabled; `auth/status` returns 503 and AP3 hides all account UI. Users therefore cannot reach authenticated export/delete despite the endpoints being exempted. **Fix:** keep existing-session refresh and a minimal account-rights UI reachable while disabling new authentication and sync; test export/delete after access-token expiry with the kill switch active.

2. **P1 — APD7/APD8 §4.3, acceptance 4 — Deletion promises contradict the revocation outbox.** The spec promises immediate deletion of all server rows, while failed Apple revocation intentionally retains an encrypted token in D1 for up to 30 days ([spec:91](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-07-11-accounts-platform-design.md:91), [spec:168](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-07-11-accounts-platform-design.md:168), [spec:277](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-07-11-accounts-platform-design.md:277)). AP9 discloses only deletion-log retention. **Fix:** explicitly carve out the bounded revocation credential, disclose its retention, and make acceptance assert either successful purge or the documented pending-outbox state.

3. **P1 — §4.5 / executable plan — Restore-time deletion replay has no gate.** The spec requires KV deletion replay before traffic resumes, but AP1–AP11 schedule no replay implementation, runbook, or restore rehearsal. Failure: Time Travel restores previously deleted accounts and traffic resumes without re-deleting them. **Fix:** add a gated recovery runbook/tool and rehearsal proving restore → KV replay → deleted users absent before reopening traffic; normalize monthly versus quarterly export cadence.

Should-fix: none.

Optional: none.

**Verdict: NO-GO.**

