# Bridge session — 2026-08-21-102720-968049

## Task

Hostile review of three data-loss fixes to workshop-store.js, a SHIPPED localStorage store with real users on 3dprintassistant.com. Review commits a442f1f, 1354bf9, fa305f4 on branch main (diff them against a442f1f~1). Read workshop-store.js and scripts/workshop-store.test.js in full, plus docs/superpowers/specs/2026-08-20-sync-v1-spec.md sections 3 and 5 which specify the intended behaviour.

The three fixes:
D-5: a version-mismatched envelope must be preserved, never overwritten. _readEnv now returns {_skew:true} and _writeEnv refuses. Profile mutators got an early _skewed() guard.
D-1: remove() soft-deletes via archived_at. list()/get() filter to live. Absent archived_at must read as LIVE so old backups still import.
D-3: addOutcome/removeOutcome set journal_updated instead of updated.

Attack these specifically:
1. Is there ANY remaining path that can persist data over a version-skewed envelope? Check every _writeEnv and _write call site, exportJSON, importJSON, and the tuning functions.
2. Does the D-1 change break any existing behaviour? Does exportJSON leaking archived rows into user backups cause a problem? Does importJSON handle archived_at correctly? Is there a path where a soft-deleted row is treated as live or vice versa?
3. Is the _isLive/absent-archived_at logic correct for every shape a legacy envelope can take, including archived_at being a non-string truthy value, empty string, or null?
4. D-3: are there other writers that still bump  when they should not, or readers that depended on outcomes bumping ? Check revertTuning, dismissSuggestion, addTuningOp, importJSON.
5. Are the new tests actually capable of failing? Flag any assertion that would pass even if the implementation were reverted. One test in this batch was already found to be degenerate against millisecond clock resolution and was rewritten with seeded timestamps - look for others of that class.
6. Backward compatibility: an old client and a new client sharing one browser profile, and an old backup file imported into the new code.

Report findings as MUST-FIX / SHOULD-FIX / OBSERVATION with file:line. Be adversarial. Do not praise. If you find nothing in a category say so explicitly.

## Codex (review)

Reviewed local `main` `fa305f4` against `d70547d` (`a442f1f~1`). Tests pass locally: `node scripts/workshop-store.test.js`.

**MUST-FIX**
- [workshop-store.js:260](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/workshop-store.js:260): `exportJSON()` under version skew emits a valid empty v1 backup: `{ v: 1, profiles: [] }`. That does not overwrite localStorage, but it can create a destructive user backup from an unreadable future envelope. It should refuse/export skew explicitly, not manufacture v1 empty data.
- [workshop-store.js:201](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/workshop-store.js:201), [workshop-store.js:226](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/workshop-store.js:226), [workshop-store.js:251](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/workshop-store.js:251): `rename`, `addOutcome`, and `removeOutcome` still operate on archived profiles because `_read()` returns archived rows and these call sites do not require `_isLive(p)`. After `remove()`, these used to be impossible because the row was gone; now deleted rows can be mutated.
- [workshop-store.js:16](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/workshop-store.js:16), [workshop-store.js:216](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/workshop-store.js:216): D-1 is not old-client compatible while staying on envelope `v:1`. A pre-D1 client reads tombstoned profiles as live, shows them, and can write them back. Storing tombstones inside `profiles[]` is the problem; an envelope-level tombstone ledger would be preserved by old `_write()` while keeping old clients from listing deleted rows.

**SHOULD-FIX**
- [workshop-store.js:41](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/workshop-store.js:41): `_read()` converts any non-string `archived_at` to `null`, so `archived_at: true` or `1` becomes live. Absent/null/empty-string-as-live is defensible; non-string truthy should fail closed or be rejected, not resurrected.
- [workshop-store.js:150](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/workshop-store.js:150): `revertTuning()` under skew returns `not-found`, not `version-skew`. It does not persist data, but it violates the skew surface contract and is untested.
- [scripts/workshop-store.test.js:428](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/scripts/workshop-store.test.js:428): the test asserts `journal_updated` is not fabricated for legacy rows, but spec §5 says old records without it default to existing `updated`. Either the spec means “sync layer fallback only” or this test encodes the wrong compatibility rule.
- [app.js:1052](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/app.js:1052): the Workshop card date still displays `updated`. After D-3, logging/removing outcomes no longer changes that visible date. Not data loss, but it is a user-visible behavior change if the date meant “last activity”; display should use a separate derived date if needed.

**OBSERVATION**
- No remaining localStorage persistence path over a skewed envelope found through `_writeEnv`, `_write`, valid `importJSON`, `addTuningOp`, or `dismissSuggestion`; `_writeEnv` blocks them. The export path above is the remaining destructive path, but via backup output rather than localStorage.
- Exporting archived rows is necessary for tombstones to travel between new clients, but with the current in-profile tombstone design it also leaks deleted full profile data into backups and old clients treat those rows as live.
- Test gaps: D5 does not cover skewed `exportJSON`, `revertTuning`, `dismissSuggestion`, or valid-v1 import into skew. D1 does not cover archived-row mutators, old-client compatibility, or non-string `archived_at`.
- Degenerate/non-discriminating assertions: [scripts/workshop-store.test.js:284](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/scripts/workshop-store.test.js:284), [scripts/workshop-store.test.js:328](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/scripts/workshop-store.test.js:328), [scripts/workshop-store.test.js:329](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/scripts/workshop-store.test.js:329), [scripts/workshop-store.test.js:332](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/scripts/workshop-store.test.js:332) all pass under the reverted behavior. The D3 seeded timestamp assertions are discriminating; I did not find another millisecond-clock degenerate in that batch.



