# Bridge session — 2026-07-11-115050-528638

## Task

Review the uncommitted docs diff for 3dprintassistant. Scope: ROADMAP/NEXT-SESSION/session log/overall plan only. Check whether the export-first plan is factually grounded, whether it mishandles the stale origin/export-phase2-20260709 branch, whether it creates unsafe merge/push/TestFlight instructions, and whether the next-session ordering is clear. Return blocking findings first, then non-blocking suggestions. Do not edit files.

## Claude (review)

[🟩🟩🟩🟩🟩 100%] Analysis complete — writing findings

---

## Code Review — Export-First Docs Diff

### MUST-FIX

**1. Plan Task 1 Step 3: `git show` commands are display-only — nothing gets applied**
`git show <sha> -- <files>` prints a diff to stdout; it does not touch the working tree. The plan says "Apply the export deltas…from:" and then lists only show commands. A fresh session agent following the step literally sees diffs printed and zero files changed. The actual application command (`git cherry-pick <sha>` per code commit, or `git show … | git apply`) is missing. This is a broken execution step in a plan explicitly designed to be handed to a next-session agent.

**2. NEXT-SESSION read-order item 8: references a branch-only file with no access instruction**
`docs/sessions/2026-07-09-cowork-appdev-export-phase2.md` does not exist on current `main` — it exists only on `origin/export-phase2-20260709`. The prompt says "from `origin/export-phase2-20260709` if the file is not on current main" but gives no command to retrieve it (`git show origin/export-phase2-20260709:docs/sessions/…` or equivalent). A fresh session agent will hit a missing file and either silently skip it or fail the read step, losing the Phase 2 implementation context.

---

### SHOULD-FIX

**3. Task 1 Step 6 batches all Phase 2 findings into one commit**
The step stages every file and commits with a single message. The standing "one finding = one commit" rule exists for the five distinct Phase 2 changes: HIGH-2 retraction, 7 dual-variant arrays, inherits verification, import hint, Beta-badge removal. Batching all into one commit collapses that audit trail and makes per-finding rollback impossible. Cherry-picking the original P2 commits individually (one commit per SHA) preserves the rule.

**4. Task 1 Step 6 live-verify grep uses an unconfirmed constant**
`grep -c BAMBU_DUAL_VARIANT_PROCESS_FIELDS` assumes that exact string exists in the recovered `engine.js`. The constant name is not verified against the P2 branch source; if it doesn't exist (e.g. if the variable is named differently or the guard is inline), the count returns 0 and the check silently passes. Should use a function or export name confirmed present in the P2 diff (e.g. the presence of the `"nil"` second-element dual-variant logic, or `exportBambuStudioJSON` changes specific to Phase 2).

---

### OPTIONAL

**5. W3 Mine-tier ROADMAP entry still reads "owner verify+merge is the next step"**
This pre-existing entry sits in the follow-ups section below the active queue, so it won't block execution — but a cold-start agent scanning the page sees an open "verify+merge" action with no note that it's deferred past the export sequence. A one-liner "deferred until after export phases" prevents a mis-prioritization read.

