# Bridge session — 2026-08-21-130349-452515

## Task

Hostile review of newly implemented code. Review commit 26e6da4 on main: gear-validate.js and scripts/gear-validate.test.js. Read both in FULL, plus docs/superpowers/specs/2026-08-20-gear-model-v2-spec.md sections 2.4, 3.1, 3.2, 3.3, and Task 5 of docs/superpowers/plans/2026-08-21-train1-web-gear-plan.md.

CONTEXT: its sibling gear-store.js took FIVE of your review rounds and twelve MUST-FIX. Nearly every defect there was one invariant re-entering under a new name: a read or a partial write silently rewriting data the caller did not touch. Assume this file has the same class of defect somewhere and go find it.

PRIORITY ORDER:

1. VALIDATION ORDER. The order is load-bearing: known-key, then membership, then cardinality coercion, then the conditional 'mine' predicate, then re-ordering to engine item order. Construct inputs that would expose a wrong order. Specifically: does 'mine' get evaluated against COERCED values? Does an unknown catalog id get caught before cardinality mangles it? Can an unknown KEY reach ?

2. TWO ADJUDICATED SPEC TENSIONS - attack both rulings, do not just accept them.
   a. A one-element array narrowed to a scalar is reported via a note but does NOT set degraded, on the grounds that spec 3.1 defines degraded as a coercion that 'lost information' while spec 2.4's wording is shorthand for the lossy case. Is that reading defensible? Is there a case where a lossless narrowing SHOULD warn the user?
   b. An empty array on a single-valued field stays an empty array rather than coercing to undefined, to keep 'pinned as none' distinct from 'absent'. Does that leave an array in a scalar slot that a downstream consumer will mishandle? Trace what app.js would do with it.

3. THE SIBLING'S DEFECT CLASS. Does inspectGear mutate its input gear, or any array shared with the store? Does applyGearToState mutate anything it should not, or leave state in a half-applied condition if a dep throws? Is the single Object.assign actually sufficient to stop updateNozzleChips (app.js:1552) wiping a pinned nozzle - trace it against the real app.js.

4. FAIL-CLOSED CLAIMS. The implementer claims several behaviors beyond the plan: unreadable fields map goes stale rather than throwing, non-conforming values are unset and stale, a store-invalid gear becomes stale, a missing mineAvailable reads as unavailable, and a vanished 'safe' fallback leaves the field unset. Verify each by probe. Are any of them wrong, unreachable, or masking a real error?

5. TEST DISCRIMINATION. 50 assertions. Name every one that passes with the implementation reverted or absent. The plan's own V-tests were written before the code; V12 and V13 were written after and are the most suspect.

6. VERDICT: GO or NO-GO for this file. Distinguish defects that would produce WRONG USER-VISIBLE BEHAVIOR from quality issues. This file does not freeze a format, so the bar is correctness, not permanence - say GO if it is correct and list quality items separately.

Be adversarial. Do not praise. If a category is clean, say so explicitly.

## Codex (review)

**Verdict**
NO-GO for `gear-validate.js`.

**Must-Fix**
- [gear-validate.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-validate.js:221): `profileMode: "mine"` is rejected as `stale` before the conditional downgrade can run when `meta.filters` is real `Engine.getFilters(state)` output. `engine.js` only includes `mine` when personal tuning exists ([engine.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/engine.js:568)), but spec §3.2 requires deleted/missing tuning to degrade to `safe`, not become `stale`. The test fixture masks this by always including `mine` in `FILTERS` while `mineAvailable` is false ([scripts/gear-validate.test.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/scripts/gear-validate.test.js:45)). Probe with real-like filters returned `state:"stale"`, `profileMode` unset, note `unknown-id`.
- [gear-validate.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-validate.js:234): `[]` on a single-valued field is applied into scalar app state. Current consumers do not support that representation: `StateCodec` declares `surface` scalar ([state-codec.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/state-codec.js:23)), persists `surface:[]`, URL-encodes it as `sq=`, `Engine.resolveProfile` falls back to `standard`, and `getWarnings` reports an invalid preset ([engine.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/engine.js:1662)). The “pinned as none” ruling is not safe unless app state gets a real scalar-none representation or downstream code is updated.

**Should-Fix**
- [gear-validate.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-validate.js:362): `Object.assign(state, resolved)` aliases arrays from `resolved` into `state`. `inspectGear` does clone store arrays, so this does not currently mutate store-owned arrays, but later `state.useCase.push(...)` mutates the caller’s `resolved.useCase`.
- [gear-validate.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-validate.js:361): dep exceptions leave state half-applied. Missing deps are guarded, but a throwing `getSlicerForPrinter` leaves reset plus merge already committed.

**Clean**
- Validation order is correct when the conditional value survives membership: unknown key is skipped before shape/membership, membership runs before cardinality, `mineAvailable` sees coerced scalar printer/material, and multi arrays reorder to engine item order.
- Unknown catalog IDs are caught before cardinality mangles them. Probe `printer:["x1c","retired"]` returned stale and left `printer` unset.
- Unknown keys do not reach `resolved`.

**Fail-Closed Claims**
- Unreadable `fields` map: true direct behavior, stale not throw. Through `GearStore`, this exact shape is mostly normalized to invalid instead.
- Non-conforming values: true, stale and unset.
- Store-invalid gear: true, stale via `gear.invalid === true`.
- Missing `mineAvailable`: false under real `Engine.getFilters` when `mine` is absent; it becomes `unknown-id` before the predicate matters.
- Vanished `safe` fallback: true only if `mine` survives membership; otherwise masked by the same membership defect.

**Tests**
`node scripts/gear-validate.test.js` passes all 50 assertions.

With the implementation reverted or absent: none pass. Parent `26e6da4^` has neither `gear-validate.js` nor `scripts/gear-validate.test.js`; if the test is present but implementation absent, `loadBrowserScript("gear-validate.js")` throws before the first `check()` call.

Weak spots: V5/V7b use impossible real metadata for unavailable `mine`; V7b does not test catalog membership before cardinality; V10 does not exercise `updateNozzleChips`; V13 asserts the adjudication but not the `app.js`/`StateCodec` fallout.

