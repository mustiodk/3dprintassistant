# Bridge session — 2026-08-01-003539-010382

## Task

Hostile implementation review of Feedback Diagnostics v2 web and Worker slice only. Compare git diff 7f0fade..87f3293 against docs/superpowers/specs/2026-07-31-feedback-diagnostics-design.md and Tasks 1-7 of docs/superpowers/plans/2026-07-31-feedback-diagnostics-v2-implementation-plan.md. Scope includes feedback contract, AES-GCM storage, D1 store, rate limiting, legacy and Printer Intake compatibility, minimized Discord alerts, owner API and retention, release manifest, RAM-only web recorder, feedback UX, owner analytics UI, privacy notice and LIA. iOS Tasks 8-10 and Cloudflare provisioning are intentionally not implemented and must not be findings unless the web slice makes the handoff unsafe. Verification already green: 51 Node tests, 69 Vitest Worker and D1 tests, release manifest check, validate-data, 18-combo walkthrough, export audit 0 FAIL and 0 warn, Wrangler dry-run, browser checks for form owner UI and privacy. Review exact code for P0 P1 P2 security privacy correctness data minimization schema drift legacy regressions runtime errors and test gaps. Output numbered findings with severity, file and line, failure scenario, narrow fix, then GO, GO-WITH-PATCHES, or NO-GO. Cap is 900 seconds.

## Claude (review)

ExitPlanMode isn't surfacing as an invokable tool in this environment — I've completed the review and written it to the plan file instead.

## Hostile Implementation Review — Feedback Diagnostics v2 (web + Worker slice)

**Scope:** diff `7f0fade..87f3293` vs. the design spec and plan Tasks 1–7. iOS/Task 8 and Cloudflare provisioning/Task 10 excluded per instructions.

Full detail written to `/Users/mustafaozturk-macmini/.claude/plans/hostile-implementation-review-of-optimized-orbit.md`. Summary:

**Findings**

1. **P1 (correctness/data-minimization)** — `functions/api/_lib/feedback-contract.js:95-108`: `capturedAt`/`captureReason`/`entryPoint` have zero type/length/enum validation server-side. A forged-Origin direct request can stuff ~30KB of arbitrary content into `entryPoint`, landing in **plaintext** `diagnostics_json` — contradicts spec §4.2's "every nested field keeps its own smaller bound" and plan Task 1 Step 3's "export closed constants for ... entry points." No `ENTRY_POINTS` constant is even exported. No test covers this.
2. **P1 (privacy)** — `feedback-contract.js` `validateBreadcrumbs` + `feedback-diagnostics.js`: breadcrumb `props` values are bounded by length/type only, never by a closed vocabulary, despite spec §5.6 calling them "allowlisted closed-vocabulary ids" — the exact justification for storing `breadcrumbs_json` unencrypted. Low practical exposure today (current call sites pass canonical ids) but unenforced.
3. **P2** — `diagnostic_completeness = "partial"` (spec §4.2) is computed in `feedback-contract.js:84` but never persisted (no column, never read in `feedback.js`).
4. **P2** — `feedback-diagnostics.js:26` + `app.js:96-97`: "different printer" preference gets mislabeled `kind: 'supported'` with an undefined `printerId`.
5. **P2** — `app.js:110`: `catalog.baselineRevision/overlaySource/contentVersion` never populated; `runtime.engineInitialized` hardcoded `true` regardless of actual state.
6. **P2** — non-`bugReport` categories' "minimal diagnostics" rule is client-only convention, unenforced server-side.
7. **P2** — client truncates breadcrumb `screen` source at 120 chars, server rejects above 80 — latent mismatch, no boundary test.

**Verified solid:** D1-authoritative/Discord-best-effort flow, `notified:false` semantics, custom-printer-text fail-closed exclusion from plaintext diagnostics (structurally enforced via `hasOnlyKeys`, not just client convention), rate-limit-before-parse ordering, collision-retry scoping, admin token timing-safe compare + ciphertext stripping + HTML escaping, AES-GCM AAD binding, legacy/Printer-Intake tee preservation, and Discord payload minimization — all confirmed by direct code read, not just trusting the test names.

**Verdict: GO-WITH-PATCHES.** No P0s — no auth bypass, no key/ciphertext defect, no path that stores promised-encrypted data in plaintext. #1 and #2 should be patched before the Task 10 O0 production gate since they erode the specific "closed-vocabulary, individually-bounded" guarantee the design uses to justify plaintext storage; both are cheap, narrow fixes. #3–#7 are data-quality/spec-drift and can ride in the same review-fix pass or be explicitly deferred. Nothing here makes the iOS Task 8 handoff unsafe — the wire contract's shape is intact.
