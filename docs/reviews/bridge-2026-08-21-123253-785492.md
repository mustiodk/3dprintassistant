# Bridge session — 2026-08-21-123253-785492

## Task

Round-4 review of a storage format about to be frozen forever. You have returned NO-GO three times: 324fbc1 (six MUST-FIX), a789317 (two), b709782 (three). Commit 0e85f60 restructures update() rather than patching it a fourth time. Your round-3 transcript is docs/reviews/bridge-2026-08-21-122117-665790.md.

Read gear-store.js and scripts/gear-store.test.js in FULL and diff 0e85f60 against b709782.

WHAT CHANGED: update() was rewritten. Validation, the write patch and the no-op decision are now all computed from ONE basis - the intended post-image of every (value,label) pair the patch touches. A key is touched if the patch names it on either side; untouched keys carry through from the raw row. Pairs are validated together, normalized together in lockstep, compared POSITIONALLY (not via _sameMap, which treats arrays as sets and would call a->Ay,b->Bee equal to a->Bee,b->Ay), and fields+labels are always written together.

PRIORITY ORDER:

1. VERIFY ALL THREE ROUND-3 MUST-FIX. CLOSED or STILL OPEN with code evidence. Construct probes; do not reason from the comments.

2. THE NEW COMPARISON. _pairKey builds a string with \u0000/\u0001/\u0002 separators. Can two DIFFERENT pair-states collide to the same key, or two identical states differ? Consider: a value or label containing those separator characters, undefined vs empty-string labels, an array value with no label array, and arrays of different lengths reaching that point.

3. EXHAUSTIVE WRITE AUDIT. For every writer - save, update, touch, archive, restore, setActiveGear, setSavePromptDismissed, markCatalogSeen - state exactly which persisted keys it can change, and whether it can change a byte the caller did not ask to change. This is the invariant the last three rounds were about; I want it confirmed or refuted exhaustively.

4. NO-OP AND CLOCK CORRECTNESS. Does updated_at move exactly when content changed? Construct: name-only change, pure value reordering, pure label re-association, adding a key, removing a key (is removal even expressible?), patching a key to its identical value, and patching against a malformed raw row.

5. REGRESSION AND NEW DEFECTS from this diff.

6. TEST DISCRIMINATION. 134 assertions. Sweep the new G28/G29 and name any that pass with the fix reverted.

7. FREEZE VERDICT. Explicit GO or NO-GO. If GO, say so plainly - I need to know when to stop. If NO-GO, list exactly what blocks it and whether each is genuinely format-freezing or merely a quality issue that could ship and be fixed later.

Be adversarial. Do not praise. If a category is clean, say so explicitly.

## Codex (review)

[🟩🟩🟩🟩🟩 100%] NO-GO for freezing `3dpa_gear_v1` at `0e85f60`.

**Must-Fix**
1. `_pairKey()` is not collision-safe, so `update()` can silently drop a real caller patch and keep `updated_at` unchanged. Code: [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:565), compare gate at [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:575), no-op at [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:603).

Probe result: before `material:["a"]`, label `["b\u0002c\u0001d"]`; patch to `material:["a","c"]`, labels `["b","d"]`. Both serialize as `a\u0001b\u0002c\u0001d`; update reports ok, writes nothing, clock does not move. Same class: absent label vs empty-string label, array-with-no-labels vs empty-label array, orphan label plus added field value `"undefined"`, and malformed raw object repaired to string `"[object Object]"`.

This is format-freezing: valid string values/labels can contain the separator characters, so the no-op decision is lossy for conforming data.

**Round-3 Must-Fix Status**
All three prior blockers are CLOSED by direct probes:
- Field change under incompatible existing label now rejects `bad-label`.
- Empty labels patch against malformed raw row does not move `updated_at` or launder the row.
- Labels-only re-association writes fields+labels coherently instead of half-applying.

**Write Audit**
At semantic persisted-key level:
- `save`: can add `gears[id].{name,fields,labels,created_at,updated_at,last_used_at,archived_at}`; does not create caller-supplied top-level junk.
- `update`: can change `name`, `updated_at`, and, when pair-changed, whole `fields` + `labels`; can normalize/drop bytes for touched keys and reserved keys. New comparator bug can also fail to change requested bytes.
- `touch`: changes `last_used_at` only on the row.
- `archive`: changes `archived_at`, `updated_at`.
- `restore`: changes `archived_at`, `updated_at`; redundant restore on an active row moves `updated_at` with no content change. Should-fix.
- `setActiveGear`: changes `settings.active_gear`, `settings.updated_at`; rewrites settings to documented keys and drops unknown settings bytes.
- `setSavePromptDismissed`: changes `settings.save_prompt_dismissed`, `settings.updated_at`; same settings rewrite.
- `markCatalogSeen`: changes `settings.catalog_seen`, `settings.updated_at`; same settings rewrite.

**Clock Matrix**
Clean for ordinary `update()` cases: name-only moves; pure value/pair reordering does not; pure label re-association moves; adding a key moves; identical scalar does not; malformed untouched data is preserved. Removal is expressible only from JS with an own property value `undefined`; `null` is rejected. Removal has collision edge cases through `_pairKey()`.

**Tests**
`node scripts/gear-store.test.js`: 134/134 pass.

New G28/G29 with the `update()` fix reverted to `b709782`: 11/15 still pass. Passing-with-revert assertions: “changing both together accepted”, “empty labels ok”, “no laundering malformed array”, “same-set new labels ok”, “persisted association intact”, “read-back pairing correct”, “labels-only ok”, “pure same pairs no-op”, “real pair change moves clock”, and both G29 assertions. They do not discriminate this diff.

**Should-Fix**
Redundant `restore()` on an active row moves `updated_at`. Settings writes intentionally drop unknown settings keys and move `settings.updated_at` even on no-op setters; acceptable only if that is the frozen settings policy.

**Verdict**
NO-GO. The blocking issue is `_pairKey()` false equality causing real content updates to be treated as no-ops. Replace string-concatenated comparison with structural positional comparison over typed pairs.



