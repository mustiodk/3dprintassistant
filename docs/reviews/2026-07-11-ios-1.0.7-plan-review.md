# iOS 1.0.7 Issue-Fix Plan — Cross-Model Review

**Date:** 2026-07-11

**Controller:** Codex

**Independent reviewer:** Claude via `bridge --mode claude-only`

**Target:** design + executable plan + release/deployment gates

**Implementation reviewed:** none (planning gate only)

## Run record

1. `bridge --health`: PASS (`v0.2.0`, cwd/git/Claude/Codex all available).
2. Initial whole-packet attempt: timed out after 600 seconds with no output;
   rejected as review evidence.
3. Focused Tasks 0–4 pass: exit 0 in 155 seconds; output
   `/tmp/bridge-2026-07-11-103651-255672.md`; verdict CONDITIONAL PASS.
4. Focused Tasks 5–9 pass: exit 0 in 204 seconds; output
   `/tmp/bridge-2026-07-11-104146-719744.md`; verdict CONDITIONAL PASS.

The timeout was resolved using the runbook’s split-scope remedy. No model edited
the repository.

## Tasks 0–4 findings and dispositions

| ID | Finding | Disposition | Patch/evidence |
|---|---|---|---|
| M1 | Weaken full-backup string equality to semantic equality | **Rejected as stated; wording patched** | Existing `WorkshopStore` documentation and `test_importWebBackup_thenExport_isByteIdentical` deliberately pin byte identity. Design/plan now state that exact contract. |
| M2 | Branch setup lacked existence guards | **Accepted** | Plan now fails closed if the reviewed plan branch is missing and reuses/creates implementation branches deterministically. |
| M3 | Task 4 lacked explicit RED execution | **Accepted** | Separate pre-implementation `WorkshopStoreTests` RED command + expected failure added. |
| S1 | `saveStateRevision` reactivity was implicit | **Accepted** | Exact `@State private var saveStateRevision = 0` declaration added. |
| S2 | `workshop_loaded` timing contradicted design | **Accepted** | Both artifacts now require event emission after `router.push(.output)` initiates navigation. |
| S3 | Exact overlay version check would drift | **Accepted** | Gate now requires `content_version >= 2026071005` plus K2 presence. |
| O1 | Feature query test did not assert all six events | **Accepted** | Six explicit SQL membership assertions added. |
| O2a | Unknown filtered-export id test lacked code | **Accepted** | Concrete valid-empty-v1 test and JSON helper added. |
| O2b | Store helper names might not exist | **Rejected with source evidence** | `read`, `Self.render`, and `envelopeExtras` verified in current `WorkshopStore.swift`. |

## Tasks 5–9 findings and dispositions

| ID | Finding | Disposition | Patch/evidence |
|---|---|---|---|
| M1 | `export_clicked` allowlist not updated for iOS properties | **Rejected as a code gap; coverage strengthened** | Current web `EVENT_KEYS.export_clicked` already allows `type`, `printerModel`, `material`, `nozzle`. Plan adds an iOS-shaped regression test for `bambu_bundle` and `text_share`. |
| M2 | `claude-only` is the wrong bridge mode | **Rejected with routing evidence** | Codex is the controller; bridge RUNBOOK §1/§2 requires `claude-only` for the independent model. Plan now comments this explicitly. |
| M3 | PR body file had no creation step | **Accepted** | PR body is now supplied inline; no temp-file dependency. |
| S4 | Merge command absent | **Accepted** | Added mergeability/review gates, explicit `gh pr merge --merge`, and local fast-forward. |
| S5 | Signed production analytics canary was prose-only | **Accepted** | Added secret-safe Node HMAC canary; it logs only success/status, never the secret. Missing local secret is a stop. |
| S6 | Post-upload release evidence lacked commit/push | **Accepted** | Explicit docs evidence commit + `git push origin main` added. |
| S7 | One-profile direct-export path implicit | **Accepted** | Plan now states one profile bypasses the picker and exports the full envelope. |
| O8 | Owner QA checklist required cross-reference | **Accepted** | All nine checks embedded in Task 9. |
| O9 | Run full suite inside final focused analytics commit | **Rejected as redundant** | Task 7 immediately runs the complete iOS suite and UI harness after the focused commit; no risk window reaches push/release. |

## Controller self-check after patches

- No version bump to 1.0.8.
- K2 SE consistently described as already live; local commit is bundled mirror.
- Current `type`/eventDetail/20-blob analytics contract preserved.
- One issue or accepted review finding per implementation commit.
- Web analytics deploy/verify precedes iOS TestFlight reliance.
- Owner TestFlight acceptance remains the App Store submission gate.

## Orca/Prusa final-validation addendum

Owner clarification after the initial GO: the last plan action must validate,
without assumptions, whether OrcaSlicer and PrusaSlicer export behavior is live.
Controller source/history inspection established that public 1.0.4 and the
earlier 1.0.7 TestFlight build expose no export action: the private
`OutputView.exportMenu` has one definition and no call site. The only native
serializer is Bambu JSON; Orca/Prusa currently have generic formatted text
behind that dormant menu and no slicer-specific XCTest.

Focused cross-model review:
`/tmp/bridge-2026-07-11-111248-252445.md` — **GO-WITH-PATCHES**.

| Finding | Disposition |
|---|---|
| Final gate did not mechanically connect the public build to the TestFlight source commit | Accepted: exact TestFlight/accepted source-commit evidence labels, equality check, commit-object verification, and history membership added. |
| Dormant `exportMenu` premise lacked a baseline command | Accepted: Task 0 now proves exactly one definition/no call site before mutation. |
| Manual Release had no action between approval and public validation | Accepted: explicit Release This Version and Ready for Distribution gate added. |
| Orca QA route lacked an exact model | Accepted: Creality K1C (`k1c`) pinned and its brand routing baseline-tested. |
| Source grep could false-negative on an interpolated accessibility identifier | Accepted: exact visible-copy/test grep is load-bearing; accessibility search is supplementary. |
| Bundle identifier was hardcoded rather than source-verified | Accepted and caught a real draft error: corrected from the zero-result `dk.mragile.printassistant` to source-owned `dk.mragile.3DPrintAssistant`. Live lookup reverified version 1.0.4. |

Apple's current official status/release documentation was also checked after
the patch: manual release starts at **Pending Developer Release**, uses
**Release This Version**, and the successful app status is **Ready for
Distribution**. The draft's older “Ready for Sale” wording was corrected.

Final bounded claim: Task 10 may declare only formatted-text sharing live, and
only after public 1.0.7 identity plus independent Orca and Prusa device checks.
Native Orca preset and Prusa INI export remain explicitly not live in 1.0.7.
- Native Orca/Prusa serializers remain explicitly open.

## Final gate

The patched packet receives one focused Claude confirmation pass limited to the
accepted/rejected disposition correctness. Its result is appended below before
the plan is marked ready.

## Final confirmation

Focused confirmation completed with exit 0 in 190 seconds; output
`/tmp/bridge-2026-07-11-104758-732469.md`.

**VERDICT: GO.** Claude verified all 18 dispositions line by line, confirmed the
two evidence-based rejections, and found no contradiction introduced by the
patches. The planning packet is ready for execution; implementation remains
owner-gated.

## Post-review controller amendment — Claude branch and clean-state gate

After the GO, the owner asked to inspect
`origin/claude/cold-start-3dpa-issues-ihs5du`. Controller verification found
that the branch exists only in the web repo. It contains six web commits for
#2-#5, passes the relevant web checks when reviewed in isolation, and its own
`docs/sessions/NEXT-SESSION.md` states that iOS still has the bugs. The plan was
therefore amended to treat the branch as validated reference/reconciliation
input, not as proof of iOS completion and not as a branch to merge wholesale.

The owner also added a completion constraint: no uncommitted dirty state should
remain when the work is done. The plan now carries that as a global constraint
and as Task 10 Step 5, requiring clean `git status --short` output for every
touched repo after intended commits/pushes. This is a controller/process
amendment; no implementation work has started.
