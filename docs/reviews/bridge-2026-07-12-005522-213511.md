# Bridge session — 2026-07-12-005522-213511

## Task

Cross-model review ROUND 9 (confirmation round) of docs/superpowers/specs/2026-07-11-accounts-platform-design.md + docs/superpowers/plans/2026-07-11-accounts-platform-plan.md (3dpa Accounts Platform pre-ratification design spec + gated plan, solo hobbyist dev). Prior reports R1-R8 in docs/reviews/bridge-2026-07-12-*.md; trend 16 -> 15 -> 8 -> 3 -> 2 -> 2 -> 1 -> 2. Both R8 findings patched: a link_intents table + Bearer-authenticated POST /api/v1/auth/link-intent minting a single-use 120s PKCE- and session-bound intent consumed by /start?link_intent=<id> (opaque non-credential id) with replay/expiry/session-invalidation/collision tests scheduled in AP2 and I1/I4; the flags-off byte-for-byte promise is scoped to account surfaces with AP7's unflagged local inventory named as a deliberate additive free feature in §4.5 and acceptance 1. ROUND 9 JOB: confirm no remaining P0/P1/P2 at the altitude of a pre-ratification design spec + gated plan (implementation-level detail belongs to per-gate build reviews; accepted design decisions are settled). If clean, issue GO plainly; otherwise list findings. Verdict: GO / GO-WITH-PATCHES / NO-GO.

## Codex (review)

### Must-fix

1. **P1 — Native link flow account takeover risk.** The callback attaches the provider identity before the app proves possession of the PKCE verifier ([spec §4.3](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-07-11-accounts-platform-design.md:215)). Thus `link_intent` is effectively a bearer credential: someone obtaining it during its 120-second life can authenticate a fresh provider identity, attach it to the victim’s account, then sign in as the victim. Session binding does not authenticate the browser caller. **Fix:** callback stores a pending identity without mutation; the app finalizes linking through a Bearer-authenticated request supplying the PKCE verifier, with same-session validation and atomic consumption. Test stolen-intent use, callback-without-finalization, verifier mismatch, replay, and session invalidation.

### Should-fix

2. **P2 — Flags-off promise remains contradictory in the plan.** The spec correctly scopes byte-for-byte behavior to account surfaces, but the plan still says every web gate with both flags off equals today’s behavior ([plan line 5](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-07-11-accounts-platform-plan.md:5)), despite unflagged AP7 inventory. Apply the same account-surface/AP7 carve-out there.

Optional: none.

Actual-code assertions concerning current routing, Workshop merge behavior, privacy text, and missing iOS auth capabilities remain accurate.

**Verdict: NO-GO.**

