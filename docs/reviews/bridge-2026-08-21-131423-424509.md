# Bridge session — 2026-08-21-131423-424509

## Task

Round-2 review of gear-validate.js. You returned NO-GO on 26e6da4 with two MUST-FIX and two SHOULD-FIX. Commit 0f58e19 applies all four. Your round-1 transcript is docs/reviews/bridge-2026-08-21-130349-452515.md.

Read gear-validate.js and scripts/gear-validate.test.js in FULL and diff 0f58e19 against 26e6da4.

WHAT CHANGED:
- membership now EXEMPTS conditional values via a CONDITIONAL_VALUES table (today only profileMode:'mine'), so it reaches the conditional rule instead of being rejected as an unknown id
- [] on a single-valued field is now reported and left UNSET rather than applied into scalar state
- applyGearToState copies arrays instead of aliasing them
- everything that can throw is computed BEFORE state is touched
- V13 was rewritten to record the reversal; V14/V15/V16 added

PRIORITY ORDER:

1. VERIFY ALL FOUR. CLOSED or STILL OPEN with probes, not by reading comments.
   - For 'mine': probe with realistic getFilters-shaped metadata in BOTH directions, and confirm a genuinely unknown profileMode value is still stale.
   - For []: confirm nothing array-shaped can now reach a single-valued key in resolved by ANY path, and that [] on a MULTI field still survives.

2. THE CONDITIONAL EXEMPTION IS A HOLE BY CONSTRUCTION. It lets a named value bypass membership. Attack it: can a gear pin profileMode:'mine' and have it APPLIED when it should not be? What happens when mineAvailable returns true but the engine genuinely does not offer mine for that pair - is the predicate and the item list ever inconsistent, and which wins? Is 'safe' itself guaranteed present?

3. THE NEW APPLY ORDER. Deps are called after the mutation now. Trace: if setActiveSlicer throws, is state already merged and is that acceptable? Is any dep called twice, skipped, or called in the wrong order relative to the merge? Does collapsePicker still run last? Confirm the single-Object.assign property that stops updateNozzleChips (app.js:1552) wiping a pinned nozzle still holds now that assignment is a key-by-key loop.

4. REGRESSION from this diff, and any NEW defect.

5. TEST DISCRIMINATION. 61 assertions. Name every one that passes with its fix reverted, especially V14/V15/V16 which were written after the code.

6. VERDICT: GO or NO-GO. This file does not freeze a format, so the bar is correctness of user-visible behavior. If the only remaining items are quality, say GO and list them separately.

Be adversarial. Do not praise. If a category is clean, say so explicitly.

## Codex (review)

Sync note: `3dprintassistant` was reported current; parent repos/sync-hold were dirty. I reviewed only this checkout at `0f58e19`.

**Verdict: NO-GO**

**Must-Fix**
1. [gear-validate.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-validate.js:138): `CONDITIONAL_VALUES.profileMode` is a normal object, and `_isConditionalValue()` does not use own-property checks. Unknown values like `toString`, `constructor`, `hasOwnProperty`, and `__proto__` bypass membership and are applied as `ok`.
   
   Probe with `profileMode.items = ['safe','tuned']`:
   `invented -> stale`, but `toString -> ok/resolved.profileMode="toString"`, same for `constructor`, `hasOwnProperty`, `__proto__`.

   Fix with null-prototype maps or `_own(CONDITIONAL_VALUES, key) && _own(CONDITIONAL_VALUES[key], value)`.

**Should-Fix**
1. [gear-validate.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-validate.js:426), [gear-validate.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-validate.js:441): prior dep-exception finding is only partly closed. `getSlicerForPrinter`/`printerRow` are precomputed and caught, but `resetFields`, `setActiveSlicer`, `setExpandedBrand`, and `collapsePicker` can still throw while/after state is touched. Probe: throwing `setActiveSlicer` left state reset+merged and skipped brand/collapse. Real [engine.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/engine.js:979) setter is currently non-throwing, so this is helper-contract risk, not a normal-path app failure.

**Closed**
- `mine` normal path: no-tuning realistic metadata omitting `mine` degrades to `safe`; tuning metadata including `mine` applies `mine`; ordinary unknown `invented` is stale. The prototype-name hole above means this category is not fully closed.
- `[]`: through `inspectGear`, empty arrays cannot reach single-valued keys; probed `surface: []` and `profileMode: []`. Multi `useCase: []` still survives.
- Array aliasing: closed; `applyGearToState` copies arrays.
- Apply order: no duplicate/skipped deps on happy path; observed `getSlicer`, `printerRow`, `reset`, merge, `setSlicer`, `setBrand`, `collapse`; collapse remains last.
- Nozzle wipe: helper’s key loop is synchronous and has no render between material/nozzle assignments. `updateNozzleChips()` only runs during `render()` at [app.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/app.js:1597). There is no `app.js` call site for `applyGearToState` yet, so this is helper-level proof only.

**Conditional Hole**
If `mineAvailable()` returns true while `profileMode.items` omits `mine`, predicate wins and `mine` is applied. With real `Engine.getFilters(state)` for the same pair and a predicate backed by the same personal-tuning source, those should be consistent because [engine.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/engine.js:571) adds `mine` from that predicate condition. The current API does not enforce that consistency.

`safe` is currently always offered by engine metadata, and the fallback path correctly checks membership; if `safe` is absent, probe returned `stale` with `fallback-unavailable`.

**Tests**
`node scripts/gear-validate.test.js`: all 61 pass.

Current 61-check suite run in-memory against `26e6da4`: 54 pass, 7 fail.

Pass with `26e6da4`: V1 all; V2 all; V3 all; V4 all; V5 all; V6 all; V7 all; V7b all; V8 all; V9 all; V10 all; V11 all; V12 all; V13 “reported”; V14 “mine survives when engine offers it”, “unknown profileMode is stale”, “left unset”; V15 “reported”, “not stale”, “multi [] survives”. V16: none.

Fail with `26e6da4`: V13 “not carried into app state”; V14 no-tuning degrade/safe/reason; V15 single `[]` not applied; V16 array copy; V16 throwing getter.



