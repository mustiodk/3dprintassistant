# Bridge session — 2026-07-12-003135-436387

## Task

Cross-model review ROUND 3 (final-gate) of docs/superpowers/specs/2026-07-11-accounts-platform-design.md + docs/superpowers/plans/2026-07-11-accounts-platform-plan.md (3dpa Accounts Platform pre-ratification design spec + gated plan for a solo hobbyist dev). Round 1 report: docs/reviews/bridge-2026-07-12-000932-423960.md. Round 2 report: docs/reviews/bridge-2026-07-12-002253-557468.md. ALL round-2 findings are now patched in the current text: (1) APD13 states the plan is executable end-to-end only under option (a) with a reviewed Phase-2 Pro companion plan required before AP4 under (b)/(c)/(d), and AP7's option-sensitivity is stated; (2) per-field rev maps replace whole-record resolution, stale LWW wording swept (§4.4 step 3, acceptance 3, AP5, I2), disjoint-field fixture added; (3) tombstones carry since_version, acks are explicit post-apply (GET never acks), server returns safe_compact_through, pushing client compacts, retired-device PUT rejected into full pull, tests listed; (4) sign-out switches active namespace to local immediately; (5) refresh_tokens.replaced_by + AEAD-sealed grace_response added; cookie/keyring tests scheduled; (6) deletion batch moves the encrypted Apple token to a revocation_outbox first (retryable, 30-day runbook fallback); (7) every v1 entitlements reference removed (deletion SQL, diagram, G-E, risk row); (8) deletion log moved to KV with rationale; (9) APD5 carries the full bambuinventory field audit keyed on stable material_id + real api.php fixture validation in AP7; (10) AP0 gains SiwA App-ID capability + Services-ID association, I1 gains entitlement + URL-scheme project mutations, single Google client; (11) I5 declares Name/Email/User ID/Other User Content + I6 ASC verification exit item; (12) integration tests pinned to real local D1 via worker.fetch; (13) dark = API-on/UI-off unified + kill-switch GDPR carve-out listed; (14) AP10 checkpoints on a non-deploying remote feature branch, >=7 sessions; (15) export scope enumerates devices + non-secret session metadata. ROUND 3 JOB: verify the round-2 patches are coherent as written; report any remaining or NEW P0/P1/P2 strictly at or above the altitude of a pre-ratification design spec + plan (do not descend to implementation-detail nits that gate-level reviews will catch during the build — those are below this artifact's purpose). Output numbered findings (severity + section + failure scenario + fix) then verdict GO / GO-WITH-PATCHES / NO-GO.

## Codex (review)

No P0 found.

Must-fix

1. **P1 — APD4 / AP5 / AP10-I2 — Per-field merge patch was not propagated.** The plan still specifies per-record revisions and iOS “per-record LWW” ([plan:97](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-07-11-accounts-platform-plan.md:97), [plan:144](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-07-11-accounts-platform-plan.md:144)). Failure: web/iOS can discard disjoint edits or implement incompatible merges; the actual `workshop-store.js` still replaces colliding profiles wholesale. **Fix:** require per-field `revs` in AP5/I2 and add the disjoint-field fixture explicitly to both gates.

2. **P1 — §4.4 / AP4 — Tombstone acknowledgment remains contradictory and retirement enforcement is incomplete.** AP4 advances device acknowledgments on GET/PUT ([plan:90](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-07-11-accounts-platform-plan.md:90)); the spec says GET never acknowledges. Additionally, rejection compares against the “oldest retained tombstone,” which no longer exists after compaction. Failure: premature compaction or a retired replica resurrecting compacted records. **Fix:** separate registration/`last_seen` from explicit post-apply acknowledgment; persist a compaction/rebootstrap watermark and reject retired-device pushes until replacement/full-pull completion.

3. **P1 — APD14 / AP5 — Sign-out patch is reversed in the executable plan.** AP5 says sign-out keeps the account namespace active ([plan:99](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-07-11-accounts-platform-plan.md:99)); APD14 requires immediate activation of `local`. Failure: account data remains visible on a shared signed-out device. **Fix:** change AP5 to switch to `local` immediately and explicitly test signed-out visibility.

4. **P1 — §4.3 / AP9 — Deletion still has two non-executable contracts.** AP9 retains the old best-effort-revoke-before-cascade flow ([plan:131](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-07-11-accounts-platform-plan.md:131)). The spec also places a KV write inside an “atomic D1 batch”; D1 and KV cannot form one transaction. Failure: failed Apple revocation loses its credential, or deletion succeeds while its restore journal is absent, allowing resurrection after Time Travel. **Fix:** write an idempotent KV deletion intent first, atomically enqueue revocation plus delete D1 data, then retry revocation independently; schedule failure/reconciliation tests and correct the retention wording.

5. **P1 — APD5 / AP7 — The “full/lossless” bambuinventory audit is still false against actual code.** Real [`api.php`](/Users/mragile.io/dev/Claude/Projects/bambuinventory/api.php:194) returns fields including `color_code`, `gradient`, and multi-state `order_status`; the ratified schema omits or ambiguously collapses them and changes `defect` from boolean to text. Failure: the promised bridge silently loses inventory state despite passing shape validation. **Fix:** ratify an explicit field-by-field mapping, preservation/defer rules, and a real export→import→export round-trip fixture.

Should-fix

6. **P2 — APD1 / APD10 / ratification scope — The sweep remains incomplete.** APD1 still says D1 stores entitlements, while APD10 forbids the v1 table; headings and plan source still say APD0–APD12 although APD13–15 are ratification decisions. **Fix:** remove the stale entitlement reference and normalize every decision range to APD0–APD15.

7. **P2 — APD7 / AP9 — Export scope is inconsistent.** APD7 includes device-registry and non-secret session metadata; AP9 schedules only users, identities, and sync documents. **Fix:** enumerate the complete APD7 scope in AP9 and its acceptance test.

8. **P2 — APD3 / AP1–AP2 — Claimed cookie/keyring tests are absent.** The spec defines `__Host-` cookies, separate versioned keyrings, and AEAD grace/provider state, but the plan schedules no cookie-attribute or key-rotation assertions. **Fix:** add cookie flags, old-key verify/decrypt, grace-response sealing, and rotation tests to AP1/AP2 exit evidence.

Optional findings: none.

**Verdict: NO-GO.** The design direction is viable, but findings 1–5 still permit data loss, shared-device disclosure, or deletion/restore failure if the plan is executed literally.

