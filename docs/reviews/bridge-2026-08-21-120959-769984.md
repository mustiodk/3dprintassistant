# Bridge session — 2026-08-21-120959-769984

## Task

Round-2 review of the APPLIED FIXES to a storage format that is about to be frozen forever. You returned NO-GO on commit 324fbc1 with six MUST-FIX and two SHOULD-FIX. Commit a789317 applies them. Your round-1 transcript is docs/reviews/bridge-2026-08-21-115951-316568.md.

Read gear-store.js and scripts/gear-store.test.js in FULL, and diff a789317 against 324fbc1.

PRIORITY ORDER:

1. VERIFY EACH FIX. For every one of your six MUST-FIX and two SHOULD-FIX, state CLOSED or STILL OPEN with evidence from the new code. Do not accept a fix on its comment or its commit message - check the code path. In particular:
   - touch() now writes the raw row back. Prove there is no OTHER non-content write path that still re-normalizes: check archive, restore, setActiveGear, markCatalogSeen, setSavePromptDismissed, and every _mutate caller.
   - _validIso now round-trips through Date. Is it airtight? Can any non-UTC-millisecond string still pass? Can a valid string be wrongly rejected?
   - write-path validation: can a non-conforming value or a mis-shaped label still reach storage through ANY path, including update() merging a patch on top of an already-non-conforming stored row?
   - settings now emits exactly four fields. Does that LOSE anything a legitimate same-version peer could have written?

2. REGRESSION. Did the fixes break anything? Specifically: does rejecting non-conforming values on write make any legitimate operation impossible? Does the raw-row touch() path skip a guard that _mutate performed? Is the manual UTF-8 encoder correct for surrogate pairs, lone surrogates, and the BMP boundary - test it against TextEncoder.

3. NEW DEFECTS introduced by this diff.

4. TEST DISCRIMINATION. 96 assertions now. Sweep the NEW ones (G19 through G22) and any you flagged as weak last round (G17 first case, G18 stored-order and tombstone assertions). Name every assertion that would pass with its fix reverted. You have caught vacuous tests in this codebase four times; assume there are more.

5. FORMAT FREEZE VERDICT. Explicit GO or NO-GO for allowing the first real browser write to freeze 3dpa_gear_v1. If NO-GO, say exactly what must change.

Be adversarial. Do not praise. If a category is clean, say so explicitly.

## Codex (review)

NO-GO for freezing `3dpa_gear_v1` at `a789317`.

Local note: `main` is currently `d4478d6`, one commit after `a789317`. I reviewed `a789317` against `324fbc1`; `d4478d6` only changes the UTF-8 fallback/test and closes one issue below.

**Findings**
MUST-FIX: write validation is still bypassable. `save()` rejects bad field-backed values, but `_validateWrite()` skips label keys absent from `fields`, so this persists: `fields:{printer:'x1c'}, labels:{material:{bad:true}}`. Evidence: [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:381) validates labels, [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:393) skips absent fields, and [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:263) keeps remaining labels.

MUST-FIX: `_mutate()` can rewrite already non-conforming rows. `update(id,{name})` skips validation unless `fields` or `labels` is in the patch, then `_mutate()` writes `_toPersist(dto)`. `archive()` and `restore()` also use `_mutate()` with no validation. I confirmed `surface:42` and mismatched `material` labels survive name/archive/restore writes. Evidence: [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:427), [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:472), [gear-store.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/gear-store.js:572).

SHOULD-FIX for `a789317`: manual UTF-8 fallback is not byte-equivalent to `TextEncoder` for lone surrogates. `\uD800` encodes as `ED A0 80` manually but `EF BF BD` via `TextEncoder`. BMP boundaries and valid surrogate pairs are correct. `d4478d6` fixes this by substituting `U+FFFD`.

SHOULD-FIX: null-prototype fix is partial. `_emptyEnv()` and settings staging are fixed, but parsed/fallback `env.gears` / `env.settings` still remain ordinary objects or `{}` in `_readEnv()`.

**Fix Status**
CLOSED: `_validIso()` now requires exact `YYYY-MM-DDTHH:mm:ss.SSSZ`, parses, and round-trips through `toISOString()`. Non-UTC, missing-ms, impossible dates fail. It rejects extended-year ISO strings, but no same-version peer writes those.

STILL OPEN: non-conforming field values. Closed for `save()` and field-patching `update()`, open through name/archive/restore on an existing bad row.

STILL OPEN: label shape. Closed for field-backed labels, open for orphan catalog labels and existing bad labels rewritten through `_mutate()`.

STILL OPEN: different-length label arrays. Closed for new field-backed writes, open when an existing bad row is name-updated, archived, or restored.

CLOSED: `touch()` no longer re-normalizes fields/labels and has the same skew/reserved/not-found/storage guards as `_mutate()`. Regression: it intentionally skips `_toPersist()`’s top-level key filter, so it preserves top-level junk on hostile rows.

CLOSED: settings writes now emit only `active_gear`, `catalog_seen`, `save_prompt_dismissed`, `updated_at`. Spec makes `fields` the extension point; no legitimate same-version peer data is lost.

**Test Discrimination**
Would pass with the relevant fix reverted:
`G17 the value array sorted bytewise`; `G17 the label array followed the SAME permutation`; `G17 values sort independently of any label array`.

`G18 and the stored order stays canonical` checks `get()` normalization, not persisted bytes. `G18 a no-op update does not create a spurious tombstone or lose the row` is broad and would pass with the old reorder-write bug.

`G19 touch reports ok`; `G19 touch moved last_used_at`; `G19 touch left updated_at alone`.

`G20 a conforming write still succeeds`; `G20 a matching-length label array is accepted`; `G20 and it was permuted in lockstep`.

`G21 the documented fields survive`.

`G22 a local offset is not a valid UTC-ms timestamp`; `G22 a missing millisecond field is not valid` both pass with the old regex because the impossible-date row hides them by taking first place.

Current HEAD tests pass, but counts differ: `a789317` has 96 runtime assertions; current `d4478d6` has 97 with `G23`.

**Required Change**
Validate the full post-image for every content write, including name-only update/archive/restore, and reject or quarantine orphan/mis-shaped labels. Keep `touch()` raw for non-content use, but document or test that preserving hostile top-level row junk is intentional. For `a789317` specifically, also include the `d4478d6` lone-surrogate UTF-8 fix before freeze.

