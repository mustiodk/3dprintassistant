# Bridge session — 2026-08-21-103854-167671

## Task

Hostile review of an implementation plan before it is executed. Read docs/superpowers/plans/2026-08-21-train1-web-gear-plan.md in full, plus its two ratified specs: docs/superpowers/specs/2026-08-20-gear-model-v2-spec.md and docs/superpowers/specs/2026-08-20-sync-v1-spec.md, and the binding decision record docs/reviews/2026-08-20-gear-model-owner-decisions.md.

CONTEXT: Tasks 1-3 of this plan are already DONE and pushed (commits a442f1f, 1354bf9, fa305f4, plus gate fixes c434d77, d3b9488, d559bee). Do not review those. Review Tasks 4 through 9, which have not been started, and the plan document as a whole.

This plan builds My Gear on the web. The storage format it creates is a FOREVER commitment: the first real browser write freezes 3dpa_gear_v1 for iCloud sync and for iOS. A format mistake here cannot be taken back.

Attack these specifically:

1. SPEC FIDELITY. Does the plan actually implement the ratified gear model v2 spec, or has it drifted? Check every section of the spec against a task. Name anything in the spec with no task.

2. THE FORMAT. Task 4 defines the envelope. Is it correct and complete against spec sections 2.2 and 2.4? Will it survive the migration table in spec 2.1? Is there any field, state, or edge case that would force a v2 envelope later? This is the highest-stakes question in the review.

3. THE STORE/VALIDATE SPLIT. The plan splits gear-store.js from gear-validate.js so the store provably never imports the engine. Is that split correct, or does it break something the spec needs? Is any logic on the wrong side of the line?

4. THE ORDERING CONTRADICTION the plan self-reports. Spec 2.4 says arrays are normalized to the engine item order at WRITE time, but the store cannot know engine order without importing the engine. The plan resolves this as alphabetical at rest plus engine order at apply time. Is that resolution actually sound? Does it break the spec goal that two devices pinning the same set produce the same stored value?

5. TEST QUALITY. Read every test in Tasks 4, 5 and 8. Flag any assertion that would pass even if the implementation were absent or reverted. This codebase has already shipped two degenerate tests this week, so be specific.

6. MISSING TASKS. What does a working My Gear on web need that no task covers? Consider: the stale repair flow, the review overlay, empty states, the save prompt dismissal, localisation, accessibility, what happens when a gear is applied and the user then changes the printer manually.

7. HOSTILE ENVELOPE. Task 4 claims null-prototype maps and reserved-key dropping. Is the implementation sketch actually safe? Can a crafted envelope still cause prototype pollution, data loss, or a throw that breaks the app?

Report MUST-FIX / SHOULD-FIX / OBSERVATION with file and section references. Be adversarial. Do not praise. If a category is clean, say so explicitly.

## Codex (review)

Verdict: **NO-GO** for Tasks 4–9. `gear-store.js`, `gear-validate.js`, and their tests do not exist yet; this review is against the plan sketches and the real APIs they will call.

**MUST-FIX**
1. **Frozen envelope is wrong.** Task 4 writes in-memory fields `id` and `invalid` into storage via `env.gears[id]` + `_write()` ([plan](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-08-21-train1-web-gear-plan.md:403), [plan](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-08-21-train1-web-gear-plan.md:375)). Spec says the record carries **no `id` field** ([gear spec](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-20-gear-model-v2-spec.md:161)). `invalid` is not in §2.2 either. Separate persisted row shape from in-memory DTO.

2. **Read-side repair violates sync rules.** `_normGear()` rewrites bad/missing timestamps to `_now()` ([plan](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-08-21-train1-web-gear-plan.md:342)). Spec forbids read-side rewrites and says missing/unparseable `created_at` sorts last, not “now” ([gear spec](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-20-gear-model-v2-spec.md:182)). This can manufacture divergent sync writes.

3. **Archive/restore does not bump `updated_at`.** `_setArchived()` only changes `archived_at` ([plan](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-08-21-train1-web-gear-plan.md:435)). Sync spec says `archived_at` is a content edit and `updated_at` moves on content edits ([gear spec](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-20-gear-model-v2-spec.md:339)).

4. **Ordering is not spec-correct.** `_order()` treats `last_used_at || created_at`, so a never-used new gear can outrank a used gear; spec says `last_used_at` nulls sort last, then `created_at`, then bytewise id ([plan](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-08-21-train1-web-gear-plan.md:383), [gear spec](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-20-gear-model-v2-spec.md:173)).

5. **Validator applies unknown keys.** `inspectGear()` resolves every unknown field into `resolved` ([plan](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-08-21-train1-web-gear-plan.md:654)). Spec says unknown keys are preserved at rest and ignored when applying ([gear spec](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-20-gear-model-v2-spec.md:221), [gear spec](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-20-gear-model-v2-spec.md:303)). Current sketch pollutes `state`.

6. **Only four fields are content-validated.** `GEAR_CATALOG_KEY` covers printer/material/nozzle/plate only ([plan](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-08-21-train1-web-gear-plan.md:643)). The other engine enum fields can disappear too; spec says disappeared enum values become `stale`, not applied blindly ([gear spec](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-20-gear-model-v2-spec.md:197)).

7. **Apply calls the real API wrong.** `applyGearToState()` calls `deps.setActiveSlicer(resolved.printer)` ([plan](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-08-21-train1-web-gear-plan.md:704)). Actual app calls `Engine.setActiveSlicer(Engine.getSlicerForPrinter(printer))` ([app.js](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/app.js:555)); `Engine.setActiveSlicer()` expects slicer id, not printer id ([engine.js](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/engine.js:979)).

8. **Apply does not clear previous state.** `Object.assign(state, resolved)` leaves old answers for fields the gear did not pin ([plan](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-08-21-train1-web-gear-plan.md:702)). That breaks “unset fields mean the wizard asks” ([gear spec](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-20-gear-model-v2-spec.md:303)). Reset known `StateCodec.FIELDS` first, then apply resolved fields.

9. **Required product tasks are missing.** No task implements D6’s My Gear list/edit/set-default/delete ([decisions](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/reviews/2026-08-20-gear-model-owner-decisions.md:101)), D9’s Home “New setup” CTA ([decisions](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/reviews/2026-08-20-gear-model-owner-decisions.md:147)), D10’s own **printers** first, not only brands ([decisions](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/reviews/2026-08-20-gear-model-owner-decisions.md:161)), or D11’s catalog-news line ([decisions](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/reviews/2026-08-20-gear-model-owner-decisions.md:176)).

10. **Settings API is incomplete.** Task 6 adds “Don’t offer this again” but Task 4 exposes no setter for `save_prompt_dismissed` ([plan](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-08-21-train1-web-gear-plan.md:793), [plan](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-08-21-train1-web-gear-plan.md:445)). Sync spec treats that field as real settings state ([sync spec](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-20-sync-v1-spec.md:54)).

**SHOULD-FIX**
- G6 is broken by construction: object-literal `__proto__` is not serialized, and the `in` check is against `{}`’s prototype. The test will not prove reserved-key handling ([plan](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-08-21-train1-web-gear-plan.md:203)).
- G9 is degenerate: `>= before` can pass without proving `last_used_at` moved; the plan itself warned to seed a past timestamp ([plan](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-08-21-train1-web-gear-plan.md:105)).
- V6 always passes because `|| g.name === 'G'` makes the fallback assertion vacuous ([plan](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-08-21-train1-web-gear-plan.md:603)).
- `profileMode: "mine"` needs a per gear printer+material predicate, not a global boolean, and must run after cardinality coercion ([plan](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-08-21-train1-web-gear-plan.md:666)).
- Browser global contract should be extended for the new script globals; current expectations only cover `StateCodec`, `WorkshopStore`, and tuning ([browser-globals.test.js](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/scripts/browser-globals.test.js:106)).
- Hostile update paths should avoid `Object.assign({}, fields, patch.fields)`; use null-prototype merge targets so crafted patch keys cannot mutate the temporary object prototype ([plan](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-08-21-train1-web-gear-plan.md:417)).
- Stale repair is contradictory: Task 7 adds a repair string, while the plan declares repair flow out of scope ([plan](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-08-21-train1-web-gear-plan.md:859), [plan](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-08-21-train1-web-gear-plan.md:1058)).

**OBSERVATION**
The store/validate split is the right boundary only in concept: store can enforce shape and required `printer`; validator must receive full filter metadata and a `mineAvailable(printer, material)` predicate. The alphabetical-at-rest ordering can satisfy “same set, same stored value” if the spec is amended and the comparator is explicit bytewise/ASCII, but the current plan cannot silently override ratified §2.4 ([plan](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-08-21-train1-web-gear-plan.md:1073), [gear spec](</Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-20-gear-model-v2-spec.md:220)).

No reviewed category is clean. I did not write files. I only ran read-only inspections plus a Node check proving the G6 fixture omits `__proto__`.



