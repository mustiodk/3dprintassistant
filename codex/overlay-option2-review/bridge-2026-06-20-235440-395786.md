# Bridge session — 2026-06-20-235440-395786

## Task

Hostile review of the iOS printer-overlay 'Option 2' change in this working diff (git diff is the subject). Context: the iOS runtime rejects the WHOLE remote overlay on any overlay-id-vs-bundled-id collision for app builds < 1.0.5 (all-or-nothing), but builds >= 1.0.5 override-MERGE the overlay by id (Part C, iOS af4bbe0). The overlay is delivered to every build >= min_app_version (currently 1.0.3). aries+mega_x graduated INTO the v1.0.5 bundle but are NOT bundled in the v1.0.4 App-Store majority, so they must stay in the overlay to reach v1.0.4 users; v1.0.5 override-merges them.

The change: (1) scripts/validate-ios-printer-overlay.js — collectBaselineUnion now skips baselines >= FIRST_OVERRIDE_MERGE_VERSION ('1.0.5') so the overlay can keep ids bundled only at >=1.0.5; (2) backfilled the 1.0.5 baseline in catalog/ios-bundled-catalog-baselines.json (snapshot of iOS bundled printers.json @ commit 518f781); (3) catalog/ios-printer-overlay-v1.json adds snapmaker brand + snapmaker_2_a350 + creator_5_pro (kept aries/mega_x/voxelab), content_version 2026061402->2026062001, recomputed payload_sha256; (4) runbook Phase 4b reconciled.

Scrutinize HARD, this is a historically-buggy area (an earlier overlay bug shipped 'published but not delivered'):
- Is excluding baselines >= 1.0.5 from the collision union SAFE, or can it let a genuinely-fatal collision through on any still-delivered build? Consider every build class: 1.0.3, 1.0.4, 1.0.5, and a hypothetical future >1.0.5.
- Boundary correctness: is '>= FIRST_OVERRIDE_MERGE_VERSION' the right comparison (vs '>')? Off-by-one?
- Does the still-REQUIRED MARKETING_VERSION-baseline existence check still hold given 1.0.5 is excluded from the union?
- Overlay payload correctness: allowed-fields, manufacturer references resolvable, content_version monotonic + under the cap, payload_sha256 == sha256(stableStringify(payload)).
- Could the new printers (snapmaker_2_a350 / creator_5_pro, the latter enclosure=active_heated, multi_color_systems=['toolchanger'], max_chamber_temp=65) violate any overlay schema/enum constraint?
- Any way this regresses v1.0.4 users (the original incident class)?
Give a GO / NO-GO with concrete findings ranked by severity.

## Codex (review)

GO.

**Must-Fix**
None.

**Should-Fix**
None for current Option 2 delivery.

**Optional**
- [scripts/validate-ios-printer-overlay.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/scripts/validate-ios-printer-overlay.js:167): `brandUnion` now doubles as both “fatal collision ids” and “known manufacturer ids”. Current `min_app_version=1.0.3` is fine, but if a future overlay raises `min_app_version >= 1.0.5`, bundled brand refs disappear from this set and valid printers under existing brands may be falsely rejected. Split collision union from known-brand union later.
- Error text still says collisions are against baselines `>= min_app_version`; after this change it really means pre-override baselines only.

**Checks**
- Boundary is correct: `>= 1.0.5` must be excluded because 1.0.5 itself has Part C override-merge. Using `>` would wrongly collide `aries`/`mega_x`.
- MARKETING_VERSION baseline check still holds independently at [scripts/validate-ios-printer-overlay.js:211](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/scripts/validate-ios-printer-overlay.js:211). It requires both `min_app_version` and current `MARKETING_VERSION` before union collection.
- v1.0.3/v1.0.4 remain protected: validator reports collision-checked baselines `1.0.3, 1.0.4`; `sparkx_i7` still collides, `aries`/`mega_x` do not.
- v1.0.5+ safe by iOS runtime evidence: [PrinterCatalogProvider.swift:214](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant-ios/3DPrintAssistant/Services/PrinterCatalogProvider.swift:214) explicitly allows bundled-id collisions and `mergedArray` override-by-id.
- `1.0.5` baseline matches iOS commit `518f781`: 13 brands, 71 printers, no missing/extra IDs; includes `aries`/`mega_x`, excludes `snapmaker_2_a350`/`creator_5_pro`.
- Overlay hash matches `sha256(stableStringify(payload))`; `content_version 2026062001` is monotonic and under cap.
- New rows fit schema: no unsupported fields; `active_heated`, `max_chamber_temp=65`, and `toolchanger` are accepted by validator/iOS sanitize path. Manufacturer refs resolve.

Verification run: `node scripts/validate-ios-printer-overlay.js`, `node scripts/validate-data.js`, `git diff --check`, plus read-only hash/baseline probes. Unit test file could not run in this read-only sandbox because it creates temp fixtures.

