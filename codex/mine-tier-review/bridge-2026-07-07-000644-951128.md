# Bridge session — 2026-07-07-000644-951128

## Task

Hostile cross-model review of the W3 Mine-tier engine train on branch mine-tier-20260706 (6 web commits 4119625..5538f2e off main 6c9d4a0; tag mine-tier-baseline-20260706). Project context: docs/3dpa-context.md. Spec: docs/specs/IMPL-044-W3W4-mine-tier-and-custom-filaments.md §5. Plan contract: docs/superpowers/plans/2026-07-06-impl-044-w3w4-plan.md Part 2. Gate record: docs/planning/MINE-TIER-GATE-LEDGER.md.

Review the diff: `git diff mine-tier-baseline-20260706..HEAD` (engine.js, app.js, state-codec.js, workshop-tuning.js, locales, scripts/walkthrough-harness.js, scripts/engine-golden-snapshot.js, scripts/fixtures/engine-golden.json, tests).

What the train does: profileMode 'mine' = Safe base + personal deltas injected via new Engine.setPersonalTuning({pairKey, offsets}|null); engine re-clamps offsets to the closed vocabulary bounds; pairKey validated against state at every resolve (stale-injection guard); temp deltas join at env-adjustment points with NEW lower floors (nozzle >= nozzle_temp_min, bed >= max(0, base-10)); fan_delta_pct on exactly fan_min_speed/fan_max_speed/cooling_fan_overhang with NEW 0-100 bounds; retraction delta post-_scaleRetraction pre-retraction_max cap; prov {source:'personal'} on touched values only; conditional getFilters 'mine' item; app.js syncPersonalTuning per render (degrades mine->safe when no tuning); share-encode maps mine->safe sender-side; validateState accepts 'mine'.

Attack surfaces to probe hard:
1. Can ANY non-mine (safe/tuned) output change? (Golden shows 36 original states byte-identical, but look for paths golden does not cover.)
2. Stale-injection: any resolve path that reads _personal WITHOUT the pairKey guard or mode gate? (getAdjustedTemps/getAdvancedFilamentSettings/resolveProfile/exportProfile/exportBambuStudioJSON/formatProfileAsText/getWarnings/calcPrintTime.)
3. Clamp/floor correctness: order of cap-then-floor; can floor > cap invert values? Bed floor max(0, base-10) vs data bed_temp_min; fan bound placement vs legacy plain cooling_fan_min key.
4. Share/persistence: any way pm=mine leaks into a share URL; localStorage restore edge cases; saved Workshop profile with mine restored on a device without tuning.
5. The speed limb: Math.round(sm[f]*(1+d)) materialization — interaction with strengthSlow/TPU/MVS caps/beginner 0.8; prov correctness when caps re-bind after the personal base drop.
6. workshop-tuning acceptedFor date extension; app.js render-order (syncPersonalTuning before getFilters AND resolve everywhere?); setView('configure') rebuild.
7. Anything in the plan Part 2 contract silently skipped or widened (task 7 migration was EXPLICITLY deferred - challenge whether the recorded rationale holds).

Constraints (read-only): edit ONLY your response file; do not modify repo files. Output: numbered findings each with severity (CRITICAL/HIGH/MEDIUM/LOW/OBSERVATION), exact file:line refs against the branch HEAD, a one-line repro/reasoning, and a verdict line GO / GO-WITH-NITS / NO-GO at the end.

## Codex (review)

1. **HIGH / must-fix** — [state-codec.js](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/state-codec.js:85), [app.js](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/app.js:221), [app.js](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/app.js:234), [app.js](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/app.js:1545)  
   Live URL sync maps `profileMode:'mine'` to `pm=safe`, then URL restore wins over localStorage on refresh; repro: select Mine on a tuned device, render writes storage as Mine but address bar as Safe, refresh restores Safe.

2. **HIGH / must-fix** — [engine.js](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/engine.js:3682), [engine.js](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/engine.js:3755), [app.js](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/app.js:1399)  
   Mine retraction delta is missing from text/reference filament export surfaces; repro `x1c|pla_basic +0.4mm`: `resolveProfile.retraction_distance = 1 mm`, Bambu export emits `1`, but `formatProfileAsText` / `exportProfile().filament.retraction_length` still emit raw `0.6 mm`.

3. **MEDIUM / should-fix** — [engine.js](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/engine.js:2517), [engine.js](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/engine.js:2546), [engine.js](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/engine.js:2607), [engine.js](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/engine.js:2617)  
   Speed provenance can claim `personal` when downstream caps erase the personal value change; repro `x1c|abs speed_multiplier_delta -0.1`: Safe outer wall speed `60 mm/s`, Mine outer wall speed also `60 mm/s`, but Mine prov says `source:'personal'`.

4. **OBSERVATION / optional** — [docs/superpowers/plans/2026-07-06-impl-044-w3w4-plan.md](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-07-06-impl-044-w3w4-plan.md:587), [docs/planning/MINE-TIER-GATE-LEDGER.md](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/docs/planning/MINE-TIER-GATE-LEDGER.md:35)  
   Task 7 remedy migration is explicitly deferred, not silent; rationale is defensible only because iOS still has no harvest consumer, but it remains a Part 2 contract variance that must stay tracked.

Verification: `node scripts/engine-golden-snapshot.js --check` passes with `NO DRIFT (39 states)`.

Verdict: **NO-GO**.

