# Bridge session — 2026-08-17-154848-547349

## Task

Plan-gate review of docs/superpowers/plans/2026-08-17-train1-my-gear-setups-plan.md (implements §2 of the ratified docs/superpowers/specs/2026-08-17-next-gen-platform-design.md). Project context: docs/3dpa-context.md. Verify against the ACTUAL code: (1) the bindings — app.js:199/204 state persistence, app.js:468-478 brand row, app.js:742-752 chip loop, workshop-store.js factory pattern, scripts/load-browser-script.js loader, iOS repo ../3dprintassistant-ios AppStatePersistence pattern, HomeView.swift:91, MaterialPickerView.swift:13-25 — are the plan's line references and claimed patterns right? (2) the gear-store.js code in Task 1: bugs, envelope-validation gaps, mutation hazards? (3) task ordering/dependency gaps, missing test coverage, iOS push-gate compliance; (4) the boot rule (persisted session wins, default setup only when restore fails) — any conflict with the actual restore path? (5) spec §2 requirements with no task. Do not re-litigate the ratified spec. Numbered findings P0/P1/P2 with exact locations + concrete fixes; VERDICT: GO or NO-GO.

## Codex (review)

**VERDICT: NO-GO**

1. **P0 / must-fix:** Task 2’s web sample will crash. The plan reassigns `state` in `docs/...plan.md:415` and `:425`, but actual `app.js` declares `const state` at `app.js:66`. It also calls nonexistent `renderAll()` at plan `:417`; actual rerender path is `render()` at `app.js:1580` plus `buildFilters()` / `restoreChipSelections()` where needed.  
Concrete fix: mutate the existing object (`Object.assign(state, applySetupToState(...))` or a mutating helper) and call the real render path.

2. **P1 / must-fix:** Web brand pool code uses the wrong printer field. Plan `:521` says `Engine.getPrinter(p.id)?.brand`; actual data/API use `manufacturer` (`app.js:457`, `app.js:571`, `engine.js:1177`, `data/printers.json:22`).  
Fix: use `Engine.getPrinter(p.id)?.manufacturer`; add a test/smoke with an owned non-primary brand.

3. **P1 / must-fix:** `validateSetup` misses the owned-nozzle rule. Plan code `:346-357` validates printer and filament pool membership, but never checks `setup.nozzle` is in that owned printer’s `nozzles`. That violates spec §2 Layer 1/2: setups are drawn from owned printers + nozzles.  
Fix: return `missing_pool_ref` when `!pp.nozzles.includes(setup.nozzle)`; add TC for printer owned with only `std_0.4` while setup uses `hrd_0.6`.

4. **P1 / must-fix:** iOS boot rule is not implementable as planned. Actual iOS restore returns a Bool (`AppStatePersistence.swift:50-67`) but `ContentView.init()` discards it (`ContentView.swift:53-55`). Task 7 then applies default setup from Home whenever default exists (`plan:573-579`), with no “restore failed” signal.  
Fix: retain/pass `didRestoreAppState`; default setup applies only when restore returned false. Add tests for restored non-default state not being overwritten.

5. **P1 / must-fix:** iOS pool-first picker task omits the printer model picker. Plan Task 9 names `BrandPickerView`, `MaterialPickerView`, and nozzle grep (`plan:607`), but actual model selection lives in `PrinterPickerView.swift:48-63` and loads brand models at `:108-112`.  
Fix: add `PrinterPickerView.swift` to Task 9 and split model groups by owned printer IDs.

6. **P2 / should-fix:** Task 1 store envelope validation is too loose. `_read()` copies raw maps (`plan:236-245`) and `updateSetup()` writes un-normalized patch values (`plan:299-304`). Malformed localStorage can leak bad row shapes into the forever schema.  
Fix: row normalizers for printers/filaments/setups, unique `order`, active-only default, and `updateSetup` coercions matching `saveSetup`.

7. **P2 / should-fix:** Browser-global coverage is missing. Existing `scripts/browser-globals.test.js:16-18` explicitly protects ready browser globals, and `index.html:249-255` is the source of script truth. Task 2 adds `gear-store.js` but no task updates that contract.  
Fix: assert `createGearStore` / `applySetupToState` and whichever browser global `app.js` consumes.

8. **P2 / optional:** The plan’s “analytics caveat = nothing to do” is too casual. Actual dashboard has `top_printers` / `top_materials` (`analytics.html:839-840`, `functions/api/analytics-query.js:99/114`) and selection events fire at `app.js:571` / `:1569`.  
Fix: add a dashboard/docs label noting My Gear can bias those charts after Train 1.

Bindings: the cited anchors mostly exist: `app.js:199/204`, `app.js:468-478`, `app.js:742-752`, `workshop-store.js` factory + module tail, `scripts/load-browser-script.js`, `AppStatePersistence`, `HomeView.swift:91`, and `MaterialPickerView.swift:13-25` are real. The material deviations are the ones above.

No files written.



