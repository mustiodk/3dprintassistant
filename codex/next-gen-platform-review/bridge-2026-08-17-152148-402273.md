# Bridge session — 2026-08-17-152148-402273

## Task

Hostile review of a NEW companion spec: docs/superpowers/specs/2026-08-17-ai-buddy-design.md (commit ea73163, which also synced §4/§8 of the already-GO'd docs/superpowers/specs/2026-08-17-next-gen-platform-design.md — check the sync introduced no contradiction with that spec's GO'd content). Project context: docs/3dpa-context.md. Owner-locked decisions NOT up for re-litigation: three tiers/credits, photos day one, iOS first, v1 voice = explain-only (no alternative numbers), v1 apply-loop = input-side proposal cards, v1.5 = Workshop-tuning value deltas. Review the SPECIFICATION of those decisions: (1) are the five loyalty layers actually enforceable as written, or is any of them wishful prompting the eval cannot police? (2) the proposal-card schema as an injection/abuse surface — model-emitted structured output driving state changes; (3) eval-suite coverage gaps (§6) — name concrete missing case classes; (4) internal contradictions within the companion or against the platform spec (e.g. Layer-4 no-numbers rule vs proposal cards, context budgets vs photos, history-local vs cross-device claims); (5) the §2 claim that input-side apply 'cannot produce an invalid profile by construction' — is that true given the engine's actual behavior (state-codec.js, engine.js getWarnings), or can an applied card land the user in a warned/incompatible state that FEELS invalid? (6) anything the apply-loop needs that the spec omits (undo, card expiry vs stale state, concurrent edits between card render and apply). Numbered findings with severity P0/P1/P2 + exact section + concrete fix; end with VERDICT: GO or NO-GO.

## Codex (review)

Scope: reviewed local `HEAD` `ea73163`. Caveat: repo has 1 unpushed local change, so this is bound to the checked-out commit, not remote truth.

1. **P0 must-fix — false safety claim**
   [companion §2](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-17-ai-buddy-design.md:91) and [platform §4](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-17-next-gen-platform-design.md:239) say input-side apply “cannot produce an invalid profile by construction.” False against code. `StateCodec.validateState()` only validates IDs independently against catalogs/filter IDs, not cross-field compatibility ([state-codec.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/state-codec.js:124)). `getWarnings()` then flags catalog-valid states as physically/warning invalid: plate not on printer, avoid plate, soft nozzle, open printer/high-shrink, etc. ([engine.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/engine.js:2106), [engine.js](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/engine.js:2195)).
   Fix: replace with “cannot directly mutate raw slicer parameters”; require post-apply engine preflight and block or explicit-confirm warning-bearing/incompatible deltas.

2. **P0 must-fix — proposal cards are under-specified as a state-mutation surface**
   [companion §2](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-17-ai-buddy-design.md:104) lets model structured output drive state changes with only `{ key, to, why }`. Missing: schema version, card id, base-state hash, created-at/expiry, max `why` length, display-only escaping rule, warnings delta, allowlisted transition policy, apply idempotency, and rejected-card audit semantics.
   Fix: define `proposal_card_v1` as a deterministic artifact. Apply only if `baseStateHash` matches current state or after refresh; recompute warnings before enablement; treat `why` as inert UI text.

3. **P1 should-fix — server-side validation contradicts “server never runs engine logic”**
   Companion says cards are “validated server-side” against catalogs ([§2](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-17-ai-buddy-design.md:106)); platform says v1 server never runs engine logic, only receives engine outputs as context ([platform §4](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-17-next-gen-platform-design.md:253)).
   Fix: split authority: Worker validates JSON/schema/basic catalog IDs; client engine validates cross-field result before render/apply. Or explicitly move shared engine/data into Worker for card validation.

4. **P1 should-fix — loyalty layers are partly wishful prompting**
   Layers 1 and 3 are prompt posture, not enforceable controls ([§1](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-17-ai-buddy-design.md:31), [§1](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-17-ai-buddy-design.md:49)). Layer 5 overclaims eval as “the enforcement mechanism” ([§1](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-17-ai-buddy-design.md:74)).
   Fix: add an enforceability matrix: deterministic validators for forbidden third-person terms, forbidden alternative parameter numbers, schema validity, clamp exceedance; eval-only sampling for tone/identity.

5. **P1 should-fix — eval §6 misses concrete failure classes**
   [§6](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-17-ai-buddy-design.md:188) lacks: warning-creating proposal cards; stale card after user edits; concurrent apply/double tap; Danish parity; image prompt injection; `why` text injection; context truncation dropping warnings/provenance; provider fallback regression; distinction between allowed hardware numbers like nozzle `0.6` and forbidden slicer parameter alternatives.
   Fix: add fixture classes and pass/fail oracles for each.

6. **P1 should-fix — apply-loop lifecycle omitted**
   The journey has dismiss/apply but no undo, expiry, stale-state handling, or concurrent edit policy ([§2](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-17-ai-buddy-design.md:94)).
   Fix: require one-tap undo from prior state snapshot, TTL/card expiry, stale-card disable with refresh, and apply idempotency.

7. **P2 optional — no-numbers rule needs a precise boundary**
   Layer 4 forbids alternative parameter values ([§1](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-17-ai-buddy-design.md:63)), but v1 cards can legitimately say nozzle `0.6` or environment IDs ([§2](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-17-ai-buddy-design.md:87)).
   Fix: define forbidden “parameter values” as output slicer/profile values, not catalog/hardware enum values.

8. **P2 optional — photo budget is not actually budgeted**
   [§5](/Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-08-17-ai-buddy-design.md:159) budgets text tokens, then says a photo is added. That is not enough for D2 worst-case pricing.
   Fix: specify max image count, dimensions/compression, provider image cost unit, and rejection behavior.

**VERDICT: NO-GO.** The companion is directionally coherent with the platform sync, but the invalid-by-construction claim and proposal-card mutation contract must be fixed before GO. No files written.



