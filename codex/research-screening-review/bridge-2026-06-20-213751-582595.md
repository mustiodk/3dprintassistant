# Bridge session — 2026-06-20-213751-582595

## Task

Hostile cross-model review of the IMPLEMENTED "research-capable printer screening" feature (the actual code, not the plan). Find REAL defects; do not rubber-stamp. Be terse; cite file:line.

To see exactly what changed, run:
  git -C 3dprintassistant diff main...research-capable-screening
and also read the modified ai-om docs (currently uncommitted in the working tree).

CHANGED FILES (3dprintassistant on branch research-capable-screening):
- functions/api/_lib/printer-mention.js — capture: Tier-1 brand expansion + Tier-2 brand-less intent (extractBrandlessIntent, isRunMember, DENY_TERMS, hasIntentTrigger, strict-digit + letter-head rule, deny tokens break runs).
- functions/api/_lib/printer-mention.test.mjs
- functions/api/feedback.js — the tee threads the new intent field.
- functions/api/feedback.intent.test.mjs
- scripts/printer-intake-scout.js — brand-less intent:"unresolved-brand" -> needs-research(researchReason:"resolve-brand", manufacturer:null); mfrLabel/NULL_BRAND_LABEL null-safe collapse key + inQueueCollapses key + candidate filename; suggestId digit-only-id guard (manufacturer-prefix + idWeak + collision check).
- scripts/printer-intake-scout.test.js
CHANGED ai-om docs (uncommitted working tree):
- ai-operating-model/docs/agents/printer-intake-scout.md (manufacturer-null needs-research exception, narrowed incomplete row, digit-id guard note)
- ai-operating-model/docs/agents/printer-addition-assistant.md (brand-resolution mission step, brand-unresolvable stop condition, ledger enum + was-unresolved-brand/brand-unresolvable + brandTokens correctiveSignal)
- ai-operating-model/docs/agents/intake-retrospective.md (brandTokens surfaced-not-diffed; new ownerResolution values flow through)

SPEC/PLAN: 3dprintassistant/docs/superpowers/specs/2026-06-20-research-capable-screening-design.md + .../plans/2026-06-20-research-capable-screening-impl-plan.md

HARD INVARIANT: no LLM in the Scout decision path (deterministic); research lives only in the Printer Addition Assistant.

Check and cite file:line:
1. Capture (printer-mention.js): any Tier-2 false positive that wrongly captures a non-printer (digit + intent + letter-head, not in DENY_TERMS)? Any real brand-less printer request wrongly dropped beyond the documented residual? Is the run-skip / cap / letter-head / deny-break logic correct (no garbage models, no swallowed real models)? Any PII leak into the stored span? Determinism intact?
2. Tee (feedback.js): intent threaded correctly; form lane and missingPrinter unchanged; fail-open intact; additive/back-compat for the Scout and S4.
3. Scout (scout.js): is EVERY manufacturer-null surface null-safe (no crash, no "null|" key, no empty-snake filename)? Is routing order safe (resin/non-FDM declines before the null branch; a resolvable model never hits it)? suggestId digit-guard edge cases (empty snake, manufacturer that snakes empty, new collision)? collapse correctness (null-brand vs real-brand same model do NOT merge; two same null-brand models DO merge)? counts / heuristicCandidates / inQueueCollapses correct? Determinism preserved (no LLM/network)?
4. Contract-vs-code: do the 3 ai-om docs accurately match the codes actual behavior (manufacturer-null only for unresolved-brand; model-only-without-intent stays incomplete; digit-id guard; ledger enum values; brandTokens surfaced-not-diffed)?
5. Any regression to existing S2/S3/S4 behavior or the test suites (49 lib/integration + 53 Scout cases)?
6. Test adequacy: what important case is untested?

Output: VERDICT (GO or GO-WITH-NITS or NO-GO) plus findings grouped CRITICAL/HIGH/MEDIUM/LOW, each citing file:line + the concrete problem + a fix. Do not edit files.

## Codex (review)

VERDICT: NO-GO

**CRITICAL**
None.

**HIGH**
Must-fix: Tier-2 “letter-head” guard is wrong.  
`functions/api/_lib/printer-mention.js:197` uses `/[a-z]/`, so digit-leading fused tokens pass: `please add 5Pro`, `please support iPhone 16Pro`, `please add 16Pro` all capture. Fix to a true head check like `/^[a-z]/` on the normalized first run token, and add tests for fused digit-leading forms.

Must-fix: PII can enter the stored heuristic span.  
`functions/api/_lib/printer-mention.js:110` only rejects long pure-number tokens after tokenization; `:138` / `:191` split `555-123-4567` into short chunks, and `:114` accepts them as model tokens. Verified: `please add Prusa 555-123-4567` stores `Prusa 555 123 4567`; `please add Bob 555-1234` stores `Bob 555 1234`. Add pre-tokenization phone-pattern rejection or preserve delimiter context and reject phone-like digit runs.

**MEDIUM**
Should-fix: platform deny terms do not suppress adjacent phone/device model tails.  
`DENY_TERMS` includes `galaxy`/`mac` at `functions/api/_lib/printer-mention.js:83-84`, but `extractBrandlessIntent` just skips denied tokens at `:193` and restarts on the next token. Verified: `please support Galaxy S24 Ultra` captures `S24 Ultra`; `please support Mac M2` captures `M2`. Split deny classes: platform/auth/material should poison the adjacent candidate; accessory deny can still allow later real printer models like `AMS Creator 5 Pro`.

Should-fix: Tier-1 multi-word brand expansion is incomplete.  
`functions/api/_lib/printer-mention.js:34` has `twotrees`, but tokenization at `:138` splits `Two Trees`; verified `please add Two Trees Sapphire Pro` returns `null`. This is a real brand-present FDM request, not the documented brandless+digitless residual. Add phrase/n-gram brand matching or normalize known multi-word brands before token scanning.

**LOW**
Optional/test adequacy: manufacturer-null `--out` skeleton content is not directly tested.  
The new Scout tests cover report routing and candidate filenames at `scripts/printer-intake-scout.test.js:560-602`, but not an actual written skeleton from `candidateSkeleton()` at `scripts/printer-intake-scout.js:650-720`. Add a brandless `--out` case asserting `manufacturer:null`, `id:null`, and the unresolved-brand risk flag.

Verification: `node --test functions/api/_lib/printer-mention.test.mjs` passed 46/46; `node --test functions/api/feedback.intent.test.mjs` passed 3/3; `node --check` passed for the changed JS files. Scout suite passed through TC17, then the read-only sandbox blocked its temp-file write at TC18, so I could not complete all 53 Scout cases here. No LLM/network path found in Scout decision logic.

