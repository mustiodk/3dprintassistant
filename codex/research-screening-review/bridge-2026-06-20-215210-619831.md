# Bridge session — 2026-06-20-215210-619831

## Task

VALIDATION re-review of the PATCHED "research-capable printer screening" implementation. A prior Codex review (transcript bridge-2026-06-20-213751-582595.md) returned NO-GO; the fixes are now COMMITTED on branch research-capable-screening (commit 1ad3813). Verify each prior finding is ACTUALLY fixed in the committed code, and find any NEW defect. Be terse; cite file:line. Do not rubber-stamp.

Review the COMMITTED branch diff: run  git -C 3dprintassistant diff main...research-capable-screening  (the fixes ARE committed). Also run the tests:  node --test 3dprintassistant/functions/api/_lib/printer-mention.test.mjs  and  node 3dprintassistant/scripts/printer-intake-scout.test.js .

Focus on 3dprintassistant/functions/api/_lib/printer-mention.js. The committed code now has:
- Tier-2 startsWithLetter = /^[a-z]/.test(normTok(run[0]))  (true head check, NOT /[a-z]/).
- isPureDigits + phoneLike(tokens, idx); phoneLike tokens break/skip runs in BOTH extractFromText (Tier-1 following loop) and extractBrandlessIntent.
- DENY_ACCESSORY (break-only) vs DENY_POISON (platform/auth/material/generic); extractBrandlessIntent computes adjPoison from DENY_POISON on the tokens immediately before/after the run and rejects when adjacent.
- normalizeMultiWordBrands() applied at the top of extractFromText (Two Trees / Raise 3D).

For EACH prior finding state RESOLVED or NOT-RESOLVED + the fix location:
1. HIGH letter-head /[a-z]/ -> 5Pro/16Pro/iPhone 16Pro.
2. HIGH PII hyphen-split phone span.
3. MEDIUM platform/material deny not poisoning adjacent tail (Galaxy S24 Ultra / Mac M2).
4. MEDIUM multi-word brand Two Trees.
5. LOW manufacturer-null --out skeleton untested.

Then hunt for NEW defects from the patches:
- does phoneLike wrongly drop a real two-group model, or break Ender 3 V3 / X1 Carbon / Sovol SV08?
- does normalizeMultiWordBrands over-match / corrupt other text?
- does DENY_POISON adjacency reject a REAL brand-less printer (false negative)?
- does /^[a-z]/ drop a real brand-less model that should capture?
- any determinism / PII / fail-open / Tier-1 regression.

Output: VERDICT (GO or GO-WITH-NITS or NO-GO) + a per-finding RESOLVED/NOT table + any NEW findings (severity + file:line + fix). Do not edit files.

## Codex (review)

VERDICT: **NO-GO**

Tests:
`node --test 3dprintassistant/functions/api/_lib/printer-mention.test.mjs` passed: 52/52.
`node 3dprintassistant/scripts/printer-intake-scout.test.js` failed in this read-only sandbox at `scripts/printer-intake-scout.test.js:241` with `EPERM` writing a temp watermark file, so I cannot claim that suite is green here.

| Prior finding | Status | Fix location |
|---|---:|---|
| HIGH letter-head `/[a-z]/` → `5Pro` / `16Pro` / `iPhone 16Pro` | RESOLVED | `functions/api/_lib/printer-mention.js:227`; tests `functions/api/_lib/printer-mention.test.mjs:247` |
| HIGH PII hyphen-split phone span | RESOLVED | `functions/api/_lib/printer-mention.js:128`, `functions/api/_lib/printer-mention.js:179`, `functions/api/_lib/printer-mention.js:223`; tests `functions/api/_lib/printer-mention.test.mjs:252` |
| MEDIUM platform/material deny not poisoning adjacent tail | RESOLVED for stated cases | `functions/api/_lib/printer-mention.js:91`, `functions/api/_lib/printer-mention.js:233`; tests `functions/api/_lib/printer-mention.test.mjs:262` |
| MEDIUM multi-word brand Two Trees | RESOLVED, but introduced new bug below | `functions/api/_lib/printer-mention.js:143`, `functions/api/_lib/printer-mention.js:168`; test `functions/api/_lib/printer-mention.test.mjs:271` |
| LOW manufacturer-null `--out` skeleton untested | RESOLVED in committed tests | `scripts/printer-intake-scout.test.js:637`; null-safe naming at `scripts/printer-intake-scout.js:144`, `scripts/printer-intake-scout.js:939` |

**Must-Fix**
HIGH: fused platform tokens still Tier-2 capture. `DENY_POISON` is whole-token only, so common spellings like `iPhone16Pro`, `GalaxyS24 Ultra`, `Pixel8 Pro`, and `MacBook M2` pass through as printer models. Runtime probe reproduced this. Fix with normalized prefix/platform detection before `isRunMember` accepts the token, not only adjacent whole-token poisoning.  
Refs: `functions/api/_lib/printer-mention.js:91`, `functions/api/_lib/printer-mention.js:207`, `functions/api/_lib/printer-mention.js:233`.

**Should-Fix**
MEDIUM: `normalizeMultiWordBrands()` over-matches ordinary prose. `I saw two trees by the road` returns `{ brand:"TwoTrees", model:"", span:"TwoTrees" }`, which would tee non-printer feedback into intake. Fix by making multi-word brand recognition context-sensitive, e.g. require a following model token or reject multi-word brand-only hits.  
Refs: `functions/api/_lib/printer-mention.js:143`, `functions/api/_lib/printer-mention.js:188`.

**Optional**
`phoneLike()` did not break the named real models I checked: `Ender 3 V3`, `X1 Carbon`, `Sovol SV08`, `Snapmaker 2`, `Creator 5 Pro`. It will reject adjacent pure-number runs like `Foo 12 34`; I did not find a concrete common FDM model that needs that shape.

