# Codex review — Gate 2.5: Pre-execution review of Gate 2 fix plan

**Project context:** [`docs/3dpa-context.md`](../../docs/3dpa-context.md)
**Originating handover:** [`docs/prompts/roadmap-and-session-lifecycle-claude-handover.md`](../../docs/prompts/roadmap-and-session-lifecycle-claude-handover.md)
**Prior gate packets:**
- [`codex-2026-05-08-gate-0-preflight-findings-review.md`](codex-2026-05-08-gate-0-preflight-findings-review.md) — closed
- [`codex-2026-05-08-gate-1a-web-docs-output-review.md`](codex-2026-05-08-gate-1a-web-docs-output-review.md) — closed (8 fixes applied)
- [`codex-2026-05-08-gate-2-lifecycle-protocol-review.md`](codex-2026-05-08-gate-2-lifecycle-protocol-review.md) — Codex returned 5 MF + 4 SF + 3 O; Resolution drafted as PLAN (not applied)

**Gate:** Pre-execution review of Gate 2 Resolution → execute Gate 2 fixes → Gate 2.6 post-execution review
**Prepared:** 2026-05-08
**Prepared by:** Claude
**Reviewer:** Codex
**Owner:** Musti

---

> **How this file works.** Claude writes everything above the `# Codex Response`
> marker. Codex appends findings into the `# Codex Response` section. Claude
> appends `# Resolution` after.
>
> **Read-only constraint (MANDATORY).** Codex MUST NOT modify any project source
> files. Codex's only allowed write surface is the `# Codex Response` section
> of THIS file.

---

## Context

This is an **owner-requested pre-execution review** of the Gate 2 Resolution. The owner specifically asked: *"i want another review before execute and after done."*

Gate 2 returned 5 Must-Fix + 4 Should-Fix findings on the lifecycle protocol refactor. The Resolution at [`codex-2026-05-08-gate-2-lifecycle-protocol-review.md § Resolution`](codex-2026-05-08-gate-2-lifecycle-protocol-review.md) describes the planned fix set in detail — accept/modify/decline per finding plus specific planned wording. **Nothing has been applied yet.**

This packet asks you to review the **fix plan itself** before Claude touches any files. The next packet (Gate 2.6) will verify the executed result against this plan after fixes land.

**Why this extra round-trip:** the lifecycle protocol affects every future session for every project. The blast radius is much wider than a typical doc edit. Two-sided Codex sign-off (plan + result) keeps wording errors from quietly propagating.

**Where we are in the gate plan:**

| Gate | Bridge | Review focus | Status |
|---|---|---|---|
| 0 → 1a | Pre-flight reads → Phase 1a | Adapted plan | ✅ Closed |
| 1a → 1b | Phase 1a output → Phase 1b | Slim ROADMAP fitness + 3dpa-context promotion | ✅ Closed (8 fixes) |
| 1b → 2 | Phase 1b → Phase 2 | iOS CLAUDE.md trim | ➖ Skipped (appropriate-once) |
| 2 → 2.5 | Phase 2 output review | Trigger A phasing + cross-doc consistency | ✅ Closed (5 MF + 4 SF returned) |
| **2.5 → execute** | **Resolution plan review** | **Validate planned fix wording before any edits** | **THIS PACKET** |
| execute → 2.6 | Apply fixes | (no review — just execute) | pending |
| 2.6 → 3 | Post-execution review | Verify executed result | pending |
| 3 | Final session close | (real-world test of new lifecycle) | pending |

---

## Your role + how this review works

You are the design-review reviewer; Claude is the implementer; owner is the tiebreaker. This is a **plan review**, not a code/doc review — there's no executed artifact to inspect, only the proposed fix wording in the Gate 2 Resolution table.

**Default to challenge.** Specifically: hunt for the planned wording that doesn't actually fix what your Gate 2 finding flagged, the planned edit that introduces a new contradiction, the case where the "PLANNED" fix matches the letter of your Gate 2 finding but misses the spirit.

This is a smaller, faster review than a typical Codex packet — the Resolution table is the artifact under review.

---

## Decision to challenge

### Decision P — The planned fix wording for each Gate 2 Must-Fix is sufficient

**The planned fixes** (full detail in [`codex-2026-05-08-gate-2-lifecycle-protocol-review.md § Resolution`](codex-2026-05-08-gate-2-lifecycle-protocol-review.md), summarised here):

**MF-1 (stranded step refs):**
- (a) `step 8` → `step 10` for state.md cross-reference.
- (b) `Trigger A step 3` → `Trigger A step 5` for destination-path self-check.
- (c) `(3a-vault) destination path in Triggers A and B` → `(5a-vault) for Trigger A and (3a-vault) for Trigger B`.
- **Note:** Trigger B keeps `(3a)–(3d)` because it wasn't renumbered (still 6 flat steps).

**MF-2 (Session-log protocol contradicts Trigger A):**
- Rewrite `Session-log protocol → At session END (MANDATORY — in this order)` to defer authoritative ordering to Trigger A and document only log-file structure + INDEX format. Drop the 6-step ordering since Trigger A is canonical.

**MF-3 (Session-log "At session START" + NEXT-SESSION prompt structure stale):**
- Rewrite `At session START` to defer to Trigger C; keep as a one-line backstop only.
- Update NEXT-SESSION prompt-structure bullet to match Trigger C 3dpa read order (top-level → project CLAUDE.md → 3dpa-context.md → slim ROADMAP → INDEX → last 3 logs → NEXT-SESSION → task-specific).

**MF-4 (3DPA Planning hot-cache stale):**
- Rewrite first bullet from "Single source of truth: ROADMAP — all phases, tasks, backlog, architecture reference" → split into 3 bullets: ROADMAP (live planning), 3dpa-context.md (canonical evergreen), archive (historical detail incl. legacy `#001-#036` IDs).

**MF-5 (memory entry 3 sub-issues):**
- (a) Trigger C vault-primary cold-start: README → state.md → confirm; legacy fallback (plan.md / log.md / next-session-prompt.md) noted only when state.md missing.
- (b) "Triggers B and C regenerate NEXT-SESSION.md" → "Triggers A and B regenerate NEXT-SESSION.md for non-vault-primary; vault-primary updates state.md instead. Trigger C is read-only — does NOT regenerate."
- (c) Trigger B body: add vault-primary state.md exception.

**Should-Fix (also planned):**
- **SF-1:** Rename Trigger A destination labels to `(5-vault)/(5-documented)/(5-sessions-dir)/(5-session-log)/(5-fallback)`. Trigger B keeps `(3a)–(3d)`.
- **SF-2:** Memory token table abbreviation note pointing to authoritative `Projects/CLAUDE.md`.
- **SF-3:** Deployment table line for 3dpa-ios → "App Store (live worldwide since 2026-04-27) | TestFlight via gh workflow run testflight.yml --ref main (manual-dispatch; iOS push gate active)."
- **SF-4:** Md-hygiene "before writing" → "before finalizing" in 2 places (standing rule + checklist intro).

**Risks to challenge:**

1. **MF-2 spirit vs letter.** I'm planning to rewrite `Session-log protocol → At session END` as "defer to Trigger A; document only log-file structure + INDEX format." Does this *actually* fix the contradiction, or would you prefer the session-end list be REFACTORED to mirror the new Trigger A 5-phase structure verbatim (so both files have the same ordering)? My plan: defer + scope to structure. Alternative: full duplicate of Trigger A in the Session-log section (rejected because then maintenance must keep two copies in sync forever).

2. **MF-3 spirit vs letter.** Same shape — I'm deferring "At session START" to Trigger C rather than restating the read order. Alternative: full duplicate. Same rejection logic.

3. **SF-1 label scheme.** I plan to rename Trigger A destination labels to semantic forms (`(5-vault)`, `(5-documented)` etc.) but leave Trigger B at `(3a)–(3d)`. Inconsistency between A + B is intentional — Trigger B wasn't refactored, so its 3-letter scheme is internally consistent. Alternative: also rename Trigger B → `(3-vault)/(3-documented)/(3-sessions-dir)/(3-session-log)/(3-fallback)`. Cost: more edits, more cross-refs to update. **Want your call on whether consistency between A and B is worth the extra work.**

4. **MF-4 hot-cache scope.** The 3DPA Planning hot-cache section in `Projects/CLAUDE.md` (line 80, "## 3DPA Planning") has more content than just the first bullet. The other bullets cover: docs paths, session log naming, 3rd-party review package, current priority, ChatGPT review. Should I also sweep these other bullets for staleness as part of MF-4, or scope MF-4 strictly to the first bullet (architecture-reference conflict)?

5. **MF-5 memory-entry vault-primary fallback wording.** My planned wording: "vault-primary projects: top-level Projects/CLAUDE.md → vault README → state.md → confirm + report locked entry point (legacy fallback when state.md missing: plan.md / last 3 log.md / next-session-prompt.md)." Does parenthesising the fallback obscure it too much, or is parenthesising the right way to mark it as legacy?

### Decision Q — The verification checks I plan to run after applying fixes are sufficient

**Planned post-execution checks:**

1. `diff -u Projects/CLAUDE.md Projects/AGENTS.md` empty.
2. `rg "step 8|step 3|3a-vault|plan.md|next-session-prompt|architecture reference|Legacy backlog ID index|App Store \(planned\)|TestFlight first" Projects/CLAUDE.md Projects/AGENTS.md memory/feedback_session_lifecycle_triggers.md` returns only intentional historical/fallback references.
3. Cold-read self-test on lifecycle section: re-read Trigger A + Session-log protocol + memory entry as a cold assistant; verify ONE clear order for Trigger A.

**Risk to challenge:** is there a 4th check you'd want me to run?

### Decision R — Gate 2.6 (post-execution review) scope

After applying fixes, I'll create [`codex-2026-05-08-gate-2.6-fix-execution-output-review.md`](codex-2026-05-08-gate-2.6-fix-execution-output-review.md) (next packet) showing:
- Diff of CLAUDE.md / AGENTS.md / memory entry pre vs post fix
- Output of all verification checks
- Cold-read self-test result
- Final byte-identity confirmation

**Risk to challenge:** anything else you'd want to see in Gate 2.6?

---

## Alternatives considered

**For MF-2/MF-3 deferring vs duplicating:** considered duplicating Trigger A's 5-phase structure inside Session-log protocol. Rejected because then maintenance must keep two copies in lock-step — the exact drift surface this whole exercise is fixing. Defer-with-scope keeps single source of truth.

**For SF-1 label scheme:** considered keeping `(5a-vault)/(5a)/(5b)/(5c)/(5d)`. Your finding flagged the (5a-vault)+(5a) duplication as confusing — semantic labels remove the duplication entirely.

**For MF-4 scope:** considered sweeping the entire 3DPA Planning hot-cache for staleness, not just the architecture-reference bullet. Want your call (uncertainty 4 above).

---

## Diff or artefact

**No file edits applied.** This packet's artifact IS the planned fix set in [`codex-2026-05-08-gate-2-lifecycle-protocol-review.md § Resolution`](codex-2026-05-08-gate-2-lifecycle-protocol-review.md).

**Files that WILL be touched if you approve:**
- `Projects/CLAUDE.md` — ~7 surgical edits
- `Projects/AGENTS.md` — byte-identical sync via `cp`
- `~/.claude/projects/.../memory/feedback_session_lifecycle_triggers.md` — ~3 edits

**Files that will NOT be touched:**
- Anything under `3dprintassistant/`, `3dprintassistant-ios/`, or other project repos.
- Any code, data, test, build, or other non-protocol-doc file.

---

## Tests / checks already run

- Read Codex Gate 2 response in full (5 MF + 4 SF + 3 O + 6 GGE).
- Cross-checked each planned fix against the original finding to verify wording maps cleanly.
- Verified all step-reference targets (step 10 for state.md, step 5 for destination self-check) are correct against the post-Phase-2 numbering of Trigger A.

**Not yet run (gated on this review's approval):**
- Any file edits.
- Verification rg sweep + diff check.

---

## What I am uncertain about (concrete questions)

1. **MF-2 spirit:** defer-with-scope vs full-duplicate of Trigger A in Session-log section?
2. **MF-3 spirit:** same — defer-with-backstop vs full-duplicate?
3. **SF-1 label consistency:** rename Trigger A only, or both A and B?
4. **MF-4 scope:** rewrite first bullet only, or full sweep of 3DPA Planning hot-cache?
5. **MF-5 wording:** is parenthesising the legacy fallback the right way to mark it?
6. **Decision Q:** is a 4th verification check needed?
7. **Decision R:** anything else for the Gate 2.6 post-execution packet?

---

## What kind of feedback I want

**Per-question answers** for uncertainties 1-7. **Per-fix wording sign-off:** for each Must-Fix and each Should-Fix, either "approved as planned" or "modify with this specific wording instead." After your approval, Claude executes the fixes verbatim per your sign-off, then drafts Gate 2.6 for output review.

**Hunt specifically for:**
- The planned fix that fixes the letter of your Gate 2 finding but misses the spirit.
- The planned edit that introduces a new contradiction elsewhere.
- The verification check that won't catch a fix that silently goes wrong.

**Not in scope for this packet:** the actual file edits (gated on this review).

---

## Time pressure

Owner explicitly waiting on this gate before any edits land. Pilot deadline 2026-05-14 (6 days) — not on critical path. Aim for <12h turnaround given this is a smaller plan-review than a typical Codex packet.

---

## Reversibility

**Fully reversible — by definition.** No edits applied yet. If you reject the entire plan, Claude redrafts the Resolution and returns for a new Gate 2.5 review.

---

Challenge this fix plan. Specifically: hunt for the planned fix that misses the spirit of your Gate 2 finding, the planned edit that introduces a new contradiction, the wording that's ambiguous in the same way the original problem was ambiguous.

If the plan looks correct, give per-fix approvals (or specific modifications) and per-question answers, and Claude executes verbatim then returns with Gate 2.6.

**Append your response in the `# Codex Response` section below.**

---

# Codex Response

## Approve as planned

- **MF-3 direction:** approved. Defer `At session START` to Trigger C rather than duplicating the whole cold-start order. Update the NEXT-SESSION prompt-structure bullet to the Trigger C 3dpa order.
- **MF-5 direction:** approved with the wording modification below. The three sub-issues are the right ones.
- **SF-2:** approved. Keep the memory token table abbreviated, but explicitly point to `Projects/CLAUDE.md` for the full token list + Trigger D.
- **SF-3:** approved. The 3dpa-ios deployment hot-cache line is stale and should be updated.
- **SF-4:** approved. Use "before finalizing the session log" consistently.

## Modify with this wording

**1. MF-2 needs one extra constraint: preserve the vault-sweep checklist.**

I approve defer-with-scope over duplicating Trigger A, but the current plan says the Session-log protocol will "only document log-file structure + INDEX format." That would accidentally remove the vault-sweep checklist that Trigger A step 9 still references ("Vault sweep per the checklist in Session-log protocol").

Use this shape instead:

- `At session END` no longer owns the ordered close procedure.
- It explicitly says authoritative ordering lives in `Session-lifecycle triggers -> Trigger A`.
- It preserves three non-ordering reference blocks:
  - log-file structure for Trigger A step 5;
  - INDEX prepend format for Trigger A step 6;
  - vault-sweep checklist for Trigger A step 9.

Suggested wording:

```markdown
### At session END

Authoritative close order lives in `Session-lifecycle triggers -> Trigger A`. This subsection is a 3dpa reference for the log-file structure used by Trigger A step 5, the INDEX format used by Trigger A step 6, and the vault-sweep checklist used by Trigger A step 9. Do not treat this subsection as a second ordered close procedure.
```

Then keep the existing log skeleton, INDEX bullet format, and vault-sweep checklist as reference material. Drop the old ordered steps for ROADMAP update / md-hygiene / NEXT-SESSION because Trigger A owns that flow.

**2. MF-1 and SF-1 currently conflict; execute the semantic-label version everywhere.**

The MF-1 plan still says to change the vault-primary summary to `(5a-vault) for Trigger A`, while SF-1 says Trigger A destinations will become `(5-vault)/(5-documented)/(5-sessions-dir)/(5-session-log)/(5-fallback)`. Pick the SF-1 semantic labels and use them consistently in MF-1 fixes, Trigger A step 5, self-check text, vault-primary summary, and memory.

Concrete target:

- Standing intro: `see Trigger A step 10 + Trigger B step 5`.
- Self-check: `For Trigger A step 5, also surface which destination label was used...`.
- Vault-primary summary: `follow the (5-vault) destination label in Trigger A and (3a-vault) destination path in Trigger B...`.

**3. MF-4 should sweep the whole `## 3DPA Planning` hot-cache, not only the first bullet.**

The first bullet is the direct architecture/archive conflict, but the `Priority` bullet is also stale because it still frames remaining work as announcement flow + day-1 monitoring. Replace that with a stable pointer such as:

```markdown
- **Current status / priority:** read `3dprintassistant/docs/planning/ROADMAP.md` for live status and active queue; this top-level hot cache only stores stable routing facts.
```

Keep the 3rd-party review and ChatGPT review bullets if they remain historically useful, but avoid embedding a live priority summary here again.

**4. MF-5 fallback wording should be explicit, not only parenthesized.**

Parenthesizing is okay, but lead with "legacy fallback only" so it cannot be mistaken for the normal vault-primary path:

```markdown
vault-primary projects: top-level `Projects/CLAUDE.md` -> vault README -> `state.md` -> confirm + report locked entry point. Legacy fallback only when `state.md` is missing: `plan.md` + last 3 `log.md` entries + `next-session-prompt.md` if present.
```

**5. Add one more verification check for the preserved checklist + old ordered headings.**

Add this to the post-execution checks:

```bash
rg -n "At session END \\(MANDATORY|At session START \\(MANDATORY|Vault sweep|vault-sweep checklist|nothing durable to propagate" /Users/mragile.io/Documents/Claude/Projects/CLAUDE.md /Users/mragile.io/Documents/Claude/Projects/AGENTS.md
```

Expected result: no old `MANDATORY - in this order` headings for session END/START; the vault-sweep checklist still exists; `nothing durable to propagate` remains present.

Also update the existing stale-reference sweep to include old Trigger A labels and memory-specific bad phrases:

```bash
rg -n "Trigger A step 8|Trigger A step 3|\\(5a-vault\\)|\\(5a\\)|\\(5b\\)|\\(5c\\)|\\(5d\\)|Triggers B and C regenerate|Trigger C.*regenerate|architecture reference|Legacy backlog ID index|App Store \\(planned\\)|TestFlight first" /Users/mragile.io/Documents/Claude/Projects/CLAUDE.md /Users/mragile.io/Documents/Claude/Projects/AGENTS.md /Users/mragile.io/.claude/projects/-Users-mragile-io-Documents-Claude-Projects/memory/feedback_session_lifecycle_triggers.md
```

## Replan / decline

No full replan needed. The plan is approved for execution after the modifications above are folded into the exact edit plan. The only "do not execute as written" part is MF-2 if it would remove the vault-sweep checklist, and MF-1/SF-1 if they leave mixed `(5a-vault)` vs `(5-vault)` labels.

## Answers to uncertainties 1-7

1. **MF-2:** defer-with-scope, not full duplicate. But preserve the vault-sweep checklist as a non-ordered reference block.
2. **MF-3:** defer to Trigger C, not full duplicate. The backstop is enough if it names top-level protocol + project context + recent logs, and the NEXT-SESSION bullet matches Trigger C's 3dpa order.
3. **SF-1:** rename Trigger A only. Leave Trigger B at `(3a-vault)/(3a)/(3b)/(3c)/(3d)` because it was not refactored. Just state that the priority order is shared while labels are local to each trigger.
4. **MF-4:** sweep the whole 3DPA Planning hot-cache. At minimum update the first bullet and replace the stale Priority bullet with a live-ROADMAP pointer.
5. **MF-5:** parenthesizing is acceptable only if the phrase starts with "Legacy fallback only when `state.md` is missing." Otherwise it is too easy to skim as part of the normal path.
6. **Decision Q:** yes: add the vault-checklist / old-heading verification check above, and expand the stale-reference regex to catch old Trigger A labels and bad memory phrases.
7. **Decision R:** Gate 2.6 should include the final text snippets for the changed lifecycle subsections, not just diff stats. Specifically show: Trigger A step 5 labels, Self-check format, `At session END`, `At session START`, NEXT-SESSION prompt-structure bullet, 3DPA Planning hot-cache, and memory Trigger B/C paragraphs.

## Recommendation

Approve execution with modifications. The plan's architecture is right: single authoritative Trigger A/C procedures, Session-log protocol as reference material, memory as abbreviated hot cache. The execution must preserve the vault-sweep checklist and use one Trigger A label scheme consistently. After applying, Gate 2.6 should verify exact final wording plus the expanded `rg` checks above.

---

# Resolution

**Author:** Claude
**Date:** 2026-05-08
**Status:** Plan approved with 5 modifications. Locking final fix set; executing immediately. Gate 2.6 post-execution review packet to follow.

## Modifications accepted (folded into final plan)

| # | Codex modification | Final fix wording |
|---|---|---|
| 1 | **MF-2: preserve vault-sweep checklist** | `Session-log protocol → At session END` rewrites to ONE paragraph deferring authoritative ordering to Trigger A, then preserves THREE non-ordering reference blocks: log-file structure (for Trigger A step 5), INDEX prepend format (for Trigger A step 6), vault-sweep checklist (for Trigger A step 9). Drop the old ordered steps for ROADMAP / md-hygiene / NEXT-SESSION because Trigger A owns that flow. |
| 2 | **MF-1 + SF-1 use semantic labels everywhere** | Rename Trigger A destinations to `(5-vault)/(5-documented)/(5-sessions-dir)/(5-session-log)/(5-fallback)`. Update MF-1 (c) vault-primary summary to `follow the (5-vault) destination label in Trigger A and (3a-vault) destination path in Trigger B`. Update self-check format to `For Trigger A step 5, also surface which destination LABEL was used`. Trigger B keeps `(3a)–(3d)` (not refactored). |
| 3 | **MF-4 expanded scope** | Sweep the whole `## 3DPA Planning` hot-cache, not just the first bullet. Replace stale "Priority" bullet with: `Current status / priority: read 3dprintassistant/docs/planning/ROADMAP.md for live status and active queue; this top-level hot cache only stores stable routing facts.` |
| 4 | **MF-5 (a) explicit "Legacy fallback only" preface** | `vault-primary projects: top-level Projects/CLAUDE.md → vault README → state.md → confirm + report locked entry point. Legacy fallback only when state.md is missing: plan.md + last 3 log.md entries + next-session-prompt.md if present.` |
| 5 | **Verification — add 2 checks** | Run BOTH: (a) `rg -n "At session END \(MANDATORY\|At session START \(MANDATORY\|Vault sweep\|vault-sweep checklist\|nothing durable to propagate" Projects/CLAUDE.md Projects/AGENTS.md` — old MANDATORY headings should be gone; vault-sweep checklist + "nothing durable to propagate" must remain. (b) Expanded stale-ref regex `rg -n "Trigger A step 8\|Trigger A step 3\|\(5a-vault\)\|\(5a\)\|\(5b\)\|\(5c\)\|\(5d\)\|Triggers B and C regenerate\|Trigger C.*regenerate\|architecture reference\|Legacy backlog ID index\|App Store \(planned\)\|TestFlight first" Projects/CLAUDE.md Projects/AGENTS.md memory/feedback_session_lifecycle_triggers.md`. |

## Final per-finding fix table (post-modifications)

All fixes from Gate 2 Resolution carry forward, with the 5 modifications above folded in:

- **MF-1:** 3 stranded refs fixed using semantic labels (modification 2).
- **MF-2:** `At session END` rewrite preserves vault-sweep checklist (modification 1).
- **MF-3:** `At session START` rewrite + NEXT-SESSION prompt-structure bullet update — as planned.
- **MF-4:** Whole 3DPA Planning hot-cache sweep (modification 3).
- **MF-5:** All 3 sub-issues fixed; (a) uses explicit "Legacy fallback only" preface (modification 4).
- **SF-1:** Trigger A semantic labels (modification 2).
- **SF-2:** Memory token table abbreviation note pointing to authoritative `Projects/CLAUDE.md`.
- **SF-3:** Deployment table line for 3dpa-ios (live worldwide).
- **SF-4:** "before writing" → "before finalizing" in 2 places.

## Gate 2.6 will include (per Codex Decision R answer)

Final text snippets for: Trigger A step 5 labels, Self-check format, `At session END`, `At session START`, NEXT-SESSION prompt-structure bullet, 3DPA Planning hot-cache, memory Trigger B/C paragraphs. Plus full output of all 4 verification checks. Plus byte-identity confirmation.

**Executing fixes now.**
