# Bridge session — 2026-07-10-181245-457366

## Task

Hostile review the Intake Autonomy v2.1 small-gates implementation plan before PR. Plan file: /Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-07-10-intake-autonomy-v2.1-small-gates-plan.md. Spec file: /Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-07-10-intake-autonomy-v2.1-evidence-retry-retrospective-design.md. Review for plan-blocking gaps, gate-order violations, wrong commands, tests that cannot pass, cross-repo/PR confusion, and any place the plan weakens the NO-GO taint invariant. Return MUST-FIX / SHOULD-FIX / OPTIONAL and state whether the plan can be PR-merged after patches.

## Claude (review)

[🟩🟩🟩🟩🟩 100%] Done — findings written

---

## Review findings

**MUST-FIX (5) — plan cannot be PR-merged:**

| # | Location | Issue |
|---|---|---|
| **M1** | R0–R8, every gate review step | All bridge commands use `--mode claude-only`. Plan's own Global Constraints say Claude-driven execution requires `--mode codex-only`. Every gate has Claude reviewing itself — no cross-model signal. |
| **M2** | R1 Step 7 (GREEN) | `baseCandidate()` provides 3 of 12 `CRITICAL_FIELDS`. `fieldPasses()` returns false for undefined fields → 9 errors → `ok: false`. `'confirmed manufacturer evidence passes'` asserts `ok === true` and fails. GREEN is structurally unreachable. |
| **M3** | R8 Step 5 | `gh pr create --body-file /tmp/intake-v21-pr.md` — that file is never created anywhere in the plan. |
| **M4** | R8 Step 5 | `git push -u origin codex/intake-v21-review-plan` — that branch is never created. No `git checkout -b` step exists anywhere. |
| **M5** | R6 Step 3 vs Step 5 | `ai-operating-model` runner contract commits before its gate review. The bridge review in Step 5 covers kickoff/runbook only; the ai-om change is the highest-risk contract file. |

---

**SHOULD-FIX (8):**

- **S1** — `validate-candidate-evidence.js` hard-codes `reason: 'research-defect'` for all failures. The spec makes the validator the sole authority for `world-absent` (weekly retries ×4). Genuinely world-absent candidates would be mis-routed to `research-defect` → one repair → `decision-required`.
- **S2** — R4 missing replay-guard test. File Structure promises it; no test uses matching `diffSha`.
- **S3** — R3 fixture `scripts/fixtures/k2-se-parked-v1.json` is committed but never created by any step.
- **S4** — R5 preflight test gives behavior-only bullet list, no actual bash script content.
- **S5** — R5 preflight modification shows `is_custody_path()` in isolation; full integration with dirty/ahead blocks not shown. Can't verify RD10 Codex must-fix #3 is met.
- **S6** — ai-om cross-repo file not in plan File Structure; surprise for executor.
- **S7** — R8 Step 1 assumes six pre-existing test files exist without verification.
- **S8** — R8 `gh pr merge` fires immediately after `gh pr create` with no CI wait.

---

**OPTIONAL (3):** R8 PR title says "Plan" for an implementation PR (O1); headline taxonomy-graph test covers only half the NO-GO invariant without R3's `assertWritableClass` (O2); bash/Node.js test mix with no CI note (O3).

---

**Bottom line:** patch M1–M5 and S1–S3 before execution begins. S1 in particular weakens a core v2.1 lane (world-absent weekly retry) without being visible at any gate.
