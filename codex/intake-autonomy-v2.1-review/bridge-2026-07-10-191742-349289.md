# Bridge session — 2026-07-10-191742-349289

## Task

Review Gate R0 for 3dprintassistant Intake Autonomy v2.1. All target files are under /Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant, NOT ai-operating-model. Read absolute paths: /Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/scripts/intake-park-taxonomy.json, /Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/scripts/intake-park-taxonomy.js, /Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/scripts/intake-park-taxonomy.test.js. Requirements: /Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/specs/2026-07-10-intake-autonomy-v2.1-evidence-retry-retrospective-design.md and /Users/mustafaozturk-macmini/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-07-10-intake-autonomy-v2.1-small-gates-plan.md Task R0. Verify no NO-GO taint can reach automatic review, unknown reasons fail closed, and tests genuinely prove the invariant rather than pass vacuously. Return GO, GO-WITH-PATCHES, or NO-GO with concrete findings.

## Claude (review)

**Verdict: NO-GO**

---

## Must-Fix

**M1 — Taint-redirect in `classifyParkReason` has zero test coverage (critical)**

Every test that passes `{ tainted: true }` uses `review-no-go`, which lands in `judgment-on-evidence` (`taintedAllowed: true`) — so the redirect branch is never evaluated:

```js
if (sidecar.tainted && def.taintedAllowed === false) {   // ← never hit by any test
  return { className: 'decision-required', ... };
}
```

Deleting this block leaves all six tests green. A tainted candidate would then be classified into `research-defect` (`reviewEntry: true`, `trigger: 'next-run'`) — the exact invariant violation R0 exists to prevent. Tests 3 and 4 prove the *static graph structure* is sound; they prove nothing about *runtime enforcement*.

**Required:** a test calling e.g. `classifyParkReason('research-defect', { tainted: true }, t)` and asserting `className: 'decision-required'` and `reviewEntry: false`.

**M2 — Test 2 (unknown reason) omits `reviewEntry: false` assertion**

"Fail closed" means no review entry. `className` and `trigger` are asserted; `reviewEntry` is not. A regression that set the fallthrough to `reviewEntry: true` would pass this test.

---

## Should-Fix

**S1 — Single-class mutation coverage.** Test 4 mutates only `research-defect`. `world-absent` and `availability-blocked` are not mutation-tested. A validator bug that skips those entries is undetected.

**S2 — No test for removing a required sanctioned edge.** Test 5 covers adding an extra edge; removing `rd3-external-evidence` from the JSON array is unproven (the second required-edge loop would catch it but its coverage is untested).

---

## Optional

**O1** — Test 1 doesn't assert `reviewEntry: false` on the `review-no-go` result.

**O2** — `REQUIRED_SANCTIONED_EDGES` is the code-side half of a two-file agreement with the JSON array; a comment clarifying this prevents silent drift.

---

## What Is Correct

Taxonomy JSON structure is sound. `validateTaxonomyGraph` correctly checks both conjunction conditions. Unknown reasons fail-closed. `needs-source-resolution` (unsuffixed) correctly maps to `decision-required`. `review-no-go-unresolved` is present and correctly assigned to `judgment-on-evidence`.

**Blocker:** M1. One missing test defeats the invariant proof. Patch that plus M2 and resubmit.

