# Bridge session — 2026-07-09-205935-870276

## Task

Second-opinion plan-gate review (one shot). Target: 3dprintassistant/docs/superpowers/plans/2026-07-09-export-phase2-bambu-hardening-plan.md — the CURRENT state already incorporates 11 findings from a prior hostile sub-agent review (dispositions: 3dprintassistant/docs/reviews/2026-07-09-export-phase2-plan-review.md — read it so you don't repeat resolved findings). Ground in the real repo: 3dprintassistant/engine.js (exportBambuStudioJSON ~3481, emission line :3533 vs frozen legacy :3398, BAMBU_PROCESS_INHERITS :3109, formatProfileAsText retraction :3704, exportProfile retraction :3777, setPersonalTuning contract :122-141), scripts/walkthrough-harness.js (W3 T4 block :1548-1580), scripts/export-audit.js (:105-115), scripts/fixtures/slicer-golden/bambu-x1c-*.json, index.html:125-140, app.js:1546-1600, docs/specs/IMPL-043-slicer-export-activation.md section 4. Focus on what a SECOND reviewer adds: (a) are any of the applied patches themselves wrong or incomplete? (b) the T2 dual-variant design: [val,val] for exactly 7 process keys, filament temps stay 1-element, variant-name keys never emitted, A1-class file added to the owner import test — any remaining BS-2.5 import hazard? (c) Task 8 exit-gate/rollback choreography incl. iOS coordination — any hole? (d) the audit checkFail contract described in the Step 2.6 note — is it implementable as described? (e) anything the first reviewer and author BOTH missed. Verdict GO / GO-WITH-PATCHES / NO-GO + numbered findings with severity and concrete patch text. Be terse; do not repeat the 11 resolved findings.

## Codex (review)

[🟩🟩🟩🟩🟩🟩🟩🟩🟩🟩 100%] Review complete.

**Verdict: GO-WITH-PATCHES.** T2 is defensible behind the X1C+A1 import gate, but four must-fix plan defects remain.

1. **MUST-FIX — The dropped ironing patch was only partially applied.**  
   Task 5 says “do not execute,” yet the goal/evaluation still include it, and ROADMAP still calls it active ([plan](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-07-09-export-phase2-bambu-hardening-plan.md:5), [ROADMAP](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/docs/planning/ROADMAP.md:81)).  
   **Patch:** remove ironing from the plan goal and T5 engine-change inventory; move ROADMAP item 81(b) into a separate deferred item triggered by a second ironing vocabulary.

2. **MUST-FIX — Inherits verification misses three exported H2 printers.**  
   `h2c`, `h2d`, and `h2s` are recognized export targets, but absent from `BAMBU_PROCESS_INHERITS`; `_findProcessParent` silently gives them X1C parents ([engine.js](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/engine.js:3084), [fallback](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/engine.js:3183)).  
   **Patch:** add H2C/H2D/H2S to Task 3’s registry query, evidence ledger, explicit map/null rows, and RED-first inheritance assertions.

3. **MUST-FIX — Baseline commands can report false green.**  
   The matrix pipeline masks `node` failure, while the unit loop explicitly converts failures to successful `echo` calls ([plan](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-07-09-export-phase2-bambu-hardening-plan.md:70)).  
   **Patch:**
   ```bash
   node scripts/profile-matrix-audit.js > /tmp/matrix.txt 2>&1 &&
     grep -v '^Generated:' /tmp/matrix.txt | shasum -a 256
   for t in scripts/*.test.js; do node "$t" || exit 1; done
   ```
   Apply identically to every repeated gate and the verification matrix.

4. **MUST-FIX — Task 8 does not converge every fallback to one tested web+iOS state.**  
   XCTest may remain `UNVERIFIED`; iOS changes are bundled as “engine sync” versus “tests” rather than separable T1/T2 commits; A1 allowlisting has no iOS reconciliation; post-merge rollback is web-only. A full T2 revert also restores the audit warning and removes the symbol that Step 8.4 unconditionally requires ([plan](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/docs/superpowers/plans/2026-07-09-export-phase2-bambu-hardening-plan.md:392)).  
   **Patch:** require before merge:

   - One matching iOS commit per T1/T2/T3 delta, containing that delta’s byte-sync and test.
   - Final web↔iOS `diff -q` and full XCTest green; `UNVERIFIED` blocks Step 8.3.
   - After revert or allowlisting: regenerate/commit owner artifacts, repeat import, sync/commit iOS, and rerun all gates.
   - Make audit/live expectations strategy-dependent: dual/allowlist expects the symbol and zero warnings; full revert expects no symbol and the documented legacy count.
   - Mirror any post-merge web rollback into the pending iOS train.

5. **SHOULD-FIX — Step 2.6’s audit premise is false.**  
   The current element-count code only iterates `proc`; it never sees filament temperatures ([export-audit.js](/Users/mragile.io/dev/Claude/Projects/3dprintassistant/scripts/export-audit.js:102)).  
   **Patch:** replace the note with explicit `checkFail`s asserting:

   - The complete set of emitted two-element process arrays equals the seven-key allowlist.
   - Both filament temperatures remain one-element while their golden second entries equal `"nil"`.
   - Variant-name keys remain absent.

   Thus, the contract is implementable, but not as currently described.

6. **OPTIONAL — Correct one grounding claim.**  
   `print_extruder_id` is `["1","1"]`, not variant names; only `print_extruder_variant` and `filament_extruder_variant` contain names. This does not change the emission decision.

No additional BS-2.5 blocker was found in `[val,val]` itself: filament temperatures staying single and variant-name omission are reasonable if the final X1C+A1 import test passes. No files were written; the repo remains clean.

