# Bridge session — 2026-07-08-232153-554173

## Task

Cross-model hostile review of a PLANNING BUNDLE (no code yet) for rebuilding the 3dpa iOS app as v2.0.0. Read these three files in this repo: docs/reviews/2026-07-08-ios-v2-rebuild-audit.md (codebase audit), docs/superpowers/specs/2026-07-08-ios-v2-rebuild-design.md (design decisions D0-D7 incl. D1a/D5a/D5b/D5c), docs/superpowers/plans/2026-07-08-ios-v2-rebuild-plan.md (gated master plan G0-G8). Also read docs/reviews/2026-07-08-ios-v2-rebuild-review.md — two hostile sub-agent reviews already ran and their patches are APPLIED; do NOT repeat their findings, instead (1) challenge any disposition you think is wrong, (2) hunt for what BOTH reviewers missed: contract violations vs docs/3dpa-context.md standing rules, cross-platform drift risks (engine.js/data byte-identical mirror, workshop envelope byte-compat, state-codec), autonomous-execution failure modes (each gate runs as a fresh autonomous Claude session), and any plan step that is unverifiable or ambiguous for a cold executor. Ground-truth against the actual repos: this repo (web, master) and ../3dprintassistant-ios (the app being rebuilt). Severity-tag findings CRITICAL/HIGH/MEDIUM/LOW with file:line evidence; end with verdict GO / GO-WITH-PATCHES / NO-GO.

## Codex (review)

**Must-Fix**

`HIGH` — The per-gate web drift rule will falsely overwrite `printers.json`.
Plan says byte-diff bundled `engine.js`, `data/`, and mirrored JS modules against web `main`, then mirror-sync first if drifted ([plan:20](docs/superpowers/plans/2026-07-08-ios-v2-rebuild-plan.md:20)). But the audit says web has 74 printers while iOS intentionally bundles 71 plus overlay, and `printers.json` differs by design ([audit:8](docs/reviews/2026-07-08-ios-v2-rebuild-audit.md:8), [audit:9](docs/reviews/2026-07-08-ios-v2-rebuild-audit.md:9)). Actual repo diff confirms only `printers.json` differs. Patch the rule: byte-diff `engine.js`, `materials/nozzles/rules`; handle printers via explicit baseline+overlay protocol or deliberate v2 baseline graduation + overlay republish.

`HIGH` — D5a default store topology is already contradicted by actual bytes.
Spec defaults to web `workshop-store.js` as authoritative writer backed by the on-disk iOS workshop file ([spec:39](docs/superpowers/specs/2026-07-08-ios-v2-rebuild-design.md:39)). But web store writes compact localStorage JSON via `JSON.stringify(env)` ([workshop-store.js:54](workshop-store.js:54)), while only `exportJSON()` emits the pretty backup envelope ([workshop-store.js:217](workshop-store.js:217)). iOS explicitly pins its file as pretty `JSON.stringify(env, null, 2)` byte-compatible backup format ([WorkshopStore.swift:5](/Users/mragile.io/dev/Claude/Projects/3dprintassistant-ios/3DPrintAssistant/Services/WorkshopStore.swift:5), [WorkshopStore.swift:90](/Users/mragile.io/dev/Claude/Projects/3dprintassistant-ios/3DPrintAssistant/Services/WorkshopStore.swift:90)). Don’t leave this as “spike may discover it”; patch D5a to require a rendering storage adapter or default to Swift write-side APIs.

`HIGH` — Cold executors are told to trust a stale canonical context.
Standing protocol starts with Trigger C / `3dpa-context.md` ([plan:17](docs/superpowers/plans/2026-07-08-ios-v2-rebuild-plan.md:17)), but the canonical app-state example still says `environment: "room_temp"` and `profileMode` only safe/tuned ([3dpa-context.md:90](docs/3dpa-context.md:90), [3dpa-context.md:92](docs/3dpa-context.md:92), [3dpa-context.md:121](docs/3dpa-context.md:121)). Actual iOS allows `normal/cold/vcold/humid` and `mine` ([AppStateWebCodec.swift:27](/Users/mragile.io/dev/Claude/Projects/3dprintassistant-ios/3DPrintAssistant/Models/AppStateWebCodec.swift:27), [AppStateWebCodec.swift:42](/Users/mragile.io/dev/Claude/Projects/3dprintassistant-ios/3DPrintAssistant/Models/AppStateWebCodec.swift:42)). Plan only doc-syncs stale “64 unit tests” ([plan:61](docs/superpowers/plans/2026-07-08-ios-v2-rebuild-plan.md:61)). Patch G0/G1 doc-sync to fix app-state, profileMode, i18n, test counts before autonomous gate work.

**Should-Fix**

`MEDIUM` — Mirrored app-layer JS modules are not made real early enough.
Spec depends on `state-codec.js` in JSCore ([spec:41](docs/superpowers/specs/2026-07-08-ios-v2-rebuild-design.md:41)) and workshop modules in JSCore ([spec:37](docs/superpowers/specs/2026-07-08-ios-v2-rebuild-design.md:37)), but actual iOS `project.yml` only bundles `engine.js` and data resources ([project.yml:35](/Users/mragile.io/dev/Claude/Projects/3dprintassistant-ios/project.yml:35)). `EngineService` only loads `engine.js` ([EngineService.swift:300](/Users/mragile.io/dev/Claude/Projects/3dprintassistant-ios/3DPrintAssistant/Engine/EngineService.swift:300)). Patch G1 to create the mirrored-JS resource home, XcodeGen resource entries, byte-diff tests, and a smoke loader before G4/G5.

`MEDIUM` — Verification protocol is underspecified for non-engine contracts.
Plan baseline command only names XCTest plus `engine-golden-snapshot` ([plan:22](docs/superpowers/plans/2026-07-08-ios-v2-rebuild-plan.md:22)), while the audit lists codec/workshop/tuning Node tests as part of the required harness set ([audit:76](docs/reviews/2026-07-08-ios-v2-rebuild-audit.md:76)). Patch standing protocol with exact commands for `state-codec.test.js`, `workshop-store.test.js`, `workshop-tuning.test.js`, and `workshop-tuning-rules.test.js`.

**Optional**

`LOW` — The planning bundle is currently untracked in this workspace. That is fine for review, but it conflicts with the spec’s “pushed = cross-machine visible” execution model ([spec:77](docs/superpowers/specs/2026-07-08-ios-v2-rebuild-design.md:77)) until committed/pushed before G1.

Verdict: **GO-WITH-PATCHES**. Patch the planning bundle before owner G0 / first autonomous G1 session.

