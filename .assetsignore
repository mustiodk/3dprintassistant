.git
.git/**
.assetsignore
.gitignore
CLAUDE.md
.claude
.claude/**
.wrangler
.wrangler/**
.DS_Store
wrangler.toml
worker.js
wrangler.noanalytics.tmp.toml
functions
functions/**
migrations
migrations/**
package.json
package-lock.json
vitest.config.mjs
node_modules
node_modules/**
# Server-side Node tooling — never served to the web. Covers the Printer Intake
# Scout, its secret config / watermark / staging output, and test fixtures.
scripts
scripts/**
# Internal docs / review packets / handovers — not part of the web app.
docs
docs/**
codex
codex/**
# Untracked local tooling artifacts, git worktrees, and internal AI handoff docs.
# A manual `wrangler deploy` uploads the working tree (including untracked dirs),
# so these must be excluded explicitly or they leak as public assets.
.superpowers
.superpowers/**
.worktrees
.worktrees/**
ai-handoffs
ai-handoffs/**
