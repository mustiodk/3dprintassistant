{
  "name": "3dprintassistant-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest",
    "generate:release": "node scripts/gen-release-manifest.mjs",
    "verify:release": "node scripts/gen-release-manifest.mjs --check"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.9",
    "vitest": "^4.1.0",
    "wrangler": "^4.36.0"
  }
}
